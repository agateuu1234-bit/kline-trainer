## Summary

Wave 3 顺位 2（Gate 治理轨 / 与顺位 1 RFC 真并行）。**0 业务代码 / 0 Swift 源改动**——仅 CI workflow + pbxproj build settings + governance 脚本 + 文档。三组互不相交改动：

- **A · app-target CI 编译守护（PR11-R3）**：新增 always-trigger workflow `app-build.yml`，在 macos-15 用 `xcodebuild build`（iOS Simulator destination，非 Catalyst：app target `SDKROOT=iphoneos` 无 `SUPPORTS_MACCATALYST`）守护 `KlineTrainer.xcodeproj` app 外壳编译。job name = required-check context `iOS app build-for-running on macos-15`。gate = `** BUILD SUCCEEDED **` 在位 + 无 `** BUILD FAILED **` + 无 anchored `error:`；**不**做 blanket no-warning（`appintentsmetadataprocessor warning:` 是良性工具噪声，实测会误 fail）。build step 在 `set -o pipefail` 下独立运行，xcodebuild 非零 exit 独立 fail job = 首要信号，grep 为次要。
- **B · 锁竖屏 + iPad 全屏窗口（R2-F3 / R3-F3）**：`project.pbxproj`（Debug+Release）orientation → 仅 Portrait（删 Landscape + UpsideDown）+ `UIRequiresFullScreen=YES`（关 iPad Split View / Stage Manager 多窗）。
- **C · required-check 治理工具泛化**：单 Catalyst context → `REQUIRED_CONTEXTS` 列表，builder 加 `--list-contexts` 作单一真相，verifier/admin/测试/fixtures 据此派生（删 verifier 两处硬编码 CATALYST 常量）。新增 partial-state fixture `ruleset-catalyst-only.json`；`ruleset-with-check.json` + 4 个 N3-preservation fixtures 加 app-build context。**实际 origin ruleset `--apply`（加第 12 条 required check）= post-merge user-TTY admin，非本 PR**。
- **Docs**：中文非-coder 验收清单 + 旋转/窗口运行时 runbook（顺位 13 阻塞依赖）+ post-merge admin runbook（含 H1 cross-PR 时序硬约束 + evidence 模板）。

权威设计/计划：`docs/superpowers/specs/2026-06-10-wave3-pr2-app-ci-orientation-design.md`（v3）+ `docs/superpowers/plans/2026-06-10-wave3-pr2-app-ci-orientation.md`。

## 评审 & 验证

- **Superpowers 全流程**：brainstorming → spec（opus 4.8 xhigh 对抗审查 **3 轮 → APPROVE**）→ writing-plans（opus 4.8 xhigh **2 轮 → APPROVE**）→ subagent-driven 实现 → verification-before-completion → 整体 whole-branch opus 4.8 xhigh **APPROVE**（+ 补做 subagent-driven 两阶段评审：spec-合规 ✅ + code-quality ✅，均 fresh subagent 独立核对）。
- **codex 通道**：按 owner 指示用 opus 4.8 xhigh 替代 codex:adversarial-review（合并时 `codex-verify-pass` 走 admin bypass + attest-override，见下）。
- **本地实测证据**：app target `** BUILD SUCCEEDED **`（Xcode 26.5 / iOS Sim）；生成 `Info.plist` 含 `UIRequiresFullScreen=true` + `UISupportedInterfaceOrientations~ipad/~iphone = [Portrait]`；governance `run-all.sh` **ALL GREEN**（15 pytest + verify + admin 三套件）。

## Test Plan

- [ ] CI：`iOS app build-for-running on macos-15` check 运行且 **绿**（首次 CI 是 Xcode-version 最终裁决，见 spec §五 R1）。
- [ ] CI：既有 11 个 required check 无回归（Catalyst / codex / smoke 等）。
- [ ] `bash tests/scripts/governance/run-all.sh` → `ALL GREEN`。
- [ ] `python3 scripts/governance/build-protection-put-payload.py --list-contexts` → `["Mac Catalyst build-for-testing on macos-15", "iOS app build-for-running on macos-15"]`。
- [ ] pbxproj 搜 `Landscape`/`UpsideDown` = 0 命中；`UIRequiresFullScreen = YES` = 2 命中。
- [ ] 完整非-coder 验收：见 `docs/acceptance/2026-06-10-wave3-pr2-app-ci-orientation.md`（13 步二元可判）。
- [ ] 运行时（device/sim，顺位 13 阻塞）：`docs/runbooks/2026-06-10-wave3-orientation-runtime-acceptance.md`（iPhone/iPad 旋转保持竖屏 + iPad 无分屏/Stage Manager 缩放）。

## Post-merge（⚠️ 时序敏感，codex H1）

合并后由管理员执行 `scripts/governance/admin-configure-required-checks.sh --apply` 把 `iOS app build-for-running on macos-15` 加入 main ruleset（第 12 条 required check，真实 mutation，**非** no-op）——**只能在「顺位 1 RFC 已 merge」或「所有 in-flight PR 已 rebase 到含 `app-build.yml` 的 main」之后**，否则新 required context 会让并行 docs-only PR 永久卡在「Expected — waiting for status」。详见 `docs/governance/2026-06-10-pr2-app-build-required-check-runbook.md`。

🤖 Generated with [Claude Code](https://claude.com/claude-code)
