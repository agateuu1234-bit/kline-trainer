# Plan M0.5 验收清单 — Concurrency Contract Doc

> 这份清单给非-coder 用户用：复制每一行"action"到 Claude Code 或 Terminal，对照"expected"判断 pass / fail。

## A. 文件落地

| # | action | expected | pass_fail |
|---|---|---|---|
| A1 | `ls docs/governance/m05-concurrency-contract.md` | 文件存在 | ☐ |
| A2 | `ls scripts/acceptance/plan_m0_5_concurrency.sh` | 文件存在 | ☐ |
| A3 | `ls docs/acceptance/2026-05-16-m0.5-concurrency-contract.md` | 文件存在（本清单自身） | ☐ |
| A4 | `ls docs/superpowers/plans/2026-05-16-m0.5-concurrency-contract.md` | 文件存在（plan 自身） | ☐ |

## B. 结构验证

| # | action | expected | pass_fail |
|---|---|---|---|
| B1 | `grep -c '^## ' docs/governance/m05-concurrency-contract.md` | 输出 `12`（12 个 H2 章节） | ☐ |
| B2 | `wc -l docs/governance/m05-concurrency-contract.md` | 行数 ≥ 200（约 220-260 行预期） | ☐ |
| B3 | `head -20 docs/governance/m05-concurrency-contract.md` | 含 `Status` + `权威来源` 块 + L655-702 行号引用 | ☐ |

## C. 聚合 acceptance（自动 71 项独立断言）

| # | action | expected | pass_fail |
|---|---|---|---|
| C1 | `bash scripts/acceptance/plan_m0_5_concurrency.sh` | 末尾 `Plan M0.5 acceptance: 71 / 71 pass, 0 fail`；exit code 0 | ☐ |

## D. Fail-path 健壮性（手动验证脚本能抓 anchor 缺失）

| # | action | expected | pass_fail |
|---|---|---|---|
| D1 | `cp docs/governance/m05-concurrency-contract.md /tmp/m05-backup.md && sed -i.bak '/^4\. ❌.*CADisplayLink/d' docs/governance/m05-concurrency-contract.md && (bash scripts/acceptance/plan_m0_5_concurrency.sh; echo "exit=$?") ; cp /tmp/m05-backup.md docs/governance/m05-concurrency-contract.md && rm docs/governance/m05-concurrency-contract.md.bak` | 输出含 `NG: ban #4: CADisplayLink 重活` + `exit=1`；恢复后 `diff docs/governance/m05-concurrency-contract.md /tmp/m05-backup.md` 空 | ☐ |

## E. Scope 边界（不应做的事）

| # | action | expected | pass_fail |
|---|---|---|---|
| E0 | `BASE=$(git merge-base origin/main HEAD); echo "$BASE"` | 输出一个 git SHA（PR merge-base 与本 branch 的 base，作为 E1-E3 的固定参照点，避免 local main 漂移导致假阳/假阴；branch-diff codex R2 finding 2） | ☐ |
| E1 | `git diff $(git merge-base origin/main HEAD) -- 'ios/**/*.swift' 'backend/**/*.py' 'backend/sql/**/*.sql' 'ios/sql/**/*.sql' '.github/workflows/**/*.yml' 'ios/Contracts/Package.swift'` | 输出为空（doc-only，零生产代码 / SQL / CI / SwiftPM 改动） | ☐ |
| E2 | `git diff $(git merge-base origin/main HEAD) -- 'kline_trainer_modules_v1.4.md' 'kline_trainer_plan_v1.5.md'` | 输出为空（不动 spec 源） | ☐ |
| E3 | `git diff $(git merge-base origin/main HEAD) -- 'docs/governance/m01-schema-versioning-contract.md' 'docs/governance/m04-apperror-translation-gate.md'` | 输出为空（不动姊妹 governance doc） | ☐ |

## F. 交叉引用一致性

| # | action | expected | pass_fail |
|---|---|---|---|
| F1 | `grep 'm01-schema-versioning-contract' docs/governance/m05-concurrency-contract.md` | 至少 1 行 hit | ☐ |
| F2 | `grep 'm04-apperror-translation-gate' docs/governance/m05-concurrency-contract.md` | 至少 1 行 hit | ☐ |
| F3 | `grep 'project_plan1e_aborted' docs/governance/m05-concurrency-contract.md` | 1 行 hit（Plan 1e 教训反向引用） | ☐ |

## G. CI（GitHub Actions） + Codex review verdict

| # | action | expected | pass_fail |
|---|---|---|---|
| G1 | `gh pr checks <pr_number>` | **`codex-verify-pass` 必须 SUCCESS**（依 CLAUDE.md §1 治理 backstop：codex 评审 verdict 必须作为 non-self-attested GitHub status check 通过）。OpenAI quota fail = **阻塞**，rerun 路径 = PR 评论 `/codex-review`（触发 `codex-review-rerun.yml` 工作流；`codex-review-verify.yml` 是 `workflow_run`-triggered 无 `workflow_dispatch` 不能手动启动）。不允许"绕过 G1 走 admin bypass"——branch-diff codex R2/R3 修正后的 strict 路径 | ☐ |
| G2 | `cat .claude/state/attest-ledger.json` 查本 PR HEAD SHA entry | **审计记录**：ledger 应含 `branch:<branch>@<HEAD>` key（codex approve 或 user TTY override 记录）。**G2 不是合并条件**——合并必须由 G1（GitHub codex-verify-pass）通过；G2 仅作 post-merge audit 与 plan-residual 决策可追溯性 | ☐ |

## H. 验收规则

- A / B / C / D / E / F 全项必须 ✓ pass_fail = ☑
- **G1（codex-verify-pass GitHub check SUCCESS）必须通过**——这是合并唯一允许的 gate；quota fail = 阻塞 + rerun，不绕过
- G2 是审计记录不充当合并条件（codex R2 finding 1 accept：local ledger 不替代 non-self-attested GitHub status check）
- 任一 A-F 项 ☒（fail）→ 不合并；回 plan 阶段修
- C1 / D1 是核心：自动断言通过 + fail-path 抓得到 = doc 治理价值的最低保证

## I. 已知 plan-residual（不阻塞本 PR merge，登记 backlog）

| # | residual | 阻塞 | 解决路径 |
|---|---|---|---|
| I1 | CI Threading Sanitizer 实施 | 否 | Wave 1 启动 PR + Simulator CI 整体迁移 |
| I2 | Swift 6 strict-concurrency 全仓启用 | 否 | Wave 1 启动 PR |
| I3 | 跨 actor 协议返回值 Sendable conformance audit script | 否 | Plan 3 P1/P2/P3a/P3b/P4 真生产代码 PR 后 |
| I4 | `assumeIsolated` 调用点 lint | 否 | 首次 prod 代码引入 `assumeIsolated` 时 |
| I5 | **codex R6 [high]**：@MainActor 5 类 row anchor 不强制要求 `@MainActor` 字符同行 | 否 | 接受 residual；理由：5 类 spec L1571/L1625/L820/L1973/L1315 实际 prod 代码已强制 `@MainActor`（PR #39/#40/#42/#44/#51 落地+contract test），doc 的 row anchor 只要类名+模块号即可识别行；R7+ 风险 = `^\| E5 \|.*TrainingEngine.*@MainActor` 收紧但仍能找下一层细化 |
| I6 | **codex R6 [medium]**：GRDB P3 row anchor 不强制 `独立` 字段（spec L683） | 否 | 接受 residual；同理由——P3a Factory 真模块 PR (#41) contract test 已校验 per-zip 独立 DatabaseQueue 行为，doc 是反向锚 |
| I7 | **codex R6 [medium]**：assumeIsolated 前置条件断言 doc-wide 而非 bullet-anchor | 否 | 接受 residual；prod 引入 `assumeIsolated` 时（I4 触发点）一并强化 |

**R5/R6 收敛说明**：codex R1-R4 抓真 bug 全部修；R5 row-scoped 重写后 R6 仍能在更细一层（要求 row 内含 rule 字符 / `独立`/ bullet-anchor）继续挖；R6 finding 是 R5 同质纵向加深而非 self-contradiction。按 `feedback_codex_plan_budget_overshoot.md` + `feedback_codex_round6_self_contradiction.md` 规则，5+ 轮 budget overrun → user TTY override accept residual 路径；实际并发模型 conformance 由 PR #37-#51 已 merged 各模块 contract test 强制，本 doc 是 governance 反向锚不重复校验 prod 行为。
