# 训练组 ↔ App 契约对齐设计（`.db` 扩展名 + 跨周期全局索引）

> 日期：2026-08-30 · 分支 `feat/trainingset-app-contract-fix` · base `main@1437529`
> 触发：2026-08-30 NAS 真机验收（runbook P13/P14）—— 手机下载 3 个训练组全部失败。
> 结论：失败与 NAS 部署无关，是**后端产物与 App 读取器之间两条一直存在、从未被端到端测过的契约不符**。

---

## 1. 问题（全部经实测，非推演）

真机症状两次，逐次定位到两条独立缺陷。

### 1.1 缺陷 A —— zip 内文件扩展名

| 侧 | 事实 | 出处 |
|---|---|---|
| 后端 | zip 内恰好一个文件，名为 `{stock_code}_{start_datetime}.db` | `backend/generate_training_sets.py:403` + `:371`（`arcname=db_path.name`）；旁注原文「文件名仍用 `{code}_{start}.db` 以保持 zip 内 arcname 契约不变」 |
| App | 要求「exactly 1 个 regular file 且 path 后缀 `.sqlite`（lowercased）」，否则 `AppError.trainingSet(.unzipFailed)` | `DefaultZipExtractor.swift:26`；契约文字 `ZipExtracting.swift:10-11` |
| 部署 spec | 自己写着「zip 内是单个 `.db`」 | `docs/superpowers/specs/2026-08-14-qmt-nas-deployment-design.md:56` |

矛盾一直摆在纸面上。用户可见症状：「训练组解压失败」。形态检查发生在解压之前，故表现为「下载成功、解压失败」。

### 1.2 缺陷 B —— 早于 3m 轴的历史 K 线，`end_global_index` 全被压成 0

后端 `assign_global_indices()`（`backend/generate_training_sets.py:273-296`）对每个周期的每根 K 线求
`j = bisect_right(three_dts, upper) - 1`，再 `egi.append(max(0, min(j, n3 - 1)))`（`:292`）。

**整根早于 3m 轴起点**的 K 线，`bisect_right` 返回 0 → `j = -1` → 被 `max(0, …)` 压成 **0**。

这不是偶发：`PERIOD_BEFORE_CAP`（`:45-46`）里 **`monthly` 的 before cap 是 `None`（有多少历史取多少）**，而 1 分钟源数据只有约一年 —— 故**每一个训练组**都必然携带大量早于 3m 轴的高周期历史。

实测（三个真实训练组，逐个查库）：

| 周期 | 行数 | 不同 `end_global_index` | 压成 0 的行数 | 早于 3m 轴起点的行数 |
|---|---|---|---|---|
| 3m | 12870 | 12870 | 1（首根，正当） | 0 |
| 15m | 2694 | 2576 | 119 | 120 |
| 60m | 786 | 646 | 141 | 142 |
| daily | 309 | 162 | 148 | 149 |
| weekly | 154 | 36 | 119 | 120 |
| monthly | 422 | 10 | **413** | 414 |

（3m 轴最早 2025-08-28；monthly 最早 1991-01-04。「压成 0 的行数」比「早于轴起点的行数」少 1，差的那根是**跨在轴起点上**的那一根 —— 它的覆盖窗口延伸进了轴内，拿到正当的非零编号。）

App 侧 `DefaultTrainingSetReader.loadAllCandles()` 第 90 行要求**每个周期内 `end_global_index` 严格递增**，任何重复 → `AppError.persistence(.dbCorrupted)`。用户可见症状：「本地数据损坏」，界面汇总为「3 个未完成」。

### 1.3 冻结契约自身的冲突

`kline_trainer_modules_v1.4.md`：

- `:752` —— 训练组不变量含「**月线前 ≥30**」；
- `:2302` R03 —— 「`global_index/end_global_index` **严格递增** + 前后端 assert」。

30 根月线 = 2.5 年历史，而 1 分钟源数据只有约一年 ⇒ **这两条要求在真实数据上不可能同时成立**。冲突写在契约里，不是某一侧写错了一行。

### 1.4 为什么藏到今天

仓库里**没有任何一条测试把「生成器真实产物」喂给「App 读取器」**。两侧各自的测试都绿：后端测自己的纯函数，App 测自己手写的 fixture。缝在中间，无人负责。

**推论（须在措辞上守住）**：App 至今**从未成功消费过一个真实生成的训练组**；此前跑通的全是 debug fixture。

---

## 2. 决策

### 2.1 采纳：路线 A —— 收紧地放宽 App 侧读取规则

**「后端不动」的准确表述**：数据格式、既有产物、`content_hash` 全部不动；**后端零代码改动**（codex R4 后的最终形态，演进见 §5.5）。改的只有 App 两个读取器的判据、契约文字，以及顶层版本号与新增测试。

**用户已拍板（2026-08-30）**：
1. 早于 3m 轴的老历史 **要当普通历史显示出来**（契约「月线前 ≥30」本来的意图 —— 看月线就是为了判大局）；
2. 扩展名一条 **改 App，让它接受 `.db`**；
3. **要补**「后端真实产物 → App 读取器」的常驻测试。

### 2.2 否决：路线 B —— 给早于轴的 K 线一个专用标记（新列或哨兵值）

语义更干净（不给 `0` 赋双重含义），但属**数据格式变更** ⇒ `TRAINING_SET_SCHEMA_VERSION` bump ⇒ **全部已生成训练组作废并重算指纹**；且本 App 可能公开上架，格式升级还要连带处理「用户手机上已下载的旧训练组」的迁移。当前真实数据链路刚打通第一次，不值得动格式。**记为后续可选演进**：若 `0` 的双重含义日后确实碍事，届时再评估。

### 2.3 否决：把 3m 轴向前延长以覆盖全部历史

券商源数据不存在那么久远的分钟级数据。物理上不可行。

### 2.4 否决：砍掉早于轴的历史（只留轴内那段）

轴内月线仅约 9 根（实测），既违反契约 `:752`「月线前 ≥30」，也让「切到月线判大局」这个训练目的落空。

---

## 3. 契约变更（精确表述）

### 3.1 跨周期全局索引

**原（`kline_trainer_modules_v1.4.md:2302` R03）**：
> `global_index/end_global_index` 严格递增 + 前后端 assert

**新**：

> **`.m3`（全局 tick 轴）**：`global_index == end_global_index == 数组下标`，从 0 起**严格递增**、无缺口、不得为 NULL。**一条不放宽。**
>
> **其它周期**：每一根的 `end_global_index` **必须逐根精确等于**由 `.m3` 时间轴反算出的值：
>
> ```
> upper   = 同周期下一根的 datetime − 1   （末根取 .m3 末根的 datetime）
> derived = clamp( bisect_right(m3_datetimes, upper) − 1 , 0 , n3 − 1 )
> 要求 end_global_index == derived
> ```
>
> 该式**就是这个字段的定义本身**（生成器 `assign_global_indices:288-292` 逐字同式）。它**同时蕴含**了「非递减」「重复只可能出现在 `0` 上（除非 `.m3` 轴内有缺口）」「`0` = 覆盖窗口至多触及 `.m3` 首根」这几条，因此**不再单列**那些较弱的规则 —— 单列会产生「永远无法独立触发」的冗余判据。
>
> ⚠️ **为什么必须是「精确等于」而不是「有序 + 范围内」**（codex R4 finding 1）：一根开在 `.m3` 第 10 根、结束于第 49 根之前的 K 线，若被标成 `10` 而非 `49`，它**依然**严格递增、依然唯一、依然在范围内、依然满足 `s <= end_global_index`。而渲染层看到 `end_global_index == 当前 tick` 就认为这根**已完成**，直接画出整根 —— 里面含着 tick 11–49 的行情，**未来数据泄露**。有序性完全约束不住取值本身。
>
> **全部周期（含 `.m3`）**：周期内 `datetime` **严格递增**。此前该条只对 `.m3` 显式成立，其它周期靠「`end_global_index` 唯一且与 `datetime` 同序」隐式成立；放宽后隐式保证消失，必须显式化（§4.4）。
>
> **读取次序**：读取端取数必须以 `(period, end_global_index, datetime)` 排序 —— 并列时若无次序键，数组次序在 SQL 语义上无定义（§4.4）。
>
> **执行位置**：App 在读取边界拒绝不满足者（`.dbCorrupted`），生产与预览两个读取器都要。**后端不再另设 assert** —— 生成器用同一个公式产出，再断言一次是**同义反复**（零判别力），本仓明令禁止这种「看起来在把关」的判据。生产者侧的回归由 §5.6 那条「经顶层装配函数产出 fixture → 喂 App 真实读取链路」的 CI 测试兜住。

### 3.2 zip 形态

**原（`ZipExtracting.swift:10-11`）**：「exactly 1 个 regular file 且 path 后缀 `.sqlite`」

**新**：「exactly 1 个 regular file 且 path 后缀为 `.sqlite` 或 `.db`（均 lowercased 比较）；其它扩展名、0 个、≥2 个、或含任何其它 regular file → `.unzipFailed`」。

`.sqlite` 保留是为了**向后兼容**既有 fixture 与测试资产；生产产物用 `.db`。

---

## 4. 为什么这样是安全的（四条论证，均有实测支撑；4.4 是自审新增的必修项）

### 4.1 「非递减」足以让二分查找正确

App 里所有依赖 `end_global_index` 的查询都是 `partitioningIndex`（Swift 的分区二分），使用点：
`TrainingEngine.swift:541 / :563 / :727`、`MarkersLayout.swift:25`、`PanLinkage.swift:27,31`。

`partitioningIndex` 的前置条件是「数组按谓词**已分区**」（前段全 false、后段全 true），而不是「严格递增」。谓词形如 `$0.endGlobalIndex > current` / `>= target`，在**非递减**序列上分区性成立。故放宽不破坏任何一处二分。

`.m3` 的严格递增与连续性另有独立校验（`DefaultTrainingSetReader.swift:153-160`、`TrainingEngine.swift:159-163, :736`），本次**一律不动**。

### 4.2 序关系约束不住取值 —— 必须逐根**精确反算**

⚠️ **本节被 codex 连打回两轮，这是第三版。** 演进过程本身是判据：

| 版本 | 主张 | 被什么推翻 |
|---|---|---|
| v1 | 既有校验 `s <= endGlobalIndex` 已使 `0` 桶自限 | R1-F2：该式只约束**开盘**，不约束收盘 |
| v2 | 给 `0` 桶补「覆盖窗口内 `.m3` 根数 ≤ 1」 | R4-F1：**非零**取值同样能过 —— 有序性根本约束不住取值 |
| **v3（本节）** | 每根非 `.m3` K 线的编号**精确等于**反算值 | — |

**v2 为什么不够**（R4-F1，已核实）：一根开在 `.m3` 第 10 根、覆盖到第 49 根之前的 K 线，被标成 `10` 而不是 `49` —— 它严格递增、唯一、在范围内、`s <= endGlobalIndex` 也满足，v2 的所有判据**全部放行**。而渲染层的逻辑是「`endGlobalIndex == 当前 tick` ⇒ 这根已完成 ⇒ 画整根」，于是 tick 10 时就把含着 tick 11–49 行情的整根画了出来 —— **未来数据泄露**，而且**安静**。

**v3 的规则**（§3.1 已给出算式）：`end_global_index` **必须逐根等于**由 `.m3` 时间轴反算的值。它就是这个字段的定义，不是近似。

**它把此前几条较弱的规则整个吃掉了**：非递减、「重复只在 0 上」、「`0` 桶覆盖窗口」—— 三条都是 v3 的推论，因此**不再单列**（单列 = 永远无法独立触发的冗余判据，本仓明令禁止）。

⚠️ **关于「两种语言两份实现会漂移」**（这是 v2 刻意只反演 `0` 那一格的理由，现予推翻）：该式仅 3 行，且**就是字段定义本身**；读取端复算它等价于对这一列做校验和。真要改定义，属跨系统契约变更 → 必然 bump `CONTRACT_VERSION`（§5.7）→ 两侧必须同步。漂移风险由版本机制覆盖，**换来的是把一整类「取值被改小」的安静泄露变成可检测**。

**实测**（三个真实训练组，逐根反算与库里存的比对）：非 `.m3` 共 **4365 / 4240 / 4366** 根，**不一致 = 0 / 0 / 0**。⇒ 该强规则对现有产物完全可满足。

#### 4.2.1 ⚠️ v3 只管**上界** —— 既有的下界判据必须**保留**（codex R5-F1）

反算式里的 `upper` 来自「**下一根**的开盘」（末根取 `.m3` 末根），因此它**完全没有约束这根 K 线自己从哪里开始**。

**反例（已亲手验算）**：`.m3` 时间戳 `[100, 200, 300]`，某周期**只有一根** K 线、开盘在 `400`、`end_global_index = 2`。

| 判据 | 计算 | 结论 |
|---|---|---|
| v3 精确反算 | 末根 ⇒ `upper = 300`；`bisect_right([100,200,300], 300) − 1 = 2` ⇒ `derived = 2` | **放行**（漏） |
| 既有下界 `s <= egi` | `s = partitioningIndex{m3.datetime >= 400} = 3`；`3 <= 2` 为假 | **拒绝**（正确） |

后果与 R4-F1 同类：tick 2 时渲染层认为这根已完成、画出整根，而它的行情整段发生在**已揭示的 `.m3` 轴之后**。**「整根落在 `.m3` 某个时间缺口里」也是同一形态。**

⇒ **结论**：`s <= endGlobalIndex`（下界）与 v3 精确反算（上界）**互补，缺一不可**，两个读取器都**同时保留**。⚠️ 本设计上一版曾指示预览读取器「把 `:130` 那块改为精确反算」—— 那是**拆掉下界防线**，已订正为「保留 + 增补」。

### 4.3 放宽不引入未来数据泄露

用 App 自己的「已揭示」算法（`endGlobalIndex <= 当前 tick`）在真实数据上算，训练起点 tick=150 时各周期可见：

| 周期 | 可见根数 | 覆盖时间 | 末根是否 ≤ 训练起点 2025-09-01 |
|---|---|---|---|
| monthly | 414 / 422 | 1991-01-05 → 2025-08-01 | 是 |
| weekly | 120 / 154 | 2023-05-04 → 2025-08-25 | 是 |
| daily | 150 / 309 | 2025-01-17 → 2025-08-29 | 是 |
| 60m | 149 / 786 | 2025-07-09 → 2025-08-29 | 是 |
| 15m | 149 / 2694 | 2025-08-18 → 2025-08-29 | 是 |
| 3m | 151 / 12870 | 2025-08-28 → 2025-09-01 | 是 |

无一根越过训练起点。且这正是 §2.1 用户要的「老历史当普通历史显示」。

### 4.4 ⚠️ 放宽会**连带**掀出一个隐藏假设：并列时数组次序无定义

这是本设计自审时抓到的、**不修就会引入新缺陷**的一条。

`DefaultTrainingSetReader.swift:67` 取数的 SQL 是：

```
ORDER BY period, end_global_index
```

**没有并列时的次序键**。在「严格递增」前提下 `end_global_index` 在周期内唯一，次序被唯一确定；一旦允许在 `0` 上并列，那 413 根月线彼此的先后**在 SQL 语义上就没有定义了**。当前它们恰好按插入顺序（= datetime 升序）返回，是因为存在 `idx_period_endidx ON klines(period, end_global_index)` 且并列时走 rowid 次序 —— **这是实现细节，不是保证**。

后果不在二分（§4.1 只要求分区性，与并列内部次序无关），而在**渲染**：`result[period]` 数组按原次序被切片绘制，次序错乱 = 月线图上 K 线前后颠倒。

同时暴露另一件事：现有代码**只对 `.m3` 校验了 datetime 严格递增**（`:161-166`），其它周期从未校验 —— 以前靠「egi 唯一 + egi 与 datetime 同序」隐式成立，放宽后这条隐式保证消失。

**实测**（三个真实训练组逐个查）：每周期 `datetime` 重复数 = **0 / 0 / 0**；「按 `(end_global_index, datetime)` 排」与「按 `datetime` 排」的结果**逐行一致**（差异行数 0 / 0 / 0）。故下面的修正对真实数据是可满足的，且不改变现有次序。

---

## 5. 改动清单

### 5.1 App —— `DefaultTrainingSetReader.swift`：次序键 + 分周期判据 + datetime 单调 + `0` 桶约束

四处，缺一不可（(a)(b)(d) 见 §4.4 与 §4.2）：

**(a) SQL 补并列次序键**（`:67`）：

```
ORDER BY period, end_global_index, datetime
```

**(b) 每周期 datetime 严格递增** —— 把现有那条只作用于 `.m3` 的校验（`:161-166`）推广到**全部周期**。这是 §4.4 里失去的隐式保证的显式替代，也是本次唯一**收紧**的判据。

**(c) `end_global_index` 判据按周期拆分**（`:90`）：

- `.m3`：`endGlobalIndex <= prev` → `.dbCorrupted`（**与现状逐字相同，一条不放宽**）；
- 其它周期：**逐根精确反算**（§3.1 的算式），`endGlobalIndex != derived` → `.dbCorrupted`。

⚠️ **不再单列**「非递减」「重复只在 0 上」「`0` 桶覆盖窗口」三条 —— 它们都是精确反算的推论，单列会得到永远无法独立触发的冗余判据（§4.2 v3）。这三条在 R1–R3 期间曾作为独立判据写在本节，**已被 R4 的精确反算整体取代**。

**(d) 反算所需的输入**：`upper` 取「同周期下一根的 `datetime` − 1」（末根取 `.m3` 末根 `datetime`），故本条**必须**在 `result` 组装完成、且 (a)(b) 已保证「数组即 datetime 升序」之后单独走一遍；`.m3` 侧用一次 `partitioningIndex` 二分求 `bisect_right`，整体 O(n log n)。

**(e) 既有下界判据 `s <= endGlobalIndex`（`:182-183`）—— 原样保留，不得删改**（§4.2.1）。它与 (c) 的精确反算互补：(c) 管上界、(e) 管下界。**必配两条拒绝用例**：① 末根开盘晚于整条 `.m3` 轴；② 整根落在 `.m3` 的时间缺口内。这两档在只有 (c) 时会被放行。

⚠️ 实现约束：(c) 的 `.m3` 分支仍在原循环里（`lastEnd` 按周期分别记）；非 `.m3` 的反算走 (d) 那一遍。**不得**把两者硬揉进同一个循环 —— 反算需要「下一根」，在流式循环里拿不到。

### 5.1b App —— `PreviewTrainingSetReader.swift`：**同一族判据的第二个执行点**（codex R2 打回后新增）

⚠️ **原文漏了这个文件。** `PreviewTrainingSetReader.validateCandles`（`:57-`）是生产读取器的**显式镜像**，文件头写着「维护契约：production `validateCandles` 改了 → 这里同步改」。其 `:70` 现为 `c.endGlobalIndex <= prev`（对**所有**周期一刀切），**不改就会把含重复 0 的真实格式数据在预览/调试路径上全部判死**，且缺失 (b)(d) 两条新判据 —— 测试与生产会**双向**背离。

**改动**：`:70` 按 §5.1(c) 拆分；`:115` 的 datetime 校验按 §5.1(b) 推广到全部周期；`:130` 那块（下界判据的镜像）**原样保留**，并**另外增补** §5.1(c)(d) 的逐根精确反算 —— ⚠️ 上一版写的是「改为」，那会拆掉下界防线（§4.2.1）。同步更新 `PreviewTrainingSetReaderTests.swift`。

⚠️ **一处必须写明的不对称**（否则实施者会照抄生产端的结论而写出零判别力的测试）：

生产读取器的数组来自 SQL `ORDER BY (period, end_global_index, datetime)`，**乱序输入根本进不来**；而**预览读取器的数组是调用方在内存里直接给的，没有任何排序保证**。故：

- 生产端：datetime 单调与反算所依赖的「数组即 datetime 升序」由 (a) 提供，测试要证的是 **(a) 真的在起作用**（见 M5）；
- 预览端：调用方可以直接塞进**乱序**或**编号被改小**的数组，两条判据在这里都是**真判据**，必须各配**行为测试**（M10b），不许照搬生产端的写法。

**全仓穷尽扫描结论**（按「判据本身」而非手头文件块，本仓明令）：`endGlobalIndex` 序关系的**闸门**在全仓恰好 2 处（生产 `:90` / 预览 `:70`）；datetime 单调闸门 2 处（生产 `:165` / 预览 `:115`，均仅管 `.m3`）；`s <= egi` 闸门 2 处（生产 `:182` / 预览 `:130`）。其余命中全是**消费方**（`partitioningIndex` 二分、`TrainingEngine` 的 `.m3` 前置断言），不是闸门，不动。

### 5.2 App —— `DefaultZipExtractor.swift:26`

后缀判据由 `.sqlite` 扩为 `.sqlite` 或 `.db`；其余形态规则（恰好 1 个、不允许任何其它 regular file、dir/symlink 不计、解压前先查形态）**逐条不变**。

### 5.3 App —— 契约文字

- `ZipExtracting.swift:10-14` 按 §3.2 改写；
- `DefaultTrainingSetReader.swift` 相关注释按 §3.1 改写，并写明 `0` 的语义与 §4.2 的自限论证（这是后人最容易误删的一条，注释必须承重）。

### 5.4 契约文件 —— `kline_trainer_modules_v1.4.md:2302`

R03 行按 §3.1 改写。⚠️ 该文件是**冻结契约**，本次是**修订而非放宽**：一侧收紧（后端新增 assert），一侧按真实数据可满足化。改动须在文件的修订记录里留一行。

### 5.5 后端 —— **零改动**（本节记录一次「加了又撤」的演进，勿删）

**最终结论：后端一行不改。**

演进过程（三轮评审推着走的，记录下来是因为结论本身反直觉）：

| 阶段 | 主张 | 结局 |
|---|---|---|
| 初稿 | 在 `assign_global_indices` 里加断言「非递减 + 重复只在 0 上」，让坏候选起点在生成时被跳过 | — |
| R1-F1 | 位置错了：`assign_global_indices` 在 `assemble_from_windows:384` 被调用，**已出重试循环**，异常会变成整只股票被跳过 | 改为放进候选回调 `_try` 内部 |
| **R4-F1 之后** | 读取端改为**逐根精确反算**（§4.2 v3）后，「非递减 / 重复只在 0 上」不再是独立契约 —— 生成器用同一个公式产出，再断言一次是**同义反复** | **整条撤回** |

⚠️ **为什么撤回不是倒退**：R1-F1 指出的是「**若**要加这个断言，位置必须在 `_try` 里」。R4 把契约升级成精确反算之后，这个断言的**内容**变成了对自身计算的复述 —— 零判别力，本仓明令禁止。留着它只会制造「看起来有两道防线」的错觉。

⚠️ **R1-F1 的实质仍然保留为条件性规则**：日后若真要在生成器侧加任何**会拒绝候选起点**的检查，**必须**放在 `build_training_windows._try` 内部（`select_valid_window` 只捕获 `_try` 抛出的 `GenerateSkipException`），否则就不是「换个起点重试」而是「整只股票产 0」。

⚠️ 生产者侧的回归防护**不靠断言，靠 §5.6(a)**：fixture 经**顶层装配函数** `assemble_from_windows` 产出，再喂给 App 的真实读取链路 —— 生成器任何一处改动（含 `:403` 的文件名、索引算法）都会在 CI 变红。

### 5.6 常驻测试 —— 补上「后端真实产物 → App 读取器」这道缝

**目标**：让 §1.4 那类缺陷**下一次在 CI 就红**，而不是等真机。

两条测试夹住这道缝：

**(a) 后端侧（Python，host pytest）**：**必须经由生产的顶层装配函数 `assemble_from_windows(...)`** 从一组合成 bar 数据造出一个**小训练组**，取其返回的 `GeneratedTrainingSet.path`（即真 zip）来做后续比对。其形态必须包含本缺陷的特征

⚠️ **不得**改成直接串调 `assign_global_indices` → `build_training_set_sqlite` → `zip_and_hash`（codex R3 finding 1，**原文就是这么写的，错**）：真正决定 zip 内成员名的是 `assemble_from_windows` 的 **`:403` `db_path = Path(_tmp) / f"{fname}.db"`**，而 `zip_and_hash:371` 只是把 `db_path.name` 抄成 `arcname`。测试若自己造 `db_path`，就对 `:403` 的改动**完全不敏感** —— 把 `:403` 改成 `.txt`，测试的产物名不变、App 侧静态 fixture 也不变，**两条测试全绿而每一次真机下载都会失败**。`assemble_from_windows` 是纯装配层（不碰 PostgreSQL），host pytest 可直接调。 —— 至少一个非 m3 周期有 **≥2 根** K 线落在 `end_global_index = 0`（即合成数据里高周期历史要早于 3m 轴起点）。断言：
1. 产物与仓库里那份 fixture 的**数据库内容**逐表一致（**不比 zip 字节** —— `zipfile.write` 会嵌入文件 mtime，跨机跨时不可复现；比字节必然假红）；
2. 产物满足 §3.1 的全部不变量（这条同时是 §5.5 assert 的正向对照）；
3. **产物的压缩包清单（archive manifest）与 fixture 的清单一致，且满足 §3.2 的形态契约**：`namelist()` 恰好 1 项、无目录项、后缀 ∈ {`.sqlite`, `.db`}。

⚠️ **第 3 条是 codex R1 finding 3 打回后补的，缺了它这套测试就复刻了缺陷 A 的那道缝**：只比数据库内容的话，生成器哪天把 `arcname` 改成 `x.txt`、或多塞一个 sidecar 文件，**数据库内容一字不变**，而 App 侧吃的是已提交的静态 fixture、也一字不变 —— **两条测试同时保持绿**，正好放过缺陷 A 那一类。清单名不含 mtime，比对是确定性的。

**必配的生产者侧变异**（证明第 3 条有判别力）：
- **把 `assemble_from_windows:403` 的 `f"{fname}.db"` 改成 `f"{fname}.txt"` → 须红**（这是**生产真正的决策点**，也是 codex R3 指出原方案够不着的那一个）；
- 把 `zip_and_hash:371` 的 `arcname` 改成不被接受的后缀 → 须红；
- 往压缩包里多写一个 regular 文件 → 须红。

⚠️ 前两条是**两个不同的绕过点**（调用方选名 vs 打包时改名），必须**各配一档**；只留其中一条会让另一条的绕过方式静默通过（本仓「同一条判据有多种正交绕过方式」那条教训）。

⇒ 生成器日后若改动索引方案**或压缩包形态**，这条会红，fixture 不会悄悄过期。

**(b) App 侧（Swift）**：把 (a) 产出并提交进仓库的 fixture 喂给**真实生产实现**跑完整链路：
`DefaultZipIntegrityVerifier` → `DefaultZipExtractor` → `DefaultTrainingSetDBFactory.openAndVerify` → `DefaultTrainingSetDataVerifier.verifyNonEmpty` → `DefaultFileSystemCacheManager.store`，断言全过。

⚠️ **防空转下限**（本仓反复踩过「测试写了却测不到」）：该测试**必须**先断言 fixture 本身具备特征 —— 「存在一个非 m3 周期，其 `end_global_index = 0` 的行数 ≥2」。若哪天有人用一份没有老历史的 fixture 替换它，这条断言先红，而不是让整条测试静默退化成空转。

⚠️ 该 fixture 同时是 §5.1(a)/(b) 的**正向档**：它必须含「同一周期内多根并列于 0 且 datetime 各不相同」这一形态，否则次序键与 datetime 单调两条判据在这条链路上取不到值。

**为什么合成 bar 数据不违反「fixture 必须照生产真实格式造」**：被测的是**产物的生成路径**，而该路径用的是**生产函数本人**；禁忌是「照想象手写产物格式」，不是「喂合成输入」。fixture 文件本身由生产代码写出，不是手搓的。

---

### 5.7 顶层 `CONTRACT_VERSION` bump `"1.13"` → `"1.14"`（codex R3 打回后新增）

⚠️ **原 §7 只考虑了训练组的 `PRAGMA user_version`，漏掉了顶层标识。**

冻结治理契约 `docs/governance/m01-schema-versioning-contract.md` §Bump 策略 **A 类**明列的触发条件里，本次命中**两条**：

- 「**改既有语义**」—— 持久化字段 `end_global_index = 0` 从「未定义/隐式」变为「该 K 线整个覆盖窗口至多触及 `.m3` 首根」的**明确语义**；
- 「**任何跨系统契约字段调整**」—— zip 成员名的生产者/读取端契约（`.sqlite` → `.sqlite` 或 `.db`）。

**先例**（同文件 bump 记录，2026-05-25 E2）：`position_data` 从「任何字节」收紧为「合法否则拒收」，**无任何 DDL 变更、纯读取端语义**，照样 bump 顶层 `"1.4"` → `"1.5"`，三套存储 sub-version 全部不动。本次形态与之完全一致。

**做法**：`PRAGMA user_version` 保持 `1`（物理 schema 确实没变）；顶层 `CONTRACT_VERSION` `"1.13"` → `"1.14"`，并**同步全部 5 个点**：

| # | 位置 | 内容 |
|---|---|---|
| 1 | `ios/Contracts/Sources/KlineTrainerContracts/Models/Models.swift:7` | `public let CONTRACT_VERSION = "1.13"` → `"1.14"` |
| 2 | `backend/qmt_pilot_db.py:827` | `CONTRACT_VERSION = "1.13"` → `"1.14"`（旁注已写明须与 Swift 侧一致） |
| 3 | `ios/Contracts/Tests/.../ModelsTests.swift:8`、`RenderStateBuilderTests.swift:1260` | 两处 `#expect(CONTRACT_VERSION == "1.13")` 同步 |
| 4 | `docs/governance/m01-schema-versioning-contract.md` | 矩阵顶层 cell `"1.13"` → `"1.14"` **且**追加一条 bump 记录（触发条件、先例、为何三套 sub-version 不动） |
| 5 | 跨语言一致性测试 | `backend/tests/test_qmt_pilot_db.py:835` 断言 Swift 源码里含 `CONTRACT_VERSION = "<同值>"`，两侧不同步会红 —— 这条是天然守卫，**不需要改**，但实施时要知道它会挡住只改一边 |
| 6 | `ios/Contracts/Tests/KlineTrainerPersistenceTests/M01MatrixSyncGuardTests.swift` | ⚠️ **codex R4-F2 打回后补的**：该测试 `:52` **把 `"1.13"` 写死**在断言里（`XCTAssertEqual(r["\`CONTRACT_VERSION\`（顶层标识）"], "\`\"1.13\"\`")`）。按第 4 项把矩阵改成 `"1.14"` 之后，**这条测试必然变红**。故它是**必须同步修改的第 6 个点**，不是「会保持绿的守卫」。同时其 `:70-76` 的**解析器双向自检**用的是一份「三行仍是旧值、但 bump 记录里出现新值」的样本 —— 该样本以 `1.12→1.13` 为素材，须刷新为本次 `1.13→1.14`，否则自检验的是上一次 bump |

⚠️ **原文在这里写错过**：曾声称 `M01MatrixSyncGuardTests`「会保持绿、能挡住漏改第 4 项」。实际是**改了第 4 项它就会红** —— 按原清单执行，验收判据 4b 根本不可能通过。教训与 §4.2 同类：**写「某某守卫会兜住」之前，必须打开那个守卫看它到底断言了什么**。

⚠️ **必须提前知道的连带后果**：`backend/qmt_pilot_db.py:2273`（闸 1）会拿 `meta["contract_version"]` 与常量逐字比对，不等即拒绝复用该库。bump 后，**任何带 `"1.13"` 的既有 QMT pilot 库/清单都会被拒**，需 `--reset` 重建。当前风险很低（4b `qmt_fetch.py` 仍零实现、pilot 出货链未投产），但**必须在实施计划里明写这条**，不能让它在别人手里变成意外。

⚠️ 本 bump **对 App 运行期行为零影响**（`CONTRACT_VERSION` 在 iOS 侧只是常量与断言，不参与任何判定）；它的作用是**让改动前后的构建在兼容性与回滚审计上可区分**。

---

### 5.8 面板联动对「并列编号」的处理（codex R5-F2 新增 —— **本次交付切成两片**）

**问题**（已核实读 `PanLinkage.swift` 全文）：

- `rightEdgeTick` 返回右缘那根的 `endGlobalIndex`。413 根月线全是 `0` ⇒ 只要右缘落在老历史里，**返回值恒为 0，位置信息整个丢失**；
- `followerOffset` 用该 tick 经 `currentCandleIndex`（首个 `endGlobalIndex >= tick`）定位 ⇒ 命中**最老的那一根**。

⇒ 把月线往回拖进老历史，联动的另一面板**跳到 1991 年**；即便同周期做一次 offset→tick→offset 往返，几百个不同位置也会**塌缩成同一个**。

⚠️ 这是**本次改动引入的**：此前含重复编号的数据在读取端就被拒，走不到渲染。**它恰好损害本次要交付的那个体验**（§2.1 用户点名要「老历史当普通历史显示」—— 显示得出来、却拖不动，等于没交付完）。

**修法（设计层定，实施切片二做）**：联动的语义本来就是**时间对齐**（RFC #4 原名「两面板 pan **时间**对齐联动」），tick 只是当年在「编号与时间同序且唯一」前提下的等价替身。该前提已被并列打破，故：

> `rightEdgeTick` 同时给出右缘候选的 **`datetime`**；`followerOffset` 定位改为「**最后一根满足 `datetime <= 目标 datetime` 且 `endGlobalIndex <= 当前 tick`（不越过已揭示前沿）的 K 线**」。
>
> 在无并列的数据上，该规则与现有 tick 规则**逐点等价**（编号在无并列时随 datetime 单调），故对既有行为零影响；并列时由 datetime 决出唯一位置。

**必配测试**：① 同周期 offset→tick→offset 往返，数据含**多根** `endGlobalIndex == 0`，断言几百个位置**不塌缩**；② 跨周期联动，leader 右缘落在老历史里，断言 follower 落到**时间对齐**的那一根而非最老一根。

**⚠️ 切片与措辞纪律**：

| 切片 | 内容 | 交付后可以说 |
|---|---|---|
| **PR-1** | §5.1 / §5.1b / §5.2 / §5.3 / §5.4 / §5.6 / §5.7 | 「真实训练组可被下载、校验、入库、显示」 |
| **PR-2** | §5.8 联动改造 | 「老历史**可浏览**」 |

⛔ **PR-1 合并后不得声称「老历史可浏览」** —— 那时它显示得出来但拖不动，联动会跳。PR-1 的验收清单必须**明写这条已知限制**。

---

## 6. 判别力要求（实施阶段硬约束）

本仓的假绿家族反复踩过，故每条新判据都必须配**变异验证**，且逐条记录「红的是哪一条」：

| # | 变异 | 期望（须写明红的是哪一条测试） |
|---|---|---|
| M1 | 把 §5.1(c) 的 `.m3` 分支从 `<= prev` 放宽成 `< prev`（允许重复） | 「m3 编号重复」用例变红（证明 m3 轴一条没被顺手放宽） |
| M2 | 删掉非 `.m3` 的精确反算 | 「编号被改小」与「跨界 K 线标 0」两条用例变红 |
| M3 | 把精确反算换成「非递减 + 范围内」（= R1 之前的强度） | 「编号被改小」仍须红 —— 这正是 R4-F1 指出的漏洞 |
| M3b | 把精确反算换成「仅 `egi==0` 时校验覆盖窗口」（= R1–R3 的 v2 强度） | 「编号被改小」仍须红 |
| M4 | 删掉 §5.1(b) 的 datetime 严格递增 | 「某非 m3 周期 datetime 乱序」用例变红 |
| M4b | 让 §5.1(b) 只对 `.m3` 生效（= 退回现状） | 同上用例仍须红（防「推广」只写了没接上） |
| M5 | 删掉 §5.1(a) 的 `, datetime` 次序键 | 构造「零桶各行的插入顺序 ≠ datetime 顺序」的库：有次序键则读取成功，无则 datetime 判据报错。⚠️ 若实测该用例在本机 SQLite 上**非确定性**，改为「SQL 文本含该次序键」的结构断言，并在报告里写明改用了结构断言及原因 |
| M6 | 删掉 §5.2 的 `.db` 分支 | §5.6(b) 变红 |
| M7 | 把 §5.2 改成「任何后缀都接受」 | 「zip 内是 `x.txt`」用例变红 |
| M8 | 把 §5.6 的 fixture 换成没有老历史的 | 防空转断言变红 |
| M9 | 删掉 §5.6(a) 第 3 条（清单比对） | M9b 与「多一个 regular 项」两个生产者变异变绿 ⇒ 本条须让它们红 |
| M9b | 把 `assemble_from_windows:403` 的 `.db` 改成 `.txt` | §5.6(a) 清单比对变红 |
| M9c | 把 §5.6(a) 改回「自己造 `db_path` 串调底层函数」 | M9b 由红转绿 ⇒ 判定为**削弱**，不许采纳 |
| M10 | 只改生产读取器、**不改** `PreviewTrainingSetReader` | 预览侧「含重复 0 的真实格式数据应放行」**正向**用例变红 |
| M10b | 预览侧保留旧 `<= prev`、不做精确反算 | 预览侧两条**行为**用例同时变红：①「重复 0 应放行」②「编号被改小应拒」 |
| M11 | 删掉 §5.1(e) 的下界判据 `s <= endGlobalIndex`（两个读取器各删一次） | §5.1(e) 那两条拒绝用例变红：①末根开盘晚于整条 `.m3` 轴 ②整根落在 `.m3` 时间缺口内 |
| M11b | 把 §5.1(e) 用 (c) 的精确反算「取代」（= R5 打回的原写法） | 同上两条仍须红 —— 证明上界与下界**互补而非冗余** |
| M12（PR-2） | 把 §5.8 的 follower 定位改回「按 tick 取首个 `>= targetTick`」 | ①同周期往返不塌缩 ②跨周期时间对齐 —— 两条联动用例变红 |

⚠️ **M2 与 M4 必须互不掩盖**：本仓踩过「两条判据互相掩盖、单独变异都不红」。故除逐条单变异外，**必须补一次 M2+M4 组合变异**，确认组合下仍有测试红，且**记录红的是哪一条**。

**正向对照（必配，防「全是拒了」的套件掩盖恒抛守卫）**：三个**真实**训练组（`851f9444` / `32892a5f` / `150d8d6c`）与 §5.6 的 fixture 必须**被放行**；任一条判据若恒抛，这些正向档会红。

⚠️ `KlineTrainerPersistence` 相关测试注意本仓既有陷阱：Catalyst scheme 必须是 `KlineTrainerContracts-Package`，`set -o pipefail`，新增 UIKit-gated 测试要同步四处基线。本设计的改动均**不在** UIKit-gated 文件内，但闸门仍按既有流程跑。

---

## 7. 兼容性与迁移

| 维度 | 结论 | 理由 |
|---|---|---|
| 训练组 SQLite schema | **不变** | 无新列、无类型变化；`training_set_schema_v1.sql` 一字不动 |
| `TRAINING_SET_SCHEMA_VERSION` / `PRAGMA user_version` | **不 bump（保持 1）** | 物理 schema 未变（无新列 / 无类型变化 / 无约束变化） |
| 顶层 `CONTRACT_VERSION` | **必须 bump `"1.13"` → `"1.14"`（§5.7）** | ⚠️ **codex R3 打回后补的**：原文只看了训练组 sub-version。冻结治理契约 A 类触发命中两条（改既有语义 / 跨系统契约字段调整），且有「无 DDL 的纯读取端语义收紧照样 bump 顶层」的先例（2026-05-25 E2，`1.4`→`1.5`） |
| 已生成的训练组（含 NAS 上这 3 个） | **全部继续可用** | 产物零改动；`content_hash` 不变 |
| 用户手机上已缓存的训练组 | **不受影响** | 缓存里存的是解压后的 sqlite，文件名由 `meta.filename`（`.zip`）规范化而来，与 zip 内条目名无关（`DefaultFileSystemCacheManager.swift:145-159`） |
| 旧版本 App 读新训练组 | **与今日相同（仍失败）** | 本次不改产物，故不产生「新数据老 App 读不了」的新错位 |
| 新版本 App 读旧训练组 | **可用** | 见下一行的限定 |
| §5.1(b) 新增「全周期 datetime 严格递增」 | **收紧之一** | 可拒掉周期内 datetime 重复/乱序的训练组。**实测三个真实训练组：重复数 0/0/0、次序一致 0/0/0** ⇒ 现有产物不受影响。被拒者本就是坏数据（渲染次序无定义） |
| §5.1(c) 非 `.m3` 改为**逐根精确反算** | **收紧之二（本次最强的一条）** | 拒掉一切「编号与 `.m3` 轴对不上」的数据，含 R4-F1 那类「取值被改小 ⇒ 安静泄露未来行情」。**实测三个真实训练组：非 m3 共 4365 / 4240 / 4366 根，逐根反算不一致 = 0 / 0 / 0** ⇒ 现有产物完全满足 |

⇒ 本次**不需要**任何迁移脚本。

⚠️ 「接受集合是超集」这句只对 `end_global_index` 与扩展名两条成立；datetime 那条是收紧，故上表**不写**「纯放宽」。

---

## 8. 明确不做（YAGNI / 范围外）

1. **路线 B**（专用标记列或哨兵值）—— §2.2 已否决，记为后续可选演进；
2. **后端改产出 `.sqlite`** —— 用户已选「改 App」；
3. **`expose.sh` 看门狗继承 flock 导致窗口期内 `close`/`renew` 恒 `LOCK_TIMEOUT`** —— 2026-08-30 实测三次稳定复现（`/proc` 三重证据），**真缺陷但与本设计无关**，独立小 PR；
4. **App 后端地址不可配（PR-3 从未实施）** —— `KlineTrainerApp.swift:18` 仍硬编码 `http://kline-trainer.local`；本次真机验收靠临时改一行绕过，**PR-3 仍 OPEN**；
5. **每只股票可切片段数受限** —— 往前需 8 个完整月且逐日 1 分钟数据完整，而 1 分钟源数据只有约一年 ⇒ 每股约 3–4 个可用月起点（实测 `000001.SZ` 落在 2025-09-01 与 2025-11-03）。**要量靠多铺股票，不是同股反复切**。属出货量规划，非本次范围；
6. **`verifyNonEmpty` 把「起点前 ≥30」施加到全部周期**（契约 `:752` 只说月线）—— 当前真实数据全部满足（各周期 before cap 为 120/150），**不改**，仅记录该处比契约更严；
7. **后端 `generate_training_sets.py` 一行不改** —— 初稿曾计划在生成器侧加断言，R4 后整条撤回（§5.5 记录了原因：与读取端精确反算重复，属同义反复）；
8. **缓存文件名那一族 `.sqlite` 判据**（`DefaultFileSystemCacheManager.swift:151` / `:165` 与其 fake `InMemoryFakes.swift:722`）—— **刻意不动**。它们规范化的是接口返回的 `meta.filename`（`.zip`），与**压缩包内成员名**无关（§7 已论证缓存路径不受本次影响）。此条明写，是为了让「§5.1b 的全仓扫描漏了这三处」这个疑问当场消解 —— 它们不属于被改的那一族。

---

## 9. 验收判据

1. 三个真实训练组（NAS 上 id=3/4/5）经**真实生产实现**跑完 §5.6(b) 那条链路，六关全过；
2. **§6 的全部变异逐条跑过 —— PR-1：M1、M2、M3、M3b、M4、M4b、M5、M6、M7、M8、M9、M9b、M9c、M10、M10b、M11、M11b，外加 M2+M4 组合变异（共 18 组）；PR-2：M12**；每组**报告里写明红的是哪一条测试**（不接受「有测试红了」）。
   ⚠️ 本条在 codex R2 被打回过：当时验收门只列到旧编号的前 7 条，而后续新增修复对应的变异恰好落在门外 —— 实施者可以**满足验收门却没证明新修复有判别力**。**新增判据必须同步进验收门**，否则等于没加；
3. 正向对照全部放行（三个真实训练组 + §5.6 的 fixture，在**生产读取器与预览读取器两侧**都放行）；
4. 后端与 App 两侧闸门按既有流程全绿（后端 Linux CI 零 skip；Catalyst 按既有配方）；
4b. **§5.7 的 6 个同步点全部改到**（含 `M01MatrixSyncGuardTests.swift:52` 的写死值与 `:70-76` 的解析器自检样本），且 `test_qmt_pilot_db.py:835`（跨语言一致性）全绿 —— 它能挡住「只改一边」；
5. **真机**：runbook P14 的 G2（状态行 3 个成功）+ G3（库里 3 行变 `sent`）+ G4（图表画出蜡烛图）全部成立；
6. **（PR-2）真机补验**：把月线往回拖进 2020 年以前的老历史，联动面板**时间对齐**、不跳到最老一根。
   ⛔ **PR-1 阶段此条不成立且不得回避** —— PR-1 的验收清单必须明写「老历史显示得出来，但拖动联动会跳，归 PR-2」（§5.8）。

⚠️ 判据 5 需重跑 runbook P12–P14；判据 1–4 在 CI 内即可闭合。

---

## 10. 残留与后续

| 编号 | 内容 | 状态 |
|---|---|---|
| TS-R1 | 路线 B（早于轴的 K 线专用标记）作为后续可选演进 | OPEN，无时限 |
| TS-R2 | `expose.sh` 看门狗锁缺陷 | OPEN，独立小 PR |
| TS-R3 | PR-3：App 后端地址可配（`KLINE_BACKEND_BASE_URL`） | OPEN，独立 PR |
| TS-R4 | `verifyNonEmpty` 的「全周期 ≥30」比契约 `:752` 更严 | 已记录，不改 |
| TS-R5 | 出货量规划：每股可用起点约 3–4 个 | 已记录，非本次范围 |

## 11. 评审轮次记录

| 轮 | 结果 | 处置 |
|---|---|---|
| （配额失败） | 无 verdict —— Codex 报 `You've hit your usage limit`，日志 1030 字节、连代码都没读 | **不计轮次**（本仓规矩：无 `Verdict:` 即非判决），原 SHA 重跑 |
| **R1** @ `a3e8c01` | `needs-attention`，3 条 high | **三条全部核实属实、全部采纳**（见下） |

**R1-F1（high）后端校验放在了重试之外** —— 属实。核实方式：读 `generate_one_training_set` → `build_training_windows`（内含重试）→ `assemble_from_windows:384 → assign_global_indices` 的调用链，确认 `select_valid_window` 只捕获 `_try` 抛的异常。原文「正好接进既有 bounded retry」**是错的**。已改：校验移进 `_try`（§5.5），并要求「第 1 候选坏、第 2 候选好」的落地测试 + 变异 M7b。

**R1-F2（high）`0` 桶的校验证明不了纯历史** —— 属实。原 §4.2 只论证了「开盘 ≤ 轴首根」，未约束收盘；跨界 K 线被标 0 会造成未来数据泄露。已改：§4.2 重写并降级原实测为旁证，§3.1 / §5.1(d) 新增覆盖窗口约束（生成器公式在 `egi==0` 情形下的逐字反演），变异 M9 / M9b。**新约束在三个真实训练组上实测违反数 0 / 0 / 0**（`egi==0` 的行分别 940 / 816 / 942 根），可收紧且不误伤。

**R1-F3（high）测试没接上 zip 形态那道缝** —— 属实。只比数据库内容 + 静态 fixture ⇒ 生产者把 `arcname` 改成 `.txt` 或多塞 sidecar，两侧测试都不会红，正好复刻缺陷 A。已改：§5.6(a) 增第 3 条「压缩包清单比对 + 形态契约」与两个生产者侧变异，变异 M10。

⚠️ **R1 无一条被判误报**。三条都改到了根因位置（不是改症状）。

| 轮 | 结果 | 处置 |
|---|---|---|
| **R2** @ `596cd65` | `needs-attention`，1 high + 1 medium | **两条均核实属实、全部采纳** |

**R2-F1（high）漏了第二个执行同族判据的读取器** —— 属实。`PreviewTrainingSetReader.validateCandles:70` 是生产读取器的显式镜像（文件头写明「production 改了这里同步改」），现为对所有周期一刀切的 `<= prev` ⇒ 不改则真实格式数据在预览/调试路径被全判死，且缺 (b)(d) 两条新判据 ⇒ 测试与生产**双向背离**。已新增 §5.1b。

⭐ 借这条把**判据族在全仓穷尽了一遍**（本仓明令「按判据本身穷尽，不是按手头文件块」）：`endGlobalIndex` 序关系闸门全仓恰好 **2** 处、datetime 单调闸门 **2** 处、`s <= egi` 闸门 **2** 处，其余命中全是消费方。⇒ 不会再有第三处。

⭐ 同时发现一处**必须写明的不对称**：生产读取器的数组来自 SQL 排序，「倒退」分支不可达；**预览读取器的数组是调用方内存直给、无排序保证 ⇒ 该分支可达**，必须配真行为测试而非不变量锁。照抄生产端结论会写出零判别力的测试。

**R2-F2（medium）验收门停在 M1–M7** —— 属实。M8–M11b 恰是 R1/R2 之后新增修复对应的变异，不进验收门 = 新修复的判别力没人验。已改 §9 判据 2：**16 组变异全覆盖**，并写明「新增判据必须同步进验收门」这条纪律。

⚠️ **R2 同样无一条被判误报。**

| 轮 | 结果 | 处置 |
|---|---|---|
| （配额失败 ×2） | 无 verdict（日志 990–1030 字节，连代码都没读） | **不计轮次**，同 SHA 重跑 |
| **R3** @ `25b06f8` | `needs-attention`，1 high + 1 medium | **两条均核实属实、全部采纳** |

**R3-F1（high）生产者测试够不着真正选文件名的那一行** —— 属实。核实：`assemble_from_windows:403` 才是决定 zip 成员名的地方（`f"{fname}.db"`），`zip_and_hash:371` 只是把 `db_path.name` 抄成 `arcname`。原方案让测试自己造 `db_path` 再串调底层三函数 ⇒ 对 `:403` **零敏感**：把它改成 `.txt`，测试产物名不变、App 侧静态 fixture 也不变，**两条测试全绿而每一次真机下载都会失败**。已改 §5.6(a)：fixture **必须经 `assemble_from_windows` 产出**；新增变异 M10b（改 `:403`）与 M10c（把方案改回去须让 M10b 由红转绿 ⇒ 判定为削弱）。

⭐ 顺带识别出**两个正交的绕过点**（调用方选名 `:403` vs 打包时改名 `:371`），各配一档 —— 本仓「同一条判据有多种正交绕过方式」那条教训。

**R3-F2（medium）漏了顶层 `CONTRACT_VERSION` bump** —— 属实。原 §7 只看训练组 sub-version。冻结契约 `m01-schema-versioning-contract.md` §Bump 策略 A 类命中两条（改既有语义 / 跨系统契约字段调整），且**有「无 DDL 的纯读取端语义收紧照样 bump 顶层」的先例**（2026-05-25 E2，`1.4`→`1.5`）。已新增 §5.7：`"1.13"` → `"1.14"`，列出 5 个同步点、两条既有守卫，并**明写连带后果**（`qmt_pilot_db.py:2273` 闸 1 会拒绝带旧版本号的既有 pilot 库，需 `--reset` 重建；当前 4b 未投产故风险低）。

⚠️ **R3 同样无一条被判误报。**

| 轮 | 结果 | 处置 |
|---|---|---|
| **R4** @ `f057bb2` | `needs-attention`，1 high + 1 medium | **两条均核实属实、全部采纳；其中 F1 促成设计升级** |

**R4-F1（high）非零编号同样能安静泄露未来行情** —— 属实，且**比前几轮更根本**。此前三轮我一直在给 `0` 这一格打补丁；R4 指出：一根开在 `.m3` 第 10 根、覆盖到第 49 根之前的 K 线被标成 `10`，**严格递增、唯一、在范围内、`s <= egi` 全过**，而渲染层认「`egi == 当前 tick` ⇒ 已完成 ⇒ 画整根」，于是把 tick 11–49 的行情画了出来。**有序性根本约束不住取值**。

**处置 = 把契约升级成「逐根精确反算」**（§4.2 v3 / §3.1）。这一条**吃掉了此前三条较弱规则**（非递减、重复只在 0 上、`0` 桶覆盖窗口），并**连带撤回了后端那条断言**（§5.5：升级后它成了同义反复，零判别力）。

⭐ **实测支撑**：三个真实训练组逐根反算，非 `.m3` 共 **4365 / 4240 / 4366** 根，**不一致 = 0 / 0 / 0** —— 强规则完全可满足。

⚠️ **这次撤回后端改动不是推翻 R1-F1**：R1-F1 说的是「**若**加该断言，位置必须在 `_try` 内」，其实质已作为**条件性规则**保留在 §5.5，供日后任何生成器侧拒绝逻辑参照。

**R4-F2（medium）版本 bump 清单漏了一个会被改红的测试** —— 属实。`M01MatrixSyncGuardTests.swift:52` **把 `"1.13"` 写死**在断言里；按 §5.7 第 4 项改矩阵，它**必然变红**，而原文却声称它「会保持绿、能挡住漏改」⇒ 验收判据 4b 按原清单**不可能通过**。已补为第 6 个同步点，并要求刷新其 `:70-76` 的解析器自检样本（现以上一次 bump 为素材）。

⭐ 教训与 §4.2 同类：**写「某某守卫会兜住」之前，必须打开那个守卫看它到底断言了什么**。本设计两次栽在同一个动作上（R1-F2 的「既有校验已自限」、R4-F2 的「既有守卫会兜住」）。

⚠️ **R4 同样无一条被判误报。**

| 轮 | 结果 | 处置 |
|---|---|---|
| **R5** @ `a6a76bb` | `needs-attention`，1 high + 1 medium | **两条均核实属实、全部采纳；F2 导致交付切成两片** |

**R5-F1（high）精确反算只管上界，不能取代下界判据** —— 属实，**已亲手验算反例**：`.m3 = [100,200,300]`，某周期单根 K 线开盘 `400`、`egi = 2`。末根规则给 `upper = 300` ⇒ `derived = 2` ⇒ **精确反算放行**；而既有下界判据算出 `s = 3`、`3 <= 2` 为假 ⇒ **正确拒绝**。「整根落在 `.m3` 时间缺口内」是同一形态。

⚠️ **这是我在 R4 修复中亲手引入的新洞**：§5.1b 曾指示预览读取器把 `:130`（下界判据镜像）**改为**精确反算 —— 等于拆掉一道必要防线。已订正为「**保留 + 增补**」，两个读取器都同时持有上界与下界两条判据（§4.2.1 / §5.1(e)），并配两条拒绝用例 + 变异 M11 / M11b。

⭐ 教训：**R4 那句「它同时蕴含了……因此不再单列」，蕴含关系只在我列举的那几条上成立，我却顺手把一条不在列举范围内的既有判据也一并替换掉了。**「新判据吃掉旧判据」必须逐条核对被吃掉的那些**到底是不是**推论。

**R5-F2（medium）并列编号毁掉面板联动** —— 属实。读 `PanLinkage.swift` 全文确认：`rightEdgeTick` 返回右缘那根的 `endGlobalIndex`（413 根月线全 `0` ⇒ 位置信息丢失），`followerOffset` 再按「首个 `>= tick`」定位 ⇒ 命中**最老那一根**。⇒ 把月线拖进老历史，联动面板跳到 1991 年；同周期往返也把几百个位置塌缩成一个。

⚠️ **这是本次改动引入的**（此前含并列的数据在读取端就被拒），且**恰好损害本次要交付的那个体验**。已新增 §5.8：联动定位改为**时间对齐**（无并列时与现有 tick 规则逐点等价，零行为影响），并把交付**切成两片** —— PR-1 契约与读取器，PR-2 联动改造；⛔ **PR-1 合并后不得声称「老历史可浏览」**，其验收清单必须明写该已知限制。

⚠️ **R5 同样无一条被判误报。五轮 11 条，全部属实、零误报。**

**变异编号在 R4 整表重排过**（共 15 条 + 1 组合）：因 R4 撤回后端改动、并把三条弱规则合并成一条，旧编号已无法一一对应。**§6 表是唯一权威**；本节 R1–R3 记述里出现的编号是**当时的**编号，仅供追溯，**不得**拿去对照 §6 表。§9 验收门已按新表同步。

---

## 附：措辞纪律

本次**不得**声称「真实数据链路已打通」「pilot 已完成」——本设计只解除两条阻断；**真机 G2/G4 通过之前**，「App 能消费真实训练组」这句话没有证据。
