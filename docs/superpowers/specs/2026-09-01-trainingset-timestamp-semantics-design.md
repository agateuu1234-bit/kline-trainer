# 训练组时间戳语义与索引订正（**后端 + 运维**，切片一）

> 日期：2026-09-01 · 分支 `feat/trainingset-app-contract-fix` · base `main@1437529`
> 由 `2026-08-30-trainingset-app-contract-design.md` **拆分而来**（那份 13 轮评审后涨到 1013 行、同时管五个子系统、严重度不降反升 —— user 2026-09-01 拍板切开）。
> **本片只改后端与运维产物，不碰任何 App 逻辑。** App 侧（读取判据 / 缓存回收 / 渲染 / 联动）归切片二，另立 spec。

---

## 1. 问题（全部实测，非推演）

### 1.1 仓库里有两套 `datetime` 标注约定，从未对过账

| 周期 | `datetime` 标的是 | 证据 |
|---|---|---|
| `3m` / `15m` / `60m` | **收盘时刻** | 实测 60m = `10:30 / 11:30 / 14:00 / 15:00`；15m 首根 `09:45`；3m 首根 `09:33`（A 股 9:30 开盘）。重采样器函数签名即 `_bucket_label_minute(close_min, minutes)` —— **有意为之** |
| `daily` | 该交易日 `00:00` | `2026-04-30 00:00` |
| `weekly` | 该周**首个交易日** `00:00` | `2026-04-27` / `2026-02-24`（春节后首个交易日，非周一） |
| `monthly` | 该月**首个交易日** `00:00` | `2026-04-01 / 03-02 / 02-02` |

而 `assign_global_indices`（`backend/generate_training_sets.py:273-296`）的注释写着「覆盖区间 **`[开盘, 下一根开盘)`**」，对**所有**周期一律 `upper = 下一根 datetime − 1`。

⭐ **切分本身没错**：实测每天 `3m` 80 根 / `15m` 16 根 / `60m` 4 根，与 A 股 4 小时交易严丝合缝，午休已处理。**缺的只是这两套约定的对账。**

⭐ **日线及以上标 `00:00` 不是缺陷**：`00:00` 到当日首根 3m（`09:33`）之间无数据，故按「下一根开始 − 1」反算与「上一交易日 15:00 收盘」**逐点一致**。

### 1.2 后果一：日内周期的「形成时刻」晚一整个周期

实测 `000001.SZ_1756656000`，2026-04-20 四根 60m：

| 标 | 库里编号指向 3m 收于 | 它其实形成于 | 差 |
|---|---|---|---|
| 10:30 | 11:27 | **10:30** | 19 根 |
| 11:30 | 13:57 | **11:30** | 19 根 |
| 14:00 | 14:57 | **14:00** | 19 根 |
| 15:00 | **次日 10:27** | **15:00** | 19 根 |

逐根新旧对照（同一产物）：

| 周期 | 旧值 − 新值 分布 | 说明 |
|---|---|---|
| `15m` | **4**（2573 根）· 3（1 根）· 0（120 根） | 一根 15m 含 **5** 根 3m ⇒ 常规位移 **4** |
| `60m` | **19**（643 根）· 8（1 根）· 0（142 根） | 一根 60m 含 20 根 3m |
| `3m` | 全 0 | 两式同解 |

⚠️ **位移不是常量**：0 的那些是轴前 K 线（新旧都 clamp 到 0），`3`／`8` 落在轴边界，末根可能不动。**迁移与测试一律不得依赖位移常量，必须逐根重算。**

### 1.3 后果二：开盘侧用「下一根」当边界，而周线序列**允许有洞**

`select_period_window`（`:100-104`）**故意删掉**跨训练起点 / 跨 `after_end` 的那根周线（既有测试 `test_generate_training_sets.py:118-129` 起点设**周三**、明写 `assert "0629" not in got`）。被删那一周的 3m 数据**在轴上是存在的** ⇒ 洞之前那根周线会被判成「在洞里的某个时刻才完成」。

**实测**：三个真实产物里，「按下一根」与「按真实日历周期」不同的根数 ——

| 周期 | 000001.SZ_1756656000 | 600519.SH_1762099200 | 000001.SZ_1762099200 |
|---|---|---|---|
| `daily` | 0 | 0 | 0 |
| **`weekly`** | 0 | **1** | **1** |
| `monthly` | 0 | 0 | 0 |

⇒ **三个包里两个已经带着这个错误**（这 3 个包起点恰好都是周一，未触发「删跨界周」；错的是别处的周历边界）。

### 1.4 为什么藏到今天

仓库里**没有任何一条测试把「生成器真实产物」喂给「App 读取器」**。两侧各自的测试都绿，缝在中间无人负责。

---

## 2. 契约变更

### 2.1 每周期 `datetime` 语义（**本次新立**）

> `klines.datetime` 的语义**按周期区分**，任何依赖它的计算**必须**显式区分：
> - **`3m` / `15m` / `60m`**：该 K 线的**收盘（结束）时刻**；覆盖区间 `(上一根 datetime, 本根 datetime]`。
> - **`daily` / `weekly` / `monthly`**：该 K 线所属周期的**开始侧标识**；覆盖区间 = **该日历周期本身**。

### 2.2 `end_global_index` 的定义

> 含义 = 「这根 K 线在全局 3 分钟轴上**于第几刻形成（完成）**」。逐根精确等于：
>
> ```
> 收盘标注（3m / 15m / 60m）：  upper = 本根 datetime
> 开盘侧标注（daily/weekly/monthly）：
>     upper = period_end(本根 datetime, 周期)      ← 本根所属【日历周期】的结束时刻
>             daily   → 该交易日 23:59:59
>             weekly  → 该 ISO 周的周日 23:59:59
>             monthly → 该自然月最后一刻
>             （一律 Asia/Shanghai）
>
> derived = clamp( bisect_right(m3_datetimes, upper) − 1 , 0 , n3 − 1 )
> end_global_index == derived
> ```
>
> ⛔ **开盘侧不得用「下一根 − 1」**：该序列**允许有洞**（§1.3）。
>
> `.m3` 自身另有不变量（`global_index == end_global_index == 数组下标`，从 0 起严格递增无缺口），**本片不改**。

### 2.3 产物代际标识

> 迁移前后的产物**互不可读**（同一张表、同一组列，但 `end_global_index` 取值语义变了）⇒ 属**两代**，`PRAGMA user_version` **`1` → `2`**。
>
> 依据是冻结 schema 文件自己写的：`training_set_schema_v1.sql:3` ——「schema 变更时 bump `user_version`，旧 version reader 直接拒收」。
>
> ⚠️ **判断依据是「新旧产物能否互读」，不是「列有没有变」。**

---

## 3. 改动清单（本片）

### 3.1 抽出 `period_end(datetime, period)` 纯函数

放在生成器侧，**供本片的索引计算与迁移脚本共用**。切片二的读取端校验将 import 或按同一定义实现 —— ⚠️ **两侧各写一份正是本次整个缺陷的成因**，切片二必须复用而非重写。

- 时区**必须** `Asia/Shanghai`；
- 周期→标注约定用**显式常量表**（`_CLOSE_LABELLED = {"3m","15m","60m"}`），**不得**用「周期名里有没有 `m`」之类的字符串把戏。

### 3.2 `assign_global_indices()` 按语义分流

`:288-292` 改为 §2.2 的两分支。其余（`bisect_right` / `clamp` / `global_index` 仅 `3m` 赋值）**一律不动**。

### 3.3 版本常量

| # | 位置 | 改动 |
|---|---|---|
| 1 | `backend/sql/training_set_schema_v1.sql:5` | `PRAGMA user_version = 1` → `2`（`:2`/`:3` 注释同步） |
| 2 | `backend/generate_training_sets.py:316` | 同上字面量。⚠️ `:313-314` 注释解释了「为何写字面量而非 f-string」（验收 grep 锚），**保留该理由** |
| 3 | `backend/generate_training_sets.py:39` | `SCHEMA_VERSION = 1` → `2` |
| 3b | ⛔ **`kline_trainer_modules_v1.4.md:2302`（R03，冻结契约）** | 现文为「`global_index/end_global_index` **严格递增** + 前后端 assert」，**与本片 §2.2 直接矛盾**。⇒ 必须在**本片**改写：`.m3` 保持严格递增且 `global_index == end_global_index == 下标`；**其它周期**为「逐根等于按 §2.1 语义反算的值」（允许重复，但重复由反算式决定而非任意）。并在该文件修订记录里留一行。<br>⚠️ **codex S1-R6-F1 打回后补**：母 spec 本有此项，写切片一时**漏掉了** —— 又是「改了做法没改契约」那一层。不改它，后端 v2 会在**两条互相矛盾的权威规则**下发布；切片二或日后照 R03 写的任何守卫，会**把每个迁移后的包都判死** |
| 3b-2nd | ⛔ **`kline_trainer_plan_v1.5.md:1285`（第二份根级权威）** | 原文「**后端 Index 预计算**：`global_index` / `end_global_index` 必须严格递增；后端 assert + 前端 DEBUG 校验」—— 与 R03 是**孪生条款**，说的是同一条规则。<br>⚠️ **Opus 评审 C5 打回后补**：3b 只处理了 `modules_v1.4.md`，穷尽范围**漏了这一份**。不改它，3b 自己给出的理由（「后端 v2 会在**两条互相矛盾的权威规则**下发布」）原样成立。⇒ **与 R03 同样改写**，并在该文件修订记录里留一行 |
| 3b2 | ⛔ **`kline_trainer_modules_v1.4.md` 的其余版本引用** | 同一份冻结契约里还有三处写着 `1`（codex S1-R7-F3，已核实）：`:146` 矩阵「训练组 SQLite `PRAGMA user_version` = 1」、`:1882` 注释「`TRAINING_SET_SCHEMA_VERSION = 1`」、`:2239` 清单「`TRAINING_SET_SCHEMA_VERSION = 1` 双方共享常量」。⇒ **一并改为 2**，否则切片二或日后维护照着这份权威文件把读取端留在 `expectedSchemaVersion: 1`，**会拒掉每一个迁移后的包**。<br>⛔ **三处都必须同时带【过渡态标注】**（Opus 评审 M5）：`:2239` 的字面语义是「**双方**共享常量」，只把值改成 `2` 会让冻结契约声称「前后端共享 = 2」，而 3c 的切片一守卫**同时**把「App 侧 = 1」认证为正确 —— 谁拿冻结契约去写读取端就会直接写 `2`、越过 §8 的次序；谁拿它去核 App 都会判 App「未同步」。**3b 反对留下 R03 的理由在这里被本片自己反向再造了一次**。<br>标注写法（`:2239` 例）：「`TRAINING_SET_SCHEMA_VERSION = 2` 双方共享常量（⚠️ 2026-09 切片一起：产物已第 2 代、**App 侧仍为 1**，直到切片二落地；过渡态的权威定义见 `CONTRACT_VERSION 1.14 / 1.15`）」。3c 的切片一断言随之改为「断言这三处**带有该过渡态标注**且值为 `2`」 |
| 3c | 守卫 | ①**在【后端与治理文档】作用域内**（`kline_trainer_*.md`、`docs/governance/`、`backend/`）不得再出现「全周期严格递增」这一旧表述（含 R03 原句）。<br>⛔ **作用域不得写成「全仓」**（Opus 评审 C5）：`ios/.../DefaultTrainingSetReader.swift:146` **逐字包含 R03 原句**、`:80` 是同一表述的中文版，而 `:90-92` 正是执行该判据的**生产代码**。写「全仓」则守卫**恒红**，要它变绿就得改 App 源文件 —— 直接违反本片开头「不碰 App 逻辑」与 3c 自己的「App 读取端仍钉在 1」。这与 3c 刚修掉的那条矛盾是**同一个形状**，只是换了判据。切片二把作用域扩到 `ios/`（见 §8）；②**分阶段**的版本守卫（codex S1-R8-F1 打回后订正）——<br>**切片一断言**：冻结契约三处（`:146`/`:1882`/`:2239`）**值为 `2` 且各自带有 3b2 规定的【过渡态标注】字样**（⚠️ **Opus 评审 R2-M3 打回后补**：3b2 承诺「3c 的断言随之改为要求带标注」，而上一版**根本没落到 3c 这一行** —— 实施者把值改成 `2`、忘了加标注，守卫照样全绿，M5 想堵的洞原样留着，B70 也没有可红的东西）、R03 与 `kline_trainer_plan_v1.5.md:1285` 已按 3b / 3b-2nd 改写；**后端生产侧**（`training_set_schema_v1.sql` / `generate_training_sets.py`）均为 `2`；**且 App 读取端仍显式钉在 `1`**（`DownloadAcceptanceRunner.swift:14` 与 `TrainingSessionCoordinator:1307`）；<br>**切片二断言**：三方同时为 `2`（收敛守卫）。<br>⚠️ **上一版把「三方同时为 2」写成切片一的守卫，与本片 §8「App 侧版本改动归切片二」及开头「本片不碰 App 逻辑」直接矛盾** —— 照 §8 做则守卫恒红，照守卫做则越界改了 App 验收契约。**本片没有任何自洽的实现能通过自己的验收**。现在这条把「生产者已升代、读取端尚未」这个**有意的过渡态**写成可断言的事实，而不是留成矛盾 |
| 3d | ⛔ **既有验收 grep 锚**（Opus 评审 M4；口径 = 全仓 grep 得出）<br>`$ grep -rn "PRAGMA user_version = 1" scripts/ docs/acceptance/` → 2 命中（`plan_b2_generate_training_sets.sh:30`、`2026-05-29-…md:46`）；加 `plan_1f_m0_1_schema_versioning.sh:48-50` 的矩阵行锚 ⇒ **合计 3 处** | 「保留该理由（验收 grep 锚）」那句「保留该理由（验收 grep 锚）」原本**指向一个本片从没定位过的东西**。实测锚在：<br>· `scripts/acceptance/plan_b2_generate_training_sets.sh:30` —— `grep -q 'PRAGMA user_version = 1' backend/generate_training_sets.py`<br>· `docs/acceptance/2026-05-29-pr-b2-generate-training-sets.md:46` —— 同一条，写在验收表里<br>· `scripts/acceptance/plan_1f_m0_1_schema_versioning.sh:48-50` —— 断言 m01 矩阵那行是 `1`<br>⇒ 三处期望值改 `2`。<br>⚠️ **诚实说明**：这两个脚本当前**已经是陈旧的**（`plan_b2_…sh:20` 的 `rng.randint(30, n - 9)` 锚实测已 MISS，`set -e` 下跑不到第 30 行；`plan_1f_…sh` 的 `CONTRACT_VERSION` 行还停在 `1.5`）⇒ 它们**不会**在 CI 里变红。仍必须明确处置的理由：本仓的教训是「**陈旧的测试与验收门会主动把缺陷固化成预期行为**」，留着它们等于留下三处说「= 1 才对」的成文判据。<br>⛔ **只改本片相关的那一行**，⛔ 不顺手修这两个脚本先前遗留的其它 stale 行（范围蔓延）。<br>第 2 项的「保留该理由」同时改为「保留该理由**并把锚点路径写进注释**」 |
| 4 | `docs/governance/m01-schema-versioning-contract.md` | 矩阵「训练组 SQLite `PRAGMA user_version`」`1` → `2`；触发条件由「训练组 schema **结构**变更」**扩写**为「结构变更**或字段语义变更导致新旧产物互不可读**」 |
| 4b | `backend/sql/tests/test_training_set_schema.sh:10-13,38` | ⚠️ **CI 硬断言 `user_version = 1`**（codex S1-R2-F3）—— 不改这里，本表**第 1 项**（`training_set_schema_v1.sql` 改 `user_version`）一落地 schema CI 立刻红。期望值与末行 PASS 文案同步改 `2` |
| 4c | ⛔ **`backend/tests/test_generate_training_sets.py:143-146`** | ⚠️ **Opus 评审 M1 打回后补**：这是**唯一**直接锁住 `end_global_index` 取值的测试，改公式后**三条断言全红**，而上一版的改动清单里根本没有它（4b 是 codex S1-R2-F3 打回后才补的**同一类**遗漏，本轮没把判据推到整个文件族）。<br>fixture `_index_windows()`（`:57-65`）：`3m=[0,10,20,30,40,50]`、`15m=[0,30,60]`、`60m=[-100,-90,40]`、`monthly=[-100,20]`。<br>**新期望值（手算，非跑生成器得出）**：<br>· `15m`：`[2, 5, 5]` → **`[0, 3, 5]`**（upper 依次 0/30/60 ⇒ `bisect_right` 1/4/6，各 −1）<br>· `60m`：`[0, 3, 5]` → **`[0, 0, 4]`**（upper −100/−90 落在 3m 轴之前 ⇒ clamp 到 0；40 ⇒ 5−1=4）<br>· `monthly`：`[1, 5]` → **`[5, 5]`**（−100 与 20 都落在 1970-01，月末 `2649599` 已超 3m 末根 ⇒ 均 clamp 到 5）<br>⛔ **期望值必须手写**（§4.1 note 3）：把改后生成器的输出抄回断言 = 生产者与校验者一起用错公式而全绿。<br>⚠️ **`monthly` 这组改后两根都被 clamp 成 5、判别力归零** ⇒ **另加一个 monthly 专用用例**，时间戳取真实量级（如 2026-04 / 2026-05 的自然月），断言逐根等于按 §2.2 反算的值 |
| 4d | `backend/generate_training_sets.py:16` 与 `:274-276` | 模块头「`# - D4 end_global_index = bisect_right(3m_dts, [open,下一open) 上界) - 1`」与 `assign_global_indices` 的 docstring「覆盖区间 `[open, 下一根 open)`」**正是 §1.1 引为缺陷证据的那段旧表述**。⚠️ §3.2 写的「其余一律不动」若按字面执行，改完后函数的自述会与它的行为**直接矛盾**（Opus 评审 M1 附带项）⇒ 这两处注释必须同步改写 |

**关于 PostgreSQL 的 `training_sets.schema_version DEFAULT 1`（`schema.sql:75`）—— 刻意保留，不改**：

- 它只在**写入方漏给该字段**时才生效；

⚠️ **原文那句「本仓只有两个写入路径且都显式给值」是错的**（codex S1-R3-F2，已逐条核实）：`docs/runbooks/2026-08-24-qmt-nas-deployment.md:613` 与 `:645`（P9 两条烟测 `INSERT`，**本次会话我自己执行过**）、`backend/sql/migrations/0004_.../rehearse.sh:206/:213/:443` **都省略了 `schema_version`**。⇒ 按原文的「全路径守卫」会**当场失败**，或被迫排除这些合法 SQL，`DEFAULT 1` 依旧能在 v2 之后**静默把行标成第 1 代**。
- 改它属 **DDL 变更** ⇒ 需要一份受治理的 migration，**且会让 P6b 形状闸门的 `schema.sql` md5 锚失配、必须重新生成整份闸门文件** —— 代价与收益不成比例；
**改法（必做，三步，次序不能反）**：
1. **先**把【可执行路径】里所有 `INSERT INTO training_sets` 补齐 `schema_version` —— 实测 **10 处**：
   · `docs/runbooks/2026-08-24-qmt-nas-deployment.md:613`、`:645`（P9 两条烟测）
   · `backend/sql/migrations/0004_.../rehearse.sh:206`、`:213`、`:443`
   · ⛔ **`.github/workflows/schema-smoke.yml:73`、`:76`、`:79`、`:82`、`:85`（五条 `psql` 可执行 SQL，全部省略该字段）**
     —— ⚠️ **Opus 评审 M2 打回后补**：上一版只列了 5 处，做完第 1 步再加第 2 步守卫，守卫**仍会当场红 5 处**，实施者被推进的正是本节自己警告的那个失败形态，而且连「哪些该排除」都没有判据；
2. **守卫作用域必须成文**（不能靠临时判断）：只扫 `backend/**`、`docs/runbooks/**`、`.github/workflows/**` 三类**可执行**路径；`docs/superpowers/plans/**` 与 `docs/superpowers/specs/**` 是**历史记述**，显式白名单并注明理由（口径见下）；
   ⭐ **计数口径（R3-M2 订正，R3 自查再订正）**：只报**可执行路径内【缺 `schema_version` 需要补】的数目 —— 10 处**，就是上面逐条列出的那些。
   ⚠️ **不是「可执行路径内的命中数」**：那三类路径下实测共 **12** 处命中，另 2 处（`backend/generate_training_sets.py:497-498`、`docs/runbooks/…-p11-…sql:63-64`）**本来就带 `schema_version`**，不需要改、但**必须被守卫覆盖**。两个数不能混。
   可复跑命令与当次输出：
   ```
   $ grep -rn "INSERT INTO training_sets" backend/ docs/runbooks/ .github/workflows/ | grep -v 'in query'
     → 12 行；逐条看后两行是否含 schema_version ⇒ 有 2 处、缺 10 处
   ```
   ⛔ **不再写「仓内共 N 处」这种含自身的总数**：本片与母 spec 里每写一句关于 `INSERT INTO training_sets` 的话，这个数就大一 —— 上一版写 27、而修 R2-M1 时新增的两句限定条款各自又含一次该字面量，当场变成 29。**同一个计数连栽三轮**（R1「已知 5 处」实为 10+、R2 白名单按行号写、R3 被自己的编辑推翻）。论证文本里的引用**不计入**，也不需要计数；
   ⛔ **作用域内还要再排除一类**（Opus 评审 R2-M1）：`backend/tests/**` 里出现在 `if "INSERT INTO training_sets" in query:` 这类**字符串谓词**中的命中不计（`test_b2_reconnect_integration.py:208`、`:699`）—— 它是 test double 的分派条件、不是 SQL，**结构上永远不可能「出现 schema_version」**，不排除则守卫恒红；
   ⛔ **必须明写匹配窗口**：以**一条 SQL 语句**为单位（从 `INSERT INTO training_sets` 起，到该语句的 `;`，或 Python 里相邻字符串字面量拼接结束），**⛔ 不得按单行 grep** —— 实测在场 12 处语句里 **5 处跨行**（`generate_training_sets.py:497-498`、`…-p11-….sql:63-64`、`rehearse.sh:206`/`:213`/`:443`），**7 处单行**（`schema-smoke.yml:73/76/79/82/85`、`deployment.md:613`/`:645`，**补上字段后仍是单行**）。按行匹配会把那 **5 处跨行**的**误报为缺字段**，所以必须按语句。
⚠️ **上一版写的是「在场每一条真 `INSERT` 都是多行的」，这是被 7 个反例推翻的假全称断言**（R4-M3）。规则本身正确且必要，但依据不成立 ⇒ 实施者一核就找到反例，最省事的结论是「依据不成立、按行 grep 就行」，**把正确的规则一起丢掉**。本仓教训：**穷尽性主张必须按字面量枚举后逐条定性**。这条匹配规则本身要配一次变异自测：把 `schema_version` 从 `:498` 挪到别处 → 守卫必须红；
3. **再**加机械守卫：断言作用域内每一处该 `INSERT` 都出现 `schema_version`。
⚠️ **次序反了会出事**：先加守卫会立刻红，进而诱使实施者「把那几处排除掉」—— 正好把缺口原样留下。
⚠️ **`.github/workflows/` 在本仓有单独的 push 仪式**（细粒度 token、单独一次 push）—— 实施计划里必须把它列成独立一步，否则会在最后一步撞上。

⚠️ **残留 TS1-R1（明写，本片不做）**：更强的做法是**去掉 PG 的 `DEFAULT 1`**（漏给字段直接 fail-closed）。不做的理由：属 DDL 变更 ⇒ 需受治理 migration，**且会让 P6b 形状闸门的 `schema.sql` md5 锚失配、必须重新生成整份闸门**。⇒ 日后 `training_sets` 若因别的原因要改 DDL，**顺带把这个默认值一起去掉**。

**顶层 `CONTRACT_VERSION` `"1.13"` → `"1.14"`**（m01 §Bump A 类命中「改既有语义」+「跨系统契约字段调整」；先例 = 2026-05-25 E2 无 DDL 的读取端语义收紧照样 bump 顶层）。同步点：

| # | 位置 |
|---|---|
| 5 | `backend/qmt_pilot_db.py:827` |
| 6 | `ios/.../Models/Models.swift:7`（**仅常量，不碰任何 App 逻辑**） |
| 7 | `ios/.../ModelsTests.swift:8`、`RenderStateBuilderTests.swift:1260` 两处 `#expect` |
| 8 | `M01MatrixSyncGuardTests.swift:52` 写死的 `"1.13"`，及 `:70-76` 解析器自检样本（现以上一次 bump 为素材，须刷新） |
| 9 | m01 矩阵顶层 cell + 追加一条 bump 记录 |

⚠️ **为什么顶层 bump 放在本片而不是切片二**：留着不 bump 会让本片合并后**治理契约处于被违反状态**。第 6/7/8 项只是常量与断言，**不涉及 App 逻辑**，与「本片不碰 App」不冲突。

⛔ **切片二必须再 bump 一次到 `"1.15"`**（codex S1-R1-F3）：本片结束时，库存已是第 2 代，而 App **仍读不了它**（既不认 `.db`、也不认 `user_version = 2`）。若切片二不再 bump，**「读不了库存的中间态 App」与「能读的完成态 App」会共用 `1.14`** —— 跨语言一致性守卫在两种状态下都绿，这个标识就失去了兼容性与回滚审计的意义。

⇒ **版本身份**：`1.14` = 「产物已升第 2 代、App 尚不支持」；`1.15` = 「App 支持第 2 代」。此条写进 §8 交接。

⚠️ **连带后果**：`backend/qmt_pilot_db.py:2273`（闸 1）逐字比对 `contract_version`，bump 后**带 `"1.13"` 的既有 QMT pilot 库会被拒**，需 `--reset` 重建。当前 4b 未投产、风险低，但必须写进实施计划。

### 3.4 迁移既有产物（不重跑生成流水线）

**做法**（每个包）：解开 → **逐根按 §2.2 重算 `15m` / `60m` / `weekly`**（`daily` / `monthly` / `3m` 与其余所有列一个字节不动）→ **写 `user_version = 2`** → 重新压缩（成员名保持 `<code>_<start>.db`）→ 重算整包 CRC32。

**迁移清单（11 个目标）**：

| # | 目标 |
|---|---|
| 1-3 | NAS 部署目录 `…/kline-trainer/training-sets/` 的 3 个 zip |
| 4-6 | **NAS 上 P7 的来源目录** `/vol1/1000/agate1234/kline-trainer-handoff-20260814/training-sets/` 的 3 个 zip —— ⚠️ **最危险的一处**：runbook P7 是**从这里往部署目录拷**的，只改部署目录 ⇒ 下次干净重部署会用陈旧包覆盖 |
| 7-8 | ⛔ **NAS 上 `$DIR/sql/` 里那两份 SQL**（`…-p11-insert-training-sets.sql` / `…-p15-reset-training-sets.sql`）—— **真正被执行的是它们**：runbook `:140` 把仓库里的拷到 `$DIR/sql/`，`:864` 执行的是 `sql/…-p15….sql`（**本次会话我自己就是从那儿跑的 P15**）。<br>⚠️ **codex S1-R13-F2 打回后补**：我的同步清单只写了**仓库里**那两份，`$DIR/sql` 命中数为 **0** ⇒ NAS 上那份会留着旧指纹与旧版本，P15 的七元组身份闸当场失配、拒绝复位。「P15 期望与包、库原子同步」这句话**原本没有依据** |
| 9-11 | PostgreSQL `training_sets` 3 行的 `content_hash` **与 `schema_version`** —— ⚠️ **但身份判定不能只靠 id + 指纹**，见下 |

⛔ **PG 侧必须先过一道「完整七元组双向比对」的身份闸**（codex S1-R5-F2）：

我上一版的 11 目标清单只用 `id + content_hash + schema_version` 认行。**这是对本仓既有保护的倒退** —— `…-p15-reset-training-sets.sql:15-17` 白纸黑字记着：期望集合必须写成**完整七元组**并与表内容做**双向 `EXCEPT ALL`**，理由正是「**指纹只有 8 位、表上没有唯一约束，单靠它认不出身份**」。母 spec 也记录过 P15 因此被打回重写过两次。

⇒ **本片照搬那套判据**：

```
【同一个连接、同一个事务，从头贯到尾】：
  0  取 B2_GENERATION_LOCK_KEY（advisory，挡住生成路径）—— ⚠️ 这就是【静默期那一次】，全协议只取一次
  1  BEGIN
  2  LOCK TABLE training_sets IN ACCESS EXCLUSIVE MODE
  3  身份闸：
  · 期望七元组（stock_code / stock_name / start_datetime / end_datetime /
    schema_version / file_path / content_hash）与表内容做【双向 EXCEPT ALL】
  · 另行断言 3 行的【生命周期状态】（status / lease 三列）符合预期
     · 任一不符 → ROLLBACK，一个字节都不动
  4  【在这个事务仍开着的期间】完成 8 个文件（6 zip + 部署 SQL 2）的持久替换
  5  只按【精确匹配出的那 3 个 id】UPDATE
  6  复核全部字段（不只是 hash 与 version）
  7  COMMIT
  持有该事务的连接必须【全程存活】：连接断 = 锁释放 = 保护消失
```

⚠️ **上一版只写了一句 `LOCK TABLE` 而没定义事务边界**（codex S1-R6-F3）：PostgreSQL 的表锁是**事务级**的 —— 不在显式事务里 `LOCK` 会直接报错；而若「身份闸」与「更新」分属两个事务，**换文件的那段时间就是无保护的**，一条人工或未覆盖到的 `INSERT` 就能让 11 目标集合失效，最终留下「包字节与元数据对不上」。

**必配测试**：文件替换阶段并发发起 `INSERT INTO training_sets` —— 断言它被锁**阻塞**（而非插入成功）。

⚠️ 若省掉这道闸：一行 `file_path` 已经漂掉的行，照样能通过「id + 指纹 + 版本」的检查、被盖上新指纹与新版本，**然后 API 返回错误元数据或 404**。

**同时必须改的写死值**（全仓 grep `851f9444` / `32892a5f` / `150d8d6c` 得出）：
⭐ **计数口径同 §3.3「关于 PostgreSQL 的 `training_sets.schema_version DEFAULT 1`」一节里的「计数口径」小段**（⚠️ 上一版把它写成了**指向 §3.4 的一个步骤序号**，而那段口径其实在 §3.3；更糟的是 §3.4 里恰好有个真实但完全无关的同号步骤（代际预检）会把人接走 —— 这比悬空引用更坏，因为读者**能找到、但找到的是错的那一个**。本段刻意不再逐字写出那个旧写法，以免机械守卫把这句订正本身当成违例）：

**可复跑命令与当次输出**（③a 要求）：
```
$ for f in docs/runbooks/2026-08-24-qmt-nas-p11-insert-training-sets.sql \
           docs/runbooks/2026-08-24-qmt-nas-p15-reset-training-sets.sql \
           docs/runbooks/2026-08-24-qmt-nas-deployment.md \
           docs/superpowers/specs/2026-08-14-qmt-nas-deployment-design.md; do
      echo "$(grep -c '851f9444\|32892a5f\|150d8d6c' $f)  $f"; done
  3  …-p11-insert-training-sets.sql
  3  …-p15-reset-training-sets.sql
  4  …-qmt-nas-deployment.md
  7  …2026-08-14-qmt-nas-deployment-design.md      ⇒ 合计 17
```
（白名单那两个文件另有若干处论证引用，按口径**不计数、不列举**。）本表列的是**必改的 17 处**（p11 3 · p15 3 · runbook 4 · deployment-design 7）；白名单那两个文件里的命中属论证引用，**不计数、不逐条列**（它们会随每轮修订增减）。

| 位置 | 内容 |
|---|---|
| `…-p11-insert-training-sets.sql` | 3 处指纹 + **期望七元组里的 `schema_version`**（`:40/:42/:44` 各写死 `1`） |
| `…-p15-reset-training-sets.sql` | 3 处指纹（`:51/:53/:55`）+ 期望七元组里的 `schema_version`（`:50/:52/:54` 各写死 `1`）|
| `2026-08-24-qmt-nas-deployment.md:536-538` | P7 期望 CRC 三行 |
| 同上 `:704` | P11 期望指纹叙述 |
| `2026-08-14-qmt-nas-deployment-design.md` | **5 处**：`:55`、`:135`、`:144-146`（权威取值表）、`:262`、`:439` |
| Mac 本地 `~/qmt_trial_out/` | 3 个 zip —— **实测仍在**（`851f9444` / `150d8d6c` / `32892a5f`，`user_version = 1`）。处置见下方「Mac 副本」一节 |
| ⚪ **白名单（历史记述，不改）** | ⛔ **按【文件】整份白名单，不按行号**：`docs/superpowers/specs/2026-08-30-trainingset-app-contract-design.md`（理由：§8 明写保留为调查记录与切片二素材）、`docs/superpowers/specs/2026-09-01-trainingset-timestamp-semantics-design.md`（理由：本片自身的清单、论证与变异条目必须逐字引用这三个指纹）。<br>⚠️ **上一版按行号写，两条都错**（Opus 评审 R2-C2）：写的是本文件 `:208`，而那句话实际在 `:220`（文件从 718 涨到 774 行，行号没跟）；且同一次提交里 M3 的修复**自己新增了第 2 处命中**（`:229` Mac 那行）却没进白名单。⇒ 照它写死守卫，白名单会白名单掉一行毫不相干的代码，真正命中的两行没被覆盖，**闭合闸恒红**，把实施者逼进下面那句刚刚明令禁止的「临时追加」。**行号会漂，文件名不会。** |

⛔ **闭合闸必须带白名单**（Opus 评审 C3）：母 spec `:575` 的原句是「命中数必须为 0（**本设计文档记述历史的地方除外，须显式白名单**）」，切成本片时那半句**被丢掉了**，闸门从「可兑现」退化成「不可兑现」—— 因为母 spec 本身是本 PR 新增的、且 §8 明写要**保留**它作切片二素材。无条件的「命中 0」只剩两条路：删掉归档记录（违反 §8），或实施者当场临时发明排除规则（正是 §3.4 自己警告的失败形态）。
⇒ **白名单必须写死在守卫脚本里，并逐条注明理由**；⛔ 不得在闸门红了之后临时追加。

### Mac 本地 `~/qmt_trial_out/` 三个 v1 包 —— 明确作废为归档（Opus 评审 M3）

⚠️ **上一版只在写死值表里给了半行，然后从每一个机制里漏掉它**：不在 11 目标清单、不在备份集、不在代际预检、不在收敛闸、不在故障注入、不在 §7 任何一条验收里。而 `2026-08-14-qmt-nas-deployment-design.md:138` 白纸黑字写着「P7 的 zip **既可从 Mac scp，也可直接用 NAS 上那份**」⇒ 迁移后任何人按这句从 Mac 拷一次，就会把 **v1 包盖回部署目录**，而库里已是 v2 指纹 ⇒ 完整性校验失败或 404，**且 11 目标收敛闸全绿、看不出问题**。B11（只迁部署目录不迁 P7 来源）对这条路径**零判别力**。

**为什么不把它升为目标 12-14**：8 个文件全在 NAS、PG 在 NAS 容器里 ⇒ 迁移脚本必然跑在 NAS 上，它**够不着** Mac 的 `~/qmt_trial_out/`。硬要覆盖就得把一个 epoch 拆到两台机器上执行，代价远超收益。

**因此本片规定（必须在同一个 PR 里做完）**：
1. Mac 三个 zip **作废为 v1 审计归档**，⛔ 不再是 P7 的可选来源；
2. 改 `2026-08-14-qmt-nas-deployment-design.md:138` → 「P7 **只能**从 NAS handoff 目录取；Mac 副本仅作 v1 审计归档，⛔ 不得再 scp 到部署目录」；
3. 同文件 `:135` 加标注「历史记述，非判据」；
4. 配一条**文案守卫**：**作用域 = `docs/runbooks/**` + `docs/superpowers/specs/2026-08-14-*`**，断言其中不再出现「既可从 Mac scp」这一类表述；**白名单 = 本片 spec 与母 spec**（理由：本片的论证段与 B74 必须逐字引用那句原话）。
   ⛔ **不得写成「全仓」**（Opus 评审 R2-C3）。口径 = 全仓 grep 得出：
   ```
   $ grep -rn "既可从 Mac" . --exclude-dir=.git
     …2026-08-14-qmt-nas-deployment-design.md:138   ← 唯一要改的那处
     …2026-09-01-…-design.md（本片）× 3            ← 论证段 / 本条定义句 / B74 期望列
   ```
   ⇒ 4 命中，其中 **3 处就在本片自己身上**（论证段、这条守卫的定义句、B74 的期望列），且**都不能删**（删了 M3 的论证与变异条目就没了）⇒ 写「全仓」则改完 `:138` 之后守卫**仍恒红**。
   ⚠️ 这跟 3c① 刚刚因为同样的理由被收窄作用域是**同一个错**，而我在同一次提交里又犯了一遍 ⇒ **本片的三条文案/闭合类守卫（闭合闸、Mac 文案守卫、B75 的 `user_version = 1` 守卫）必须统一按同一套写法**：`作用域` + `白名单文件` + `每条白名单的理由`，三者缺一不可。

### ⭐ 一切以【epoch】为单位（codex S1-R13-F1 打回后确立）

本协议**每跑一轮**（首次迁移、回滚后重做、曝光后向前修）都是一个独立的 **epoch**，有自己的：

| 每 epoch 各一份 | 说明 |
|---|---|
| **epoch id** | 显式生成、写进清单与完成记录 |
| **阶段清单** | 记录本轮 11 个目标的「期望旧值 / 期望新值 / 状态」 |
| **备份命名空间** | `.bak.<epoch>` —— ⛔ 不同 epoch 的备份**互不覆盖、互不参与对方的判定** |
| **完成记录 / rollback 记录** | 均带 epoch id |
| **活跃 epoch 记录** | ⭐ **全局只有一份**，指认「当前未决的是哪一个 epoch」；见下 |

⚠️ **为什么必须 epoch 化**：曝光后的向前修是 **v2 → 修好的 v2**，它这一轮的「旧代」是**已曝光的 v2**；而本片又要求**原始的 v1 备份留到验收通过**。若两者共用一个备份命名空间，四态判定会拿 v1 备份去比「本轮的旧代（v2）」⇒ **判为「与旧代不符」→ 中止**；硬用它则会在半途把系统还原成**没人能消费的 v1**。⇒ **v1 备份属 epoch 1，只作重建与审计；epoch 2 备份的是当前已曝光的 v2。**

**必配故障注入**：在「**v1 备份集与 epoch 1 完成记录都还在**」的前提下跑 epoch 2 并中途中断，断言恢复只依据 **epoch 2** 的清单与备份，且 **epoch 1 的备份逐字节未被触碰**。

#### ⭐ 活跃 epoch 记录 —— epoch 化引入的新失效点（codex S1-R14-F2 打回后补）

⚠️ **上一版只说「每轮各有自己的一套」，却没说【重启后怎么知道自己在哪一轮】**。这不是措辞问题，而是 **epoch 化自己引入的一个新失效点**，且恰好落在最要命的边界上：

> epoch 1 **已完成**（v1 备份与完成记录都还在），epoch 2 跑到**一半被杀**。
> 重启的脚本看到的是一堆文件 —— 它**没有任何依据**判断「现在该认领 epoch 2」：
> · 若**新开 epoch 3**：它按规矩不许读 epoch 2 的备份，而 epoch 2 已经把一部分目标换成了新代 ⇒ **半截状态没有任何备份可依**；
> · 若**挑到 epoch 1**：epoch 1 的备份是 **v1**，而 epoch 2 的旧代是 **v2** ⇒ **还原成错误的一代**（正是 §「为什么必须 epoch 化」要避免的那件事）。

⇒ 「epoch 隔离」把「拿错备份」的风险从**内容层**推到了**选择层**；不定义选择规则，等于没解决。

**因此规定【活跃 epoch 记录】**：

```
· 存放在**专用目录**里，协议断言其中**至多一份未决**；与备份同一文件系统；
  原子创建（临时文件 → fsync → 改名 → fsync 父目录）
  ⚠️ 为什么不用「一个固定路径」：固定路径上第二次启动会**静默覆盖**掉前一轮的
  活跃记录 —— 而「上一轮未决」这个事实正是我们要保住的证据，覆盖掉它
  就等于回到「重启后不知道自己在哪一轮」。用目录才**表达得出**「多于一份」这个
  必须中止的形态（崩溃测试 3 才有东西可测）
· 内容 = epoch id + 类型（正向迁移 / rollback-to-v1 / 向前修）+ 本轮清单路径 + 备份命名空间
· 【它必须在任何备份创建与任何目标改动之前就位】
    ⛔ 先动数据后写记录 = 留下「有半截改动、却没有任何记录指认它属于哪一轮」的状态
· 只在【完成记录写下】或【rollback 归档】那一刻被【归档并清除】（归档，不是删除）
· ⭐ **每一个终止分支在返回之前，都必须幂等地归档清除【与本 epoch 匹配的】活跃记录**
    （codex S1-R15-F1）：「先写记录、后清活跃」这个次序本身是对的（记录必须先耐久），
    但它必然留下一个崩溃窗口 ⇒ **恢复路径有义务把后半截补完**。
    ⛔ 任何一条终止分支写「立即退出，不写任何东西」都会把 epoch 永久钉死
· ⛔ 活跃记录存在期间【不得开始另一个 epoch】——
    正向、回滚、向前修一律先认领它；要换类型必须先把它走完或显式回滚归档
```

**必配崩溃测试**（**四条**，缺一不可）：
1. 在「**活跃记录已就位、备份尚未开始**」处杀掉 → 重启必须**认领同一个 epoch**，且不产生第二份活跃记录；
2. 在「**epoch 1 完成记录 + v1 备份都在**，epoch 2 替换到一半」处杀掉 → 重启必须认领 **epoch 2**、只用 epoch 2 的备份，且 **epoch 1 备份逐字节未动**；
3. 人为放进**两份未决**活跃记录 → 脚本必须**中止报警**，⛔ 不得自动挑一个（挑错 = 拿错备份还原错代）；
4. ⭐ 在「**完成记录已 fsync、活跃记录尚未清除**」处杀掉（**正向与回滚各一次**）→ 重启后必须
   把活跃记录**归档清除**并干净退出，随后**必须能立刻开下一个 epoch**（回滚 / 向前修）。
   ⚠️ 这一条是「epoch 不被永久钉死」的唯一直接证据。

**执行协议 —— 全量备份 + 校验 + 失败重做（⚠️ 刻意**不**做「断点续做」）**：

```
0 静默：见下「静默期」——【所有写入方】停掉并持锁

0b 活跃 epoch 解析 —— ⚠️【**纯只读**，一个字节都不创建】（必须在代际预检【之前】：
   「旧代 / 新代」的期望值本身来自本轮清单）：
    · 【恰好一份未决活跃记录】 → 本次运行**必须认领它**，进入【恢复路径】：沿用其
      epoch id、清单、`.bak.<epoch>` 命名空间；⛔ 不得新开 epoch、⛔ 不得读写其它
      epoch 的清单或备份 → 直接进第 1 步
    · 【多于一份未决】       → ⛔ 中止并报警（不该出现的形态；自动挑 = 拿错备份还原错代）
    · 【不存在】             → 按下列**只读**判定分流，⛔ 此处仍不许创建任何东西：
        ⓐ 有【未归档的 rollback 记录】 → ⛔ 中止并打印指引：要重做迁移必须由操作者
           显式给出 `--new-epoch`（其语义含「确认归档该 rollback 记录」）
        ⓑ 有【有效完成记录】且 11 目标全为它声明的新代 → 【幂等空操作】直接退出，
           ⛔ 不生成 epoch id、不写清单、不建备份目录、不落任何文件
        ⓒ 其余（11 目标全为旧代、无 rollback 记录）→ 判定为「需要迁移」，进第 0c 步

0c 开新 epoch（⚠️ 只有 0b 判为「需要迁移」**且**操作者显式给了 `--new-epoch` 才执行）：
    · 没给 `--new-epoch` → 打印「将要做什么」后退出（dry-run），**不写任何东西**
    · 给了 → 生成 epoch id → 写清单（本轮 11 目标的期望旧值 / 期望新值 / 状态）
      → fsync 清单与父目录 → 原子改名就位活跃记录 → fsync 父目录；
      **这一切都在任何备份与改动之前**

⚠️ **0b / 0c 为什么必须拆开**（本轮自查发现，非 codex）：上一版把「不存在活跃记录
   → 就开新 epoch」直接写进 0b，于是**每一次**没有活跃记录的运行都会先造出
   epoch id + 清单 + 备份命名空间，然后才由第 1 步判断「其实没事可做」。这同时
   打断了本片自己的两条判据：
   ① §7 与正向对照③ 要求「已迁移后重跑 = **幂等空操作**」—— 而它每跑一次就多一个
      epoch 目录，不是空操作；
   ② 本节与 B44 要求「要再次迁移必须由操作者**显式**开新一轮 epoch，不得靠重跑脚本
      达成」—— 而脚本自己就在开，「显式」**没有任何机制兑现**（写了必须 X，却没核
      既有机制兑现得了 X）。

1 代际预检（⚠️ 必须在任何写入之前）：把 11 个目标各自判定为【旧代 / 新代】
    —— 期望值一律取自【本 epoch 清单】，⛔ 不得取自别的 epoch、也不得现场推断
    · 全部旧代 **且【没有有效的 rollback 记录】** → 继续第 2 步
    · 全部旧代 **但【存在有效 rollback 记录】** → 【先幂等归档清除活跃记录】
      再 ⛔【退出，不迁移】
        理由：回滚成功后的状态**正是**「全 v1」。正向脚本若只看代际，就会把
        「刚被人工回滚掉的状态」读成「该迁移了」，**把数据又推回 v2**——
        直接违反 B42。要再次迁移，必须由操作者【显式开新一轮 epoch】
        （归档旧的 rollback 记录并写入新 epoch 标识），不得靠重跑脚本达成
    · 全部新代 且【完成记录存在且可验】 → 【先幂等归档清除活跃记录】再退出
        ⚠️ ⛔ 不是「不写任何东西」（codex S1-R14 那版的原话）：完成记录先写、
        活跃记录后清，两者之间崩溃留下的正是「记录有效 + 活跃记录还在」——
        重启走到本分支若原样退出，活跃记录**永远清不掉**，而规则又禁止
        「活跃记录存在期间开新 epoch」⇒ 后续回滚与向前修**被永久锁死**，
        只能人工去动元数据（正是最不该手工动的东西）
    · 全部新代 但【完成记录缺失】 → 这正是「PG 已提交、记录未写」时崩溃的形态：
        重新对 11 个目标逐一算指纹复核 → 全对则 fsync 后【补写完成记录】
        并【归档清除活跃记录】再退出；
        任一不符 → 走【全量还原】回旧代（此时备份仍在），再从头重做
    · 混合     → 【先做还原】把已换的还原成旧代，再回到本步重判

2 备份（按【备份集状态】分支，⚠️ 不是简单的 no-clobber）：
    先判定备份集处于哪一态，再动作：
      · 【不存在】     → 全部创建
      · 【齐全且可验】 → 逐个校验其内容确为【旧代】；通过则【直接复用】，不重建
      · 【部分存在】   → 先校验已有的确为旧代，再【只补齐缺的那些】
      · 【存在但与旧代不符】→ 【中止并报警】，⛔ 不覆盖、不猜测
    · 备份内容 = 8 个文件的 .bak（6 个 zip + NAS `$DIR/sql/` 的 P11/P15 两份 SQL）
      + PG 3 行的【两份、用途不同、⛔ 不得合并】：
        ⓐ **还原用预映像** = `(id, content_hash, schema_version)`
           —— 回滚第 5 步**只 UPDATE 这两列**；⛔ 不含 status / lease 四列
        ⓑ **身份与路由用快照** = `(id, 完整七元组, status, lease_id,
           lease_expires_at, reserved_at)` —— **只读、只用于比对与审计，永不写回**
    ⚠️ **上一版把两者写成同一份「预映像」，两处定义还互相矛盾**（Opus 评审 R6-C1）：
      本步写的是「含完整七元组 + 生命周期状态」，而下方「PG 的还原方式也要改」那段
      写的是「只有 (id, content_hash, schema_version)」。合起来必然二选一地做错：
      按前者实现 ⇒ 回滚第 5 步会把 status / lease **一起写回迁移前的值**，
      若迁移前是 `sent`（本片必配测试②构造的正是这个形态）就**复活已消费库存**
      —— 正是 B45 明令要避免的；按后者实现 ⇒ 第 2 步的身份闸**拿不到生命周期**，
      备份里根本没存，闸门无从判定。⇒ 拆成两份，各司其职
    · ⚠️【每一份备份的创建本身也必须原子】：同文件系统临时文件 → fsync →
      与【仍是旧代的源】逐字节复验 → 互斥原子改名就位 → fsync 父目录。
      少了这步：拷到一半被杀/掉电会留下【半截 .bak】，而四态判定会把它
      当成「与旧代不符」→ 中止报警 ⇒ 明明旧目标还完好无损，却变成
      【无限期停机】，并逼操作者手工去动唯一的回滚副本
    · 对每个 .bak、预映像文件、【及其父目录】逐一 fsync —— 全部落盘后才许进第 3 步

3 执行 —— ⚠️【本步就是上面「同一个连接、同一个事务，从头贯到尾」那张表】：
    ⛔ 不得把「换文件」与「PG 更新」拆成两个事务（后果见下方 §「上一版只写了一句
       LOCK TABLE」；B34 / B35 两条变异正是钉这里）
    3.0 暂存目录产出【新包 6 份 + 新的部署 SQL 2 份】→ 逐个自校验
        · 两份部署 SQL 的新内容【由本 epoch 清单生成】，不得人工填指纹；
          生成后与仓库副本【逐字节比对】，不一致即中止（B51）
        · 对每个暂存文件 fsync（⚠️ 在开事务【之前】做完，别把打包耗时压在持锁期内）
    3.1 **在静默期已持锁的【同一个 PostgreSQL 连接】上** BEGIN → LOCK TABLE training_sets
        IN ACCESS EXCLUSIVE MODE
        ⛔ **不得在这里第二次取 B2_GENERATION_LOCK_KEY**（Opus 评审 R2-M4）：advisory lock 是
           **session 级**的。① 若另开连接取 → `pg_try_advisory_lock` 返回 **false** ⇒ 脚本在
           这里**确定性中止**，而中止点恰好是「备份全做完、8 个文件一个没换」，服务还被我们
           故意停着，本协议对「拿不到锁」**没有任何已定义的分支**；② 若同连接取两次 → session
           级锁**可重入计数**，取两次要放两次，而静默期只写了「释放锁」一次 ⇒ 该连接终生持锁，
           §7「**分库验收**」那一条去连生产库跑 P15 时会被自己挡住
           （⛔ **凡是带 `§N` 前缀的引用，一律按名，不得带序号** —— `§N.M`、「§N 第 M 条」、
             「§N 第 M 步」三种形式全禁，**不论它写在哪一节**。
             ⭐ **本条刻意用字母占位符 `N` / `M` 写出反例，而不是写具体数字** —— 这样守卫
             不需要为「规则定义与变异条目」另开白名单（本片其余三条文案类守卫都得开）。
             ⛔ 后续修订不得把它改回具体数字。被引条目须先有一个**不含数字的粗体短名**，
             且该名字在被引小节内**唯一命中、且该命中不是引用本身**。
             ⚪ **不带 `§N` 前缀**的同小节顺序引用（「继续第 2 步」「进第 0c 步」）**明确豁免**：
             它们与被引步骤在同一个代码块里一起被读，编号漂移当场可见。
             ⚠️ **判据必须按【书写形式】分类，不能按【目标在不在别的小节】**（Opus 评审 R6-M1）：
             R4-M1 那个真实缺陷写的是「§三点四 第一步」（此处刻意用汉字数字复述，避免触发守卫），
             而**它自己就在 §3.4 里**（真实目标在 §3.3）
             —— 守卫只读得到写下来的那个小节号，按「目标分类」会判成「同小节」而**放行**，
             恰好漏掉危险性最大的那一类：**写错小节号、且那一节恰好有个同号的无关步骤**。
             ⚠️ **上一版把作用域写成「任何小节」，当前树上实测 32 处违例**，其中 §3.4 协议内部
             的 `3.0`–`3.5` 与 `5` **根本没有名字可换**（R5-C1）—— 这是本片第三次写出「开局就红
             的门」，而且方向与前两次相反：前两次是**收窄**，这次是我主动**扩大**。
             ⇒ 判据的准入标准见 §7「**本 spec 自身的机械自检**」开头那条总则）
    3.2 身份闸：完整七元组【双向 EXCEPT ALL】+ 生命周期状态断言
        · 任一不符 → ROLLBACK，一个字节都不动
    3.3 【在这个事务仍开着的期间】逐个 rename 8 个文件到目标位置，
        每换一个就对其【目标父目录】fsync   ← 少了这步，rename 的目录项可能没落盘
    3.4 只按身份闸精确匹配出的那 3 个 id UPDATE（content_hash + schema_version）
    3.5 复核全部字段（不只是 hash 与 version）→ COMMIT
    ⚠️ 持有该事务的连接必须【全程存活】：连接断 = 锁释放 = 保护消失

4 校验：11 个目标逐一比对期望新指纹 + schema_version；全对才算成功
    · 写入并 fsync 一条【完成记录】，随后【归档并清除活跃 epoch 记录】
    · ⚠️ 未清除活跃记录 = 下次重跑会把【已完成的 epoch】当成未决去认领，
      「重跑应为幂等空操作」当场不成立

5 失败 / 中断 / 重启后：重跑本脚本 —— 第 0b 步先认领**同一个** epoch，
    再由第 1 步的代际预检把它导向
    「退出」「还原后重做」或「继续」；⛔ 在 11 目标复核一致之前，
    不得启回 api / scheduler
```

⚠️ **「全部新代」为什么要再分两态**（codex S1-R6-F2）：上一版只写「全新 → 立即退出」。可**PG 事务提交之后、完成记录写下之前**崩溃，得到的正是「11 个都新、记录没有」这个状态 —— 按上一版会**直接退出**：不补记录、不复核持久性、不还原，而服务还被我们**故意停着**，脚本对此**没有任何已定义的结局**。与 B25 及故障注入要求自相矛盾。

⚠️ **备份集为什么要分四态**（codex S1-R5-F1）：我上一版写的是「互斥创建，已存在则失败」。那条**让重启恢复变成不可能** —— ① 备份阶段崩溃 ⇒ 部分 `.bak` 已存在 ⇒ 重跑走到第 2 步直接失败；② 混合态还原之后备份集是齐全的 ⇒ 回到第 2 步同样失败。⇒ **我自己要求的故障注入用例根本过不了**，只能靠没写进文档的手工操作，而那恰恰会危及唯一的回滚副本。四态分支同时满足「不覆盖」与「可恢复」。

⚠️ **第 1 步（代际预检）是 codex S1-R4-F2 打回后新增的**：原文写「执行前无条件备份」，与另一条要求「重跑应是幂等空操作」**组合起来会毁掉唯一的回滚快照** —— 第二次跑时会拿 v2 状态覆盖 v1 备份，之后真机验收若失败就**没有任何一代可以回滚**。⇒ 预检 + no-clobber 两道一起，才能保证「第二次成功运行后，所有备份与预映像**逐字节不变**」（这条要写成测试）。

⚠️ **第 3 步的 fsync 是 codex S1-R4-F3 打回后新增的**：原文只对**备份**要求 fsync，没管**新写入**的那一侧。后果是 —— 校验时从页缓存读得到内容，但内容或 rename 的目录项**并未落盘**；掉电后可能出现「PG 已提交 v2 指纹、而包退回旧代或残缺」。**「校验即标记」这句话在重启后不成立**，所以第 4 步要落一条 fsync 过的完成记录，且第 5 步要求重启后必须重新复核 11 目标才允许启回服务。

⚠️ 还原路径同样适用上述 fsync 与原子 rename 规则（还原不是「随便拷回去」）。

### 迁移【已成功完成】之后的回滚模式（codex S1-R7-F4 新增）

⚠️ **上一版有个洞**：协议在「11 目标全新 + 完成记录有效」时**立即退出**；而本片又要求 `.bak` **留到切片二真机验收通过**才删 —— 这等于承认「验收失败时需要它们」，**却没有任何命令能从完成态把它们用起来**。⇒ 切片二若验收失败，`api` 必须继续停着，而操作者**没有一条已定义、已测试的路**把 8 个文件与 3 行还原回 v1。

**因此定义一个显式的回滚模式**（与正向迁移同样严格）：

```
rollback-to-v1（人工显式触发，绝不自动）：
  0  与正向同样的静默：api / scheduler / CLI 全停 + 取 advisory 锁
  1  BEGIN → LOCK TABLE training_sets IN ACCESS EXCLUSIVE MODE
  2  身份闸（v2 侧），⚠️【两段判据，性质不同，⛔ 不得混成一次相等比对】：
     2a 代际身份：按 **v2 的完整七元组**（含 content_hash + schema_version）
        做双向 EXCEPT ALL；不符 → ROLLBACK 中止
     2b 生命周期**只作【路由判据】**：任一行不是 `unsent`、或 lease 三列非空
        ⇒ 判为【已曝光 / 在途】⇒ 中止并指引改走 `recover-after-exposure`
        ⛔ **不得**把生命周期与备份里记下的旧值做**相等比对**
  3  校验 8 个 .bak（6 zip + 部署 SQL 2）与 PG 预映像齐全且确为 v1
  4  原子还原 8 个文件（临时文件 → fsync → 改名 → fsync 父目录）
  5  按 id UPDATE 回预映像（幂等）
  6  复核 11 目标全部回到 v1
  7  COMMIT → 归档（不是删除）完成记录，写 rollback 记录，
     并【归档清除活跃 epoch 记录】
  ⛔ 回滚完成前不得启回任何服务
```

⚠️ **回滚自己也要有预检与恢复态**（codex S1-R8-F2）：上一版把 PG 还原 `COMMIT` 排在「归档完成记录」**之前**。崩溃若发生在两者之间，得到的是「**11 目标全 v1、而 v2 完成记录还在**」——
· 重跑 `rollback-to-v1`：第 2 步的 **v2 身份闸**要求现状是 v2 ⇒ **直接中止**；
· 改跑正向迁移：它看到「全旧代」⇒ **又把数据迁回 v2**。
⇒ 服务全程停着，操作者**没有已定义的出路**，只能手工去动记录 —— 而那正是最不该手工动的东西。

**因此回滚必须有自己的预检**（与正向对称）：

```
rollback 预检（每次启动先跑）：
  · 先按同一规则解析【活跃 epoch】：有未决的就认领它、多于一份则中止、
    没有则新开一个【类型 = rollback-to-v1】的 epoch 并在动手前就位
  · 11 目标全 v2 + 有 v2 完成记录        → 正常回滚（走上面 0–7 步）
  · 11 目标全 v1 + v2 完成记录仍在(未归档) → 【补做归档 + 幂等清除活跃记录】
                                            并 fsync，然后退出
  · 11 目标全 v1 + 已有 rollback 记录     → 【先幂等归档清除活跃记录】再退出
                                            （rollback 记录已落盘、活跃记录未清
                                             正是回滚侧的同型崩溃窗口）
  · 文件/PG 处于混合态                   → 按备份继续还原到全 v1，再回本步重判
  · 备份集不齐或与 v1 不符               → 中止报警，⛔ 不猜测
⚠️ 陈旧的【正向完成记录】必须由本预检确定性处置（归档），
   ⛔ 不得留给正向迁移脚本去解释 —— 它会把「全旧代」读成「该迁移了」
```

⚠️ **第 2 步为什么要拆成 2a / 2b**（Opus 评审 R6-C2）：上一版写的是「七元组 **+ 生命周期状态**」一次相等比对。而 §7「分库验收」的 (b) 步会**在已迁移的生产库上跑 P15**，P15 按主键把 `status` 改成 `unsent`、lease 三列置 NULL（`…p15….sql:118-123`）—— 这是**本片自己安排的合法操作**。于是：若迁移前那 3 行不是 `unsent`（本片必配测试②构造的正是「已 `sent`」），跑完 (b) 之后现状与备份记的旧值**必然不等** ⇒ 回滚在第 2 步**确定性中止**。
而 `.bak` 被要求**留到切片二真机验收通过**才删 —— 也就是说，在备份存在的整个窗口里（正是它存在的理由：切片二验收失败要能回到已知良好起点），回滚**已经过不了自己的第一道闸**。⇒ 相等比对给出的是**假阴性**：代际明明还是 v2，只因一次合法的生命周期变更就把唯一的退路封死。
拆开之后：**代际**用相等比对（它确实不该变），**生命周期**只用来判「该不该走这条路」（B45 想防的正是「把生命周期纳入匹配」）。

⭐ **顺带补上 epoch 清单的一个定义缺口**：生命周期状态**不进清单的期望值**（它会被 P15 与切片二的正常操作合法改动）；0c 时刻只记一份**只读快照**，供审计与 2b 的路由参考。

⚠️ **上面这套只适用于【曝光前】回滚**（codex S1-R9-F2）——「迁完了、但手机还没下载过任何东西」。

### 曝光后的恢复 ——【**不是**回滚到 v1，而是**就地修 v2 向前走**】（codex S1-R11-F1 整节重写）

⚠️ **上一版的前提被推翻了。** 我写的是「还原到 v1 → 库存可用 → 再下载一次」。**那个状态不存在**：

| 客户端 | 能不能消费 v1 包 |
|---|---|
| **改动前的 App**（现网这版） | **不能** —— 它只认 `.sqlite`，而真实产物里是 `.db`（本片 §6.3 自己写着）；旧索引它也照样拒 |
| **切片二的 App** | **不能** —— 它要求 `user_version = 2` 并按新公式逐根校验 |

⇒ **没有任何一代 App 能消费 v1 包。** 把库存还原成 v1 再用 P15 放出来，得到的不是「恢复」，而是**一场停机**：行是可下载的，但每个客户端都拒。

**因此本片明确规定**：

1. ⛔ **v1 备份不是服务恢复手段** —— 它的用途只有两个：**重建**（迁移搞砸时回到已知良好的起点，好重来一次）与**审计**；
2. **曝光后（手机已下载过）的恢复，一律是「向前修 v2」**：

```
recover-after-exposure（人工显式触发）：
  1 ⭐【就是再跑一次本节的迁移协议，只是输入换成「修好的 v2」】——
    ⛔ 不得另造一个「简易修复分支」。必须复用同一套：
      · 同一份【11 目标清单】（部署目录 3 + P7 来源 3 + NAS `$DIR/sql/` 2 + PG 3 行）
      · 同一套【写死值同步清单】（§3.4 那张表：P11/P15 两份 SQL、runbook 两处、
        deployment-design 五处、Mac 本地副本）
      · 同一套【备份四态 + 原子 + fsync + 单事务 + 完成记录】
    ⇒ 重打包必然改 content_hash，而**P15 的期望七元组是本片自己要维护的**，
      在同一个协议里被一并更新，出协议时它已与库里的行匹配
  2 跑 runbook 既有的 P15 复位生命周期
    —— 此刻行是【修好的 v2】，而 P15 期望已在第 1 步同步过 ⇒【正好匹配】
  3 复位【前】必须满足 runbook 既有纪律：对外入口关闭 +
    手机上【长按删除 App 再重装】（否则上一轮下载的数据还在，G2/G4 观察作废）
  4 ⛔ 全程不静默改生命周期列 —— 复位是 P15 显式做的
```

⚠️ **上一版把第 1 步写成了一个轻量分支**（codex S1-R12-F1）：那样「重打包 ⇒ 指纹变」之后，
P15 仍拿**旧的写死七元组**认身份 ⇒ **确定性中止、不复位** ⇒ G4 失败后**没有任何重试路径**。
根因是我把「向前修」当成了迁移之外的东西 —— 它其实**就是一次迁移**，只是输入不同。
⇒ 现在明令复用同一套协议与同一份同步清单，指纹与 P15 期望在**同一个协议内原子地一起更新**。

3. **`rollback-to-v1` 的用途也要说准**：它把产物退回**已知良好的起点**，**为的是重做一次迁移**，⛔ **不是**为了让 App 去用 v1 数据。文档里任何「回滚后库存可用」的说法都作废。

⚠️ 若哪天真的必须让 App 消费 v1，那需要一个**经过测试的过渡版 App**（同时接受 `.db`、`user_version = 1`、旧索引语义），并且要跑**完整的下载 → 解压 → 打开 → 读取**链路才算数（只跑 HTTP 下载**不算**）。**本片不做，也不承诺。**


**必配测试**：
① **曝光前**：从已成功完成的 v2 状态执行 `rollback-to-v1`，断言 11 目标全回 v1、完成记录被**归档**（非静默丢弃），且断言其目的被正确表述为「回到已知良好起点以便重做迁移」——⛔ **不得**出现「库存可用」之类的断言；
② **曝光后**：构造「P14 走到 G3 之后、3 行已 `sent`、G4 失败」的场景，断言**曝光前那套回滚会明确中止并给出指引**；
③ **曝光后端到端（必须故意改动全部指纹）**：跑 `recover-after-exposure`，让修复**改掉全部 3 个 `content_hash`** → 用**真实的 P15 脚本**（其期望值由该协议同步更新）复位 → 再下载一次**能成功**；并断言**没有任何一步静默改过生命周期列**（复位是 P15 显式做的）；
④ 故障注入下沉到**每一次还原改名之后**、**PG `COMMIT` 之后**、以及**归档前后**各一次，断言每一处中断都能由预检确定性收敛（不出现「两个脚本互相把数据推来推去」）。

⚠️ **本节曾有一段「为什么不写三段状态机」的论证，现已【整段作废】**（codex S1-R7-F1）：那段话主张「不存在需要标记消歧的状态」「不需要 PG 反向事务，`pg_dump` 还原即可」——**两句都已被后续轮次推翻**（S1-R3-F1 指出 `pg_dump` 重放会主键冲突；S1-R6-F2 指出「11 个都新、记录未写」正是真实崩溃态（原文写 `9`，是补进 NAS 两份部署 SQL 之前的旧目标数，本轮已订正））。留着它会让实施者按**互斥的两套算法**行事，做出「不写完成记录」或「回滚撞主键冲突」的实现。

⇒ **上面那段编号流程（第 0 / 0b / 0c / 1 / 2 / 3 / 4 / 5 步，其中第 3 步已把事务结构逐条内联）是本节【唯一】的规范来源**；下面的说明只解释「为什么这么定」，不再重复定义算法。

⚠️ **本轮修掉的一处致命自相矛盾（Opus 评审 C1）**：上一版的第 3 步写的是「8 个文件全部就位**后**，PG 3 行在一个事务里更新并提交」，而本节前面那张表写的是「**在这个事务仍开着的期间**完成 8 个文件的持久替换」—— **两套互斥的算法**，且「唯一规范来源」这句话恰好指向**弱的那套**。照弱的那套实现，本节 §「上一版只写了一句 `LOCK TABLE`」自己论证过的后果当场成立：身份闸与更新分属两个事务，**换文件的那段时间完全无保护**；而 B34 / B35 两条变异在那套算法上**恒不成立**（它本来就没有 `BEGIN` 与身份闸）⇒ 又一次「没有任何自洽的实现能通过自己的验收」。另一个佐证：回滚模式用的一直是**强的那套**，正向却是弱的那套。

⇒ **用「可完整还原 + 可重复执行」换掉「可断点续做」**，状态空间从「每目标 × 各阶段」塌缩成「成功 / 未成功」两态。⛔ 实施时**不得**为了「省时间」加断点续做。

⚠️ **但「重做」有一个前提，我上一版漏了**（codex S1-R3-F1）：**重做能成立，前提是备份本身已经持久化**。原文那句「不需要 fsync」**是错的** —— 它只对「续做用的阶段状态」成立；**掉电时，只经过页缓存的 `.bak` 可能根本没落盘，而原件已被替换 ⇒ 仅此一份的原始产物永久丢失**。

**第 2 步（备份）必须做到**（⚠️ 本节旧版沿用的是加 0b/0c 之前的旧编号，已对齐正文协议）：

```
对 8 个文件的 .bak（6 个 zip + 部署 SQL 2）、PG 预映像文件、以及【它们各自的父目录】逐一 fsync；
全部 fsync 完成并复验可读之后，才允许进入第 3 步（执行 / 替换）。
（父目录也要 fsync —— rename 的原子性不等于目录项已持久）
```

⚠️ **PG 的还原方式也要改**（同条）：原文写「从 `pg_dump` 恢复」**不可执行** —— 那 3 行的 id 还在，重放 dump 会**主键冲突**，而不是把两列改回去。
**改为**：备份阶段存下 3 行的 **ⓐ还原用预映像 `(id, content_hash, schema_version)`**（与第 2 步定义的两份备份口径一致）；回滚 = 一条**按 id `UPDATE` 回该预映像**的**幂等**事务，**只写 `content_hash` 与 `schema_version` 两列**，⛔ status / lease 四列一律不动（B45 / B46）。

**故障注入必须覆盖两类边界**：进程被杀 **与** 掉电（后者用「不 fsync 就断电」的等价手段模拟），断言两类下都能还原到旧代。

**必配故障注入**：在第 3 步的**每个替换点之后**各中断一次（含 PG 事务提交前/后），断言：还原后 11 个目标**全部回到旧代**，且重做一次后**全部到达新代**。
⚠️ **中断点必须逐一枚举到 10 个**（Opus 评审 C2 订正）：**8 次文件 rename 之后各一次**（含**两份部署 SQL 各自的 rename**）+ **PG `COMMIT` 之前一次、之后一次** = 10。
⛔ 不是「11 个」—— 那 3 行在**同一个事务**里更新，物理上切不出「第 1 行已换、第 2 行未换」的中断点；为了凑够 11 而把 PG 拆成 3 个事务，正是 C1 与 B34 明令禁止的做法。
⛔ 也不得只覆盖 6 个 zip 与 PG。

⚠️ **验收必须真跑那两份【部署副本】—— 但两者跑在【不同的库】上**（codex S1-R14-F1 提出、S1-R15-F2 订正）：

上一版写「迁移后实际执行 P11 与 P15」，**这条判据自己不可能通过**：`…-p11-insert-training-sets.sql:47-61` 明写前置「`training_sets` 必须为空，不空就 `RAISE EXCEPTION`、一行都不动」（它的前置是 P10 已清空表）；而本迁移**刻意保留那 3 行的 id 与生命周期状态**。⇒ 在已迁移的生产库上跑 P11 **确定性失败**；为让它过而先清表，是一次**计划外的破坏性操作**，会丢掉行 id 与 `status`/lease 状态。

**订正后的验收程序（每一步必须写明【针对哪个库】）**：

| 步 | 目标库 | 跑什么 | 断言 |
|---|---|---|---|
| a | **一次性克隆库**（用完即弃，schema 与生产同源，`training_sets` **初始化为空** = P11 的成文前置） | 依次执行 **NAS 上那两份部署副本** P11 → P15 | 两者的七元组身份闸**都通过**；且它们的期望值确为**新代**（`schema_version = 2` + 迁移后的新指纹）|
| b | **已迁移的生产库** | **只**执行部署副本 **P15** | 身份闸通过、复位成功。P15 是非破坏的：身份闸过后**按主键**只改 `status` + lease 三列（共 **4** 列，与 §「身份闸」那句「status / lease 三列」是同一组），且**幂等**（其文件头已注明「已经是 unsent 时重复执行同样安全（已验）」）|
| c | — | — | ⛔ **绝不在生产库上执行 P11**；⛔ 不得为「让 P11 过」而清空 `training_sets` |

⚠️ 为什么 a 步不能用仓库副本或等价重写代替：本片要保的正是「**runbook `:864` 真正会执行的那两份文件**」与包、库同步。跑一份等价 SQL 只证明我会写 SQL，不证明部署副本被更新过。

**其它硬要求**：

| 要求 | 做法 | 不做会怎样 |
|---|---|---|
| **静默期（改法见下）** | **停掉 `api` 容器**，而不是只关 tailscale 入口 | 并发下载读到「旧元数据 + 新字节」，撞完整性校验；已被接受的请求与 **NAS 本机直连**的流量，关 serve 根本挡不住 |
| 确定性压缩 | 固定 `ZipInfo.date_time` 与压缩参数，使同一输入恒产出同一字节 | `zipfile` 默认嵌 mtime ⇒ **重跑一次 CRC 就变** ⇒ 迁移不幂等 |
| 回滚副本 | 原包留 `.bak`，**直到切片二真机验收通过**才删 | 原始数据只此一份，出错无退路 |

**静默期的正确做法**（codex S1-R1-F1 打回后订正）：

⚠️ **原文写「先 P16 关入口」是不成立的**，两个理由：
1. 本仓已实测三次：`expose.sh` 的看门狗**继承了 flock**，窗口期内 `close` 恒 `LOCK_TIMEOUT`、**关不掉**（母 spec 已记录，我却同时把它列为「范围外」又依赖它）；
2. 就算关成功了，**关 tailscale 入口挡不住两类流量** —— 已被接受的在途请求，以及 **NAS 本机直连 `127.0.0.1:8010`** 的访问。

**改为**：

⚠️ **只停 `api` 还不够**（codex S1-R2-F1）：本仓还有**独立于 API 的写入方** —— `backend/app/scheduler_main.py`（`build_generate_batch`）与生成器 CLI，**都能在 API 不在的情况下产出 zip 并往 `training_sets` 插行**。它们若在迁移中途跑起来，11 目标清单当场过期，还会产出**旧代契约**的新包。

```
迁移前：① docker compose stop api
        ② 停掉 / 确认没有 scheduler 与生成器 CLI 在跑
        ③ 【整个迁移期间持有 B2_GENERATION_LOCK_KEY】（生成器自己就用这把锁，
           持住 = 任何生成路径都进不来，且这是【可证明】的互斥而非口头约束）
        验证 ⓐ`docker compose ps` 无 api  ⓑ宿主机 8010 无监听
             ⓒ本机 curl 连不上  ⓓ锁已持有  ⓔ`training_sets` 恰好 3 行且 id 与清单一致
迁移后：11 目标收敛校验通过 → 释放锁（**取一次、放一次**；⛔ 协议中途不得重复取，见第 3.1 步）
        ⛔ 【不启回 api】，⛔ 【也不启回 scheduler / 生成器 CLI】—— 见下
```

⛔ **切片一结束时 `api` 必须保持停止**（codex S1-R2-F4；上一版我写了「校验通过 → `start api`」，与 §8「两片之间不得启回 api」**自相矛盾**）：此刻库存已是第 2 代，而 App 仍拒 `.db` 与 `user_version = 2`。启回 api ⇒ 若持久化的 tailscale serve 配置没被成功清掉，**不兼容的客户端会立刻恢复访问、预占库存、然后在确认前失败**，把库存烧成 `reserved`。

⇒ **`api` 与对外入口一律等到切片二部署完、且共用 fixture 的端到端测试通过之后才启回。**

⛔ **`scheduler` 与生成器 CLI 同样要停到切片二之后**（codex S1-R5-F3；上一版我只把「停 scheduler」写成迁移期间的临时措施，迁移后就释放了）：

- `backend/app/scheduler.py:5` 写明其职责是「`unsent<=40` 就调 `generate_batch` **补到 100**」⇒ 一启动就会**批量补货**；
- 而 `backend/docker-compose.yml:96-103` **已经白纸黑字记着**：生成器在**宿主机**上跑、把**宿主机绝对路径**写进 `file_path`，「**产出的行在容器内会全部 404**」。

⇒ 启回 scheduler = 往库里塞进**几十行 schema-v2 但下载必 404** 的记录，**且它们全在那 11 个被校验的目标之外**。

⚠️ 这条与「`file_path` 宿主/容器语义未统一」这个既有残留是同一个根（PR-2 已明写、本次不修）。**在那个残留解决之前，任何自动补货都必须保持关闭**，并把「scheduler 与 CLI 仍处于停止」写进本片的最终验收。

⚠️ **数据库清单校验要做两次**（暂存前、提交前各一次）：确认 `training_sets` 仍是那 3 行、id 未变 —— 防止「静默期开始后仍有人插了行」这种情况被静默带过。

⭐ 这比「修好 `expose.sh` 再迁移」更强：停容器**同时**掐断 tailnet 与本机两条路径，且**不依赖任何待修缺陷**。`expose.sh` 的锁缺陷仍是独立残留（§6.4），但**不再是本片的前置阻塞**。

⛔ **收口闸**：**11 个目标全部收敛到同一代之前，不得启回 `api`、更不得开对外入口**。写成脚本里的显式检查，不是嘱咐。

---

## 4. 判别力要求（实施阶段硬约束）

每条新判据必配变异，且**逐条记录红的是哪一条测试**：

| # | 变异 | 期望 |
|---|---|---|
| B1 | 删掉 §3.2 的语义分流（全部走「下一根 − 1」） | 跨**午休**（`11:30`→`14:00`）与跨**日**（`15:00`→次日 `10:30`）两条边界用例变红 |
| B2 | 把分流反过来（日线及以上走收盘式、日内走下一根式） | `daily` / `monthly` 的「逐根不变」用例变红 |
| B3 | 开盘侧改回「下一根 − 1」 | 「起点为**周三**、跨界周被删」用例变红（洞前那根周线落进洞里） |
| B4 | `period_end` 时区改成 UTC | 周界 / 月界附近用例变红 |
| B5 | 迁移用**位移常量**（一律减 19）而非逐根重算 | `15m` 用例变红（常规位移是 **4**） |
| B6 | 迁移**不改** `weekly` | 「`weekly` 应变 0/1/1 根」用例变红 |
| B7 | 迁移顺手也改了 `daily` | 「`daily`/`monthly`/`3m` 逐根不变」用例变红 |
| B8 | 迁移不写 `user_version = 2` | 「产物代际标识 = 2」用例变红 |
| B9 | 不改 `SCHEMA_VERSION`（`:39`） | 「新产物登记为 schema 2」用例变红 |
| B10 | 迁移不更新 PG 的 `schema_version` / 两份 SQL 期望值 | P11 / P15 闸门变红 |
| B11 | 只迁移部署目录、**不迁移 P7 来源目录** | 「来源闸」变红（干净重部署会覆盖回陈旧包） |
| B12 | 漏改任意一处写死指纹 | 「全仓 grep 旧指纹命中数 = 0（白名单外）」变红 |
| B65 | 闭合闸不设白名单（要求无条件命中 0） | 闸门恒红 ⇒ 实施者只能删掉归档的调查记录（违反 §8）或当场临时排除；「白名单写死在守卫里且逐条注明理由」的自测变红 |
| B66 | 第 3 步改回「8 个文件全部就位**后**再开 PG 事务」 | B34 / B35 的用例变红（身份闸与 UPDATE 分属两个事务、换文件期间无保护）|
| B67 | 故障注入按「11 个中断点」做 | 「中断点恰为 10 且 PG 3 行始终在**同一个事务**里」的断言变红（凑够 11 只能拆事务）|
| B68 | 3c 守卫①的作用域写成「全仓」 | **本片改动全部落地后守卫仍红**（`DefaultTrainingSetReader.swift:146`/`:80`/`:148`、`PreviewTrainingSetReader.swift:59` 属切片二）⇒「本片交付时该守卫为绿」变红。<br>⚠️ **观测量是「改完之后」而不是「当前树上」**（Opus 评审 R2-M2）：收窄后的作用域在**当前树上也红**（`plan_v1.5.md:1285` + `modules:2302` 两处未改）⇒ 变异体与对照组当前同色、无判别力。这条守卫是「改完才绿」的**收敛型**，不是「开局就绿」的防退化型，⛔ 不得援引后者那条原则（会诱导实施者把判据放宽到开局就绿）|
| B69 | 3b 只改 `modules_v1.4.md`、不改 `kline_trainer_plan_v1.5.md:1285` | 「两份根级权威对 index 递增性的表述一致」守卫变红 |
| B70 | 3b2 三处只改值、不加过渡态标注 | 「冻结契约的表述与 3c 的切片一守卫互不矛盾」用例变红（契约说双方=2，守卫认证 App=1）|
| B71 | 改动清单不含 `backend/tests/test_generate_training_sets.py` | 后端 CI 变红（`15m`/`60m`/`monthly` 三条断言）|
| B72 | 4c 三条期望值改成**测试运行时调用 `period_end` / `assign_global_indices` 现算**（共享 oracle） | 把生成器的 `upper` 公式改错后这三条断言**仍绿** ⇒ 该写法必须被禁止。<br>⚠️ **上一版把变异体写成「跑一次抄回字面量」，观测量不可能出现**（Opus 评审 R2-M2）：抄成字面量后再改公式，断言必红。真正会「仍绿」的是**共享 oracle** 这种写法 |
| B73 | `INSERT` 守卫作用域不含 `.github/workflows/**` | 守卫落地后当场红 5 处（`schema-smoke.yml:73/76/79/82/85`）|
| B74 | Mac 三个 v1 包不作废、`2026-08-14-…-design.md:138` 不改 | 「全仓不再出现『既可从 Mac scp』一类表述」文案守卫变红 |
| B76 | 闭合闸白名单**按行号**写（而非按文件） | 「本片行数变动后闭合闸仍可跑通」自测变红 —— 行号漂了就白名单错行 |
| B77 | Mac 文案守卫写成「全仓」且不带白名单 | 守卫在**改完 `:138` 之后仍红**（本片自身 3 处逐字引用不能删）|
| B78 | `INSERT` 守卫**按单行 grep** 而非按语句 | **那 5 处跨行语句**（`generate_training_sets.py:497-498`、`…p11….sql:63-64`、`rehearse.sh:206/:213/:443`）误报为缺字段而变红；反向变异（把 `schema_version` 从 `:498` 挪走）守卫**必须**变红 |
| B79 | `INSERT` 守卫作用域不排除 test double 的字符串谓词 | 守卫恒红（`test_b2_reconnect_integration.py:208`/`:699` 结构上不可满足）|
| B80 | 第 3.1 步**另开连接**或**第二次**取 `B2_GENERATION_LOCK_KEY` | 「备份完成、替换未开始时确定性中止」用例变红；同连接重复取则「取放次数相等」断言变红 |
| B81 | §7「**本 spec 自身的机械自检**」的判据②恢复成「§3.3 / §3.4 也要编号连续」 | 门**在当前树上就红**（三张表的标签分别是非整数序列 / 区间 / 字母）⇒「本片交付时该条自检全绿」变红 |
| B82 | 带 `§N` 前缀的引用改用序号（而非按名） | 「带 `§N` 前缀的引用一律按名」守卫变红；并在被引小节插入任一新条目后，交叉引用指向错误条目。<br>⚪ 反向变异（把禁令扩到「任何小节内的条目/步骤」）应让「本片交付时该守卫为绿」变红 —— 实测当前树 32 处违例，其中 §3.4 的 `3.0`–`3.5` 与 `5` 无名可换 |
| B84 | 自检③ 恢复成「全文每一处计数都要附命令与输出」，或在 ③a/③b 的判据里内嵌字面量数字 | 门**在当前树上就红**（结构性计数根本没有命令可附；内嵌的数字会被下一次提交推翻 —— R5-M1 实证：同一次提交把表改成 86，而判据里那个 83 没跟）⇒「本片交付时该条自检全绿」变红 |
| B85 | 跨小节按名引用指向一个**被引小节内不存在**的名字、给被引条目起**含数字**的名字、或让该名字在被引小节内**命中两次**（引用之外还有顺带提及）| 「锚点在被引小节内唯一命中且该命中不是引用本身」守卫变红（R4-M2 实证：`全部变异逐条跑过` 全文只命中引用处自己；R5-M3 实证：`分库验收` 曾在 §7 内命中两次）|
| B86 | 匹配窗口的理由写成「在场**每一条** `INSERT` 都是多行」 | 「穷尽性主张按字面量逐条定性」守卫变红 —— 实测 12 处里 **7 处单行**，全称断言有 7 个反例 |
| B87 | 备份把「还原用预映像」与「身份/路由用快照」**合成一份** | 回滚第 5 步把 status / lease 一起写回 ⇒「回滚**不得复活已消费库存**」用例变红（迁移前为 `sent` 的场景）|
| B88 | 回滚第 2 步把生命周期列与备份记下的旧值做**相等比对** | 「跑过分库验收 (b) 的 P15 之后回滚仍可用」用例变红 —— P15 合法地把 4 列改成 unsent/NULL，相等比对必然失配、把唯一退路封死 |
| B89 | 序号引用禁令按「目标在不在别的小节」分类（而非按书写形式） | 写错小节号的「§N 第 M 步」型引用被误判为同小节而放行 ⇒ R4-M1 的用例变红 |
| B83 | 任一计数写成**含本片自身**的「仓内共 N 处」 | 「每个计数都写明口径、并附可复跑命令与其当次输出」守卫变红；且往正文里补一句引用后该数当场过期 |
| B75 | 三处既有验收 grep 锚不改 | 「作用域内不存在『期望 `user_version = 1`』的成文判据」守卫变红。<br>⛔ **该守卫的作用域与白名单必须成文**（Opus 评审 R2-C3 同族）：作用域 = `scripts/acceptance/**` + `docs/acceptance/**`；白名单必须含 `docs/acceptance/2026-06-14-wave3-pr13b-fixture-smoke.md:16`（它锚的是 App 侧 `DebugTrainingSetWriter` 的常量，**切片一保持 `1` 是正确的**，不排除则误伤）|
| B13 | 用默认 `zipfile` 写法（不固定 `date_time`） | 「同一输入连跑两次 → 字节与 CRC 完全相同」变红 |
| B14 | 就地改而非暂存 + 原子替换 | 「中途中断后目录里不得出现半成品包」变红 |
| B15 | 在「文件已换、PG 未提交」处崩溃后重跑，脚本「跳过已完成项」而不核实际指纹 | 收口闸变红（11 目标未收敛） |
| B16 | 删掉「11 目标收敛前不得开入口」这道闸 | 半迁移状态下开门的用例变红 |
| B17 | 静默期只停 `api`、不持 `B2_GENERATION_LOCK_KEY` | 「迁移中途 scheduler / CLI 能插入新行」用例变红 |
| B18 | 不改 `test_training_set_schema.sh` 的期望值 | schema CI 变红（这条本身就是判据 —— 它证明该锚点被认领了） |
| B19 | 去掉「插入必须显式给 `schema_version`」守卫 | 「隐式取默认值 = 1」用例变红 |
| B20 | 迁移协议改回「断点续做」（跳过已完成目标） | 故障注入用例变红：在某个替换点中断后重跑，11 目标无法保证全部收敛到同一代 |
| B21 | 切片一末尾把 `api` 启回来 | 「切片一交付后 api 仍停止」用例变红 |
| B22 | 去掉第 1 步的代际预检（回到「无条件备份」） | 「第二次成功运行后备份与预映像逐字节不变」用例变红（v1 备份被 v2 覆盖） |
| B23 | 备份改成可覆盖（非 no-clobber） | 同上用例变红 |
| B24 | 去掉第 3 步对暂存包 / 目标父目录的 fsync | 掉电故障注入用例变红（PG 已提交 v2 而包退回或残缺） |
| B25 | 去掉第 4 步的完成记录与「重启后必须复核 11 目标」 | 「重启后未复核即启回服务」用例变红 |
| B27 | 备份集改回「互斥创建，已存在即失败」 | 「备份阶段中断后重跑」与「混合态还原后重跑」两条故障注入用例变红 |
| B28 | 备份集「部分存在」时直接全量重建 | 「已有备份逐字节不变」用例变红 |
| B29 | PG 身份闸只比 `id + content_hash + schema_version` | 「某行 `file_path` 漂移但指纹一致」用例变红（会被盖新版本并放行） |
| B30 | 去掉迁移后「全部字段复核」 | 同上用例在迁移后阶段变红 |
| B32 | 不改 `kline_trainer_modules_v1.4.md:2302`（R03） | 3c 那条「全仓不得再出现旧表述」守卫变红 |
| B33 | 「全部新代」不分两态（回到「一律立即退出」） | 「PG 已提交、完成记录未写时崩溃」故障注入用例变红 |
| B34 | 身份闸与 UPDATE 拆成两个事务 | 「文件替换期间并发 INSERT 应被阻塞」用例变红 |
| B35 | 用 `LOCK TABLE` 但不显式 `BEGIN` | 该语句直接报错 ⇒ 用例变红（证明事务边界本身是判据） |
| B40 | 回滚不做自己的预检（无「全 v1 + 未归档记录」这一支） | 「PG COMMIT 之后、归档之前崩溃」故障注入用例变红（回滚中止、正向又迁回 v2） |
| B41 | 回滚不落 rollback 完成记录 | 「已回滚完成后重跑应为空操作」用例变红 |
| B47 | 曝光后恢复走「还原 v1 再放出去」而不是「就地修 v2」 | 端到端用例变红 —— 还原成 v1 后**没有任何一代 App 能消费**（旧 App 只认 `.sqlite`、切片二 App 只认 `user_version=2`），复位出来的库存全被客户端拒 |
| B52 | 备份不按 epoch 命名（共用一个命名空间） | 「v1 备份与 epoch1 记录都在时跑 epoch2」用例变红（四态判定拿 v1 比 v2、中止或误还原） |
| B53 | 恢复时参考了别的 epoch 的清单/备份 | 「epoch1 备份逐字节未被触碰」断言变红 |
| B54 | 活跃 epoch 记录**在备份/替换之后**才写（或干脆不写） | 「活跃记录已就位、备份未开始时被杀」用例变红 —— 重启认不出本轮 epoch，会新开一轮 |
| B55 | 存在**多份未决**活跃记录时自动挑一个，而非中止 | 「人为放两份未决记录」用例变红（挑错 = 拿错代的备份还原） |
| B56 | 完成 / 回滚时**不归档清除**活跃记录 | 「完成后再跑一次应为幂等空操作」用例变红 —— 脚本会把已完成的 epoch 当未决认领 |
| B57 | 重启时新开 epoch 而不认领未决的那个 | 「epoch1 完成 + epoch2 替换到一半」用例变红（半截状态无备份可依） |
| B58 | 备份 / 替换 / 还原 / 故障注入只覆盖 6 个 zip + PG，不含两份**部署 SQL** | 「跑 NAS 上 `sql/…p11….sql` 与 `sql/…p15….sql`」用例变红（身份闸失配） |
| B59 | 任一**终止分支**原样退出、不归档清除活跃记录（尤其「全新代 + 完成记录可验 → 立即退出」） | 「完成记录已 fsync、活跃记录未清时被杀」用例变红 —— 重启后开不了下一个 epoch，回滚与向前修被永久钉死 |
| B60 | 回滚侧的终止分支（「全 v1 + 已有 rollback 记录 → 已完成，退出」）不清活跃记录 | 回滚版的同一条崩溃用例变红 |
| B61 | 在**已迁移的生产库**上执行部署副本 P11（或为让它通过而先清空 `training_sets`） | 验收变红 —— P11 的空表前置 `RAISE EXCEPTION`；清表则丢行 id 与生命周期状态（属计划外破坏性操作） |
| B62 | 把开 epoch 挪回 0b（无活跃记录就直接建 id / 清单 / 备份目录） | 「已迁移后重跑 = 幂等空操作」用例变红 —— 每跑一次多一个 epoch 目录 |
| B63 | `--new-epoch` 缺省也照跑（不要求操作者显式意图） | 「须显式才能再次迁移」用例变红（B44 的机制侧证据）|
| B64 | 活跃记录存成**一个固定路径**而非专用目录 | 崩溃测试 3（两份未决 → 中止）**无法构造**⇒ 该用例退化成恒真，变异自测变红 |
| B50 | 同步清单里不含 NAS 上 `$DIR/sql/` 那两份 | 「用 runbook 原话那条命令（跑 `$DIR/sql/…p15….sql`）复位」的用例变红 —— 部署副本仍是旧期望、身份闸失配 |
| B51 | P15 期望值由人工填写而非从 epoch 清单生成 | 「仓库副本与部署副本内容一致且都等于本 epoch 清单」的守卫变红 |
| B49 | 曝光后向前修走「简易分支」，不同步 P15 的期望七元组 | 「修复改掉了全部 3 个 content_hash → 再跑真实的 P15 复位脚本」用例变红（P15 认不出身份、中止不复位） |
| B48 | 文档里仍宣称「回滚到 v1 后库存可用」 | 文案守卫变红（该说法已作废；v1 备份只用于重建与审计） |
| B45 | 曝光后用「曝光前」那套回滚（把生命周期列纳入匹配） | 「G3 已 sent、G4 失败」场景用例变红（闸门中止或复活已消费库存） |
| B46 | `recover-after-exposure` 静默还原生命周期列 | 「回滚后生命周期列必须保持现状」用例变红 |
| B43 | 正向预检不看 rollback 记录（只看代际） | 「回滚完成后再跑正向脚本」用例变红 —— 数据会被推回 v2 |
| B44 | 允许不开新 epoch 就重新迁移 | 「须显式 epoch 才能再次迁移」用例变红 |
| B42 | 陈旧正向完成记录交给正向脚本处置 | 「回滚后再跑正向脚本不得把数据迁回 v2」用例变红 |
| B36 | 备份创建不走「临时文件 + 原子改名」 | 「拷贝 .bak 中途被杀」故障注入用例变红（会留半截 .bak 并把下次运行推进中止分支） |
| B37 | 不提供完成态回滚模式 | 「从已完成的 v2 回滚到 v1」用例变红 |
| B38 | 回滚跳过 v2 侧身份闸 | 「现状并非我们迁上去的那一代时必须中止」用例变红 |
| B39 | 只改 R03、不改冻结契约其余三处版本引用 | 3c 的收敛守卫变红（三处版本号未同时为 2） |
| B31 | 迁移后启回 scheduler | 「切片一交付时 scheduler / CLI 仍停止」用例变红 |
| B26 | CI 不比对「当前生成器输出 vs 已提交 fixture」 | 「生成器改了序列化 / user_version 而 fixture 未更新」用例变红（两侧本会双绿） |

### 4.1 ⭐ 跨端接缝的**生产者半边**（本片必须交付；codex S1-R1-F2）

§1.4 把「没有任何测试把生成器真实产物喂给 App 读取器」认定为两个缺陷共同的藏身之处。母 spec 归档后，**若本片只做后端变异 + 产物自洽校验，两侧的静态 fixture 仍会各自漂移，同一类回归照样静默。**

**本片交付**：

1. 用**生产顶层装配函数** `assemble_from_windows(...)` 从一组合成 bar 数据产出一个**小训练组**，提交进仓库作为**跨端共用 fixture**；
   ⚠️ **不得**自己串调 `assign_global_indices` → `build_training_set_sqlite` → `zip_and_hash`：真正决定 zip 成员名的是 `assemble_from_windows:403`（`f"{fname}.db"`），绕过它的测试对该行**零敏感**。
2. 该 fixture 的形态必须**同时覆盖本片三类特征**：① 至少一个非 m3 周期有 ≥2 根落在 `end_global_index = 0`（轴前历史）；② 含跨**午休**与跨**日**的日内边界；③ 含一处**被删的跨界周**（起点设为周中）。
3. 断言：**压缩包清单**（恰好 1 个成员、后缀 ∈ {`.sqlite`,`.db`}、无 sidecar）+ **逐周期索引向量**与**独立人工推算**的期望值逐根相等。
   ⚠️ **期望值必须手写**，不得由「跑一遍生成器」得出 —— 否则生产者与校验者可以**一起用错公式而全绿**，那正是缺陷藏住的机制。

4. ⭐ **CI 每次都要把「当前生成器的输出」与「已提交的 fixture」对齐**（codex S1-R4-F1）：

   ```
   每次 CI：重新调 assemble_from_windows(...) 产出一份 → 与仓库里那份 fixture 比对
            比【逻辑内容】：压缩包成员清单 + user_version + 每张表的全部行
            ⛔ 不比 zip 原始字节（zipfile 嵌 mtime，跨机跨时不可复现，比字节必然假红）
   ```

   ⚠️ **缺了这条，§1.4 那道缝会原样复活**：生成器日后改了序列化 / DDL / 版本，**生产者侧的测试对着新鲜输出照样绿**，而 App 侧对着**陈旧的已提交 fixture** 也照样绿 —— 两边都绿，缺陷照旧静默。

**移交切片二**：必须用**这一个 fixture**（不是自己另造一份）跑通真实的解压 → 打开 → 读取链路。⇒ 三方（当前生成器输出 / 已提交 fixture / App 读取链路）钉在同一件产物上，任何一方漂移都会红。

**正向对照（必配，防「全是拒了」的套件掩盖恒抛守卫）**：健康输入必须被放行 —— ① `daily`/`monthly`/`3m` 逐根不变；② 迁移后的 3 个包内容自洽（`user_version = 2`、索引逐根等于 §2.2 反算值）；③ 迁移脚本对**已迁移**的包重跑一次为**幂等空操作**；④ 上述 fixture 通过全部断言。

⚠️ **B1 与 B3 必须互不掩盖**：另配一次 **B1+B3 组合变异**，确认组合下仍有测试红并记录是哪一条。

---

## 5. 兼容性

| 维度 | 结论 |
|---|---|
| 训练组 SQLite **表结构** | **不变**（无新列 / 无类型变化 / 无约束变化） |
| `PRAGMA user_version` / `TRAINING_SET_SCHEMA_VERSION` | **`1` → `2`**（§2.3：判据是「能否互读」） |
| 顶层 `CONTRACT_VERSION` | **`"1.13"` → `"1.14"`** |
| 既有 3 个产物 | **需就地迁移**（§3.4）；`content_hash` 会变 |
| 现有 App（未改） | 迁移后拿到 v2 包会报 `.versionMismatch` —— **不是回归**：它本来就消费不了任何真实训练组（zip 内是 `.db` 而它只认 `.sqlite`，属切片二） |
| QMT pilot 库 | 顶层 bump 后带 `"1.13"` 的既有库被闸 1 拒，需 `--reset`（4b 未投产，风险低） |

---

## 6. 明确不做（YAGNI / 归切片二）

1. **App 侧一切逻辑**：读取判据、缓存回收、进行中合成、面板联动 —— 归切片二；
2. **运行期能力协商 / 双格式过渡版 App**：未上架、单用户单机、次序可控，`user_version` 已足以给出可恢复信号。⚠️ **一旦上架必须重新评估**；
3. **`.db` / `.sqlite` 扩展名之争**：属切片二（改 App 接受 `.db`，user 已拍板）；
4. **`expose.sh` 看门狗继承 flock 导致窗口期内关不掉**：真缺陷，独立小 PR；
5. **App 后端地址不可配（PR-3 从未实施）**：`KlineTrainerApp.swift:18` 仍硬编码，独立 PR。

---

## 7. 验收判据

1. ⭐ **变异逐条跑过**：§4 全部 89 组变异（B1–B89）+ 1 组组合（B1+B3）逐条跑过，**每组写明红的是哪一条测试**；
   ⚠️ **本条第七次踩同一个坑**（codex S1-R3-F3）：上一轮加了 B17–B21，验收门却还写着「16 组」—— 与既有教训「改了表没改门」完全同型。
   ⇒ **配一条机械校验**：断言「§4 变异表行数」==「本条列举的编号个数」，对不上就红。比「记得同步」可靠。
2. 正向对照三条全部放行；
3. 后端 CI 全绿（Linux，**零 skip**）；iOS 侧因第 6/7/8 项常量改动而受影响的测试全绿；
4. 迁移完成后：11 个目标指纹一致、全仓 grep 旧指纹**在白名单外命中 0**、来源闸通过、脚本重跑为**幂等空操作**
   —— 判据是「**文件系统零新增**」：不新增 epoch id / 清单 / 备份目录 / 任何文件，
   ⛔ 不是「结果看起来一样就算」（0b 只读、0c 才创建，正是为兑现这条）；
5. `daily`/`monthly`/`3m` 逐根不变；`weekly` 实测变 **0 / 1 / 1** 根；`15m`/`60m` 按 §1.2 分布变化。

6. §4.1 的跨端 fixture 通过全部断言（清单形态 + 逐周期索引向量对**手写**期望值）；
7. 迁移期间**所有写入方**确实停着：`api` 停、scheduler/CLI 无进程、`B2_GENERATION_LOCK_KEY` 全程持有、`training_sets` 行数与 id 在暂存前与提交前**两次校验**一致；
8. **切片一交付时 `api`、`scheduler`、生成器 CLI 三者均处于停止状态**，对外入口未开（⛔ 启回归切片二，且 scheduler 还需等 `file_path` 宿主/容器语义残留解决）；
9. 故障注入：在**全部 10 个中断点**（8 次文件 rename 之后各一次，含两份部署 SQL；PG `COMMIT` 前后各一次）各中断一次，断言「全量还原后 11 目标全回旧代」且「重做一次后全到新代」；⛔ 不得为凑数把 PG 3 行拆成多个事务；
10. **活跃 epoch 四条崩溃测试**全过（另需断言「无 `--new-epoch` 时为 dry-run，文件系统零新增」）：①活跃记录已就位/备份未开始被杀 → 认领同一 epoch；②epoch1 完成 + epoch2 半途被杀 → 认领 epoch2 且 epoch1 备份逐字节未动；③两份未决记录 → 中止报警；④**完成记录已 fsync、活跃记录未清**处被杀（正向与回滚各一次）→ 重启归档清除后能**立刻开下一个 epoch**；
11. ⭐ **本 spec 自身的机械自检**（Opus 评审 R2-C1 后新增，R3-C1 / R4-C1 / R5 三次订正）：
    ⛔ **总则（本片写任何机械判据都适用）**：**在当前树上实测可绿，才允许写成门**。
    一条开局就红的守卫 = 给实施者发放宽许可证 —— 他要么把它放宽成恒真、要么整条跳过，
    连带把同一节里真正有效的判据一起废掉。本片已因违反这条栽过四次（R3-C1 把编号连续
    套到三张表、R4-C1 要求全文每处计数附命令、R5-C1 把序号禁令扩到任何小节、
    R5-M1 在判据里内嵌「83」）。⇒ 写完任何一条判据，**先在当前树上跑一遍**。
    - **①【结构】** 全文不得存在**表格断裂行** —— 两种形态都要查：**中间型**（非 `|` 开头的非空行夹在表格中间，后面整段掉出表格）与**尾部型**（紧跟表格最后一行之后，最后一格的内容掉出去）；另加**列数一致性**（同一张表所有行列数相同，防止未转义的 `|` 把一行劈成多列）；再加**跨小节锚点唯一性**（对每处 `§N「X」` 形式的引用，断言 `X` 在 §N 内**恰好命中一次、且该命中不是引用本身**）。⭐ 三项在当前树上**实测全绿**，可直接当门。
    - **②【编号】仅对 §4 变异表**：行数 == §7「**变异逐条跑过**」那一条列举的编号个数，且 `B1…BN` 无缺号、无重复、无越界。
      ⛔ **不得把「编号连续」套到 §3.3 / §3.4**（R3-C1）：§3.3 的行标签是 `1 2 3 3b 3b-2nd 3b2 3c 3d 4 4b 4c 4d`（**根本不是整数序列**），§3.4 迁移清单是区间标签（`1-3`/`4-6`/`7-8`/`9-11`，**4 行对 11 个编号**），按库分开的那张验收表是 `a/b/c`，写死值表**没有编号**。上一版那条判据**在四张表的三张上开局就红** —— 而本仓的教训是「**一条开局就红的守卫 = 给实施者发放宽许可证**」，实施者要么把它放宽成恒真，要么整条自检跳过，**连带把真正有效的①和③一起废掉**。
      §3.3 / §3.4 改为断言：**行标签唯一**，且**每一个标签都在 §7 或实施计划里被引用到**（这才是真正想防的「条目掉了没人发现」）。
    - **③【计数】两条，且判据里【不得内嵌字面量数字】**（R4-C1 拆分 → R5 砍简）：
      - **③a【由仓库 grep 得出的数字】**（含对本 spec 自身的 grep）一律附**口径 + 可复跑命令 + 当次输出**。⛔ 不写「恰好 N 处」这种把范围钉死的话 —— 上一版就是这么写的，当场漏掉了另外几处同类（R5-M2）。⛔ 也不再写含本片自身的「仓内共 N 处」总数（R3-M2：那种写法每往正文多写一句就过期一次）。
      - **③b【结构性计数】**（目标数 / 文件数 / 中断点数 / 变异组数）必须与它所枚举的**清单或表逐一对得上**；⛔ 判据两侧都指向**活数据**，不得写成 `N = a+b+c` 这种内嵌字面量的等式 —— 上一版写死了「83 组变异」，而**同一次提交**已经把表改成 86（R5-M1）。这条自检的意义正是防数字漂移，它自己更不该内嵌一个会漂的数字。
      - ⚪ **不在本自检范围**：§1.2 / §1.3 两节表格内的一切**实测根数**（属一次性实测记录，复核方式 = 用 §1.2 的三个产物按 §2.2 的公式重算）。
      ⚠️ **③ 已被砍过两轮**（R4-C1「全文每一处计数」→ 三类；R5 → 两条且禁字面量）。原因每次相同：**一条开局就红的守卫 = 给实施者发放宽许可证**，③ 一旦被放宽或跳过，会连带把实测全绿、真正有用的①一起废掉，而①正是这条自检存在的全部理由。
    <br>⚠️ **这条门的来历与一个必须交接的后果**：R8（`1152521`）那次「把 3c 守卫拆成切片一断言 / 切片二断言」的修复，用了**真换行**写多行单元格，从此把 `4`（m01 治理契约）与 `4b`（`test_training_set_schema.sh` 这道 CI 硬门）**挤出了改动清单表格**，一直持续到 R15。⇒ **codex R9–R15 共 7 轮读到的 §3.3 清单都在 `3c` 处截止**，`4` 与 `4b` 从未以「清单条目」的形式进入过那 7 轮的视野。**实施计划必须对这两条做一次独立复核**，⛔ 不得默认「它们已经被评审过很多轮」；
12. 部署副本按 §3.4 那张表**分库验收**：（a）一次性克隆库（`training_sets` 初始化为空）上依次真跑 NAS 的 P11 → P15，两者身份闸都过且期望值确为新代；（b）已迁移的生产库上**只**跑 P15，身份闸通过且复位成功；（c）⛔ 生产库上绝不跑 P11、绝不为此清表。

⚠️ **本片交付后可以说**：「日内周期与周线的形成时刻已订正；既有 3 个产物已迁移至第 2 代」。
⛔ **不得说**：「手机能用了」「真实数据链路打通」—— App 侧一行未改，它仍消费不了任何训练组。

---

## 8. 与切片二的交接

**切片二（App 侧）必须承接的既定事实**（不得再自行推演）：

1. §2.1 的两套标注约定、§2.2 的索引定义 —— **契约已定**，切片二只负责**校验**产物是否符合；
2. `period_end` 必须**复用**本片的定义（§3.1），不得另写一份；
3. 产物代际 = `user_version 2`；App 侧 `TRAINING_SET_SCHEMA_VERSION` 与 `TrainingSessionCoordinator:1307` 那个**写死的字面量 `1`** 必须同步（⚠️ 该字面量的历史理由 residual E6a-R1 已过期）；
4. 迁移前的 v1 包会被版本闸拒为 `.versionMismatch`（**不是** `.dbCorrupted`）—— 切片二的「旧公式」测试必须用**合成档**（`user_version = 2` + 旧公式索引）才能走到索引校验那一层；
5. ⛔ **次序**：本片先落地并完成迁移，切片二才动；两片之间**不得启回 `api`、不得开对外入口**；
6. ⛔ **切片二必须把顶层 `CONTRACT_VERSION` 再 bump 到 `"1.15"`**（§3.3）：`1.14` 标识「产物已第 2 代、App 尚不支持」，`1.15` 标识「App 已支持」。两态共用一个版本号会让跨语言守卫在两种状态下都绿，标识失去意义；
7. ⛔ **App 侧仍在断言「全周期严格递增」的五处，由切片二一并改写**（Opus 评审 C5 交接）：
   · `ios/.../KlineTrainerPersistence/DefaultTrainingSetReader.swift:80`（中文表述）、`:90-92`（**执行该判据的生产代码**：`r.endGlobalIndex <= prev` → `.dbCorrupted`）、`:146`（**逐字引用 R03 原句**）、`:148`
   · `ios/.../PreviewFakes/PreviewTrainingSetReader.swift:59`（镜像）
   ⇒ 切片一的 3c 守卫①**作用域不含 `ios/`**；切片二落地时才把作用域扩到 `ios/`，那条守卫此时才是「收敛守卫」。
   ⚠️ 只改注释不改 `:90-92` **更糟** —— 会留下一条自述与行为相反的注释；
8. **本片交付的跨端 fixture（§4.1）是切片二的必用输入** —— 切片二不得另造一份，否则两侧静态样本会各自漂移，缺陷 A/B 那类回归照旧静默。

⛔ **一条必须带进实施计划的交接**（Opus 评审 R3 复核历史后确立）：§3.3 的 `4`（m01 治理契约矩阵）与 `4b`（`backend/sql/tests/test_training_set_schema.sh`，schema CI 硬门）因 R8 引入的表格断裂，**在 codex R9–R15 共 7 轮里都不在「清单条目」的视野内**。⇒ 实施时对这两条做**独立复核**，⛔ 不得默认「已被多轮评审过」。

**母 spec 归档**：`2026-08-30-trainingset-app-contract-design.md` 保留为**调查记录与切片二的素材**（13 轮 29 条 finding 的逐条处置全在其中），**不再直接实施**。
