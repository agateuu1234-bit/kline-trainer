# Plan 1f — M0.1 Schema Versioning Contract 设计 Spec

**Status**: drafted 2026-04-22；本文件是 Plan 1f 的设计 spec（brainstorming 输出）；后续走 writing-plans → subagent-driven-development → codex adversarial review → PR。

## 1. 目标

把 `kline_trainer_modules_v1.4.md` M0.1 章节中与 schema 版本管理相关的三类规则冻结为 repo 内权威治理 doc：

1. **CONTRACT_VERSION 矩阵**（spec L133-148）：顶层 `"1.4"` + 5 个子版本维度（PostgreSQL / 训练组 SQLite / app.sqlite / Swift 模型 / P2 journal states）+ 各自变更触发 bump 的条件
2. **Bump 策略二分**（spec L137-138 + L287-291）：破坏性变更 vs 本地兼容新增 + DAO/reader 对未知 state 的处理规则
3. **Migration Rollback 规则**（spec L151-157）：PostgreSQL / app.sqlite / 训练组 SQLite 三条 owner + forward/rollback 文件对的要求

## 2. 非目标（out-of-scope）

- **不实施具体 migration SQL**（spec L161-291 的 `content_hash` 两阶段 + `download_acceptance_journal` + `0003_v1.4_purge_leased` 由 Plan 2 B3 owner 落地）
- **不定义 `CONTRACT_VERSION` 常量代码**（Python / Swift 代码层的常量定义由 Plan 2 / Plan 3 模块 PR 落地）
- **不实施 L2232 CI assert**（需先有常量存在；登记为未来强制点）
- **不动 spec**（`kline_trainer_modules_v1.4.md` 只读；发现 spec 本身 drift 时 pushback 到 Plan 1e 教训处理）
- **不吸收 M0.5 consolidation**（已 scope 为独立 Plan 1g；Plan 1f 只管 M0.1 schema 版本）

## 3. 架构

**单一治理 doc**：`docs/governance/m01-schema-versioning-contract.md`，与 `m04-apperror-translation-gate.md` / （未来）`m05-*` 同目录，parallel 结构。doc-only，零代码，零 CI workflow 新建。

**配套聚合 acceptance 脚本**：`scripts/acceptance/plan_1f_m0_1_schema_versioning.sh`。**关键设计**（吸收 Plan 1e R7 MEDIUM 教训）：每个必须 anchor 做**独立**断言，**不用聚合阈值**；删任何单一 anchor 必须能独立 FAIL。

## 4. Doc 章节骨架（8 节）

> **注**：doc 顶部有一个 `> **Status**：...` blockquote + `# M0.1 ...` H1；下列 7 个是真正的 `^## ` H2 章节（Status 不是 H2，不计）。acceptance 脚本断言 `^## ` 数量 = 7。

| § H2 | 标题 | 内容来源 | 大致行数 |
|---|---|---|---|
| 1 | 用途 | 与 m04/m05 对齐 | ~10 |
| 2 | CONTRACT_VERSION 矩阵 | spec L133-148 表格 verbatim | ~25 |
| 3 | Bump 策略（破坏性 vs 本地兼容） | spec L137-138 + L287-293 配套 DAO reader 规则 | ~40 |
| 4 | Migration Rollback 规则 | spec L151-157 表格 verbatim | ~15 |
| 5 | 应用范围（模块 PR 引用表） | 参考 m05 §八 格式：Plan 2 B3 / Plan 3 P2/P3a/P4 | ~25 |
| 6 | 未来强制点（Plan 1f 不实施） | L2232 CI assert / L161-291 具体 migration SQL / 跨语言常量同步 lint | ~15 |
| 7 | 交叉引用 | spec L133-157 / L287-293 / L2232 / m04 / Plan 1e aborted 教训 | ~10 |

此外顶部有 Status blockquote（含权威来源 callout，显式列出 spec 行号）+ H1 标题。

**权威来源 callout**（§0）显式写明：
- 主体条款：spec L133-157（v1.3 新增 / v1.4 拆分 bump 策略）
- Bump 策略二分：spec L137-138 + L287-291（DAO reader 未知 state 处理）
- 历史决议：spec L2407（R4-codex modify，把"journal state 增删均 bump 顶层"缩小为本地兼容新增可只 bump 子版本）
- 应用清单来源：spec L2097（Wave 0 M0.1 交付验收项）

## 5. Acceptance script 设计（26 项独立断言）

> **重要**：本 spec 是 brainstorming 输出；writing-plans 阶段 Task 2 Step 1 的完整脚本是**权威**最终定义。下面是设计意图的分组说明；实际项目数以 plan 为准。

### 文件 / 结构检查（2 项）
1. file: m01 contract doc 存在且非空
2. structure: **7 H2 sections**（注：doc 顶部 `> **Status**：...` 是 blockquote 不是 H2，真正 `^## ` H2 只 7 个：用途 / CONTRACT_VERSION 矩阵 / Bump 策略 / Migration Rollback 规则 / 应用范围 / 未来强制点 / 交叉引用）

### 内容锚点（每个独立，不聚合；吸收 Plan 1e R7 MEDIUM 教训）

**6 子版本维度**（每个独立 `grep -q`，聚合被禁）：
- 3. anchor: 顶层版本 `"1.4"` 字面串
- 4-8. anchor: 5 个子维度（PostgreSQL / 训练组 SQLite / app.sqlite / Swift 模型 / P2 journal states）

**4 bump 策略 anchor**（9-12）：
- 9. anchor: bump 策略"破坏性"关键词
- 10. anchor: bump 策略"本地兼容"关键词
- 11. anchor: "未知 state 处理" 规则存在
- 12. anchor: "fail-safe 忽略策略" 关键词

**3 Migration Rollback anchor**（13-15）：
- 13. anchor: `forward.sql` 关键词
- 14. anchor: `rollback.sql` 关键词
- 15. anchor: `AppError.trainingSet(.versionMismatch)` 引用

**3 Owner 独立**（16-18）：
- 16. owner: B3 PostgreSQL
- 17. owner: P4 app.sqlite
- 18. owner: P3a 训练组 Factory

### 交叉引用（19-20）
- 19. cross-ref: spec `kline_trainer_modules_v1.4.md` + 行号 L133
- 20. cross-ref: `m04-apperror-translation-gate`（姊妹 governance doc）

### m04 stub regression（21-22，不 regression Plan 1d hotfix）
- 21. m04 gate stub 文件存在
- 22. m04 stub `TODO Plan 3 P1` 仍开

### Plan 1/1b/1c/1d regression（23-26）
- 23-26. Plan 1 / 1b / 1c / 1d acceptance 脚本 exit 0

**总计 26 项**（比 Plan 1e 13 项翻倍，因 anchor 全独立断言，不聚合；每 anchor 删除必然 FAIL）。

## 6. 非 coder 验收清单设计（8 项）

按 `workflow-rules.json` verification_template：中文 / action-expected-pass_fail / 禁用 "应该没问题 / looks fine"。

1. 聚合脚本整体 PASS（26 项）
2. 单 anchor 删除 sanity（Plan 1e R7 MEDIUM 修复验证——删除 "未知 state" 行必 FAIL）
3. m01 doc 7 H2 章节完整
4. 3 owner 角色齐全
5. M0.4 gate stub 不 regression（TODO Plan 3 P1 仍 ≥5 条）
6. Plan 1/1b/1c/1d acceptance 全绿
7. PR files 数组只含本 plan 4 个新文件（spec / plan / doc / script），不含任何 .swift/.py/.sql 代码
8. codex:adversarial-review PR verdict = approve

## 7. Plan 风险识别 + 缓解

| 风险 | 缓解 |
|---|---|
| codex 再发现 spec drift（Plan 1e 模式） | 已做 cross-ref scan（见本 spec §4 来源行号），找到的唯一未直接 bind 位点是 L2232（CI assert）→ 已登记为未来强制点，不列为 Plan 1f 实施范围 |
| aggregate 关键词阈值弱（Plan 1e R7 教训） | acceptance 改 17 项独立断言，每 anchor 单独可 FAIL |
| spec 引用的 migration SQL（L161-291）是否应在本 plan 纳入 | 否；已明确 out-of-scope（Plan 2 B3 实施） |
| 与 Plan 1g (M0.5) 重复劳动 | 两 plan 共享的 acceptance script pattern 抽象为通用模板（参考 Plan 1d），但内容完全独立 |

## 8. 依赖 / 不依赖

**依赖（hard prereq）**：
- Plan 1 (PR #23) merged — schema.sql / training_set_schema_v1.sql / ios/sql/app_schema_v1.sql 存在
- Plan 1d (PR #26 + #27) merged — m04 gate stub doc 存在（本 plan §7 交叉引用）

**不依赖**：
- Plan 1e (已 aborted)
- Plan 1g (后续，independent)
- 任何 Swift/Python 代码 layer（全 doc-only）

## 9. 执行切换提示

Spec approved（用户 2026-04-22 授权 autonomous）→ 直接 invoke `superpowers:writing-plans` 起草 Task 列表。Plan 预估 2 个 task：
- Task 1：起草 `docs/governance/m01-schema-versioning-contract.md`
- Task 2：写 `scripts/acceptance/plan_1f_m0_1_schema_versioning.sh`

然后 subagent-driven-development 执行 + codex:adversarial-review 收敛 + 用户终端 push PR + 非 coder 验收清单 evidence。
