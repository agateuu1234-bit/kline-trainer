# 划线工具扩充 · P1b 拆分补充 spec（P1b-1 / P1b-2）

> **母 spec**：`docs/superpowers/specs/2026-07-04-drawing-tools-expansion-design.md`（下称「母 spec」）。
> 本文件**只覆盖母 spec 的 §15 P1 行的交付粒度**，不改任何母 spec 的设计决策（D1…D22 全部继续生效）。
> **基线**：`96d2ac4`（P1a 契约地基已 merge，PR #140）。
> **日期**：2026-07-10。

---

## 0. 为什么要拆

母 spec 的 P1 是一个阶段，实际含 7 大子项（外壳 §2 / 设置面板 §3 / 节点 §6 / 选中·删除·锁定·撤销 §7 / 多锚泛化 §5.0 / 周期绑定 §10 / 手势消歧 §14）+ 5 个工具，估算 2500–3500 行生产代码。

P1a（纯契约 + 持久层，零 UI）已作为 PR #140 独立 merge。**剩余部分即本文件所称 P1b**，仍然过大：

- 违反 `CLAUDE.md` 治理约束「每 PR ≤3 子项、≤500 行」（P1a 已是超额特批）。
- P1a 的实证：单个大 PR 在本地 codex 对抗 review 走了 21 轮 29 finding 仍未自然 approve。体量越大越不收敛。

故 P1b 拆为**两个顺序 PR**：P1b-1、P1b-2。

## 1. 拆分结论（决策表）

| 决策 | 结论 | 理由 |
|---|---|---|
| **D23 切分线** | P1b-1 = 外壳 §2 + 设置面板/样式系统 §3 + 选中·删除·锁定·撤销 §7（**不含节点**）+ 水平线升级 §5.1 + 周期绑定渲染 §10。<br>P1b-2 = 多锚泛化 §5.0 + 节点模型与拖节点 §6 + 手势消歧 §14 + 趋势线 §5.2 / 通道线 §5.3 / 箱体 §5.11 / 折线 §5.4。 | 节点必须与「拖节点」同期落地，否则 P1b-1 会 ship 出**拖不动的死节点**（母 spec D19 明令禁止 ship 未接线控件）。周期绑定 §10 与工具无关（只改渲染过滤判据），单靠水平线即可完整演示与测试，放前面可减轻 P1b-2 体量。 |
| **D24 底栏一次到位** | P1b-1 即按母 spec §2 建**两行常驻栏**：上行类型行、下行 5 键。类型行**当期只渲染水平线 1 个图标**。P1b-2 只往类型行里填图标，底栏骨架不再改。 | 母 spec D22：类型行只显示/启用已落地工具，不得 ship 死图标。底栏布局一次定型 = 一次验收。长按设置面板的触发点就是类型行里的工具图标，没有类型行则面板需要临时入口，P1b-2 还得拆掉。 |
| **D25 撤销语义** | 撤销栈**深度 1**（母 spec §7 已定「各仅一步」）。**入栈动作 = 画线 / 删线 / 改样式 / 锁定·解锁**。做了新动作 → 前进（↪）置灰。**进画线模式时建栈，退出画线模式时清空**。 | 画线模式是一个有界的编辑会话，退出即提交。跨模式保留会让用户在交易了几十个 tick 之后意外撤销掉一条线；且跨模式保留会立刻牵出「撤销栈要不要随 autosave 落盘 / 断点续局后 ↩ 是否还灵」的新决策面，与本阶段无关。 |
| **D26 复盘过渡** | P1b-1 的新两行底栏**只在训练 / 再次训练（replay）出现**。**复盘模式完全不动**，继续使用现有 `DrawingToolFloatingView` 浮动铅笔钮，行为一字不改。P5 时复盘切到 6 键新底栏，届时删除 `DrawingToolFloatingView`。 | 母 spec D19：复盘专属控件（隐藏键、复盘删除、hiddenIds 持久化、clear-saved）全在 P5。若 P1b-1 让浮动钮退役而复盘又不上新底栏 → 复盘失去画线入口 = **功能回归**。若复盘上 5 键新底栏 → 其中 🔒/🗑 在复盘必须置灰（复盘删除属 P5、原训练线按 §7 只可隐藏/显示）= **死控件**，两者都不可接受。 |
| **D27 选中态视觉** | P1b-1 的选中态 = **线渲染为选中蓝 + 底栏 🗑/🔒 由灰变亮**，**不显节点圆**。P1b-2 补上两端/各转折点的实心圆节点。 | mockup「选中态」屏（`2026-07-03-drawing-tools-expansion.html`）画的是「线变蓝 + 带蓝外圈实心圆节点 + 🗑高亮」。D23 把节点推后，故 P1b-1 = 该视觉去掉圆点。选中反馈仍然完整可见（线变色 + 底栏键活化）。 |
| **D28 契约不动** | **P1b-1 与 P1b-2 均为纯 UI / 渲染层 PR：零迁移、不 bump `CONTRACT_VERSION`（保持 `1.11`）。** | `DrawingObject` 的 18 字段（含 `id` / `period` / `lineSubType` / `lineStyle` / `thickness` / `colorToken` / `labelMode` / `locked` / `text` / `fontSize` / `textColorToken` / `textForm` / `tailAnchor`）已由 P1a 全部落地（`Models/Models.swift:236-254`），迁移 0009 已随 1.11 ship。 |

---

## 2. P1b-1 交付范围（权威清单）

### 2.1 做

1. **画线模式外壳（母 spec §2，仅训练 / replay）**
   - 顶栏「结束」**左侧**加固定「画图」图标钮，与「结束」留明显间距。
   - 点「画图」→ 底部升起两行常驻栏；顶栏「结束」→「退出」。
   - 点「退出」→ 两行栏落下、恢复训练底栏、「退出」→「结束」。
   - **退出后所有画线惰性**：不可点选 / 不可拖动 / 不可删除。仅画线模式内可选中编辑。
   - 画布约束：画线只落在 **K 线主图区**（成交量 / MACD 副图不可画）；上下两面板都能画。
   - 画线模式内保留图表操作：单指横滑=平移、单指竖滑=切周期、双指=缩放、单击=落锚/选中。
     （**注**：本期不引入节点，故不存在「起手是否落在节点上」的分支；手势消歧 §14 的新增分支属 P1b-2。本期只需保证现有 `drawingMode` 语义下平移 / 切周期 / 缩放不被吞掉，并加测试证之。）

2. **两行底栏（母 spec §2 + 本文 D24）**
   - 上行 = **类型行**：本期只渲染水平线 1 个图标；选中态浅蓝框 + 浅蓝字。
   - 下行 = **5 键**：①类型（收/展类型行）②锁定 ③删除 ④撤销 ⑤前进。图标 only、无文字、同训练底栏高度。
   - 类型行收起后：只能用上次所选工具继续画；展开才能换（本期只有一个工具，收起 = 让出一行图表高度）。

3. **统一长按设置面板（母 spec §3）**
   - **长按**类型行里的工具图标 → 类型行上方弹出浮层卡片（无气泡尾巴）。短按只选工具。
   - 四组控件：线型子类 `[直线][射线][线段]` / 线样式 `[实线][虚线1..4]` / 粗细 5 档 / 颜色 9 色 / 标注 `[隐藏][显示][左][右]`。
   - 不可用项**只灰掉，不写任何「不适用」说明字**。
   - **昼夜禁色**：白天禁「白」、夜间禁「黑」，自动灰。
   - **水平线的可选矩阵**（母 spec §3.1）：直线 ✅ / 射线 ✅ / 线段 灰；标注可选 隐藏 / 左 / 右（**选射线时「左」再灰**；「显示」灰）。

4. **样式系统真正接入渲染**
   - `HorizontalLineTool.render` 现写死 `strokeRGBA = (0.82, 0.40, 0.0)` + `ctx.setLineWidth(1.5)`（`Drawing/HorizontalLineTool.swift:31-42`），**改为消费 `DrawingObject` 的 `colorToken` / `lineStyle` / `thickness` / `labelMode` / `lineSubType`**。
   - `colorToken` → 实际 RGBA 由**主题解析**（昼 / 夜两套），保证可读性。
   - 渲染 helper 必须是 **host 可测的纯函数**（非 `View`、非 `@MainActor` 隔离），沿现有 `CoordinateMapper` 风格。

5. **水平线升级（母 spec §5.1）**
   - 线型：**直线**（全宽横线）/ **射线**（自落点向右到主图右缘）。**无线段**。
   - 标注：隐藏 / 左 / 右（价格）。价格标签**紧贴线上方、不压线**；射线时靠右缘，**防贵股 4 位整数价溢出**（须裁剪或右对齐至主图右缘内）。
   - hitTest：`|point.y - lineY| ≤ 容差`（复用现有 `HorizontalLineTool.hitTest`）。

6. **选中 / 删除 / 锁定 / 撤销·前进（母 spec §7，去节点）**
   - **选中**：画线模式内单击 → 遍历**该面板当前周期所有可见画线**逐个 `hitTest`，命中返回 `DrawingObject.id`（**不用数组下标**）。命中后线渲染为选中蓝，底栏 🗑 / 🔒 由灰变亮。
     - 注：`hitTest` 在 P1a 之后仍是**从未被生产代码调用**的协议方法（仅测试用）。本期是它第一次接线。
   - **删除**：点 🗑 → 弹确认 `确定删除划线？[删除][取消]`。选中线被锁 → 🗑 灰。
   - **锁定 / 解锁**：短按 🔒 锁定选中线（🔓→🔒 图标态）。**锁定线仍可被选中**（否则无法解锁），但 🗑 灰、设置面板全灰、不可改样式。**不在线旁画小锁图标**。`locked` 随画线一起落盘。
   - **撤销 / 前进**：见 D25。撤销直接改引擎里的画线数组，因此照常走现有 autosave 落盘路径。

7. **周期绑定渲染（母 spec §10）**
   - `RenderStateBuilder.swift:65-68` 现按 `drawing.panelPosition == (panel == .upper ? 0 : 1)` 过滤 → **改为按 `drawing.period == 该面板当前显示的周期` 过滤**。
   - 叠加现有 `drawing.revealTick <= tick` 渐显规则**不变**。
   - 某周期不在上下任一面板显示 → 其画线暂不渲染，切回再现。
   - 提交新画线时 `DrawingObject.period` 取 `anchors.first.period`。`panelPosition` 仍记当时面板，但**不再参与渲染判据**。

8. **设置面板的作用对象（消歧，母 spec §7 隐含）**
   - **有选中线** → 面板改的是那条线（按 `id` 定位）。
   - **无选中线** → 改的是「该工具下一条要画的线」的默认值。
   - 该 per-tool 默认值存在**内存、整局有效、不落盘**（持久化的全局默认属 P6 §13）。

### 2.2 不做（明确排除，加测试证之）

| 不做项 | 归属 |
|---|---|
| 节点圆点、拖节点、删单节点 | P1b-2（§6 / §7） |
| 手势消歧新增分支（起手是否落在节点上） | P1b-2（§14） |
| 多锚落点泛化（`shouldCommit` 按 `requiredAnchors` 收锚） | P1b-2（§5.0） |
| 趋势线 / 通道线 / 箱体 / 折线（含折线画制中的临时 4 键） | P1b-2 |
| 黄金率 / 波浪尺 / 周期线 / 斐波那契 / 时间尺 | P2 |
| 标注（text）工具、字号 / 字色 / 3 形式 / 气泡尾巴 / 输入流程 | P3 |
| 局部放大镜、吸附 | P4 |
| 复盘 6 键栏、隐藏原训练线、复盘删除、hiddenIds 写入、删空清已复盘 | P5 |
| 主页齿轮「画线设置」全局默认 | P6 |

### 2.3 必须存在的负向测试（防 ship 死控件 / 防回归）

1. **复盘模式下新两行底栏不存在**；`DrawingToolFloatingView` 仍存在且行为不变（D26 / 母 spec D19）。
2. **类型行本期只含 1 个图标**（水平线）；不渲染任何其它 `DrawingToolType` 图标（母 spec D22）。
3. **退出画线模式后**，单击图表不选中任何画线、`hitTest` 不被调用、🗑 / 🔒 / ↩ / ↪ 全部不可达。
4. **画线模式内**：单指横滑仍平移、单指竖滑仍切周期、双指仍缩放（不被画线吞掉）。
5. **锁定线**：🗑 灰、设置面板全灰；但仍可被选中并解锁。
6. **周期绑定**：某周期不在任一面板显示时，其画线不进 `RenderStateBuilder` 输出；切回后出现；`revealTick > tick` 的线仍不渲染。
7. **`routeDrawingCommit` 全字段存活**：提交后 `id` / `period` / 五个样式字段 / `locked` 不丢（P1a 已加，本期回归保护）。

---

## 3. P1b-2 交付范围（备忘，不在本次 plan 内）

多锚落点泛化 §5.0（`handleDrawingTap` 改用 `manager.activeTool` + `requiredAnchors` 收锚，折线不定锚数特例）；节点模型 §6（纯黑/纯白实心圆、仅选中态显示、拖节点几何实时跟随、仅折线可删单节点）；手势消歧 §14（起手落在选中线的节点上 → 节点拖动；否则按方向分平移 / 切周期）；四个工具 §5.2 / §5.3 / §5.4 / §5.11（含折线画制中临时 4 键 `[取消划线][完成划线][回退][前进]`）。类型行届时填满 5 个图标。

---

## 4. 非程序员验收清单（P1b-1）

治理要求：每阶段交付必须附「动作 / 预期 / 通过-不通过」的中文清单，由非程序员在模拟器或真机上逐条执行。

| # | 动作 | 预期 | 通过 / 不通过 |
|---|---|---|---|
| 1 | 进入训练，看顶栏 | 「结束」左边出现「画图」图标钮，两者之间有明显间距 | |
| 2 | 点「画图」 | 底部升起两行栏（上行 1 个水平线图标，下行 5 个图标键）；顶栏「结束」变成「退出」 | |
| 3 | 在 K 线主图区点一下 | 落下一条水平线 | |
| 4 | 在成交量 / MACD 副图区点一下 | **不落线**（副图不可画） | |
| 5 | 长按类型行的水平线图标 | 类型行上方弹出设置卡片，含 线型子类 / 线样式 / 粗细 / 颜色 / 标注 五组 | |
| 6 | 在设置卡片里看「线段」和「标注-显示」 | 两者是灰的、点不动，且**卡片上没有任何解释文字** | |
| 7 | 选「射线」 | 「标注-左」也变灰 | |
| 8 | 白天模式下看颜色行 | 「白」是灰的、点不动 | |
| 9 | 切到夜间模式再看颜色行 | 「黑」是灰的、点不动 | |
| 10 | 设成 红色 + 虚线 + 3 档粗细 + 标注靠右，关掉卡片，再画一条线 | 新线是红色虚线、比默认粗、右侧贴着价格标签，标签不压在线上 | |
| 11 | 单击已有的一条线 | 线变蓝；底栏 🗑 和 🔒 从灰变亮 | |
| 12 | 点 🗑 | 弹出「确定删除划线？[删除][取消]」 | |
| 13 | 点「删除」 | 线消失 | |
| 14 | 点 ↩（撤销） | 线回来了 | |
| 15 | 点 ↪（前进） | 线又没了 | |
| 16 | 再点 ↪ | 没反应（↪ 已置灰，深度只有一步） | |
| 17 | 选中一条线，点 🔒 | 图标变成已锁状态；🗑 变灰；长按工具图标弹出的卡片整片是灰的 | |
| 18 | 再点 🔒 | 解锁，🗑 恢复可用 | |
| 19 | 点底栏①「类型」键 | 类型行收起，图表多出一行高度；再点展开 | |
| 20 | 在上半面板（假设显示 60 分）画一条线，然后单指竖滑切周期，让 60 分挪到下半面板 | 那条线**跟着 60 分跑到下半面板**显示 | |
| 21 | 继续切周期，让 60 分完全不显示 | 那条线暂时消失 | |
| 22 | 切回来让 60 分重新显示 | 那条线重新出现，位置不变 | |
| 23 | 画线模式内单指横向拖 | 图表左右平移（没被画线吞掉） | |
| 24 | 画线模式内双指捏合 | 图表缩放正常 | |
| 25 | 点「退出」 | 两行栏落下、恢复训练底栏、「退出」变回「结束」 | |
| 26 | 退出画线模式后单击一条线 | **没有任何反应**（不变蓝、不出现删除按钮） | |
| 27 | 结束这一局，进入复盘 | 复盘里还是原来那个**可拖动的浮动铅笔钮**，**没有**两行底栏 | |
| 28 | 在复盘里用浮动钮画线 | 行为跟改造前一模一样 | |
| 29 | 退出 App 重进、续上这一局 | 之前画的线（颜色 / 线型 / 粗细 / 标注 / 锁定状态）全部还在，且 ↩ / ↪ 是灰的 | |
| 30 | 再次训练（replay）模式 | 同样有「画图」钮和两行底栏，行为与训练一致 | |

---

## 5. 契约 / 验收指针

- `CONTRACT_VERSION` 保持 `"1.11"`；**无 schema 迁移**；`user_version` 保持 `7`。
- 三绿门（作者亲核，clean build）：host `swift test` 全绿 + Mac Catalyst `build-for-testing` SUCCEEDED + iOS build。
- 流程：本 spec → codex 对抗 review 收敛 → `superpowers:writing-plans` → codex → `superpowers:subagent-driven-development` → 三绿 → `superpowers:requesting-code-review` + whole-branch codex → PR。
