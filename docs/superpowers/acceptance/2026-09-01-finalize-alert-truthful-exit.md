# 验收清单：「结算入账失败」弹窗补非破坏性出口 + 文案改实话

**这次改了什么**：只改了**一个弹窗**——它在打完一局、成绩写不进历史记录时才会出现。
原来它只有「重试」和「放弃」两个按钮，而且**下面那句说明是反的**：写着「进度保留至最近存档」，
实际上按「放弃」会把**整局进度永久删掉**。现在补了一个真的会保留进度的出口，并把话说实。

**⛔ 没有改任何行为逻辑** —— 「重试」还是重试，「放弃本局」还是真的弃局；只是多了一条安全出口，
并且把每个按钮的后果说清楚了。

---

## 一、正常使用（这几条现在就能测）

| # | 动作 | 预期 | 通过/不通过 |
|---|---|---|---|
| 1 | 正常打完一整局，结算入账 | 与改动前**完全一样**，正常写进历史记录，**不会**弹出任何新窗口 | ☐ 通过 ☐ 不通过 |
| 2 | 训练中点左上角「返回」 | 与改动前**完全一样**，进度保存后退出 | ☐ 通过 ☐ 不通过 |
| 3 | 点「结束本局」→ 确认 | 与改动前**完全一样** | ☐ 通过 ☐ 不通过 |
| 4 | 打开历史记录做一次复盘 | 与改动前**完全一样** | ☐ 通过 ☐ 不通过 |

> ⚠️ 第 1–4 条的通过标准都是「跟以前一模一样」。**本次改动只影响那个失败弹窗，正常路径一个像素都不该变。**

## 二、那个弹窗本身（⚠️ 正常用是看不到的）

**为什么看不到**：它只在「结算写入失败」时出现，而本地写入平时不会失败。要看到它，得**人为制造一次写入失败**——
最现实的办法是**把设备存储塞满**再打完一局。若不便复现，第 5–8 条可记「未复现」，
并以下面「三、代码层面的保障」作为替代证据。

| # | 动作 | 预期 | 通过/不通过 |
|---|---|---|---|
| 5 | 制造写入失败后打完一局，看弹出的窗口 | 有**三个**按钮：**重试** / **退出本局** / **放弃本局**（原来只有两个） | ☐ 通过 ☐ 不通过 ☐ 未复现 |
| 6 | 看「放弃本局」的颜色 | 显示成**红色**（系统据此提示这是破坏性操作）。原来它不是红的 | ☐ 通过 ☐ 不通过 ☐ 未复现 |
| 7 | 看窗口里那段说明文字 | **不再**出现「进度保留至最近存档」这句话；改为分别说清三条出口的后果：退出本局**保留进度**并告诉你**去首页点「继续训练」**回来（⚠️ **不是**去历史记录找 —— 这一局还没入账，历史记录里根本没有它）、若此刻写不进去则**保留到最近一次自动存档**、放弃本局会**丢弃**本局进度 | ☐ 通过 ☐ 不通过 ☐ 未复现 |
| 8 | 点「退出本局」，回到首页 | 首页那个大按钮显示**「继续训练」**（不是「开始训练」）—— 说明这一局还在；点它能接着打 | ☐ 通过 ☐ 不通过 ☐ 未复现 |
| **8b** | **⭐**存储已满，但**本局之前成功自动存过档**（打了一会儿才把存储塞满），点「退出本局」 | **照样能退出**，回首页后按钮仍是**「继续训练」**——这一局没丢（退回到最近那次自动存档）。⚠️ 若被弹回一个只能「重试／放弃」的窗口，就是**不通过** | ☐ 通过 ☐ 不通过 ☐ 未复现 |
| **8c** | **⭐⭐最要紧的一条**：存储**从一开始就满**（这一局**一次都没存成功过**），点「退出本局」 | **不会退出**，而是弹出「**暂时退不出本局**」，明说「现在退出会把这一局全部丢失，所以没有退出」，让你先清理存储。⚠️ **如果它直接把你送回首页、而首页没有「继续训练」，就是不通过** —— 那意味着整局被悄悄丢掉了 | ☐ 通过 ☐ 不通过 ☐ 未复现 |
| **9b** | 存储满的情况下点「放弃本局」（清除存档也会失败） | 弹出「**放弃未完成**」，明说「本局没有被放弃，你仍在这一局里」，**不会**把你送回首页。⚠️ 若它送你回了首页却其实没删掉，就是**不通过** | ☐ 通过 ☐ 不通过 ☐ 未复现 |
| 9 | （可选，会真的删数据）重来一次，改点「放弃本局」 | 这一局**被丢弃**，不再出现在「未完成」里 —— 与它红色的警示一致 | ☐ 通过 ☐ 不通过 ☐ 未复现 |

## 三、代码层面的保障（第 5–8 条无法复现时的替代证据）

- **四条源码守卫**（`FinalizeFailureAlertSourceGuardTests`）分别钉死：恰好三个按钮 / 「退出本局」走的是**不依赖存储层**那条路 / 「放弃本局」标了破坏性 / 文案不再含那句假话、指对回去的地方、并说清最坏保留到哪一档；
- **五条真行为测试**（`FinalizeAlertSafeExitTests`，不是扫源码，是真跑逻辑）：落盘成功 / 落盘失败但有旧存档时可安全退出且存档原样留存 / **⭐⭐既没落成也没有旧存档时⛔绝不结束会话**（否则整局只在内存里，一退就没）/ 三态如实区分 / 反向对照证明它一次都没碰「清空」那条路；
- **六条源码守卫**，其中两条专门钉死界面：退出必须**分三态处置**且「保不住」那一态**不得离开本局**、放弃必须**成功才离开**（⛔ 不得吞掉错误就回首页）；
- 这四条在改动**之前是红的、之后是绿的**（先红后绿，证明它们真的在测这件事）；
- 另做了一次**变异验证**：把「退出本局」改成走删除那条路，**只有对应那一条守卫变红**，其余不受影响；
- **Catalyst 真编译真执行**（这个界面文件只在真机环境下编译，普通测试根本不编译它），闸门脚本判绿。

## 四、这次**没有**做的事

- ⛔ 没有改「放弃本局」的行为 —— 它仍然会真的清掉这一局（用户确实想扔时必须扔得干净，否则每次开 App 都会被同一个结算不了的坏局纠缠）；
- ⛔ 没有碰划线工具的任何东西（本次与划线无关，是结算流程自己的问题）；
- ⛔ 没有改另外两个弹窗（它们本来就是对的，本次是把落单的这个改得跟它们一致）。

## 五、⚠️ 本次**发现但没修**的一个独立问题（已另立待办）

**缓存清理会把「正在用的」训练组文件删掉。**

- 实测：`DefaultFileSystemCacheManager.evictIfNeededLocked` **纯按时间排序**砍掉最旧的（上限 20 个训练组），**完全不保护**当前这一局正在用的那份；而且**每次下载入库都会触发**。
- 后果：后台下载够多的话，当前这一局的训练组文件可能被删掉 —— 那时候首页的「继续训练」就打不开了。
- **⛔ 这不是本次改动引入的**，它影响的是**「继续训练」这个功能本身**，跟这个弹窗无关；本次改动也没让它变坏（改之前用户在这个弹窗里只有「放弃」，必删）。
- **⛔ 为什么不在这次顺手修**：在「退出」那一刻验证文件还在，**关不上这个洞** —— 用户退出**之后**照样会被淘汰。真正能修好的办法只有**把在用文件钉住、不许淘汰**，那是缓存子系统的改动。
- **建议单独立项**：给缓存加「在用 / 有 pending 引用的文件不参与淘汰」的保护，并配「下载 21 个训练组后，当前局的文件仍在」的回归测试。
