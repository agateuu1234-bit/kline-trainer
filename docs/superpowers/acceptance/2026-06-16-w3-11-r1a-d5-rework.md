# W3-11-R1a redux — offsetBounds D5 rework（reveal RFC #113 后）验收清单

> **本交付性质**：纯 Render 层几何 helper 的 **rebase + D5 重做**（reveal RFC #113 `bb0d597` merged 后）。`geometryCore.upperBound` 收紧为 `max(0,baseStartIndex)`（禁前窥语义下沉）、`offsetBounds` 返回 `OffsetBounds` struct（D5：`minOffset=0`/`maxOffset=max(0,base)·step` + `bounceEdges` 类型级编码单边 bounce 非对称）。**零运行时行为改动**（makeViewport 输出 == reveal-on-main，行为保真）+ **不接线任何 engine/gesture/EdgeBounceModel**（R1b-wire scope）。
>
> 故验收判据 = **确定性单元测试 + 范围 gate + reveal 行为零回归 + codex APPROVE 落账**，而非 App 屏幕肉眼效果。
>
> **如何用本清单**：你（非编码者）逐条照抄「操作命令」到终端回车，把屏幕输出对照「预期输出」，吻合即「判定」勾 ✅，不吻合勾 ❌。每条二元（是/否）判据，无需读代码。
>
> **运行前置**：终端先进入工作树根目录（含空格，须带引号）：
> ```bash
> cd "/Users/maziming/Coding/Prj_Kline trainer/.claude/worktrees/wave3-pr13-completion"
> ```

---

## 第 1 条 · 范围 gate（改动文件白名单 fail-closed）

**目的**：确认本交付只动 1 个生产 Swift + 1 个测试 Swift + 3 个文档 + 1 个本验收，**没有**碰 engine/gesture/容器/Reducer/EdgeBounceModel/DecelerationAnimator 等。

**操作命令**（整段粘贴回车）：
```bash
set -euo pipefail
ALLOW='ios/Contracts/Sources/KlineTrainerContracts/Render/RenderStateBuilder.swift
ios/Contracts/Tests/KlineTrainerContractsTests/Render/RenderStateBuilderTests.swift
docs/superpowers/specs/2026-06-15-w3-11-r1-bounce-wiring-design.md
docs/superpowers/plans/2026-06-15-w3-11-r1a-bounds-geometry.md
docs/superpowers/plans/2026-06-15-wave3-fastfollow-parallelization.md
docs/superpowers/acceptance/2026-06-16-w3-11-r1a-d5-rework.md'
changed=$(git diff --name-only origin/main...HEAD)
extra=$(comm -23 <(echo "$changed" | sort -u) <(echo "$ALLOW" | sort -u))
if [ -z "$extra" ]; then echo "GATE-PASS：仅白名单文件改动"; else echo "GATE-FAIL：越界文件："; echo "$extra"; fi
```

**预期输出**：单行 `GATE-PASS：仅白名单文件改动`。

**判定**：屏幕打印 `GATE-PASS` → ✅；出现 `GATE-FAIL` 及任意文件名 → ❌。

---

## 第 2 条 · offsetBounds D5 数值契约（minOffset=0 / maxOffset=max(0,base)·step）

**目的**：确认 offsetBounds 按 reveal D5 产出——最新边 `minOffset=0`（当前 tick，禁前窥）、最老边 `maxOffset=710`（base 71·step 10）。

**操作命令**（`--filter` 按**函数名**匹配）：
```bash
cd ios/Contracts && swift test --filter offsetBounds_known 2>&1 | grep "✔ Test"; cd - >/dev/null
```

**预期输出**：`✔ Test "offsetBounds（reveal D5）：count=200/currentIdx=150/width=800 → max=710,min=0,step=10" passed ...`。

**判定**：该 test 行显示 `passed` → ✅；无输出或 `✘` → ❌。

---

## 第 3 条 · reveal 行为零回归（makeViewport 输出 == reveal-on-main）

**目的**：确认 geometryCore 重构后 makeViewport 行为与 reveal-on-main 逐位一致——reveal 自带的「禁前窥」测试全绿。

**操作命令**（两步：①数 reveal- 前缀 passed 测试 ②确认 suite 内 0 失败）：
```bash
cd ios/Contracts && echo "reveal-passed=$(swift test --filter RenderStateBuilderTests 2>&1 | grep -E '✔ Test \"reveal ' | wc -l | tr -d ' ')"; cd - >/dev/null
cd ios/Contracts && echo "fail=$(swift test --filter RenderStateBuilderTests 2>&1 | grep -c '✘ Test')"; cd - >/dev/null
```

**预期输出**：`reveal-passed=4`（reveal 前向滚动禁 / 早 tick 修复 / 不变量扫描 / backward 历史 四条 passed）+ `fail=0`。

**判定**：`reveal-passed` ≥ 4 且 `fail=0` → ✅；`fail` > 0 或 `reveal-passed` < 4 → ❌。

---

## 第 4 条 · bounceEdges 单边非对称（最新边硬钳，.min 永不可弹）

**目的**：确认 `OffsetBounds.bounceEdges` 类型级编码非对称——最新边（minOffset）**永不在 bounceEdges**（硬钳），仅最老边可弹。这是 R1b-wire 单边 bounce 的契约来源（codex R3）。

**操作命令**：
```bash
cd ios/Contracts && swift test --filter offsetBounds_bounceEdgesAsymmetryInvariant 2>&1 | grep "✔ Test"; cd - >/dev/null
```

**预期输出**：`✔ Test "offsetBounds.bounceEdges 不变量（codex R3）：.min 永不可弹（硬钳）+ [.max] iff 有滚动空间" passed ...`。

**判定**：该 test 行 `passed` → ✅；无输出或 `✘` → ❌。

---

## 第 5 条 · 空 candle 退化（codex R1，不依赖 caller currentIdx）

**目的**：确认空 candle（candleCount==0）时 offsetBounds 返 [0,0] 不漏（含哨兵 currentIdx=0）。

**操作命令**：
```bash
cd ios/Contracts && swift test --filter offsetBounds_emptyCandlesDegenerate 2>&1 | grep "✔ Test"; cd - >/dev/null
```

**预期输出**：`✔ Test "offsetBounds 空 candle 退化（codex R3）：candleCount==0 → [0,0]，任意 currentIdx 哨兵（含 0）不漏" passed ...`。

**判定**：该 test 行 `passed` → ✅；无输出或 `✘` → ❌。

---

## 第 6 条 · 全量 host 测试 + Mac Catalyst build 零回归

**目的**：确认全包 host 单测全绿 + Mac Catalyst 测试 build 成功（governance 闸门）。

**操作命令**（两步，逐条回车；第二步约 30 秒）：
```bash
cd ios/Contracts && swift test 2>&1 | grep -E "Test run with"; cd - >/dev/null
cd ios/Contracts && xcodebuild build-for-testing -scheme KlineTrainerContracts -destination 'platform=macOS,variant=Mac Catalyst' 2>&1 | tail -1; cd - >/dev/null
```

**预期输出**：第一行 `Test run with 1032 tests in 144 suites passed`（数字 ≥ 1032，0 failures）；第二行 `** TEST BUILD SUCCEEDED **`。

**判定**：两行分别含 `passed`（0 failures）与 `TEST BUILD SUCCEEDED` → ✅；任一含 `failed`/`FAILED`/`error` → ❌。

---

## 第 7 条 · codex 对抗性 review APPROVE 落账（ledger）

**目的**：确认本分支 head 的 codex:adversarial-review 已 `approve` 且写入 ledger（治理强制 trust-boundary review channel）。

**操作命令**：
```bash
grep -F "wave3-w3-11-r1-bounce-wiring@$(git rev-parse HEAD)" .claude/state/codex-attest-ledger.jsonl 2>/dev/null | grep -o '"verdict":"[a-z-]*"' | tail -1
```

**预期输出**：`"verdict":"approve"`。

**判定**：打印 `"verdict":"approve"` → ✅；空输出或非 approve → ❌。
（注：本分支 head 在「加入本验收 doc」后会重跑 codex 落账，故 `$(git rev-parse HEAD)` 与 ledger 中 approve 条目一致；若 ledger 路径不同，以 `codex-attest` 运行时打印的 `[codex-attest] ledger updated` + 同一 head SHA 为准。）

---

## 残留（移交 R1b-wire，非本交付义务）

- **`W3-11-R1b-wire` 单边 bounce（codex R2/R3）**：现有 `EdgeBounceModel` 把任一端点当对称弹簧；reveal 下 R1b **必须**把最新边（`minOffset=0`，`bounceEdges` 不含 `.min`）实现为硬钳——抑制最新边负速 / below-min spring，否则 fling 会 spring `offset<0` = 前向揭示。R1a 已在 render 层兜底（offset<0 钳 upperBound 无前向间隙，测 `offsetBounds_minOffsetIsHardClampNotSpring`）+ 类型级 `bounceEdges` 编码；R1b 须按此单边化接线。详见 spec §六 B4 NOTE。
- **R1b-wire / R1b-drag 全量接线**：engine/gesture 接线 + B4 overscroll 渲染 + drag-clamp + Coordinator 每帧喂 bounds + 归一/strand + device runbook（spec §八）。
