# W3-11-R1b-drag 拖拽期跟手橡皮筋阻尼 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 手指按住拖过最老边时内容带 iOS 橡皮筋阻尼跟手、松手回弹到最老边；最新边保持硬钳无给。

**Architecture:** engine 侧新增 per-panel `dragRaw` 累加器（未阻尼累计意图位移）+ 1 个平台无关纯函数 `RubberBand.damp`；`applyPanOffset`(public 带 bounds) 改为 raw→offset 单边映射；`endPan` 加 drag-overscroll 无条件弹回分支；`cancelPan`/`recordRenderBounds` 补 dragRaw 清理/重同步。**零渲染层改动**（B4 已渲 `offset>maxOffset`），**零 EdgeBounceModel 物理改动**，**零手势契约改动**。

**Tech Stack:** Swift 6（strict concurrency complete）、Swift Testing（`@Test`/`#expect`）、CoreGraphics、host 平台无关测试优先。

**Spec:** `docs/superpowers/specs/2026-06-16-w3-11-r1b-drag-design.md`（v1.1，opus 4.8 xhigh 对抗性 review R1 APPROVE）。

**评审通道:** 改 `ios/**/*.swift`（trust-boundary）→ `codex:adversarial-review`（配额耗尽 fallback opus 4.8 xhigh）+ Mac Catalyst build-for-testing。

---

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `ios/Contracts/Sources/KlineTrainerContracts/ChartEngine/RubberBand.swift`（新） | iOS 标准橡皮筋阻尼纯函数（平台无关） | Create |
| `ios/Contracts/Tests/KlineTrainerContractsTests/RubberBandTests.swift`（新） | RubberBand 性质测试（§六.1） | Create |
| `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift` | dragRaw 存储+accessor / applyPanOffset D2 / beginPan seed / endPan D3 / cancelPan E4 / recordRenderBounds E5 | Modify |
| `ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineDragDampingTests.swift`（新） | drag 阻尼/反拖/单边/无空间/endPan 弹回/lifecycle/cancel/resize（§六.2-9） | Create |
| `ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift` | `dragFullClamp` 测试语义更新为阻尼（§六.10） | Modify |
| `docs/superpowers/acceptance/2026-06-16-w3-11-r1b-drag-acceptance.md`（新） | 非-coder 验收 checklist | Create |

**复用既有测试 harness**：`TrainingEngineBounceWiringTests.makeEngine(count:tick:)`（static，@testable 同模块可调）→ count=200/tick=150 → `mainW=800/step=10/base=71/maxOffset=710/bounceEdges=[.max]`；`bounds=CGRect(0,0,800,600)`；`FakeFrameDriver.fire(dt)` 驱动动画；`e.upperPanel.offset` 读值。

---

## Task 1: RubberBand 纯函数

**Files:**
- Create: `ios/Contracts/Sources/KlineTrainerContracts/ChartEngine/RubberBand.swift`
- Test: `ios/Contracts/Tests/KlineTrainerContractsTests/RubberBandTests.swift`

- [ ] **Step 1: 写失败测试**（§六.1 性质 + 退化）

```swift
// RubberBandTests.swift
import Testing
import CoreGraphics
@testable import KlineTrainerContracts

@Suite("RubberBand 橡皮筋阻尼纯函数")
struct RubberBandTests {
    @Test("damp(0,d)==0、负 over→0")
    func zeroAndNegative() {
        #expect(RubberBand.damp(over: 0, dimension: 800) == 0)
        #expect(RubberBand.damp(over: -50, dimension: 800) == 0)
    }

    @Test("单调增 + 永远 < over（被压缩）")
    func monotonicAndCompressed() {
        var prev: CGFloat = 0
        for x in stride(from: CGFloat(1), through: 5000, by: 50) {
            let y = RubberBand.damp(over: x, dimension: 800)
            #expect(y > prev)          // 单调增
            #expect(y < x)             // 阻尼压缩
            prev = y
        }
    }

    @Test("渐近上界 d：大 over → 趋近但 < dimension；巨值无 NaN/overflow")
    func asymptote() {
        // damp(100_000,800) = (1 − 1/(100000·0.55/800+1))·800 ≈ 788.53（98.5% of d，已逼近但 < 800）
        #expect(RubberBand.damp(over: 100_000, dimension: 800) < 800)
        #expect(RubberBand.damp(over: 100_000, dimension: 800) > 785)         // 已逼近 d（实际 ≈788.53）
        let huge = RubberBand.damp(over: .greatestFiniteMagnitude, dimension: 800)
        #expect(huge.isFinite)
        #expect(huge <= 800)
    }

    @Test("退化 dimension<=0 → 不阻尼（返 over）")
    func degenerateDimension() {
        #expect(RubberBand.damp(over: 123, dimension: 0) == 123)
        #expect(RubberBand.damp(over: 123, dimension: -5) == 123)
    }

    @Test("f'(0)≈c=0.55：边缘起点斜率")
    func slopeAtEdge() {
        let eps: CGFloat = 0.001
        let slope = RubberBand.damp(over: eps, dimension: 800) / eps
        #expect(abs(slope - 0.55) < 0.01)
    }
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd ios/Contracts && swift test --filter RubberBandTests`
Expected: 编译失败（`RubberBand` 未定义）。

- [ ] **Step 3: 实现 RubberBand**

```swift
// RubberBand.swift
import CoreGraphics

/// iOS UIScrollView 同款橡皮筋阻尼（拖拽期跟手过界压缩）。平台无关纯函数。
/// f(x) = (1 − 1/(x·c/d + 1))·d，c=0.55。性质：f(0)=0、单调增、f(x)<x(x>0)、渐近上界 d、f'(0)=c。
enum RubberBand {
    static let c: CGFloat = 0.55

    /// - Parameters:
    ///   - over: 越界距离（应 ≥0；<0 视作 0）。
    ///   - dimension: 主图宽（应 >0；≤0 退化为不阻尼，直接返 over，由上层 floor 兜底）。
    static func damp(over: CGFloat, dimension: CGFloat) -> CGFloat {
        guard over > 0 else { return 0 }
        guard dimension > 0 else { return over }       // 非有限/退化几何：不阻尼
        return (1 - 1 / (over * c / dimension + 1)) * dimension
    }
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd ios/Contracts && swift test --filter RubberBandTests`
Expected: PASS（5 tests）。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/ChartEngine/RubberBand.swift ios/Contracts/Tests/KlineTrainerContractsTests/RubberBandTests.swift
git commit -m "feat(r1b-drag): RubberBand iOS 标准橡皮筋阻尼纯函数 + 性质测试"
```

---

## Task 2: engine dragRaw 存储 + 生命周期（beginPan seed / endPan·cancelPan 清）

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift`（存储 ~54 后、accessor ~606 后、beginPan :652、endPan :683、cancelPan :699）
- Test: `ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineDragDampingTests.swift`

> 本 task 只装生命周期脚手架（dragRaw 被 seed/清，但 `applyPanOffset` 映射仍是 Task 3 引入）——此时 dragRaw 被 seed 但累加/映射尚未接，故仅测 lifecycle。

- [ ] **Step 1: 写失败测试**（§六.8 lifecycle）

```swift
// TrainingEngineDragDampingTests.swift（新文件头）
import Testing
import Foundation
import CoreGraphics
@testable import KlineTrainerContracts

@MainActor
@Suite("TrainingEngine drag 跟手橡皮筋阻尼（R1b-drag）", .serialized)
struct TrainingEngineDragDampingTests {
    static let bounds = TrainingEngineBounceWiringTests.bounds
    static func makeEngine() -> (TrainingEngine, () -> [FakeFrameDriver]) {
        TrainingEngineBounceWiringTests.makeEngine(count: 200, tick: 150)
    }

    @Test("dragRaw 生命周期：beginPan seed=offset / endPan·cancelPan 清 / 两面板独立")
    func dragRawLifecycle() {
        let (e, _) = Self.makeEngine()
        #expect(e.debug_dragRawFor(.upper) == nil)
        e.beginPan(panel: .upper)
        #expect(e.debug_dragRawFor(.upper) == e.upperPanel.offset)   // seed=归一后 offset
        #expect(e.debug_dragRawFor(.lower) == nil)                   // 面板独立
        e.endPan(velocity: 0, renderBounds: Self.bounds, panel: .upper)
        #expect(e.debug_dragRawFor(.upper) == nil)                   // endPan 清
        e.beginPan(panel: .lower)
        e.cancelPan(panel: .lower)
        #expect(e.debug_dragRawFor(.lower) == nil)                   // cancelPan 清
    }
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd ios/Contracts && swift test --filter dragRawLifecycle`
Expected: 编译失败（`debug_dragRawFor` 未定义）。

- [ ] **Step 3: 实现 dragRaw 存储 + accessor + 生命周期接线 + 测试钩子**

存储（紧随 `activeBounds` 声明 `:55` 后，即 `ActiveDecel` 结构体闭合 `}` `:54` 之下那行）：
```swift
/// R1b-drag（D1）：单指 drag 期**未阻尼累计意图位移**（raw）。`beginPan` seed=归一后 offset；
/// `applyPanOffset` 累加；`endPan`/`cancelPan` 清 nil；`recordRenderBounds` resize 时重同步（E5）。
/// `@ObservationIgnored`：纯 numeric（同 activeBounds 模式）。
@ObservationIgnored private var dragRaw: (upper: CGFloat?, lower: CGFloat?) = (nil, nil)
```

accessor（紧随 `setActiveBounds` `:606` 后）：
```swift
private func dragRawFor(_ panel: PanelId) -> CGFloat? {
    panel == .upper ? dragRaw.upper : dragRaw.lower
}
private func setDragRaw(_ v: CGFloat?, panel: PanelId) {
    if panel == .upper { dragRaw.upper = v } else { dragRaw.lower = v }
}
```

测试钩子（`#if DEBUG` 暴露 private 给 @testable host 测；放文件末尾 extension）：
```swift
#if DEBUG
extension TrainingEngine {
    /// R1b-drag 测试专用：读 dragRaw（生命周期断言）。
    func debug_dragRawFor(_ panel: PanelId) -> CGFloat? { dragRawFor(panel) }
}
#endif
```

`beginPan`（`:652-655`，在 `interruptDeceleration` 之后、`reduce(.panStarted)` 之前加 seed）：
```swift
public func beginPan(panel: PanelId) {
    interruptDeceleration(panel: panel)
    setDragRaw(panelState(panel).offset, panel: panel)   // R1b-drag D1：raw 基线=归一后 offset∈[0,maxOffset]（E1）
    _ = reduce(.panStarted, on: panel)
}
```

`endPan`（public 带 bounds，`:683` 体首加清）：
```swift
public func endPan(velocity: CGFloat, renderBounds: CGRect, panel: PanelId) {
    setDragRaw(nil, panel: panel)                        // R1b-drag：清 raw（Task 4 再加 D3 弹回分支）
    let ob = RenderStateBuilder.offsetBounds(engine: self, panel: panel, bounds: renderBounds)
    guard case .startDeceleration(let v) = reduce(.panEnded(velocity: velocity), on: panel) else { return }
    if v > 0 && ob.bounceEdges.contains(.max) {
        setActiveBounds(ActiveDecel(bounds: ob, allowOverscroll: true), panel: panel)
        animator(for: panel).start(initialVelocity: v, fromOffset: panelState(panel).offset,
                                   minOffset: ob.minOffset, maxOffset: ob.maxOffset)
    } else {
        setActiveBounds(ActiveDecel(bounds: ob, allowOverscroll: false), panel: panel)
        animator(for: panel).start(initialVelocity: v)
    }
}
```

`cancelPan`（`:699-701`，体首加清；E4 归一留 Task 5）：
```swift
public func cancelPan(panel: PanelId) {
    setDragRaw(nil, panel: panel)                        // R1b-drag：清 raw
    _ = reduce(.panEnded(velocity: 0), on: panel)
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd ios/Contracts && swift test --filter dragRawLifecycle`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineDragDampingTests.swift
git commit -m "feat(r1b-drag): dragRaw 存储+accessor+生命周期（beginPan seed / endPan·cancelPan 清）"
```

---

## Task 3: applyPanOffset D2 单边映射（核心：阻尼跟手 + 反拖解绕 + 单边硬钳）

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift`（public `applyPanOffset` `:665-670`）
- Test: `TrainingEngineDragDampingTests.swift`

- [ ] **Step 1: 写失败测试**（§六.2/3/4/5/6）

```swift
// 追加到 TrainingEngineDragDampingTests

@Test("过最老边阻尼：offset=maxOffset+damp 且 maxOffset<offset<raw（D2 killer）")
func dragPastEdgeDamps() {
    let (e, _) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))
    let raw = ob.maxOffset + 600
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: raw, renderBounds: Self.bounds, panel: .upper)   // 从 0 一帧推到 raw
    let mainW: CGFloat = 800
    let expected = ob.maxOffset + RubberBand.damp(over: 600, dimension: mainW)
    #expect(abs(e.upperPanel.offset - expected) < 1e-6)
    #expect(e.upperPanel.offset > ob.maxOffset)            // 确实过界
    #expect(e.upperPanel.offset < raw)                     // 确实被压缩（非 vacuous）
}

@Test("反拖解绕：过界后回拉 → 连续单调降回 maxOffset 再 1:1（raw 累加器正确）")
func reverseDragUnwinds() {
    let (e, _) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    for _ in 0..<6 { e.applyPanOffset(deltaPixels: 100, renderBounds: Self.bounds, panel: .upper) }  // raw=600（<maxOffset 710，仍界内 1:1）
    #expect(abs(e.upperPanel.offset - 600) < 1e-6)         // 界内 1:1
    for _ in 0..<3 { e.applyPanOffset(deltaPixels: 100, renderBounds: Self.bounds, panel: .upper) }  // raw=900 > 710 → 阻尼
    let overOffset = e.upperPanel.offset
    #expect(overOffset > ob.maxOffset)
    var prev = overOffset
    for _ in 0..<3 { e.applyPanOffset(deltaPixels: -100, renderBounds: Self.bounds, panel: .upper); #expect(e.upperPanel.offset < prev); prev = e.upperPanel.offset }  // 单调降
    #expect(e.upperPanel.offset <= ob.maxOffset + 1e-6)    // 回到界内
}

@Test("界内 1:1 回归：[0,maxOffset] 内 drag 与硬钳逐字等价")
func inBoundsLinear() {
    let (e, _) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset * 0.5, renderBounds: Self.bounds, panel: .upper)
    #expect(abs(e.upperPanel.offset - ob.maxOffset * 0.5) < 1e-6)   // 1:1
}

@Test("最新边硬钳无给（单边 killer）：往最新边推 → 恒 0、反向立即响应、永不 <0")
func newestEdgeHardClamp() {
    let (e, _) = Self.makeEngine()
    e.beginPan(panel: .upper)                               // 起于 offset 0（autoTracking tick=150 → 0）
    e.applyPanOffset(deltaPixels: -500, renderBounds: Self.bounds, panel: .upper)  // 往最新边硬推
    #expect(e.upperPanel.offset == 0)                      // 硬钳 0，不给
    e.applyPanOffset(deltaPixels: 200, renderBounds: Self.bounds, panel: .upper)   // 立即反向
    #expect(abs(e.upperPanel.offset - 200) < 1e-6)         // 无死区，立即响应到 200（raw=max(0,-500)+200=200）
}

@Test("无滚动空间硬钳（E6）：maxOffset==0 → 任意 drag 恒 0")
func noScrollRoomHardClamp() {
    let (e, _) = TrainingEngineBounceWiringTests.makeEngine(count: 5, tick: 0)   // count≤visibleCount → 无空间
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(!ob.bounceEdges.contains(.max))                // 无滚动空间
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: 500, renderBounds: Self.bounds, panel: .upper)
    #expect(e.upperPanel.offset == 0)                      // 不给
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd ios/Contracts && swift test --filter TrainingEngineDragDampingTests`
Expected: **两个 killer** `dragPastEdgeDamps`/`reverseDragUnwinds` **失败**（当前硬钳，offset 停 maxOffset）。`newestEdgeHardClamp`/`inBoundsLinear`/`noScrollRoomHardClamp` 为**回归守卫**——旧硬钳在界内/最新边/无空间的行为与新 raw 映射偶合，故前后都绿（非 fail-first；保留以防 D2 改动回归这些边界）。

- [ ] **Step 3: 实现 D2 映射**

替换 public `applyPanOffset(deltaPixels:renderBounds:panel:)` 体（`:665-670`）：
```swift
public func applyPanOffset(deltaPixels: CGFloat, renderBounds: CGRect, panel: PanelId) {
    let ob = RenderStateBuilder.offsetBounds(engine: self, panel: panel, bounds: renderBounds)
    let cur = panelState(panel).offset
    // R1b-drag D1：raw 累加器（未阻尼累计意图位移）。E1：beginPan 已 seed；防御惰性回退当前 offset。
    let raw0 = dragRawFor(panel) ?? cur
    let raw = max(0, raw0 + deltaPixels)                     // E2 下钳 0：最新边硬钳无给 + 无反拖死区
    setDragRaw(raw, panel: panel)
    // D2 单边映射
    let target: CGFloat
    if !ob.bounceEdges.contains(.max) {                      // E6 无滚动空间（maxOffset==0）→ 硬钳 0
        target = min(raw, ob.maxOffset)
    } else if raw <= ob.maxOffset {                          // 界内 1:1（回归）
        target = raw
    } else {                                                 // 最老边阻尼
        let mainW = ChartPanelFrames.split(in: renderBounds).mainChart.width
        target = ob.maxOffset + RubberBand.damp(over: raw - ob.maxOffset, dimension: mainW)
    }
    if target != cur { applyOffsetDelta(target - cur, panel: panel) }   // L2-new：省 0-delta 空 bump
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd ios/Contracts && swift test --filter TrainingEngineDragDampingTests`
Expected: PASS（含 Task 2 lifecycle + Task 3 五条）。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineDragDampingTests.swift
git commit -m "feat(r1b-drag): applyPanOffset D2 单边映射（最老边阻尼跟手 + 反拖解绕 + 最新边硬钳）"
```

---

## Task 4: endPan D3 — drag-overscroll 松手无条件弹回

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift`（public `endPan` 分派）
- Test: `TrainingEngineDragDampingTests.swift`

- [ ] **Step 1: 写失败测试**（§六.7）

```swift
@Test("endPan 从 overscroll 慢松手（v=0）→ 启动弹簧回 maxOffset（非 no-op strand）D3 killer")
func endPanFromOverscrollZeroVelocitySprings() {
    let (e, fakes) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset + 600, renderBounds: Self.bounds, panel: .upper)  // 拖到 overscroll
    #expect(e.upperPanel.offset > ob.maxOffset)
    e.endPan(velocity: 0, renderBounds: Self.bounds, panel: .upper)            // 慢松手
    for _ in 0..<300 { _ = fakes().last?.fire(1.0 / 60.0) }
    #expect(abs(e.upperPanel.offset - ob.maxOffset) < 1.0)                     // 弹回 maxOffset（非 strand 在界外）
}

@Test("endPan 从 overscroll 内向甩（v<0）→ 弹回 maxOffset 不过冲/不下穿")
func endPanFromOverscrollInwardVelocitySprings() {
    let (e, fakes) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset + 600, renderBounds: Self.bounds, panel: .upper)
    e.endPan(velocity: -2000, renderBounds: Self.bounds, panel: .upper)       // 内向甩
    var minSeen = e.upperPanel.offset
    for _ in 0..<300 { _ = fakes().last?.fire(1.0 / 60.0); minSeen = min(minSeen, e.upperPanel.offset) }
    #expect(abs(e.upperPanel.offset - ob.maxOffset) < 1.0)                    // 落 maxOffset
    #expect(minSeen >= ob.maxOffset - 2.0)                                    // 临界阻尼不显著下穿（容 settle 容差）
}

@Test("对照回归：界内 offset<maxOffset + v=0 endPan → 不弹（R1b-wire 既有）")
func endPanInBoundsZeroVelocityNoSpring() {
    let (e, fakes) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset * 0.5, renderBounds: Self.bounds, panel: .upper)
    let before = e.upperPanel.offset
    e.endPan(velocity: 0, renderBounds: Self.bounds, panel: .upper)
    for _ in 0..<60 { _ = fakes().last?.fire(1.0 / 60.0) }
    #expect(abs(e.upperPanel.offset - before) < 1e-6)                         // 不动（v=0 界内不弹）
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd ios/Contracts && swift test --filter endPanFromOverscroll`
Expected: `endPanFromOverscrollZeroVelocitySprings` 失败（当前 v=0 落 else plain decel → `start(0)` no-op → offset strand 在 overscroll）。

- [ ] **Step 3: 实现 D3 弹回分支**

改 public `endPan` 分派条件（合并 drag-overscroll 必弹）：
```swift
public func endPan(velocity: CGFloat, renderBounds: CGRect, panel: PanelId) {
    setDragRaw(nil, panel: panel)
    let ob = RenderStateBuilder.offsetBounds(engine: self, panel: panel, bounds: renderBounds)
    guard case .startDeceleration(let v) = reduce(.panEnded(velocity: velocity), on: panel) else { return }
    let offset = panelState(panel).offset
    // R1b-drag D3：drag-overscroll（offset>maxOffset）松手**不论速度方向**都弹簧回 maxOffset（防慢松手 no-op strand）；
    //   否则 R1b-wire 机制 A 既有分派。L1 不变量：post-drag 面板恒 .freeScrolling → .panEnded 返 .startDeceleration（含 v=0）
    //   → 本分支可达；**勿把 overscroll 检查移出本 guard / 勿放松 guard**（autoTracking/drawing 不经此路且已 offset=0）。
    if offset > ob.maxOffset || (v > 0 && ob.bounceEdges.contains(.max)) {
        setActiveBounds(ActiveDecel(bounds: ob, allowOverscroll: true), panel: panel)
        animator(for: panel).start(initialVelocity: v, fromOffset: offset,
                                   minOffset: ob.minOffset, maxOffset: ob.maxOffset)
    } else {
        setActiveBounds(ActiveDecel(bounds: ob, allowOverscroll: false), panel: panel)
        animator(for: panel).start(initialVelocity: v)
    }
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd ios/Contracts && swift test --filter TrainingEngineDragDampingTests`
Expected: PASS（含三条 endPan）。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineDragDampingTests.swift
git commit -m "feat(r1b-drag): endPan D3 — drag-overscroll 松手无条件弹回（防慢松手 strand）"
```

---

## Task 5: cancelPan E4 归一 + recordRenderBounds E5 dragRaw 重同步

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift`（`cancelPan` + `recordRenderBounds`）
- Test: `TrainingEngineDragDampingTests.swift`

- [ ] **Step 1: 写失败测试**（§六.9 E4 + E5）

> **L2**：E4 测试**必须**先经 `makeEngine`（其内已 `recordRenderBounds(bounds)` seed）→ `lastRenderedBounds != .zero`，否则 cancelPan 归一 against [0,0] 致 vacuous。`makeEngine` 已 seed，达标。

```swift
@Test("cancel-于-overscroll（E4）：drag 过界后 cancelPan → 归一 maxOffset、dragRaw==nil")
func cancelAtOverscrollNormalizes() {
    let (e, _) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset + 600, renderBounds: Self.bounds, panel: .upper)  // 越界
    #expect(e.upperPanel.offset > ob.maxOffset)
    e.cancelPan(panel: .upper)                                  // 两指接管/drawing 截获
    #expect(abs(e.upperPanel.offset - ob.maxOffset) < 1e-6)    // 归一 maxOffset（无残留越界间隙）
    #expect(e.debug_dragRawFor(.upper) == nil)
}

@Test("resize 中途 active drag（E5）：bounds 变 → offset 归新几何 + dragRaw 重同步")
func resizeMidDragResyncsDragRaw() {
    let (e, _) = Self.makeEngine()
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset, renderBounds: Self.bounds, panel: .upper)   // 到最老边 710
    let narrow = CGRect(x: 0, y: 0, width: 400, height: 600)                                 // 窗口变窄 → 新几何
    e.recordRenderBounds(narrow, panel: .upper)
    let nb = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: narrow)
    #expect(e.upperPanel.offset <= nb.maxOffset + 1e-6)        // offset 归新 [0,newMax]
    #expect(e.debug_dragRawFor(.upper) == e.upperPanel.offset) // dragRaw 重同步=归一后 offset
    // 续拖一帧不应基于 stale raw 跳变（delta 0 → offset 不变）
    let after = e.upperPanel.offset
    e.applyPanOffset(deltaPixels: 0, renderBounds: narrow, panel: .upper)
    #expect(abs(e.upperPanel.offset - after) < 1e-6)
}
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd ios/Contracts && swift test --filter "cancelAtOverscrollNormalizes|resizeMidDragResyncsDragRaw"`
Expected: 两条失败（cancelPan 当前不归一；recordRenderBounds 当前不重同步 dragRaw → `debug_dragRawFor != offset`）。

- [ ] **Step 3: 实现 E4 + E5**

`cancelPan`（E4 归一，复用 `renderBounds(panel)`=lastRenderedBounds）：
```swift
public func cancelPan(panel: PanelId) {
    setDragRaw(nil, panel: panel)
    _ = reduce(.panEnded(velocity: 0), on: panel)
    // R1b-drag E4：cancel-于-overscroll（两指接管/drawing 截获于越界）→ 归一 maxOffset 防残留间隙。
    //   复用 lastRenderedBounds（每帧 render 前 recordRenderBounds 已 seed；不改 public 签名）。
    let ob = RenderStateBuilder.offsetBounds(engine: self, panel: panel, bounds: renderBounds(panel))
    let cur = panelState(panel).offset
    let clamped = min(max(cur, ob.minOffset), ob.maxOffset)
    if clamped != cur { _ = reduce(.offsetApplied(deltaPixels: clamped - cur), on: panel) }
}
```

`recordRenderBounds`（E5：归一后若 drag 活跃则重同步 dragRaw，加在 `:793` clamp 之后、方法末）：
```swift
    if clamped != cur { _ = reduce(.offsetApplied(deltaPixels: clamped - cur), on: panel) }
    // R1b-drag E5：resize 中途 active drag → 重同步 dragRaw 到归一后 offset（防下一帧 delta 基于 stale raw 跳变）。
    if dragRawFor(panel) != nil { setDragRaw(panelState(panel).offset, panel: panel) }
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd ios/Contracts && swift test --filter TrainingEngineDragDampingTests`
Expected: PASS（全 suite）。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineDragDampingTests.swift
git commit -m "feat(r1b-drag): cancelPan E4 归一 + recordRenderBounds E5 dragRaw 重同步"
```

---

## Task 6: 更新 dragFullClamp 既有测试（§六.10）+ 全量回归 + 验收 checklist

**Files:**
- Modify: `ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift`（`dragFullClamp`）
- Create: `docs/superpowers/acceptance/2026-06-16-w3-11-r1b-drag-acceptance.md`

- [ ] **Step 1: 更新 dragFullClamp 测试为 R1b-drag 阻尼语义**

替换 `TrainingEngineBounceWiringTests.dragFullClamp`（旧断言 `推过 maxOffset → 钳 maxOffset` 现已为阻尼跟手）：
```swift
// §六.3→R1b-drag 更新：drag 过最老边现为「阻尼跟手」（非硬钳）；最新边仍硬钳 0
@Test("drag 过最老边 → 阻尼跟手（offset>maxOffset 但被压缩）；推过 0 → 仍硬钳 0")
func dragPastEdgeRubberBands() {
    let (e, _) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset + 999, renderBounds: Self.bounds, panel: .upper)
    #expect(e.upperPanel.offset > ob.maxOffset)                  // 过界（阻尼跟手，非硬钳停 maxOffset）
    #expect(e.upperPanel.offset < ob.maxOffset + 999)            // 被压缩
    e.applyPanOffset(deltaPixels: -99999, renderBounds: Self.bounds, panel: .upper)
    #expect(e.upperPanel.offset == 0)                            // 最新边仍硬钳 0（单边不变量）
}
```

- [ ] **Step 2: 全量 host 回归**

Run: `cd ios/Contracts && swift test`
Expected: 全绿，0 failures（baseline 1069 + RubberBand 5 + DragDamping ~11 ≈ 1085；`dragFullClamp` 已改名 `dragPastEdgeRubberBands` 不计净增）。逐项确认无既有 suite 回归（机制 A 分派/onUpdate floor-full/B4 makeViewport/EdgeBounceModel/drawing/interaction 全保持绿）。

- [ ] **Step 3: 写非-coder 验收 checklist**

`docs/superpowers/acceptance/2026-06-16-w3-11-r1b-drag-acceptance.md`：表格列（操作 / 预期 / 通过-不通过），中文，覆盖 §六.1-10 各一条二元判据（用 `swift test --filter <函数名>` 真输出格式）+ §六.11 device runbook（拖过最老边带阻力跟手 / 松手轻放回弹 / 松手带速 overscroll 后回落 / 拖向最新边无给 / 满屏不动 / 过界中途旋转无残留 / 过界中途两指或开画线无残留）+ codex（或 opus fallback）APPROVE 落账行。**forbidden phrases 见 `.claude/workflow-rules.json`，禁用。** 范围 gate：白名单 3 prod 文件（RubberBand.swift 新 / TrainingEngine.swift 改）+ 3 test 文件 + 1 acceptance doc + spec/plan；**0 渲染层改动**（`git diff` 对 `RenderStateBuilder.swift`/`makeViewport`/`KLineView*` 为空）、**0 EdgeBounceModel 物理改动**、**0 手势契约改动**（`ChartGestureArbiter.swift` 未改）。

- [ ] **Step 4: Commit**

```bash
git add ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift docs/superpowers/acceptance/2026-06-16-w3-11-r1b-drag-acceptance.md
git commit -m "test(r1b-drag): dragFullClamp→dragPastEdgeRubberBands 语义更新 + 非-coder 验收 checklist"
```

---

## 验收 checklist（中文，交付时落 `docs/superpowers/acceptance/2026-06-16-w3-11-r1b-drag-acceptance.md`）

见 Task 6 Step 3。host 测核各条用 `swift test --filter` 真函数名 + 二元判据；device runbook 7 项；范围 gate（白名单 + 三 0 改动）；评审 APPROVE 落账。

---

## Self-Review

- **Spec 覆盖**：D1 dragRaw（Task 2）/ D2 映射（Task 3）/ D3 endPan 弹回（Task 4）/ D4 RubberBand 公式（Task 1）/ D5 零渲染（Task 6 gate 验证）/ E1-E7（Task 2/3/5 测）/ §六.1-10 全 task 覆盖 / §六.11 device runbook（Task 6 acceptance）。✓
- **L1（spec review）**：Task 4 endPan 分支注释显式标 freeScrolling 不变量「勿移出 guard / 勿放松」。✓
- **L2（spec review）**：Task 5 E4 测试经 `makeEngine`（已 seed recordRenderBounds），注释点名 `lastRenderedBounds != .zero` 前提。✓
- **类型一致**：`dragRawFor`/`setDragRaw`/`debug_dragRawFor` 命名贯穿；`RubberBand.damp(over:dimension:)` 签名 Task 1 定义、Task 3/测试一致引用；`ChartPanelFrames.split(in:).mainChart.width` 与 offsetBounds extraction 同源。✓
- **无占位符**：每 step 含真实代码/命令/期望。✓
- **行为中性边界**：旧 internal `applyPanOffset(deltaPixels:panel:)`/`endPan(velocity:panel:)` 逐字不动（@testable 旧测试不变）；只改 public 带 bounds 签名 + 新增。✓

---

## Review Provenance（opus 4.8 xhigh 对抗性 review 代 codex，per user 指示）

- **R1 = NEEDS-ATTENTION**：reviewer 实编（swiftc Swift 6 strict-concurrency + 数值仿真）验证全部承重事实正确（mainW=800 无 gutter / maxOffset=710 / damp(600,800)=233.63 / 弹簧 frame 37 settle / count=5 无空间 / 编译 clean / 签名不变行为中性 / 两 killer 真 fail-first）。揪出 **1 High**（`asymptote` 断言 `>790` 数学错，实 788.53 → CI 必红）+ **1 Medium**（Step-2 误将 `newestEdgeHardClamp` 列 fail-first，实为前后皆绿回归守卫）+ 2 Low（存储锚 off-by-one / baseline 计数近似）。
- **修**：asymptote 阈值 790→785（实 788.53）；Step-2 prose 改「两 killer fail-first + 三回归守卫」；存储锚 :54→:55。
- **R2 = APPROVE**：独立复验三修复正确（788.53∈(785,800)、`.greatestFiniteMagnitude` 不 overflow 得 800.0、三回归守卫 OLD/NEW 逐位相同、:55 确为 activeBounds 声明）+ 承重事实全复确认 + 引用源码体逐字匹配实文件 + 0 新缺陷。**plan 收敛。**
