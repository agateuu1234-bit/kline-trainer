# E2 RFC — spec §4.2 PositionManager 重审 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 Wave 1 顺位 7 / 第 9 个 PR——把 PositionManager 的 precondition-vs-throwing 设计决议（方向 B）以权威 rationale block 写进 spec §4.2，更新 §E2 交叉引用与 §15.4 H2 账本，兑现 §15.4 residual H2；**0 行业务代码**，实施本体（PositionManager.swift）= 顺位 8。

**Architecture:** 纯文档变更。在 `plan_v1.5.md` §4.2 PositionManager 代码块下方插入设计理由块 §4.2.1–§4.2.8（信任边界 / stdlib 一致性 / 威胁模型 / 溢出语义 / 否决方案 / 验收 / 迁移+bump 义务门 / invariantsHold contract）+ 代码块内 `positionTier` 占位符最小标注；`modules_v1.4.md` §E2 扩交叉引用；ledger §15.4 H2 行状态更新；新增非 coder 中文验收清单。bump 执行（CONTRACT_VERSION/m01/acceptance）MANDATORY 推迟顺位 8。

**Tech Stack:** Markdown only。验证 = grep + `git diff --stat`（无 SwiftPM 测试，无代码改动）。设计来源 = `docs/superpowers/specs/2026-05-24-e2-rfc-spec-4.2-reaudit-design.md`（design-stage opus 4.7 xhigh R1→R3 APPROVE 收敛）。

---

## Task 0 — §15.3 评审策略前置（per `docs/governance/wave1-plan-template.md`）

- [ ] **局部对抗性评审（必）：** 本 plan scope 内对抗性评审；**用户 session 开头明示用另一个 Claude opus 4.7 xhigh effort 做对抗性 review（非 codex）**，是 session 契约（per memory `feedback_review_tool_switch_must_ask`）。design-stage 已跑 R1→R3 APPROVE；plan-stage + branch-diff 各一轮收敛（4-5 轮内或 escalate，per `feedback_codex_plan_budget_overshoot`）。
- [ ] **集成层评审（N/A）：** 本 PR 0 代码、0 桥接/编排；E2 实施在顺位 8，集成在 Wave 2。
- [ ] **性能评审（N/A）：** doc-only，无性能热点。

完成 Task 0 才进 Task 1 实施。

---

## Spec snapshot（grep-verified，2026-05-24 复核）

| 锚点 | 文件:行 | 当前内容 |
|---|---|---|
| §4.2 PositionManager 代码块 | `kline_trainer_plan_v1.5.md` L595(标题)/L630-662(struct)/L664(无效操作处理) | 裸 `mutating` 值类型，无 rationale block；`positionTier` 占位 L656-660 返回 0 |
| §E2 模块条目 | `kline_trainer_modules_v1.4.md` L1489-1491 | "（见 v1.5 §4.2；类型加 Equatable）" |
| §15.4 H2 账本行 | `docs/governance/2026-05-17-wave0-signoff-ledger.md` L33 | `\| H2 \| E2 PositionManager 三连 abort \| PR #36 closed \| Wave 1 启动前 spec §4.2 重审窗口 \|` |
| persistence baseline | `ios/.../AppState.swift:90` + `app_schema_v1.sql:55` | `positionData: Data` / `position_data TEXT NOT NULL`（已 shipped，当前不透明 base64 往返） |
| §M0.4（澄清归属） | `kline_trainer_modules_v1.4.md` L599 | 在 **modules**，非 plan；受 m04 gate 管辖（故 invariantsHold 内联 §4.2.8 不放 §M0.4） |

## 设计决策（reviewer 速查 —— design-stage 已收敛）

| 主题 | 决策 | 来源 |
|---|---|---|
| buy/sell 错误处置 | precondition trap（不 throws/不 Result） | 方向 B，§4.2.1/§4.2.5 |
| 持久化 decode | throwing 自定义 decoder + invariantsHold | §4.2.1 入口 2 / §4.2.8 |
| 5 层防御 | 全保留，按 ingress 归类（②③ throws 守磁盘；①④⑤ trap 守进程内） | §2.1 表 |
| force-close 不越界 | 依赖 caller 不变量 `holding==position.shares`（同入口 1a，trap），**非**按构造 | R2-High#1，§4.2.1 入口 1b |
| invariantsHold "≈" | 相对容差（非 ==），吸收 buy 除-乘 + JSON Double 往返误差 | R2-M#6，§4.2.8 |
| CONTRACT_VERSION/m01 bump | **应当发生**（不推翻 v1 R2），但**执行**推迟顺位 8（MANDATORY 门） | R1-C#1/C#2/H#3，§4.2.7 |
| positionTier | caller-derived；顺位 8 移除 member；本 RFC 仅标注契约 | R1/R2-M#8，§4.2.8 注 |

---

## Task 1：plan §4.2 插入 rationale block §4.2.1–§4.2.8 + positionTier 标注

**Files:**
- Modify: `kline_trainer_plan_v1.5.md`（在 §4.2 PositionManager 代码块闭合 ``` 之后、`**无效操作处理：**` 之前插入；并改 `positionTier` 注释）

- [ ] **Step 1：在 PositionManager 代码块闭合 ``` 之后插入以下整段（§4.2.1–§4.2.8）**

精确位置：找到 §4.2 内 PositionManager `struct` 代码块的闭合三反引号（其后紧跟 `**无效操作处理：**`），在闭合三反引号与 `**无效操作处理：**` 之间插入空行 + 下列内容：

````markdown
**§4.2 PositionManager — API 契约与设计理由（v1.5 §4.2 重审 · Wave 1 顺位 7 RFC 落地）**

`PositionManager.buy / sell` 在违约输入上 `precondition` trap，**不** `throws`、**不**返回 `Result`。Codable 反序列化（持久化入口）用 throwing 自定义 decoder + `invariantsHold` 守门。下列 8 节 codify API 不变量、信任边界与处置策略，作为顺位 8 实施的权威契约。

**§4.2.1 Trust-boundary 定位**

PositionManager 的 ingress：

- **入口 1a（进程内交易）**：UI → E3 `TradeCalculator.quoteBuy/quoteSell` → `Result<Quote, TradeReason>`；`.failure` 终止于 UI Toast，`.success(quote)` → E5 → `position.buy/sell`。传入 buy/sell 的输入**已被 E3 Result 通道验证**（资金不足/tier 非法/价格非法/佣金溢出）；违约 ⟺ 调用方 programmer error。
- **入口 1b（进程内局终强平）**：E5 局终（`globalTickIndex >= maxTick`）→ E3 `forceCloseOnEnd(holding:...)` → 裸 `SellQuote`（**无 Result**）→ `position.sell`。`forceCloseOnEnd` 只守 `holding>0/price`（否则全零报价 → `sell(0)` no-op），它**不读** `position.shares`；不越界依赖 **caller 不变量 `holding == position.shares`**——与入口 1a 同信任类（caller 契约，违约 = programmer error → trap），**非**"按构造保证"。
- **入口 1c（进程内重建）**：E6 `resumePending()` → decoder 重建 = 归**入口 2**；`replay(recordId:)` → 从头跑 → 走入口 1a/1b；`review(recordId:)` → 只读 → 无 mutation 入口。
- **入口 2（持久化 load）**：SQLite `position_data`(TEXT base64) → `Data` → `JSONDecoder` → `PositionManager.init(from:)`；`DecodingError` / `invariantsHold==false` → load 失败终止。**唯一真实的外部不可信入口**（存档可能损坏/被篡改）。

所有进程内入口（1a/1b/1c）违约均为 caller programmer error → **trap**；只有持久化入口（2）→ **throws**。contract 不绑定具体 caller 身份；新增 caller 须满足同一前置（buy/sell 传入已验证 quote；force-close 传入 `holding==shares`）。

**§4.2.2 Swift stdlib 处置一致性**

Swift stdlib 二分：programmer error / contract violation → **trap**（`Array.subscript`、`Dictionary!`、`Int.+`、`UnicodeScalar.init`）；I/O / parse / external-input error → **throws**（`JSONDecoder.decode`、`String(contentsOf:)`、`FileHandle.read`）。buy/sell 违约输入在 §4.2.1 守门后只剩 caller programmer error，归 trap；decoder 处理 external input，归 throws。

**§4.2.3 Threat model（输入域 vs 类型上界）**

| 量 | 真实上界（粗量级工程估算，**非 spec 引用**） | 类型上界（量级） | gap |
|---|---|---|---|
| `averageCost`（Double, 元/股） | ~10⁴ | ~10³⁰⁸ | ~304 |
| `shares`（Int, 股） | ~10⁶–10⁸ | ~10¹⁸–10¹⁹ | ~10–13 |
| `totalInvested`（Double, 元） | ~10¹⁰ | ~10³⁰⁸ | ~298 |

数字为粗量级工程估算（**非 spec 引用**：spec 内无价格区间、无日内笔数上界），**不作 contract 上界**，仅说明输入域与类型上界 gap。合成 `+inf` / Int 溢出需输入接近 `Double.greatestFiniteMagnitude` / `Int.max`，唯一构造路径是 SQLite 文件被外部进程篡改（root 已陷落，sandbox 已破）。decoder ingress 已守 `isFinite` + `invariantsHold`，进程内 ingress 由 E3（1a）+ caller 不变量（1b）守 → buy/sell mutating 路径二次守门 ROI 为负。

**§4.2.4 数值溢出语义**

- **Int 路径**（`self.shares + shares`）：Swift 默认 trap on overflow。
- **Double 路径**（`self.totalInvested + totalCost`）：IEEE 754 saturating 至 ±inf，**不** trap。

实现要求：① mutation 前用 `addingReportingOverflow` / `isFinite` 预检合成结果，违约即 `precondition` trap（trap message 含违约参数 + 当前 state）；② 预检通过后写入；③ debug build end-of-function `assert(invariantsHold)` 兜底，不作主守门。

**§4.2.5 Considered alternatives**

| 提案 | 处置 | 理由 |
|---|---|---|
| buy/sell public API `throws` | 拒 | caller 在 §4.2.1 守门后的 dead branch 上被迫写 `try!`/`try?`，徒增噪声。 |
| 改 `Result<_, TradeReason>` 与 E3 统一 | 拒 | 不同 error class：E3 处理 caller-recoverable error；§4.2 处理 invariant violation。统一为 false symmetry。 |
| buy/sell 进程内 defense-in-depth | 拒 | defense-in-depth 适用于跨进程/网络/用户 trust boundary；§4.2 进程内同 MainActor，二次 throws 守门 ROI 为负（§4.2.3）。 |

**§4.2.6 Acceptance 与 testability**

- `precondition` 语义 = fail-fast on programmer-contract violation，debug/release 均保留 trap，**不**视为正常控制流。
- buy/sell 违约输入路径**不在 test surface**（contract 假设上游已守门，违约路径无 caller-side 责任可测）。caller-recoverable error 的 test matrix 在 §E3。
- Trap message 含违约参数 + 当前 Position state，便于 production crashlog 反查。

**§4.2.7 Migration note + 顺位 8 bump 义务门**

本 contract 假设 v1 sandbox 单用户、无外部 import。v2 若引入云同步 / 跨设备 / CSV import 等新 ingress，§4.2 contract 须重评（可能升 throws/Result）。

`position_data` 已是 shipped schema 列（当前当不透明字节往返）；顺位 8 引入的 typed throwing decoder 把其解释从"任何字节"收紧为"合法 PositionManager 否则拒收"，属对**已 shipped 持久化契约的语义收紧**，命中 m01 §Bump 策略 A 类"改既有语义" → **bump 应当发生**。bump 执行（`CONTRACT_VERSION` / m01 矩阵 / acceptance 脚本三件套）**MANDATORY 与 decoder 代码同 PR（顺位 8）落地**——本 docs-only RFC 不执行 bump（避免 spec↔m01 同 PR 自循环；无 decoder 时改版本常量空头）。**顺位 8 = required predecessor**：顺位 8 前不得有 PR 引入 typed `position_data` reader。enforcement = 本节 spec 义务 + 顺位 8 plan Task 0 必检 + 顺位 8 acceptance hook（grep `PositionManager.init(from:)` ⟹ 断言同 diff 含 `CONTRACT_VERSION` delta，缺则 fail）。

**§4.2.8 invariantsHold contract**

```swift
private static func invariantsHold(shares: Int, averageCost: Double, totalInvested: Double) -> Bool
```

O(1)，校验：

- `shares ≥ 0`
- `totalInvested ≥ 0` ∧ `totalInvested.isFinite`
- `(shares == 0) ⟺ (totalInvested == 0)`
- `shares > 0 ⟹ averageCost > 0 ∧ averageCost.isFinite ∧ |averageCost*Double(shares) - totalInvested| ≤ tol * max(1, |totalInvested|)`

**"≈" 必须用相对容差，不可用 `==`**：`buy` 存 `averageCost = newTotal/Double(newShares)`、`totalInvested = newTotal`，decoder 复检 `(newTotal/newShares)*newShares ≈ newTotal` 非位精确（叠加 JSON Double 十进制往返）→ 用 `==` 或过紧 epsilon 会拒收 app 自己写出的合法存档；`sell` 安全（`totalInvested = averageCost*shares` 是精确赋值）。`tol` 具体值由顺位 8 定并给测试。

使用点：decoder `init(from:)`（false → throw `DecodingError`）/ buy-sell 候选态（false → `precondition` trap）/ debug `assert` 兜底。

**`positionTier` 注**：下方代码块 `var positionTier: Int { 0 }` 是占位符；档位需"初始资金"（不在 PositionManager 状态内）→ 判定 **caller-derived**（E4/E5 依初始资金 + 当前持仓推导）；**顺位 8 从 PositionManager 移除此 member**。本 RFC 仅 codify 此契约，不改算术。
````

- [ ] **Step 2：改 §4.2 代码块内 `positionTier` 注释（最小标注）**

把代码块内（约 L655-660）：

```swift
    /// 当前仓位档位（0~5）
    var positionTier: Int {
        // 根据实际持仓比例映射到最接近的档位
        // 具体实现取决于初始资金和当前持仓
        0
    }
```

改为：

```swift
    /// 当前仓位档位（0~5）—— ⚠️ 占位符：见 §4.2.8 positionTier 注（caller-derived，顺位 8 移除本 member）
    var positionTier: Int {
        // 占位返回 0；真实档位由 caller（E4/E5）依初始资金 + 当前持仓推导（顺位 8 落地）
        0
    }
```

- [ ] **Step 3：验证 8 节标题齐全 + positionTier 标注就位**

Run:
```bash
grep -nE "^\*\*§4\.2\.[1-8] " "kline_trainer_plan_v1.5.md"
grep -n "positionTier.*占位符\|顺位 8 移除本 member" "kline_trainer_plan_v1.5.md"
grep -c "MANDATORY 与 decoder 代码同 PR（顺位 8）" "kline_trainer_plan_v1.5.md"
```
Expected: 第一条列出 §4.2.1–§4.2.8 共 **8 行**（`^\*\*` 锚定行首标题，排除正文里的 "§4.2.1 守门" 等交叉引用）；第二条命中 positionTier 标注；第三条 = 1。

- [ ] **Step 4：确认无 .swift / CONTRACT_VERSION / m01 改动（证明 bump 推迟）**

Run:
```bash
git diff --name-only | grep -E "\.swift$|\.py$" || echo "OK: 0 code files"
git diff | grep -nE "CONTRACT_VERSION|user_version|m01.*矩阵" || echo "OK: 0 version/m01 changes"
```
Expected: 两条都打印 `OK: ...`。

- [ ] **Step 5：Commit**

```bash
git add kline_trainer_plan_v1.5.md
git commit -m "docs(e2-rfc): plan §4.2 加 §4.2.1–§4.2.8 设计理由块 + positionTier 标注（顺位 7）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2：modules §E2 扩交叉引用

**Files:**
- Modify: `kline_trainer_modules_v1.4.md` L1489-1491

- [ ] **Step 1：替换 §E2 条目内容**

把：

```markdown
### E2 持仓管理模块 `PositionManager.swift`

（见 v1.5 §4.2；类型加 `Equatable`）
```

改为：

```markdown
### E2 持仓管理模块 `PositionManager.swift`

（见 v1.5 §4.2；类型加 `Equatable`）

**API 契约与设计理由**：详 `kline_trainer_plan_v1.5.md` §4.2.1–§4.2.8（trust-boundary / stdlib 一致性 / 威胁模型 / 溢出语义 / considered alternatives / acceptance / migration + 顺位 8 bump 义务门 / `invariantsHold` contract）。要点：buy/sell `precondition` trap（不 throws/Result）；持久化用 throwing 自定义 decoder + `invariantsHold`；`positionData` 已 shipped，typed decoder 的语义收紧触发 `CONTRACT_VERSION`/m01 bump，**执行 MANDATORY 推迟顺位 8 同 PR**（本类型实现本体 = 顺位 8）。
```

- [ ] **Step 2：验证交叉引用就位**

Run:
```bash
grep -n "§4.2.1–§4.2.8\|执行 MANDATORY 推迟顺位 8" "kline_trainer_modules_v1.4.md"
```
Expected: 命中新增段落（≥1 行）。

- [ ] **Step 3：Commit**

```bash
git add kline_trainer_modules_v1.4.md
git commit -m "docs(e2-rfc): modules §E2 扩交叉引用 → plan §4.2.1–§4.2.8 + bump 推迟顺位 8

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3：ledger §15.4 H2 行状态更新

**Files:**
- Modify: `docs/governance/2026-05-17-wave0-signoff-ledger.md` L33

- [ ] **Step 1：更新 H2 行末列**

把（L33）：

```markdown
| H2 | E2 PositionManager 三连 abort | PR #36 closed | Wave 1 启动前 spec §4.2 重审窗口 |
```

改为：

```markdown
| H2 | E2 PositionManager 三连 abort | 顺位 7 RFC 已决议（spec §4.2.1–§4.2.8） | 实施 + CONTRACT_VERSION/m01 bump 执行 deferred 顺位 8（MANDATORY 门，见 plan §4.2.7） |
```

- [ ] **Step 2：验证账本行更新**

Run:
```bash
grep -n "H2 .*顺位 7 RFC 已决议\|deferred 顺位 8（MANDATORY" "docs/governance/2026-05-17-wave0-signoff-ledger.md"
```
Expected: 命中更新后的 H2 行。

- [ ] **Step 3：Commit**

```bash
git add docs/governance/2026-05-17-wave0-signoff-ledger.md
git commit -m "docs(e2-rfc): ledger §15.4 H2 → 顺位 7 RFC 已决议 / 实施+bump deferred 顺位 8

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4：新增非 coder 中文验收清单

**Files:**
- Create: `docs/acceptance/2026-05-24-pr-e2-rfc.md`

- [ ] **Step 1：创建验收清单文件，内容如下**

````markdown
# 验收清单 — E2 RFC（spec §4.2 PositionManager 重审 · Wave 1 顺位 7 / 第 9 个 PR）

> 给非程序员逐条核对。每条：照"动作"敲命令 → 比对"期望" → 在"通过"打 ✓/✗。命令在仓库根目录运行。

| # | 动作 | 期望 | 通过 |
|---|---|---|---|
| 1 | 终端运行：`git diff --stat main...HEAD` | 列出**恰好 6 个文件**，且都以 `.md` 结尾：① `kline_trainer_plan_v1.5.md` ② `kline_trainer_modules_v1.4.md` ③ `docs/governance/2026-05-17-wave0-signoff-ledger.md` ④ `docs/acceptance/2026-05-24-pr-e2-rfc.md` ⑤ `docs/superpowers/specs/2026-05-24-e2-rfc-spec-4.2-reaudit-design.md`（设计文档）⑥ `docs/superpowers/plans/2026-05-24-pr-e2-rfc-spec-4.2-reaudit.md`（本计划文档） | ☐ |
| 2 | 运行：`git diff --name-only main...HEAD \| grep -E '\.swift$\|\.py$'` | **无任何输出**（0 个代码文件改动） | ☐ |
| 3 | 运行：`git diff main...HEAD \| grep -E 'CONTRACT_VERSION\|user_version'` | **无任何输出**（版本号 bump 确实推迟到顺位 8，本 PR 不动） | ☐ |
| 4 | 运行：`grep -cE '^\*\*§4\.2\.[1-8] ' kline_trainer_plan_v1.5.md` | 输出 **8**（§4.2.1 到 §4.2.8 八节标题齐全；`^\*\*` 锚行首，排除正文交叉引用） | ☐ |
| 5 | 运行：`grep -n 'precondition' kline_trainer_plan_v1.5.md \| head` | 命中 §4.2 内"buy/sell … precondition trap，不 throws"字样（确认决定写进 spec） | ☐ |
| 6 | 运行：`grep -n '唯一真实的外部不可信入口' kline_trainer_plan_v1.5.md` | 命中（§4.2.1 把持久化标为唯一外部入口） | ☐ |
| 7 | 运行：`grep -n 'MANDATORY 与 decoder 代码同 PR（顺位 8）' kline_trainer_plan_v1.5.md` | 命中（顺位 8 bump 义务门写进 §4.2.7） | ☐ |
| 8 | 运行：`grep -n '顺位 7 RFC 已决议' docs/governance/2026-05-17-wave0-signoff-ledger.md` | 命中（§15.4 H2 账本行已更新） | ☐ |
| 9 | 运行：`grep -n '§4.2.1–§4.2.8' kline_trainer_modules_v1.4.md` | 命中（§E2 交叉引用已指向新理由块） | ☐ |
| 10 | 打开 `kline_trainer_plan_v1.5.md` 找到 `positionTier` 行 | 注释含"占位符 … 顺位 8 移除本 member"（确认占位符已标注、未被当真实现） | ☐ |

**任一条 ✗ → 不得 merge。** 第 2/3 条是硬门（证明本 PR 真的 0 代码、bump 真的推迟）。
````

- [ ] **Step 2：验证验收清单文件创建成功**

Run:
```bash
test -f "docs/acceptance/2026-05-24-pr-e2-rfc.md" && grep -c "| ☐ |" "docs/acceptance/2026-05-24-pr-e2-rfc.md"
```
Expected: 输出 `10`（10 条验收项）。

- [ ] **Step 3：Commit**

```bash
git add docs/acceptance/2026-05-24-pr-e2-rfc.md
git commit -m "docs(e2-rfc): 非 coder 中文验收清单（10 条 · 顺位 7）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review（writing-plans 自查）

**1. Spec coverage（对 design 文档每节）：**
- §二决议（方向 B / 5 层归类）→ Task 1 §4.2.1 + §2.1 表内容 ✓
- §三改动范围（6 文件）→ Task 1-4 + 已提交的 design 文档 + 本 plan 文档 ✓
- §四 §4.2.1–§4.2.8 大纲 → Task 1 Step 1 全文 ✓
- §五 bump 推迟 + MANDATORY 门 → Task 1 §4.2.7 + Task 2 + Task 3 ✓
- §七停止规则 → 不入 spec（属 review 过程规则，记 design 文档）；plan 不需 task ✓
- §八验收策略 → Task 4 清单 ✓
- §九 residuals → 记 design + ledger H2，无独立 task（顺位 8 scope）✓

**2. Placeholder scan：** 无 TBD/TODO；§4.2.8 `tol` 具体值"由顺位 8 定"是**有意 residual**（本 RFC 定容差**形式**，值随实现，已在 design §九 residual 记录），非占位失败。

**3. Type/命名一致性：** `invariantsHold(shares:averageCost:totalInvested:)`、`init(from:)`、`forceCloseOnEnd(holding:)`、`positionData`、`CONTRACT_VERSION`、`resumePending/replay/review` 全 plan 内一致，且与源文件签名核对过（design R3 已验）。

**4. Scope：** 仅 plan §4.2 / modules §E2 / ledger H2 + 验收清单 + design 文档；无 m01/CONTRACT_VERSION/.swift 改动（Task 1 Step 4 + Task 4 #2/#3 硬门守）。符合 outline §3.2 授权。
