# Plan 1 — M0.1 DB Schema 契约冻结 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 冻结 M0.1 数据库 schema 契约（PostgreSQL + 训练组 SQLite + app.sqlite 三套 DDL + CONTRACT_VERSION 矩阵 + migration rollback 规则），并把 `kline_trainer_modules_v1.4.md` §15.3 三段评审策略补入 `docs/governance/adversarial-review-template.md`。M0.2/M0.3/M0.4/M0.5 分别由 Plan 1b/1c/1d/1e 独立交付。

**Architecture:** SQL DDL 一把到位交付 v1.4 fresh state，不写历史 migration（Wave 1 起由 B3/P4 owner 增量）。**两层验证**：
- **本地快速迭代**：PostgreSQL 用 `pglast`（libpg_query Python 绑定）做语法 + AST 对象断言（零 daemon），SQLite 用 `sqlite3` CLI（macOS 自带）建临时 .db 部署 + 查表断言。
- **CI 权威 runtime 验证**（round 7 codex 要求）：GitHub Actions 每个 PR 起 ephemeral `postgres:15` service container，用 `psql -v ON_ERROR_STOP=1 -f schema.sql` 跑真实部署；SQLite 同样在 Ubuntu runner 里跑两个 sqlite3 脚本。确保 pglast parser-level PASS 不会 mask 运行期 DDL failure。
- CONTRACT_VERSION 矩阵 + migration rollback 规则由 Plan 1f 单独交付。生产部署（NAS PostgreSQL）归 Wave 1 B3 owner，不在本 plan scope。

**Tech Stack:** PostgreSQL 15 语法（通过 `pglast` 解析）/ SQLite 3 CLI（macOS 自带）/ Python 3 + pytest（复用 `backend/tests/` 已有 pattern）。

**工具选型说明**：Plan 1 原计划用 Docker ephemeral container 部署 PostgreSQL schema 验证。2026-04-21 Task 1 实施时发现：本机无 Docker（Plan 0a 的 `backend/docker-compose.yml` 是 NAS 部署模板，未本地验证过）；NAS PostgreSQL 是生产实例，不承担 ephemeral test。改用 `pglast`（Postgres 官方 libpg_query C 解析器的 Python 绑定）做**纯语法 + AST 对象断言**——零基础设施、CI 友好、与真实 Postgres parser 100% 一致。生产部署归 Wave 1 B3 owner。

**依赖**：无。Plan 0a PR #18 已 merged，CLAUDE.md §2 非 coder 验收清单 / §4 skill gate / §1 codex:adversarial-review 流程已就位。

**Plan 拆分背景**：原 Plan 1 覆盖 M0.1-M0.5 全部契约（2447 行 / 83 KB），codex-attest round 1 因文件超 codex 单次上下文窗口被 skip（memory `feedback_post_bootstrap_audit_findings.md` F1 已登记此 residual）。2026-04-21 用户裁决 Option A：物理拆为 5 个子 plan（1/1b/1c/1d/1e），每个独立 codex-attest。本文件是拆分后的 Plan 1（仅 M0.1）。

**产出位置汇总**：

| 文件 | 类型 | 对应 spec |
|---|---|---|
| `docs/governance/adversarial-review-template.md` | 模板更新 | §15.3 |
| `backend/sql/schema.sql` | PostgreSQL DDL | M0.1（DDL 部分） |
| `backend/sql/training_set_schema_v1.sql` | 训练组 SQLite DDL | M0.1（DDL 部分） |
| `ios/sql/app_schema_v1.sql` | app.sqlite DDL | M0.1（DDL 部分） |
| `backend/tests/test_schema.py` | 本地 pglast 语法+AST 测试 | M0.1 DDL 本地快速迭代 |
| `backend/requirements-dev.txt` | 追加 `pglast>=6` | M0.1 DDL 验收 |
| `.github/workflows/schema-smoke.yml` | CI ephemeral Postgres + SQLite runtime 验证 | M0.1 DDL runtime 验收 |
| `backend/sql/tests/test_training_set_schema.sh` | 部署测试 | M0.1 DDL 验收 |
| `backend/sql/tests/test_app_schema.sh` | 部署测试 | M0.1 DDL 验收 |
| `scripts/acceptance/plan_1_m0_1_db_schema.sh` | 聚合验收脚本 | M0.1 DDL 验收 |

**后续 plan 占位**（不在本 plan scope）：
- **Plan 1f** — M0.1 CONTRACT_VERSION 矩阵 + Migration rollback 规则 + 部署顺序（`docs/contracts/contract-version-matrix.md` + `docs/contracts/migration-rollback-rules.md`；codex round 4/5 发现 migration atomicity / rollback policy 需要更深入的独立设计，本 plan 仅冻结 DDL 不锁 migration 治理）
- Plan 1b — M0.2 REST API（`backend/openapi.yaml`）
- Plan 1c — M0.3 Swift 数据模型（`ios/Contracts/` SwiftPM 包）
- Plan 1d — M0.4 错误处理（`AppError.swift`）
- Plan 1e — M0.5 并发约定（`docs/contracts/concurrency-rules.md`）

---

## Task 0: 把 §15.3 三段评审策略补入 adversarial-review 模板

**Files:**
- Modify: `docs/governance/adversarial-review-template.md`

**背景**：memory `project_review_strategy_deferred.md` 登记了 Plan 0a 时推迟的模板补齐动作 —— 把 `kline_trainer_modules_v1.4.md` §15.3 的三段评审节点（Wave 0 局部 / Wave 2 集成 / Phase 5 性能）写入 `docs/governance/adversarial-review-template.md` 作为独立 section。Plan 1 首次交付业务代码，此时补入时机合适（一次性补齐，后续 Plan 1b-1e 直接用）。

- [ ] **Step 1: 读现有模板确认锚点**

Run: `sed -n '1,40p' docs/governance/adversarial-review-template.md`
Expected: 输出 40 行，末尾是"闭环规则"列表，文件以"中间操作自动执行..."结束。

- [ ] **Step 2: 在"闭环规则"之后追加"按阶段评审策略"section**

把下面整段附加到 `docs/governance/adversarial-review-template.md` 末尾（在 `## 闭环规则` section 之后）：

```markdown

## 按阶段评审策略（来自 modules v1.4 §15.3）

v1.0 → v1.4 已经过 4 轮 codex 对抗性评审，累计 56 项修订收敛。后续评审按下列三段触发，不再做 spec 全量评审。

### 阶段 1：Wave 0 局部定向评审

| | 内容 |
|---|---|
| 触发时机 | Wave 0 编码中，某个子模块契约落地时发现硬伤（编译不过 / 字段缺失 / 类型不闭合 / 与 spec 不一致） |
| Review 维度 | 仅聚焦**该子模块**的契约实现 vs spec 声明 |
| 对照基线 | `kline_trainer_modules_v1.4.md` + `kline_trainer_plan_v1.5.md` 相关 section |
| 产出 | per-module verdict：approve / needs-attention / blocker |
| 收敛预算 | 按 `.claude/workflow-rules.json` `adversarial_review_loop.max_rounds` = 3 |

### 阶段 2：Wave 2 集成层评审

| | 内容 |
|---|---|
| 触发时机 | Wave 2 完成（C8 桥接 + E5/E6 编排跨模块协议落地）之后、进入 Wave 3 之前 |
| Review 维度 | 跨模块"契约声明 vs 实际实现"一致性：U → E6 → E5 → P* → B3、C8 → C1b/c、reducer action 矩阵覆盖率 |
| 对照基线 | M0.3 / M0.4 / 各模块 spec 声明 vs Wave 2 交付代码 |
| 产出 | 跨模块一致性 verdict + 未覆盖 action 清单 |
| 收敛预算 | max 3 轮 |

### 阶段 3：Phase 5 性能评审

| | 内容 |
|---|---|
| 触发时机 | Phase 5 磨光开始前，Instruments 采集过至少一轮真机数据之后 |
| Review 维度 | 性能目标（`kline_trainer_plan_v1.5.md` §一"单帧 <4ms"）对照；Core Graphics 调用次数 / bitmap cache 引入时机 / CADisplayLink 回调重载 |
| 对照基线 | Instruments Time Profiler + Core Animation 数据 |
| 产出 | 性能热点清单 + 优化建议；若 >4ms 热点无法消除 → 触发 Bitmap Cache 引入决策 |
| 收敛预算 | max 3 轮 |

**不在本策略覆盖范围内**：spec 全量评审（v1.4 之后不再做）；governance / hook / workflow-rules 变更（走 CLAUDE.md "Repository governance backstop" §1 codex:adversarial-review）。
```

- [ ] **Step 3: 验证追加结果**

Run: `wc -l docs/governance/adversarial-review-template.md && grep -c "按阶段评审策略" docs/governance/adversarial-review-template.md`
Expected: 行数较原始增加 ~40 行；"按阶段评审策略" 计数 = 1。

- [ ] **Step 4: 提交**

```bash
git add docs/governance/adversarial-review-template.md
git commit -m "docs(governance): append §15.3 staged review strategy to adversarial-review template

Per modules v1.4 §15.3: Wave 0 local / Wave 2 integration / Phase 5 perf.
Memory project_review_strategy_deferred.md Task 0 for Plan 1."
```

---

## Task 1: PostgreSQL DDL（M0.1）

**Files:**
- Create: `backend/sql/schema.sql`
- Modify: `backend/requirements-dev.txt`（追加 `pglast>=6`）
- Create: `backend/tests/test_schema.py`

**背景**：对应 spec M0.1 + `kline_trainer_plan_v1.5.md` §3.1。v1.4 fresh state 直接包含 `content_hash CHAR(8) NOT NULL`、lease 三列（lease_id / lease_expires_at / reserved_at）、`UNIQUE(stock_code, start_datetime)` 约束。

**工具**：用 `pglast`（Postgres 官方 libpg_query C 解析器的 Python 绑定）做纯语法 + AST 对象断言。不起 Docker / daemon；CI 和本地同一套。test 文件走已有 `backend/tests/` 的 pytest pattern（复用 `test_health.py` 的模式）。

- [ ] **Step 1: 创建 `backend/sql/` 目录并写入 schema.sql**

```bash
mkdir -p backend/sql
```

写入 `backend/sql/schema.sql`：

```sql
-- Kline Trainer PostgreSQL schema v1.4
-- 覆盖范围：stocks / klines / training_sets
-- Baseline：v1.4 fresh state（含 lease 三列 + content_hash CHAR(8) NOT NULL + UNIQUE）
-- 变更策略：加列只追加；破坏性变更走 migration forward/rollback（Wave 1 B3 owner）

BEGIN;

CREATE TABLE IF NOT EXISTS stocks (
    code VARCHAR(10) PRIMARY KEY,
    name VARCHAR(50) NOT NULL
);

CREATE TABLE IF NOT EXISTS klines (
    id BIGSERIAL PRIMARY KEY,
    stock_code VARCHAR(10) NOT NULL REFERENCES stocks(code),
    period VARCHAR(10) NOT NULL,
    datetime BIGINT NOT NULL,
    open DECIMAL(10,2) NOT NULL,
    high DECIMAL(10,2) NOT NULL,
    low DECIMAL(10,2) NOT NULL,
    close DECIMAL(10,2) NOT NULL,
    volume BIGINT NOT NULL,
    amount DECIMAL(16,2),
    ticket_index INTEGER,
    ma66 DECIMAL(10,4),
    boll_upper DECIMAL(10,4),
    boll_mid DECIMAL(10,4),
    boll_lower DECIMAL(10,4),
    macd_diff DECIMAL(10,6),
    macd_dea DECIMAL(10,6),
    macd_bar DECIMAL(10,6),
    UNIQUE(stock_code, period, datetime)
);

CREATE INDEX IF NOT EXISTS idx_klines_lookup ON klines(stock_code, period, datetime);

CREATE TABLE IF NOT EXISTS training_sets (
    id SERIAL PRIMARY KEY,
    stock_code VARCHAR(10) NOT NULL,
    stock_name VARCHAR(50) NOT NULL,
    start_datetime BIGINT NOT NULL,
    end_datetime BIGINT NOT NULL,
    schema_version INTEGER NOT NULL DEFAULT 1,
    file_path VARCHAR(255) NOT NULL,
    content_hash CHAR(8) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    status VARCHAR(10) NOT NULL DEFAULT 'unsent',
    lease_id UUID NULL,
    lease_expires_at TIMESTAMPTZ NULL,
    reserved_at TIMESTAMPTZ NULL,
    CONSTRAINT uq_stock_start UNIQUE (stock_code, start_datetime),
    -- v1.4 M0.1 runtime invariants (codex round 8 finding)
    CONSTRAINT ck_content_hash_crc32_lowercase CHECK (content_hash ~ '^[0-9a-f]{8}$'),
    CONSTRAINT ck_status_enum CHECK (status IN ('unsent', 'reserved', 'sent')),
    CONSTRAINT ck_lease_state_invariant CHECK (
        (status = 'unsent'
            AND lease_id IS NULL
            AND lease_expires_at IS NULL
            AND reserved_at IS NULL)
        OR
        (status IN ('reserved', 'sent')
            AND lease_id IS NOT NULL
            AND lease_expires_at IS NOT NULL
            AND reserved_at IS NOT NULL)
    )
);

CREATE INDEX IF NOT EXISTS idx_training_sets_lease
  ON training_sets(lease_id) WHERE lease_id IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_training_sets_lease_expire
  ON training_sets(lease_expires_at) WHERE status = 'reserved';

COMMENT ON COLUMN training_sets.content_hash
  IS 'zip 文件 CRC32 十六进制（8 字符，小写），由 B2 生成、P2 校验。';
COMMENT ON COLUMN training_sets.status
  IS '状态机：unsent → reserved → sent（详见 modules v1.4 M0.1 状态机不变量表）。';

COMMIT;
```

- [ ] **Step 2: 在 `backend/requirements-dev.txt` 追加 pglast**

当前文件内容：

```
pytest>=8.0
httpx>=0.27
```

改为：

```
pytest>=8.0
httpx>=0.27
pglast>=6
```

然后安装：

```bash
cd backend && python -m pip install -r requirements-dev.txt && cd ..
```

Expected: `pglast` 成功安装；`python -c "import pglast; print(pglast.__version__)"` 输出 ≥ 6 的版本号。

- [ ] **Step 3: 写 pytest + pglast 语法校验测试**

写入 `backend/tests/test_schema.py`：

```python
"""Validate backend/sql/schema.sql: PostgreSQL 15 syntax + expected AST objects.

Uses pglast (libpg_query bindings) so no Docker or daemon is required.
Production deployment validation (on NAS PostgreSQL) is Wave 1 B3 owner scope.
"""
from __future__ import annotations

from pathlib import Path

import pglast
from pglast.ast import (
    AlterTableStmt,
    Constraint,
    CreateStmt,
    IndexStmt,
)

SCHEMA_PATH = Path(__file__).parent.parent / "sql" / "schema.sql"


def _parse_schema() -> list:
    sql = SCHEMA_PATH.read_text(encoding="utf-8")
    return pglast.parse_sql(sql)


def test_schema_is_valid_postgres_syntax():
    """Schema must parse without error via libpg_query (same parser real Postgres uses)."""
    stmts = _parse_schema()
    assert len(stmts) > 0, "empty parse result"


def test_three_tables_created():
    """Expect CREATE TABLE for stocks, klines, training_sets."""
    stmts = _parse_schema()
    table_names = {
        s.stmt.relation.relname
        for s in stmts
        if isinstance(s.stmt, CreateStmt)
    }
    assert table_names == {"stocks", "klines", "training_sets"}, (
        f"expected {{stocks,klines,training_sets}}, got {table_names}"
    )


def test_training_sets_content_hash_char8_not_null():
    """training_sets.content_hash must be CHAR(8) NOT NULL."""
    stmts = _parse_schema()
    training_sets = next(
        s.stmt
        for s in stmts
        if isinstance(s.stmt, CreateStmt)
        and s.stmt.relation.relname == "training_sets"
    )
    col = next(
        e for e in training_sets.tableElts
        if hasattr(e, "colname") and e.colname == "content_hash"
    )
    type_names = [n.sval for n in col.typeName.names]
    # pglast expresses CHAR(N) as pg_catalog.bpchar with typmods=[N]
    assert type_names[-1] in ("bpchar", "char"), f"content_hash type: {type_names}"
    typmods = col.typeName.typmods or ()
    assert len(typmods) == 1
    assert typmods[0].val.ival == 8, f"content_hash length: {typmods[0].val.ival}"
    not_null = any(
        getattr(c, "contype", None) is not None
        and c.contype == pglast.enums.ConstrType.CONSTR_NOTNULL
        for c in (col.constraints or ())
    )
    assert not_null, "content_hash must be NOT NULL"


def test_training_sets_has_uq_stock_start():
    """Table-level UNIQUE(stock_code, start_datetime) must be named uq_stock_start."""
    stmts = _parse_schema()
    training_sets = next(
        s.stmt
        for s in stmts
        if isinstance(s.stmt, CreateStmt)
        and s.stmt.relation.relname == "training_sets"
    )
    table_constraints = [
        e for e in training_sets.tableElts if isinstance(e, Constraint)
    ]
    names = [c.conname for c in table_constraints if c.conname]
    assert "uq_stock_start" in names, f"constraints: {names}"


def test_training_sets_has_lease_columns():
    """Expect lease_id UUID / lease_expires_at TIMESTAMPTZ / reserved_at TIMESTAMPTZ."""
    stmts = _parse_schema()
    training_sets = next(
        s.stmt
        for s in stmts
        if isinstance(s.stmt, CreateStmt)
        and s.stmt.relation.relname == "training_sets"
    )
    cols = {
        e.colname: [n.sval for n in e.typeName.names]
        for e in training_sets.tableElts
        if hasattr(e, "colname")
    }
    assert cols.get("lease_id", [])[-1:] == ["uuid"], f"lease_id: {cols.get('lease_id')}"
    assert cols.get("lease_expires_at", [])[-1:] == ["timestamptz"], cols.get("lease_expires_at")
    assert cols.get("reserved_at", [])[-1:] == ["timestamptz"], cols.get("reserved_at")


def test_training_sets_partial_indexes_exist():
    """idx_training_sets_lease (partial WHERE lease_id IS NOT NULL) + idx_training_sets_lease_expire."""
    stmts = _parse_schema()
    indexes = {
        s.stmt.idxname: s.stmt
        for s in stmts
        if isinstance(s.stmt, IndexStmt)
    }
    assert "idx_training_sets_lease" in indexes
    assert "idx_training_sets_lease_expire" in indexes
    assert "idx_klines_lookup" in indexes


def test_klines_unique_stock_period_datetime():
    """klines needs UNIQUE(stock_code, period, datetime) (inline table constraint)."""
    stmts = _parse_schema()
    klines = next(
        s.stmt
        for s in stmts
        if isinstance(s.stmt, CreateStmt) and s.stmt.relation.relname == "klines"
    )
    uniques = [
        c for c in klines.tableElts
        if isinstance(c, Constraint) and c.contype == pglast.enums.ConstrType.CONSTR_UNIQUE
    ]
    found = False
    for c in uniques:
        keys = [k.sval for k in (c.keys or ())]
        if set(keys) == {"stock_code", "period", "datetime"}:
            found = True
            break
    assert found, "klines UNIQUE(stock_code, period, datetime) missing"


def test_training_sets_has_required_check_constraints():
    """v1.4 M0.1 runtime invariants (codex round 8): three CHECK constraints by name."""
    stmts = _parse_schema()
    training_sets = next(
        s.stmt
        for s in stmts
        if isinstance(s.stmt, CreateStmt)
        and s.stmt.relation.relname == "training_sets"
    )
    checks = [
        c for c in training_sets.tableElts
        if isinstance(c, Constraint) and c.contype == pglast.enums.ConstrType.CONSTR_CHECK
    ]
    names = {c.conname for c in checks if c.conname}
    required = {
        "ck_content_hash_crc32_lowercase",
        "ck_status_enum",
        "ck_lease_state_invariant",
    }
    missing = required - names
    assert not missing, f"missing CHECK constraints: {missing}; got {names}"
```

- [ ] **Step 4: 运行失败路径 sanity check**

先故意跑一次空 schema 文件，确认测试会 FAIL 而非 silently pass：

```bash
cp backend/sql/schema.sql /tmp/schema.sql.bak
: > backend/sql/schema.sql
cd backend && python -m pytest tests/test_schema.py -q; RC=$?; cd ..
mv /tmp/schema.sql.bak backend/sql/schema.sql
[[ $RC -ne 0 ]] && echo "FAIL expected"
```

Expected: pytest 报多个 assertion error / empty parse result，至少 5 test failed；最后 shell 打印 "FAIL expected"。

- [ ] **Step 5: 运行通过路径**

```bash
cd backend && python -m pytest tests/test_schema.py -v && cd ..
```

Expected: 8 passed（每个断言一个 test）；末尾 `8 passed in X.XXs`。

- [ ] **Step 6: 提交**

```bash
git add backend/sql/schema.sql backend/requirements-dev.txt backend/tests/test_schema.py
git commit -m "feat(m0.1): PostgreSQL schema v1.4 baseline + pglast AST test

Replace Docker-based deploy test with pglast (libpg_query) syntax + AST assertions.
Local validation needs no daemon; production deployment on NAS PostgreSQL is
Wave 1 B3 owner scope (see Plan 1 2026-04-21 tooling choice E)."
```

---

## Task 2: 训练组 SQLite DDL（M0.1）

**Files:**
- Create: `backend/sql/training_set_schema_v1.sql`
- Create: `backend/sql/tests/test_training_set_schema.sh`

**背景**：v1.5 §3.2 的训练组 schema，`PRAGMA user_version=1` 作版本号，不 rollback（用 schema_version 直接拒收旧文件，见 Task 4 rollback 规则表）。

- [ ] **Step 1: 写入 `backend/sql/training_set_schema_v1.sql`**

```sql
-- Kline Trainer 训练组 SQLite schema v1
-- 每个训练组生成为独立 .db 文件，PRAGMA user_version=1 标识版本
-- 不支持 rollback：schema 变更时 bump user_version，旧 version reader 直接拒收

PRAGMA user_version = 1;

CREATE TABLE meta (
    stock_code TEXT NOT NULL,
    stock_name TEXT NOT NULL,
    start_datetime INTEGER NOT NULL,
    end_datetime INTEGER NOT NULL
);

CREATE TABLE klines (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    period TEXT NOT NULL,
    datetime INTEGER NOT NULL,
    open REAL NOT NULL,
    high REAL NOT NULL,
    low REAL NOT NULL,
    close REAL NOT NULL,
    volume INTEGER NOT NULL,
    amount REAL,
    ma66 REAL,
    boll_upper REAL,
    boll_mid REAL,
    boll_lower REAL,
    macd_diff REAL,
    macd_dea REAL,
    macd_bar REAL,
    global_index INTEGER,
    end_global_index INTEGER NOT NULL
);

CREATE INDEX idx_period_endidx ON klines(period, end_global_index);
CREATE INDEX idx_period_datetime ON klines(period, datetime);
```

- [ ] **Step 2: 写 SQLite DDL 部署验证脚本**

写入 `backend/sql/tests/test_training_set_schema.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
SCHEMA="$REPO_ROOT/backend/sql/training_set_schema_v1.sql"
DB=$(mktemp -t training_set_schema.XXXXXX.db)
trap 'rm -f "$DB"' EXIT

sqlite3 "$DB" < "$SCHEMA"

# PRAGMA user_version 必须 = 1
UV=$(sqlite3 "$DB" "PRAGMA user_version;")
if [[ "$UV" != "1" ]]; then
  echo "FAIL: expected user_version=1, got $UV"
  exit 1
fi

# meta + klines 两表存在（排除 SQLite 内部表 sqlite_sequence，AUTOINCREMENT 会创建它）
TABLES=$(sqlite3 "$DB" "SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%' ORDER BY name;" | paste -sd, -)
if [[ "$TABLES" != "klines,meta" ]]; then
  echo "FAIL: expected tables klines,meta; got '$TABLES'"
  exit 1
fi

# 2 个 idx_* 索引（sqlite_autoindex_* 已被 LIKE 'idx_%' 自动排除）
IDX=$(sqlite3 "$DB" "SELECT count(*) FROM sqlite_master WHERE type='index' AND name LIKE 'idx_%';")
if [[ "$IDX" != "2" ]]; then
  echo "FAIL: expected 2 idx_* indexes, got $IDX"
  exit 1
fi

# end_global_index NOT NULL
ENDIDX_NOTNULL=$(sqlite3 "$DB" "SELECT \"notnull\" FROM pragma_table_info('klines') WHERE name='end_global_index';")
if [[ "$ENDIDX_NOTNULL" != "1" ]]; then
  echo "FAIL: klines.end_global_index expected NOT NULL; got notnull=$ENDIDX_NOTNULL"
  exit 1
fi

echo "PASS: training_set_schema_v1.sql deploys with user_version=1"
```

然后 `chmod +x backend/sql/tests/test_training_set_schema.sh`。

- [ ] **Step 3: 运行失败路径（sanity check）**

```bash
cp backend/sql/training_set_schema_v1.sql /tmp/ts_schema.bak
echo "PRAGMA user_version = 2;" > backend/sql/training_set_schema_v1.sql
./backend/sql/tests/test_training_set_schema.sh || echo "FAIL expected"
mv /tmp/ts_schema.bak backend/sql/training_set_schema_v1.sql
```
Expected: 脚本输出 "FAIL: expected user_version=1, got 2" 并退出非 0；最后 shell 打印 "FAIL expected"。

- [ ] **Step 4: 运行通过路径**

```bash
./backend/sql/tests/test_training_set_schema.sh
```
Expected: "PASS: training_set_schema_v1.sql deploys with user_version=1"，退出码 0。

- [ ] **Step 5: 提交**

```bash
git add backend/sql/training_set_schema_v1.sql backend/sql/tests/test_training_set_schema.sh
git commit -m "feat(m0.1): training set SQLite schema v1 + deploy test"
```

---

## Task 3: app.sqlite DDL（M0.1）

**Files:**
- Create: `ios/sql/app_schema_v1.sql`
- Create: `backend/sql/tests/test_app_schema.sh`

**背景**：v1.5 §3.3 + v1.2 扩充（`training_records.final_tick`）+ v1.3 新增（`download_acceptance_journal` 表）+ v1.4 拆 bump 策略中提到的 `pending_training.cash_balance / drawdown` denormalize。起点 fresh state 直接含所有 v1.4 列；`leased` 行永不存在（v1.4 删），无需 purge migration。

- [ ] **Step 1: 写入 `ios/sql/app_schema_v1.sql`**

```bash
mkdir -p ios/sql
```

写入 `ios/sql/app_schema_v1.sql`：

```sql
-- Kline Trainer app.sqlite schema v1（v1.4 fresh state）
-- GRDB DatabaseMigrator 在 P4 初始化时 registerMigration("0001_v1.4_baseline") 中执行本文件
-- 变更策略：破坏性变更新增 migration id + 反向 migration（P4 owner）

PRAGMA user_version = 1;

CREATE TABLE training_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    training_set_filename TEXT NOT NULL,
    created_at INTEGER NOT NULL,
    stock_code TEXT NOT NULL,
    stock_name TEXT NOT NULL,
    start_year INTEGER NOT NULL,
    start_month INTEGER NOT NULL,
    total_capital REAL NOT NULL,
    profit REAL NOT NULL,
    return_rate REAL NOT NULL,
    max_drawdown REAL NOT NULL,
    buy_count INTEGER NOT NULL,
    sell_count INTEGER NOT NULL,
    fee_snapshot TEXT NOT NULL,
    final_tick INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE trade_operations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id INTEGER NOT NULL REFERENCES training_records(id),
    global_tick INTEGER NOT NULL,
    period TEXT NOT NULL,
    direction TEXT NOT NULL,
    price REAL NOT NULL,
    shares INTEGER NOT NULL,
    position_tier TEXT NOT NULL,
    commission REAL NOT NULL,
    stamp_duty REAL NOT NULL,
    total_cost REAL NOT NULL,
    created_at INTEGER NOT NULL
);

CREATE TABLE drawings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id INTEGER NOT NULL REFERENCES training_records(id),
    tool_type TEXT NOT NULL,
    panel_position INTEGER NOT NULL,
    is_extended INTEGER NOT NULL DEFAULT 0,
    anchors TEXT NOT NULL
);

CREATE TABLE pending_training (
    id INTEGER PRIMARY KEY CHECK (id = 1),
    training_set_filename TEXT NOT NULL,
    global_tick_index INTEGER NOT NULL,
    upper_period TEXT NOT NULL,
    lower_period TEXT NOT NULL,
    position_data TEXT NOT NULL,
    fee_snapshot TEXT NOT NULL,
    trade_operations TEXT NOT NULL,
    drawings TEXT NOT NULL,
    started_at INTEGER NOT NULL,
    accumulated_capital REAL NOT NULL,
    cash_balance REAL NOT NULL,
    drawdown TEXT NOT NULL
);

CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

CREATE TABLE download_acceptance_journal (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    training_set_id INTEGER NOT NULL,
    lease_id TEXT NOT NULL,
    state TEXT NOT NULL,
    state_entered_at INTEGER NOT NULL,
    last_error TEXT,
    sqlite_local_path TEXT,
    content_hash CHAR(8),
    UNIQUE (training_set_id, lease_id)
);

CREATE INDEX idx_journal_state ON download_acceptance_journal(state);
```

- [ ] **Step 2: 写 app.sqlite DDL 部署验证脚本**

写入 `backend/sql/tests/test_app_schema.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
SCHEMA="$REPO_ROOT/ios/sql/app_schema_v1.sql"
DB=$(mktemp -t app_schema.XXXXXX.db)
trap 'rm -f "$DB"' EXIT

sqlite3 "$DB" < "$SCHEMA"

# 6 张表（按字母序比对）：download_acceptance_journal / drawings / pending_training / settings / trade_operations / training_records
# 排除 SQLite 内部表（AUTOINCREMENT 会在 sqlite_master 生成 sqlite_sequence 行）
TABLES=$(sqlite3 "$DB" "SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%' ORDER BY name;" | paste -sd, -)
EXPECTED="download_acceptance_journal,drawings,pending_training,settings,trade_operations,training_records"
if [[ "$TABLES" != "$EXPECTED" ]]; then
  echo "FAIL: tables mismatch"
  echo "  expected: $EXPECTED"
  echo "  got:      $TABLES"
  exit 1
fi

# training_records.final_tick 列存在（v1.2 必须）且 NOT NULL
FT=$(sqlite3 "$DB" "SELECT count(*) FROM pragma_table_info('training_records') WHERE name='final_tick' AND \"notnull\"=1;")
if [[ "$FT" != "1" ]]; then
  echo "FAIL: training_records.final_tick missing or nullable"
  exit 1
fi

# pending_training.cash_balance + drawdown 存在（v1.3 必须）且均 NOT NULL
CB=$(sqlite3 "$DB" "SELECT count(*) FROM pragma_table_info('pending_training') WHERE name IN ('cash_balance','drawdown') AND \"notnull\"=1;")
if [[ "$CB" != "2" ]]; then
  echo "FAIL: pending_training.cash_balance / drawdown missing or nullable"
  exit 1
fi

# download_acceptance_journal UNIQUE(training_set_id, lease_id)
# SQLite 为 table-level UNIQUE 创建 sqlite_autoindex_* 索引（sql IS NULL），用 pragma_index_list 查 unique=1 更稳
UQ=$(sqlite3 "$DB" "SELECT count(*) FROM pragma_index_list('download_acceptance_journal') WHERE \"unique\"=1;")
if [[ "$UQ" -lt "1" ]]; then
  echo "FAIL: journal UNIQUE(training_set_id, lease_id) missing"
  exit 1
fi

# idx_journal_state 存在
JIDX=$(sqlite3 "$DB" "SELECT count(*) FROM sqlite_master WHERE type='index' AND name='idx_journal_state';")
if [[ "$JIDX" != "1" ]]; then
  echo "FAIL: idx_journal_state missing"
  exit 1
fi

echo "PASS: app_schema_v1.sql deploys with all v1.4 columns"
```

然后 `chmod +x backend/sql/tests/test_app_schema.sh`。

- [ ] **Step 3: 运行失败路径（sanity check）**

```bash
cp ios/sql/app_schema_v1.sql /tmp/app_schema.bak
# 故意删除 final_tick 列
sed -i.orig '/final_tick/d' ios/sql/app_schema_v1.sql
./backend/sql/tests/test_app_schema.sh || echo "FAIL expected"
mv /tmp/app_schema.bak ios/sql/app_schema_v1.sql
rm -f ios/sql/app_schema_v1.sql.orig
```
Expected: 输出 "FAIL: training_records.final_tick missing or nullable" 并退出非 0；最后 shell 打印 "FAIL expected"。

- [ ] **Step 4: 运行通过路径**

```bash
./backend/sql/tests/test_app_schema.sh
```
Expected: "PASS: app_schema_v1.sql deploys with all v1.4 columns"，退出码 0。

- [ ] **Step 5: 提交**

```bash
git add ios/sql/app_schema_v1.sql backend/sql/tests/test_app_schema.sh
git commit -m "feat(m0.1): app.sqlite schema v1.4 baseline + deploy test"
```

---

## Task 4: Plan 1 聚合 acceptance 脚本

**Files:**
- Create: `scripts/acceptance/plan_1_m0_1_db_schema.sh`

**背景**：Plan 0a 建立了 `scripts/acceptance/plan_0a_toolchain.sh` 作为聚合入口。Plan 1 对齐同样的一键验收模式。

- [ ] **Step 1: 写聚合脚本**

写入 `scripts/acceptance/plan_1_m0_1_db_schema.sh`：

```bash
#!/usr/bin/env bash
# Plan 1 聚合验收：M0.1 DB schema（DDL 部分）
# 涵盖：PostgreSQL / 训练组 SQLite / app.sqlite 三套 DDL + §15.3 模板补齐
# CONTRACT_VERSION 矩阵 + migration rollback 规则移至 Plan 1f（独立设计）
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO_ROOT"

PASS=0
FAIL=0
declare -a FAILED

run() {
  local label="$1"; shift
  echo ""
  echo "========== $label =========="
  if "$@"; then
    echo "OK: $label"
    PASS=$((PASS + 1))
  else
    echo "NG: $label"
    FAIL=$((FAIL + 1))
    FAILED+=("$label")
  fi
}

# ---- 文档存在性 ----
run "doc: adversarial-review §15.3 section" bash -c "grep -q '按阶段评审策略' docs/governance/adversarial-review-template.md"

# ---- SQL DDL 部署 ----
run "sql: PostgreSQL schema (pglast AST)" \
    bash -c "cd backend && python -m pytest tests/test_schema.py -q"
run "sql: training set SQLite schema"       ./backend/sql/tests/test_training_set_schema.sh
run "sql: app.sqlite schema"                ./backend/sql/tests/test_app_schema.sh

# ---- 汇总 ----
echo ""
echo "============================================"
echo "Plan 1 (M0.1 DDL) acceptance: $PASS passed, $FAIL failed"
if [[ $FAIL -gt 0 ]]; then
  echo "Failed items:"
  for f in "${FAILED[@]}"; do echo "  - $f"; done
  echo "PLAN 1 FAIL"
  exit 1
fi
echo "PLAN 1 PASS"
```

然后 `chmod +x scripts/acceptance/plan_1_m0_1_db_schema.sh`。

- [ ] **Step 2: 运行聚合脚本（通过路径）**

```bash
./scripts/acceptance/plan_1_m0_1_db_schema.sh
```
Expected: 末行 "Plan 1 (M0.1 DDL) acceptance: 4 passed, 0 failed" + "PLAN 1 PASS"，退出码 0。

- [ ] **Step 3: 运行失败路径（sanity check）**

```bash
# 临时破坏 §15.3 section 标题，触发 doc check FAIL
cp docs/governance/adversarial-review-template.md /tmp/art.bak
sed -i.orig 's/按阶段评审策略/按阶段XXXXX/' docs/governance/adversarial-review-template.md
./scripts/acceptance/plan_1_m0_1_db_schema.sh || echo "PLAN 1 FAIL expected"
mv /tmp/art.bak docs/governance/adversarial-review-template.md
rm -f docs/governance/adversarial-review-template.md.orig
```
Expected: 输出 "NG: doc: adversarial-review §15.3 section" + "PLAN 1 FAIL" + shell 打印 "PLAN 1 FAIL expected"。

- [ ] **Step 4: 最终通过确认**

```bash
./scripts/acceptance/plan_1_m0_1_db_schema.sh
```
Expected: "PLAN 1 PASS" 再次出现。

- [ ] **Step 5: 提交**

```bash
git add scripts/acceptance/plan_1_m0_1_db_schema.sh
git commit -m "feat(plan-1): aggregate M0.1 DB schema acceptance script"
```

---

## Task 5: GitHub Actions schema-smoke CI workflow（runtime 验证）

**Files:**
- Create: `.github/workflows/schema-smoke.yml`

**背景（codex round 7 finding）**：pglast 是 parser-level 校验，**不足以证明 schema 在真实 Postgres 上能部署执行**。真实 runtime 问题（跨版本类型差异、REFERENCES 语义、partial index 表达式、COMMENT ON 权限等）只有实际 `psql -f schema.sql` 才能暴露。本 task 在 GitHub Actions 加一层 runtime 验证：每个 PR 起 ephemeral `postgres:15` service + Ubuntu runner 里跑 sqlite3，保证 pglast local PASS **不会 mask** runtime DDL failure。

**与 NAS 的边界**：本 workflow 用 GH Actions service container（一次性、不触碰 NAS 生产 DB）。NAS 上生产部署仍归 Wave 1 B3 owner 在正式 migration 阶段处理。

- [ ] **Step 1: 写入 workflow 文件**

写入 `.github/workflows/schema-smoke.yml`：

```yaml
name: Schema Smoke Test

on:
  pull_request:
    paths:
      - 'backend/sql/**'
      - 'backend/tests/test_schema.py'
      - 'ios/sql/**'
      - '.github/workflows/schema-smoke.yml'
  push:
    branches: [main]

jobs:
  postgres-runtime-smoke:
    name: PostgreSQL 15 ephemeral deploy
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: schema_smoke
          POSTGRES_DB: kt_schema_smoke
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      PGPASSWORD: schema_smoke
      PGHOST: 127.0.0.1
      PGPORT: 5432
      PGUSER: postgres
      PGDATABASE: kt_schema_smoke
    steps:
      - uses: actions/checkout@v4
      - name: Wait for postgres ready
        run: |
          for i in $(seq 1 30); do
            pg_isready -h 127.0.0.1 -p 5432 -U postgres && break
            sleep 1
          done
      - name: Deploy schema.sql
        run: psql -v ON_ERROR_STOP=1 -f backend/sql/schema.sql
      - name: Assert 3 application tables
        run: |
          COUNT=$(psql -tAc "SELECT count(*) FROM pg_tables WHERE schemaname='public' AND tablename IN ('stocks','klines','training_sets');")
          [[ "$COUNT" == "3" ]] || { echo "FAIL: expected 3 tables, got $COUNT"; exit 1; }
      - name: Assert uq_stock_start constraint
        run: |
          COUNT=$(psql -tAc "SELECT count(*) FROM pg_constraint WHERE conname='uq_stock_start';")
          [[ "$COUNT" == "1" ]] || { echo "FAIL: uq_stock_start missing"; exit 1; }
      - name: Assert content_hash CHAR(8) NOT NULL
        run: |
          LEN=$(psql -tAc "SELECT character_maximum_length FROM information_schema.columns WHERE table_name='training_sets' AND column_name='content_hash';")
          NULL=$(psql -tAc "SELECT is_nullable FROM information_schema.columns WHERE table_name='training_sets' AND column_name='content_hash';")
          [[ "$LEN" == "8" && "$NULL" == "NO" ]] || { echo "FAIL: content_hash expected CHAR(8) NOT NULL, got length=$LEN nullable=$NULL"; exit 1; }
      - name: Assert lease partial indexes
        run: |
          psql -tAc "SELECT indexname FROM pg_indexes WHERE tablename='training_sets' AND indexname IN ('idx_training_sets_lease','idx_training_sets_lease_expire');" | sort | paste -sd, - | grep -q 'idx_training_sets_lease,idx_training_sets_lease_expire'
      # v1.4 M0.1 runtime invariant smoke tests (codex round 8)
      - name: Reject uppercase content_hash (ck_content_hash_crc32_lowercase)
        run: |
          ! psql -v ON_ERROR_STOP=1 -c "INSERT INTO training_sets(stock_code, stock_name, start_datetime, end_datetime, file_path, content_hash) VALUES ('X', 'Y', 0, 0, 'f', 'ABCDEF12');"
      - name: Reject non-enum status (ck_status_enum)
        run: |
          ! psql -v ON_ERROR_STOP=1 -c "INSERT INTO training_sets(stock_code, stock_name, start_datetime, end_datetime, file_path, content_hash, status) VALUES ('X', 'Y', 0, 0, 'f', 'abcdef12', 'bogus');"
      - name: Reject unsent row with lease populated (ck_lease_state_invariant)
        run: |
          ! psql -v ON_ERROR_STOP=1 -c "INSERT INTO training_sets(stock_code, stock_name, start_datetime, end_datetime, file_path, content_hash, status, lease_id, lease_expires_at, reserved_at) VALUES ('X', 'Y', 0, 0, 'f', 'abcdef12', 'unsent', '00000000-0000-0000-0000-000000000001', NOW(), NOW());"
      - name: Reject reserved row without lease (ck_lease_state_invariant)
        run: |
          ! psql -v ON_ERROR_STOP=1 -c "INSERT INTO training_sets(stock_code, stock_name, start_datetime, end_datetime, file_path, content_hash, status) VALUES ('X', 'Y', 0, 0, 'f', 'abcdef12', 'reserved');"
      - name: Accept a valid unsent row (positive control)
        run: |
          psql -v ON_ERROR_STOP=1 -c "INSERT INTO training_sets(stock_code, stock_name, start_datetime, end_datetime, file_path, content_hash) VALUES ('TEST', 'T', 0, 0, '/tmp/f.zip', 'abcdef12');"
          COUNT=$(psql -tAc "SELECT count(*) FROM training_sets WHERE stock_code='TEST';")
          [[ "$COUNT" == "1" ]] || { echo "FAIL: valid insert did not persist"; exit 1; }

  sqlite-runtime-smoke:
    name: SQLite training-set + app schemas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install sqlite3
        run: sudo apt-get update && sudo apt-get install -y sqlite3
      - name: training_set_schema_v1.sql smoke
        run: ./backend/sql/tests/test_training_set_schema.sh
      - name: app_schema_v1.sql smoke
        run: ./backend/sql/tests/test_app_schema.sh
```

- [ ] **Step 2: 本地 YAML 合法性校验**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/schema-smoke.yml'))"
```
Expected: 无输出、退出码 0（YAML 可被 PyYAML 解析）。

- [ ] **Step 3: 失败路径 sanity check**

```bash
cp .github/workflows/schema-smoke.yml /tmp/schema-smoke.yml.bak
echo "this: is: broken: yaml" > .github/workflows/schema-smoke.yml
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/schema-smoke.yml'))" 2>&1 || echo "YAML FAIL expected"
mv /tmp/schema-smoke.yml.bak .github/workflows/schema-smoke.yml
```
Expected: PyYAML 抛 parser error；shell 打印 "YAML FAIL expected"。

- [ ] **Step 4: CI 真实执行验证只能发生在 push/PR 后**

Runtime 证据是 PR 开出后 GitHub Actions 运行结果（`gh run list -w schema-smoke.yml` 查看）。本 Task 不在本地跑 CI；本地 PASS 等价于 YAML 合法 + workflow 文件存在。

- [ ] **Step 5: 提交**

```bash
git add .github/workflows/schema-smoke.yml
git commit -m "feat(plan-1): CI schema-smoke workflow (ephemeral postgres:15 + sqlite)

Addresses codex round 7 finding: pglast parser-only validation can miss
runtime DDL failures. Every PR touching SQL now runs real psql -f against
ephemeral postgres:15 service + sqlite3 schema deploy, gating M0.1 acceptance
on authoritative runtime behavior."
```

---

## 非 coder 验收清单（CLAUDE.md 治理 §2 / workflow-rules.json verification_template）

按 `.claude/workflow-rules.json` 的 `verification_template` 要求：中文 / action-expected-pass_fail；禁用"验证通过即可 / 看起来正常 / 应该没问题 / should work / looks fine"。

### 验收项 1：聚合脚本整体通过

| | 内容 |
|---|---|
| Action | 在仓库根目录终端运行 `./scripts/acceptance/plan_1_m0_1_db_schema.sh` |
| Expected | 末尾行依次出现 `Plan 1 (M0.1 DDL) acceptance: 4 passed, 0 failed` 和 `PLAN 1 PASS` |
| Pass / Fail | 两行同时满足 = PASS；任一不满足 = FAIL |

### 验收项 2：§15.3 三段评审策略已补入模板

| | 内容 |
|---|---|
| Action | 运行 `grep -c -E '阶段 [123]：(Wave 0 局部｜Wave 2 集成｜Phase 5 性能)' docs/governance/adversarial-review-template.md`（全角竖线 U+FF5C；或等价地去转义反斜杠：`grep -c -E "阶段 [123]：(Wave 0 局部|Wave 2 集成|Phase 5 性能)"`） |
| Expected | 输出数字 `3` |
| Pass / Fail | 输出 = 3 = PASS；其它数字 = FAIL |
| 备注 | Markdown 表格里 `\|` 是管道字符转义；运行时必须用**未转义的 `\|`（U+007C）或改写**上面两种形式。直接从表格 copy 的 `\\|` 在 `-E` 下是字面反斜杠+管道而非 alternation（codex round 8 finding）。 |

### 验收项 3：三套 SQL DDL 都能通过各自验证

| | 内容 |
|---|---|
| Action | 先 `cd backend && python -m pytest tests/test_schema.py -v && cd ..`；再依次 `./backend/sql/tests/test_training_set_schema.sh`、`./backend/sql/tests/test_app_schema.sh` |
| Expected | 第 1 条：pytest 输出以 `7 passed` 结束；第 2 条末行 `PASS: training_set_schema_v1.sql deploys with user_version=1`；第 3 条末行 `PASS: app_schema_v1.sql deploys with all v1.4 columns`；三者退出码全 0 |
| Pass / Fail | 三条命令都满足各自 Expected 且退出码全 0 = PASS；任一 FAIL / 报错 / 退出码非 0 = FAIL |

### 验收项 4：CI schema-smoke workflow 存在且 YAML 合法

| | 内容 |
|---|---|
| Action | 运行 `test -f .github/workflows/schema-smoke.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/schema-smoke.yml'))" && echo WF_OK` |
| Expected | 输出 `WF_OK` |
| Pass / Fail | 输出 WF_OK = PASS；文件缺失或 YAML 解析失败 = FAIL |

### 验收项 5：CI schema-smoke 在 PR 上真实 PASS（runtime 验证）

| | 内容 |
|---|---|
| Action | PR 开出后在 PR 页面查看 Checks tab，或 `gh pr checks <PR_NUM>`。等待 `PostgreSQL 15 ephemeral deploy` 和 `SQLite training-set + app schemas` 两 job 跑完 |
| Expected | 两个 job 都 `✓ Success`；workflow `Schema Smoke Test` 状态为 `completed success` |
| Pass / Fail | 两 job 都 Success = PASS；任一 failure / skipped / pending 超过 5 分钟 = FAIL |

### 验收项 6：提交记录完整

| | 内容 |
|---|---|
| Action | 运行 `git log --oneline plan-1/m0.1-db-schema ^origin/main \| wc -l` |
| Expected | 输出数字 ≥ 6（对应 Task 0 / Task 1 / Task 2 / Task 3 / Task 4 / Task 5 各一条业务 commit；plan draft/fix commits 已先行入 branch） |
| Pass / Fail | 数字 ≥ 6 = PASS；< 6 = FAIL |

### 验收项 7：codex-attest ledger 已更新

| | 内容 |
|---|---|
| Action | 运行 `grep -q 'm0.1-db-schema\|plan1-m0.1\|M0.1' .claude/state/attest-ledger.json && echo LEDGER_OK` |
| Expected | 输出 `LEDGER_OK` |
| Pass / Fail | 输出 LEDGER_OK = PASS；无输出（未登记）= FAIL（表明 codex-attest 没有拿到 approve verdict） |

---

## Self-Review（起草后自查）

1. **Spec 覆盖（仅 M0.1 DDL 子集）**：
   - M0.1 PostgreSQL DDL（含 lease 三列 / content_hash CHAR(8) NOT NULL / UNIQUE）→ Task 1 ✅（pglast 本地 + CI runtime 双层）
   - M0.1 训练组 SQLite DDL（PRAGMA user_version=1）→ Task 2 ✅（sqlite3 本地 + CI runtime 双层）
   - M0.1 app.sqlite DDL（含 final_tick / cash_balance / drawdown / download_acceptance_journal）→ Task 3 ✅（sqlite3 本地 + CI runtime 双层）
   - §15.3 三段评审策略模板补齐（memory defer 项）→ Task 0 ✅
   - 聚合 acceptance → Task 4 ✅
   - CI runtime 验证（round 7 codex 补强）→ Task 5 ✅

2. **范围外确认**（不在 Plan 1 scope）：
   - **M0.1 CONTRACT_VERSION 矩阵 + migration rollback 规则 + 部署顺序 → Plan 1f**（2026-04-21 scope 缩小决策：codex round 4/5 揭示 migration atomicity + rollback policy 需独立深入设计，不宜与 DDL 一起 freeze）
   - M0.2 OpenAPI → Plan 1b
   - M0.3 Swift 模型（enums / K 线 / REST DTO / AppState）→ Plan 1c
   - M0.4 AppError + Reason → Plan 1d
   - M0.5 并发约定 → Plan 1e
   - P2 `JournalState` enum 本身（Task 3 只建 journal 表 `state TEXT NOT NULL` 列，不定义 enum value set）→ Plan 3（P2 模块）

3. **Placeholder 扫描**：
   - 无 TBD / TODO / 实现 later
   - 前向引用 "Wave 1 B3 owner" / "Plan 1c/3 落地 GRDB migrator" 是真实依赖关系，非占位符

4. **类型 / 字段一致性**：
   - `content_hash CHAR(8)`：Task 1 Postgres NOT NULL + Task 3 app.sqlite `download_acceptance_journal.content_hash CHAR(8)`（此处 nullable 因 "已校验时填充"，与 Postgres 侧 NOT NULL 不矛盾；验证测试 Task 1 Step 2 显式检 Postgres 侧 NOT NULL）
   - `PRAGMA user_version=1`：Task 2 + Task 3 两套 SQLite 一致
   - 聚合脚本 `plan_1_m0_1_db_schema.sh` 引用的三个 `./backend/sql/tests/test_*.sh` 路径与 Task 1/2/3 创建的一致
   - SQLite test 脚本已处理 `sqlite_sequence` / `sqlite_autoindex_*` 内部对象（round 3 codex 发现修复）

5. **forbidden_phrases 扫描**（`.claude/workflow-rules.json`）：
   - 验收清单 / 步骤 expected 全部采用 "输出 XXX" / "退出码 0" 等具体判据，无禁用措辞

6. **大小对照**：trim 后 plan ~790 行 / ~31 KB，在 codex branch-diff 256 KB 预算内；round 2 tooling gap 不再出现

7. **Codex review 历程记录**（透明起见）：
   - Round 1/2：tooling gap（MAX_UNTRACKED_BYTES cap）→ 物理拆 + commit
   - Round 3：SQLite tests 未排除 sqlite_sequence → 已修
   - Round 4：rollback 政策自相矛盾 → 去掉矛盾节
   - Round 5：PG migration 原子性未定义 → 触发 Option B scope 缩小，Task 4 移至 Plan 1f
   - Round 6：approve ✅（ledger ec57a9e）
   - 2026-04-21 Task 1 实施发现本机无 Docker → 改用 pglast 纯语法+AST 测试（Option E）
   - Round 7：pglast parser-only 覆盖不足 → 补 Task 5 CI ephemeral postgres runtime 验证（两层并存）
   - Round 8：training_sets 缺 CHECK 约束（content_hash/status/lease 状态机）+ 验收项 2 grep `\|` escape 错 → 已修（加 3 个 CK + 正反 5 个 invalid-insert 测试 + grep 修成全角 `｜` 或未转义 `|`）

8. **依赖链**：Plan 1 → Plan 1b / 1c / 1d / 1e / 1f（各自独立评审）；Plan 1 本身无前置依赖

---

## 执行切换提示

Plan 1 推荐执行路径（本 plan 已拆小到适合 subagent-driven-development）：

1. 分支 `plan-1/m0.1-db-schema` 已存在（本 plan 起草过程中创建）。若开 worktree 请 `superpowers:using-git-worktrees` 基于此分支。
2. `superpowers:subagent-driven-development` 派 subagent 并行执行：
   - 第 1 波（4 subagent 并行）：Task 0 / Task 1 / Task 2 / Task 3
   - 第 2 波（单 agent）：Task 4（聚合验收依赖前面全部 test 脚本产出）
3. 每个 subagent 内部用 `superpowers:test-driven-development`
4. 全部通过后：`superpowers:verification-before-completion` → push + PR → 代码侧 codex review 工作流触发
