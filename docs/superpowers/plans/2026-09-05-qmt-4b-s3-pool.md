# QMT 4b 切片 S3：`export_log` 预筛 + 分层 seeded 储备池 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 造出「从 `export_log.csv` 的解析结果，到一份冻结的分层候选名单与本批要尝试的槽位清单」这条**纯函数**管线，让 S4（拷贝引擎）拿到确定性的工作单。

**Architecture:** 新增单一模块 `backend/qmt_pool.py`，**不碰文件系统、不碰数据库、不做网络 IO**（与 S2a 的读侧校验同规格）。它上接 `qmt_ingest.parse_export_log` 的产物（`dict[(code, period)] -> ExportLogEntry`），下出三样东西：预筛统计、冻结宇宙 `{SH: [...], SZ: [...], BJ: [...]}`、以及本批槽位清单。落盘与提交是 S2b 的 `qmt_manifest` 的事，本片一个字节都不写。

**Tech Stack:** Python 3、`random.Random`、`datetime` + `zoneinfo`、pytest。**不引入任何新依赖**。

**Spec:** `docs/superpowers/specs/2026-07-27-qmt-plan4b-fetch-design.md` §4.2 / §4.3 / §4.4
（切片边界见 `docs/superpowers/specs/2026-08-21-qmt-plan4b-slice-map.md` 第二节「五个切片」）

---

## Global Constraints

以下数值**逐字来自 spec**，每个 task 的要求都隐含包含本节。

- **市场三层**：`MARKETS = ("SH", "SZ", "BJ")`（`qmt_manifest.MARKETS`，键必须恰为这三个，不多不少）。
- **预筛阈值**：`k_daily ≥ 39`（= `31 + 8`，来自 `generate_training_sets.py:128` 的 `if n < 31 + months`）；`k_1m ≥ 8`。**边界必须精确**：`k_daily == 39` 放行 / `== 38` 剔；`k_1m == 8` 放行 / `== 7` 剔（spec §9 验收 3）。
- **默认配额**：`SH=120 / SZ=160 / BJ=120`。
- **重试上限**：补拉时只重试 `attempts < 2` 的失败条目（spec §4.4「有界重试」）。
- **纯函数**：本片新增代码**不得**出现 `open(` / `os.` 文件操作 / `asyncpg` / `requests`。
- **不 bump `manifest_version`**：本片产出的 `quota` / 预筛统计 / `failures` 都是**非必需顶层键**，靠 `validate_manifest` 的「原样返回、绝不做白名单投影」（O4-F10）与 `_carry_forward_persisted_keys`（S2-F40）流转。`qmt_manifest.MANIFEST_VERSION` **保持 1 不动**。
- **时区**：任何从 Unix 秒推导日期/月份的地方，**必须**经 `qmt_normalize.trading_date()`。该函数 docstring 写死「所有日期分组/比对的唯一入口，禁 UTC/naive」。
- **解释器**：这台机器的 `PATH` 上**没有** `python` 这个命令（实测 `command not found`）。
  一律用仓库自带的虚拟环境：从 `backend/` 出发是 `../.venv/bin/python`（Python 3.11.15，
  pandas / numpy / pytest 都在里面）；从仓库根出发是 `./.venv/bin/python`。
  **`python3` 也不行**——homebrew 那个 3.14 没装依赖。
- **测试命令**（CI 逐字相同，见 `.github/workflows/backend-tests.yml:48`）：
  ```bash
  cd backend && ../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/pytest-report.xml
  ```
  **CI 对 `skipped > 0` 直接判失败**——本片不得引入任何 `skip` / `xfail` / `importorskip`。
- **中文**：新代码的 docstring 与注释用中文，与 `qmt_manifest.py` / `qmt_ingest.py` 同风格；注释要写**为什么**，不写**是什么**。
- **异常族按「坏数据从哪来」分，不按顺手分**（开工前扫描 · 裁定 1）：
  | 数据来源 | 异常 | 本片哪些地方 |
  |---|---|---|
  | `export_log.csv` **内容**不合规 | `QmtSchemaError`（`qmt_normalize`） | `month_span` 的区间反向、`freeze_universe` 的坏码/重复码 |
  | **调用方没按次序调用** | `ValueError` | `month_span` 收到 `None` 时间戳、`fresh_slots` 的坏 `cursor`/`quota` |
  | **账本**内容不合规 | `ManifestInvalidError`（`qmt_manifest`） | `retry_slots` 的坏 `failures` 条目 |
  依据 = spec §5 明写「`export_log.csv` 缺失/零字节/截断 → **沿用 `parse_export_log` 的
  `QmtSchemaError`**，干净拒绝非裸 traceback」，而 `parse_export_log` 对**内容**不合规
  （认不出的 period / 重复键 / 缺列）同样抛 `QmtSchemaError`。旁证：`qmt_manifest.py:38`
  本来就在导入 `QmtSchemaError`。
  ⚠️ **测试必须断言精确的族**：`QmtSchemaError` 是 `ValueError` 的**子类**，
  写 `pytest.raises(ValueError)` 的话两种实现都会绿 —— 判别力为零。
- **`typing` 还是 `collections.abc`**：跟随 `qmt_manifest.py:28` 的既有写法，
  用 `from typing import Iterable, Mapping`（**一行**）。

### 上游契约（已在 main 上，逐字核实过，不得凭记忆改写）

```python
# backend/qmt_ingest.py:32
@dataclass(frozen=True)
class ExportLogEntry:
    code: str          # 已过 _norm_code 规范化
    period: str        # "1m" | "daily"（_LABEL_TO_PERIOD 已把真实导出的 "1d" 映射为 "daily"）
    status: str        # 真实取值："ok" | "empty"
    rows: int
    first_time: int | None   # ⚠️ Unix 秒，不是打包整数；status != "ok" 时为 None
    last_time: int | None    # ⚠️ 同上
    source: str

# backend/qmt_ingest.py:54
def parse_export_log(path) -> dict[tuple[str, str], ExportLogEntry]: ...
```

> ⚠️ **`first_time` / `last_time` 是 Unix 秒**：`qmt_ingest.py:87-88` 已经把打包整数经
> `parse_qmt_datetime` 转成了 Unix 秒。本片若照 spec 字面把它们当 `YYYYMMDD` 解析，
> 得到的是天文数字级的假月份，而**所有股都会通过预筛**——一个安静的、全绿的错误。

### 下游契约（S2a/S2b 已立的读侧判据，本片产出必须能过）

```python
# backend/qmt_manifest.py:264  _validate_source_snapshot
#   source_snapshot.universe 必须是三层 dict，键恰为 SH/SZ/BJ；
#   每个元素匹配 ^\d+\.(SH|SZ|BJ)\Z 且**后缀与所在层一致**。
# backend/qmt_manifest.py:348  _validate_cursor
#   cursor 三层 int；0 <= cursor[mk] <= len(universe[mk])（**闭区间**，取到 n 表示该层已取遍）。
```

---

## 开工前必读：本片与 S4 的边界

**本片做**：预筛判据、分层、seeded 打乱、冻结名单、配额解析、本批槽位派生（含失败重试的挑选）。

**本片不做**（写在这里防止顺手做）：

| 不做的事 | 归属 |
|---|---|
| 真的去拷文件、`.part` → `replace`、`--max-bytes` 流式扣减 | S4 |
| `cursor` / `pool_order` / `failures` 的**落盘**与提交 | S2b 已做（`commit_stock` / `commit_final`） |
| 崩溃恢复三档、`.inflight.json` | S4 |
| 七条源边界闸、`qmt_fetch` 命令行、`--quota` 的 argparse 接线 | S5 |
| 读 `export_log.csv` 这个动作本身（打开文件） | S5（本片只吃已解析好的 dict） |

**S3 交付后，`backend/qmt_fetch.py` 依然不存在。** 交付口径见文末。

---

## File Structure

| 文件 | 责任 |
|---|---|
| `backend/qmt_pool.py`（**新建**） | 本片全部逻辑。纯函数，约 260 行。 |
| `backend/tests/test_qmt_pool.py`（**新建**） | 本片全部测试。 |
| `backend/qmt_manifest.py` （**改**，仅 Task 4） | 把 `_STOCK_CODE_RE` 提为公开 `STOCK_CODE_RE`，供本片复用同一条判据。**不复制一份近似实现**。 |
| `docs/superpowers/specs/2026-07-27-qmt-plan4b-fetch-design.md`（**改**，仅 Task 1） | 按真实导出实测更正三处，登记为 S3-F1/F2/F3。 |

### PR 打包（每个 PR ≤ 3 个子项）

| PR | 含 Task | 交付物 |
|---|---|---|
| **S3-1** | Task 1、2、3 | spec 对齐 + 月跨度 + 三条预筛判据与统计 |
| **S3-2** | Task 4、5 | 分层冻结宇宙 + 配额解析 |
| **S3-3** | Task 6、7 | 本批槽位派生（新槽位 + 失败重试） |

每个 PR 独立可评审、独立全绿。**次序强制**：S3-2 依赖 S3-1 的 `prefilter`，S3-3 依赖 S3-2 的 `freeze_universe`。

---

### Task 1: 按真实导出实测对齐 spec（S3-F1 / F2 / F3）

切片地图 `2026-08-21-qmt-plan4b-slice-map.md` 第三节写死：「真跑结论回来后若与 spec 冲突，**先改 spec 再进 S3/S5 的 plan**」。三条冲突里的第①条（源根定义）已由 S2-F1 修好；剩下两条外加一条语义歧义在本 task 收口。**先改 spec，再写一行代码。**

**Files:**
- Modify: `docs/superpowers/specs/2026-07-27-qmt-plan4b-fetch-design.md`

**Interfaces:**
- Consumes: 无（本 task 不产代码）
- Produces: 后续 task 的判据来源。Task 3 的三条预筛判据、Task 5 的配额语义、Task 6 的区间算法都引本 task 定下的文字。

- [ ] **Step 1: 更正 §4.4 的 BJ 配额理由（S3-F1）**

找到这一行（`§4.4`，原第 274 行）：

```
  - BJ 给 120 而非按地板（≥8）等比缩：北交所 2021-11 开市，2023-04 之后上市的股票均不满 39 个月，预筛 (b) 会大批剔除，通过预筛的 BJ 股预计仅 100~150 只 → 配额 120 实质接近「全拉通过预筛的 BJ」。
```

整行替换为：

```
  - BJ 给 120 而非按地板（≥8）等比缩：北交所 2021-11 开市，2023-04 之后上市的股票均不满 39 个月，预筛 (b) 对 BJ 的剔除率结构性地高于另外两层（**2026-08-23 挂载实测**：BJ **197/324** 通过，61%；而 SH 2202/2350、SZ 2740/2934 均在 93% 以上）。**⚠️ 配额 120 并不「接近全拉」（S3-F1 更正）**——它占 BJ 可用池的 61%，第二批尚余 77 只可续，第三批该层必然穷尽。仍给 120 而不按地板等比缩到约 32，理由**不是**「反正快拉光了」，而是 BJ 的**成功率**才是稀缺资源：`2026-07-27-qmt-plan4c-pilot-shipment-design.md` 的样例报告即设想过「BJ 层池穷尽（消费 120/120），地板 8 未达（实得 5）」这一档——等比缩会让 BJ 地板从「紧张」直接变成「结构上不可能」。
```

- [ ] **Step 2: 给 §4.3 补上实测筛出率（S3-F2）**

在 §4.3 判据表下方那句「三条都取**保守下界**……」**之后**、「**预筛统计写进 `fetch_manifest.json`**」**之前**，插入：

```
**真实导出上的实测筛出率（2026-08-23 挂载实测，5608 只股）**：

| 判据 | 剔除 |
|---|---|
| (a) `status != "ok"` | **1** 只（`301583.SZ`，两个周期都是 `empty`，且两个 K 线目录里都没有它的 CSV）|
| (b) `k_daily < 39` | **468** 只 |
| (c) `k_1m < 8` | **0** 只（命中集合被 (b) 完全覆盖）|
| **通过预筛** | **5139 / 5608（91.6%）** |

⚠️ **不要把预筛读成一道窄门（S3-F2 更正）**：在**当前**这份导出上它只筛掉 8.4%，
判据 (c) 一只都没剔到。保留 (c) 的理由是它零成本，**且下一次导出未必长这样**——
QMT 只导出约**一年**的 1m 数据（实测覆盖月数：min 1 / 中位 13 / max 14），
导出窗口一旦缩到 8 个月以内，(c) 立刻从「恒不命中」变成主力判据。
**它不是死代码，是一条便宜的下界。** 本节「把 N 只随机股换成 N 只高质量候选」
说的是**方向**，不是量级；实现者不得据此假定预筛会大幅缩小宇宙。
```

- [ ] **Step 3: 钉死配额的语义（S3-F3）**

在 §4.4 的「**补拉靠「按层自动续」，不设 `--skip-first`（R1-F1 修正）**」那一段**之后**插入：

```
**配额是「累计目标」，不是「每批数量」（S3-F3）**：`quota[market]` 表示**这个 staging
在该层总共要尝试到 `universe[market]` 的第几个**，而不是「本次运行再拉多少只」。
故本批新槽位的区间是 `[cursor[market], min(quota[market], len(universe[market])))`；
`cursor[market] >= quota[market]` 时该层本批**不取新股**（失败台账的重试照跑）。

> **为什么必须钉死**：原文只说「配额是**首批下注**」与「提高配额拉下一批」，
> 没说清 `quota` 与 `cursor` 怎么合成。按「每批数量」实现的话，用默认配额重跑一次
> 就会**再拉 120 只 SH**，而操作者以为自己只是重跑。这与 R1-F1（全局 `--skip-first`）
> 是同一个家族：**把「走到了哪里」与「这次做了多少」混为一谈**——
> R3-F2 已经在 `cursor` vs `len(pool_order)` 上栽过同一跤第二次。
```

- [ ] **Step 4: 在 §11 末尾追加 S3 实施轮小节**

在文件末尾（`## S2 实施轮` 那一节之后）追加。⚠️ **标题必须是二级 `##`**，
与 `## S2 实施轮` 并列 —— 写成 `###` 会让整节被大纲吞进 S2 里面，
变成「上一轮的子节」而不是「下一轮的记录」：

```
## S3 实施轮（2026-09-05 起，4b 切片 S3 实施前的实测对齐）

本节登记「真实导出实测与 spec 原文不符」而产生的更正，编号续 S2-F 之后。
判据同 S2 实施轮：**每条都写明怎么发现的、实测数字是什么、改了什么、为什么不改另一种**。

| # | 严重度 | 问题 | 核实方式 | 处置 |
|---|---|---|---|---|
| S3-F1 | medium | **§4.4 给 BJ 配额的理由基于一个被实测推翻的预测** —— 原文写「通过预筛的 BJ 股预计仅 100~150 只 → 配额 120 实质接近『全拉通过预筛的 BJ』」，实测 **197** 只，120 只占 61%。理由死了之后，「为什么 BJ 用 15 倍于地板的配额、而 SH/SZ 只有 4 倍」**没有任何论证托底**，下一个读者极易把它当笔误「顺手改成 32」 | 2026-08-23 挂载实测（`project_qmt_real_source_measurements`）；配额数字与地板数字逐条对 §4.4 与 4c spec 原文 | 换掉理由、保留数字 120：真正的理由是 BJ 的**成功率**稀缺（4c 样例报告设想过「消费 120/120 仅得 5 只」），等比缩到 32 会让地板结构上不可能。**不动数字**——真要调它得等 4c 跑出真实成功率，现在调是拍脑袋 |
| S3-F2 | low | **§4.3 会让读者高估预筛的收窄幅度** —— 三条判据都标「✅ 必拒」，正文写「把 N 只随机股换成 N 只高质量候选」，而实测通过率 **91.6%**、判据 (c) 剔除 **0** 只。实现者可能据此假定宇宙会被大幅缩小（例如省掉分页、或把 (c) 当死代码删掉）| 同上 | 补实测表 + 明写「(c) 恒不命中是**当前这份导出**的性质，不是判据的性质」（1m 中位覆盖 13 个月，窗口一缩它就成主力）|
| S3-F3 | **medium** | **`quota` 与 `cursor` 怎么合成，spec 从未写明** —— 只有「配额是首批下注」「提高配额拉下一批」两句散文。按「每批数量」实现则默认配额重跑一次会**再拉 120 只**；按「累计目标」实现则重跑是空操作。两种实现都能自圆其说，而**错的那种不会报任何错**，只会安静地多拉一批并把 `cursor` 推到 240 | 通读 §4.4 全节 + §9 验收 4，确认无一处定义二者的合成方式 | 钉死为**累计目标**：新槽位区间 `[cursor, min(quota, len(universe)))`。理由=「提高配额拉下一批」这句话只在累计语义下成立。**S3 Task 6 为两个方向各配一档测试** |
```

- [ ] **Step 5: 用旧措辞全文 grep，确认零残留**

```bash
cd "/Users/maziming/Coding/Prj_Kline trainer"
grep -rn "100~150\|100-150\|接近「全拉\|实质接近" docs/superpowers/specs/ docs/superpowers/plans/
```

Expected: **零命中**。（本仓栽过的形态：改动落在「解释处」、没落在执行者真正会读的那一处，同一份文档里规则与它自己的明文否定相隔十几行。）

- [ ] **Step 6: 确认没动任何判据数字**

```bash
cd "/Users/maziming/Coding/Prj_Kline trainer"
grep -n "SH=120\|SZ=160\|BJ=120\|31 + 8\|k_daily\|k_1m" docs/superpowers/specs/2026-07-27-qmt-plan4b-fetch-design.md | head
```

Expected: 配额仍是 `SH=120 / SZ=160 / BJ=120`，阈值仍是 39 / 8。**本 task 只改理由与语义，不改任何数字**——改数字要另走 brainstorming。

- [ ] **Step 7: Commit**

```bash
git add docs/superpowers/specs/2026-07-27-qmt-plan4b-fetch-design.md
git commit -m "docs(4b-S3): 按真实导出实测对齐 spec（S3-F1 BJ 配额理由 / S3-F2 实测筛出率 / S3-F3 配额累计语义）"
```

---

### Task 2: `month_span` —— 跨越几个自然月

三条预筛判据全是**边界判据**，而它们共用同一个月数计算。这个计算错一点，
整份预筛就在边界附近系统性偏移——**而且不会抛任何异常**。故单独成 task。

**Files:**
- Create: `backend/qmt_pool.py`
- Test: `backend/tests/test_qmt_pool.py`

**Interfaces:**
- Consumes: `qmt_normalize.trading_date(epoch) -> datetime.date`（已在 main 上）
- Produces: `month_span(first_epoch: int, last_epoch: int) -> int`

- [ ] **Step 1: 写失败测试**

新建 `backend/tests/test_qmt_pool.py`：

```python
# backend/tests/test_qmt_pool.py
"""QMT 4b 切片 S3：预筛 + 分层储备池（纯函数）的测试。"""
from __future__ import annotations

import datetime as dt
from zoneinfo import ZoneInfo

import pytest

from qmt_normalize import QmtSchemaError
from qmt_pool import month_span

_SH = ZoneInfo("Asia/Shanghai")


def _sh(y: int, m: int, d: int, hh: int = 0, mi: int = 0) -> int:
    """造「上海时间的这一刻」的 Unix 秒——与 qmt_normalize.parse_qmt_datetime 同口径。"""
    return int(dt.datetime(y, m, d, hh, mi, tzinfo=_SH).timestamp())


def test_a_range_inside_one_calendar_month_spans_one():
    assert month_span(_sh(2025, 7, 4), _sh(2025, 7, 31)) == 1


def test_two_adjacent_days_across_a_month_boundary_span_two():
    assert month_span(_sh(2025, 7, 31), _sh(2025, 8, 1)) == 2


def test_span_crosses_a_year_boundary():
    assert month_span(_sh(2024, 12, 31), _sh(2025, 1, 1)) == 2


def test_span_counts_calendar_months_not_elapsed_time():
    # 2023-01 .. 2025-03 = 2023 全年 12 + 2024 全年 12 + 2025 的 1~3 月 3 = 27
    assert month_span(_sh(2023, 1, 31), _sh(2025, 3, 1)) == 27


def test_month_span_is_measured_in_shanghai_not_utc():
    """日线时间戳正好落在**当地 00:00**，按 UTC 算会退回上一个月。

    QMT 日线是 8 位打包整数（如 20250801），`parse_qmt_datetime` 用 `%Y%m%d`
    解析后按 Asia/Shanghai 本地化 —— 于是**每一个**日线 first_time 都恰好是
    当地 00:00 = 前一天 16:00 UTC。某只股的日线若从某月 1 日开始，
    UTC 实现会把它算进上个月、跨度凭空多一，**而且不抛任何异常**。
    """
    assert month_span(_sh(2025, 8, 1), _sh(2025, 8, 20)) == 1


def test_month_span_refuses_an_empty_timestamp():
    """`status != "ok"` 的行时间戳是 None（qmt_ingest 刻意保留条目）。

    调用方必须先过 status 门再算月数；够不到这条纪律时要**响亮地**失败，
    而不是抛一个来路不明的 TypeError。

    ⚠️ 它必须**不是** `QmtSchemaError`：这是**代码**的次序问题，不是数据不合规。
    两族混同的话，S5 的命令行会把一个内部 bug 报成「你的 export_log 有问题」，
    把操作者指向一个根本不存在的问题。
    """
    with pytest.raises(ValueError, match="空时间戳") as exc:
        month_span(None, _sh(2025, 1, 1))
    assert not isinstance(exc.value, QmtSchemaError)


def test_month_span_refuses_a_reversed_interval():
    """首尾写反是 **export_log 的内容**坏了，不是调用方的错 —— 故属 QmtSchemaError。

    ⚠️ 断言必须钉**精确的族**：`QmtSchemaError` 是 `ValueError` 的子类，
    写成 `pytest.raises(ValueError)` 的话，两种实现都绿，判别力为零。
    """
    with pytest.raises(QmtSchemaError, match="区间是反的"):
        month_span(_sh(2025, 8, 1), _sh(2025, 7, 1))

```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q
```

Expected: FAIL —— `ModuleNotFoundError: No module named 'qmt_pool'`。

- [ ] **Step 3: 写最小实现**

新建 `backend/qmt_pool.py`：

```python
# backend/qmt_pool.py
"""QMT 4b 切片 S3：`export_log` 预筛 + 分层 seeded 储备池（纯函数）。

Spec: `2026-07-27-qmt-plan4b-fetch-design.md` §4.2 / §4.3 / §4.4。

本模块**不碰文件系统、不碰数据库、不做网络 IO**（与 S2a 的读侧校验同规格）：
上接 `qmt_ingest.parse_export_log` 的产物，下出「冻结的分层候选名单」与
「本批要尝试的槽位」，交给 S4 的拷贝引擎去执行。落盘是 S2b `qmt_manifest` 的事。
"""
from __future__ import annotations

from qmt_normalize import QmtSchemaError, trading_date

# §4.3 的两条月数下界。
# 39 = 31 + 8：`generate_training_sets.py:128` 的 `if n < 31 + months`。
# 8：训练窗口需要的完整月数。
MIN_DAILY_MONTHS = 39
MIN_1M_MONTHS = 8


def month_span(first_epoch: int, last_epoch: int) -> int:
    """`[first, last]` 跨越的**不同自然月数**（含两端），按 Asia/Shanghai。

    ⚠️ **必须经 `trading_date()`**：入参是 **Unix 秒**（`qmt_ingest` 已把 QMT 的
    打包整数转过一道，见 `qmt_ingest.py:87`），而日线时间戳恰好落在当地 00:00
    = 前一天 16:00 UTC。按 UTC 算，任何从某月 1 日开始的日线都会被算进上个月，
    跨度凭空多一 —— **不抛异常、只是安静地把边界附近的股多留或多剔**，
    而三条预筛判据全是边界判据。`trading_date` 的 docstring 也写死了
    「所有日期分组/比对的唯一入口，禁 UTC/naive」。
    """
    if first_epoch is None or last_epoch is None:
        # 调用方没按次序调用（status 门必须先跑）—— 这是**代码**的问题，不是数据的问题。
        raise ValueError(
            "month_span 收到空时间戳 —— `status != \"ok\"` 的 export_log 行没有"
            "有效时间戳（qmt_ingest 刻意保留条目并置 None）。调用方必须**先过**"
            "status 门再算月数。"
        )
    if first_epoch > last_epoch:
        # 首尾时间戳被写反了，这是 **export_log 的内容**不合规 —— 与 parse_export_log
        # 对认不出的 period / 重复键的处置同族（spec §5），S5 的 CLI 据此映射 rc=2。
        raise QmtSchemaError(
            f"export_log 的时间戳区间是反的：first={first_epoch} > last={last_epoch}")
    a = trading_date(first_epoch)
    b = trading_date(last_epoch)
    return (b.year - a.year) * 12 + (b.month - a.month) + 1
```

- [ ] **Step 4: 跑测试确认它绿**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q
```

Expected: 7 passed。

- [ ] **Step 5: 变异验证 —— 证明时区那一档真的在测时区**

按本仓纪律，「有测试变红」是假信号，必须问「红的是**哪一条**」。且 Python
变异**必须先清字节码缓存**（失效判据是 `(mtime秒, size)`，等长改动 + 同秒复原会
让陈旧 `.pyc` 继续生效）。

```bash
cd backend
find . -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null
cp qmt_pool.py /tmp/qmt_pool.pristine.py
# 变异：把上海时区换成 UTC
../.venv/bin/python - <<'PY'
import re, pathlib
p = pathlib.Path("qmt_pool.py")
s = p.read_text(encoding="utf-8")
s = s.replace("    a = trading_date(first_epoch)\n    b = trading_date(last_epoch)",
              "    import datetime as _d\n"
              "    a = _d.datetime.utcfromtimestamp(first_epoch).date()\n"
              "    b = _d.datetime.utcfromtimestamp(last_epoch).date()")
p.write_text(s, encoding="utf-8")
PY
find . -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null
PYTHONDONTWRITEBYTECODE=1 ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q -p no:cacheprovider
cp /tmp/qmt_pool.pristine.py qmt_pool.py
find . -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null
diff -q qmt_pool.py /tmp/qmt_pool.pristine.py && echo "已复原到 pristine"
```

Expected（**已实测，不是预测**，2026-09-06）：变异后 **4 条变红 / 3 条存活**。

| 变红 | 为什么 |
|---|---|
| `test_month_span_is_measured_in_shanghai_not_utc` | 主档：`_sh(2025,8,1)` 在 UTC 下退回 7-31 |
| `test_two_adjacent_days_across_a_month_boundary_span_two` | 两端在 UTC 下**都**落进 7 月 → 得 1 而非 2 |
| `test_span_crosses_a_year_boundary` | 同理，两端都落进 2024-12 → 得 1 而非 2 |
| `test_span_counts_calendar_months_not_elapsed_time` | 末端 `_sh(2025,3,1)` 退回 2 月 → 得 26 而非 27 |

| 存活 | 为什么 |
|---|---|
| `test_a_range_inside_one_calendar_month_spans_one` | 两端都在 7 月中旬，8 小时偏移跨不出月界 |
| 两条异常档 | 在日期算术**之前**就抛错，根本走不到被变异的那两行 |

> ⚠️ **血溅面大是对的，不是"变异打歪了"**：日线时间戳**全部**落在当地 00:00，
> 所以凡是用了某月 1 日或月末那一天的档，在 UTC 下都会移位。这恰恰说明
> 时区这一步的影响面比直觉大。**别把"红了不止一条"当成变异写错的信号** ——
> 判据是「**主档有没有红**」，以及「存活的那几条能不能逐条讲清为什么不该动」。

复原后 `diff -q` 必须报一致。

> ⚠️ **变异跑完必须 `diff -q` 回 pristine**：本仓栽过一次「前台变异被工具超时杀掉、
> `finally` 没执行、`_require(True,` 残留在生产代码里而 1362 个测试照样全绿」。

- [ ] **Step 6: 跑全量，确认没碰坏别处**

```bash
cd backend && ../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/s3-t2.xml
../.venv/bin/python - <<'PY'
import xml.etree.ElementTree as ET
r = ET.parse("/tmp/s3-t2.xml").getroot()
print({k: sum(int(s.get(k, 0)) for s in r.iter("testsuite"))
       for k in ("tests", "skipped", "failures", "errors")})
PY
```

Expected: `failures=0 errors=0 skipped=0`，`tests` 比 main 多 7。

- [ ] **Step 7: Commit**

```bash
git add backend/qmt_pool.py backend/tests/test_qmt_pool.py
git commit -m "feat(4b-S3): month_span —— 按 Asia/Shanghai 算自然月跨度（预筛三条判据的共用地基）"
```

---

### Task 3: `prefilter` —— §4.3 三条判据 + 统计

**Files:**
- Modify: `backend/qmt_pool.py`
- Test: `backend/tests/test_qmt_pool.py`

**Interfaces:**
- Consumes: `month_span`（Task 2）、`qmt_ingest.ExportLogEntry`
- Produces:
  - `PrefilterStats`（frozen dataclass，字段 `total` / `rejected_incomplete` / `rejected_status` / `rejected_daily_span` / `rejected_1m_span` / `passed`，方法 `as_manifest_field() -> dict`）
  - `prefilter(entries: dict[tuple[str, str], ExportLogEntry]) -> tuple[list[str], PrefilterStats]`
    —— 返回的 code 列表**按字典序升序**。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/test_qmt_pool.py`（先把 import 行改成
`from qmt_pool import PrefilterStats, month_span, prefilter`）：

```python
from qmt_ingest import ExportLogEntry


def _entry(code: str, period: str, *, status: str = "ok",
           first: int | None = None, last: int | None = None, rows: int = 100):
    return ExportLogEntry(code=code, period=period, status=status, rows=rows,
                          first_time=first, last_time=last, source=code)


def _start_of_span(months: int) -> int:
    """造一个起点，使 [起点, 2025-12-31] 恰好跨 `months` 个自然月。"""
    total = 2025 * 12 + 12 - (months - 1)
    y, m = divmod(total - 1, 12)
    return _sh(y, m + 1, 1)


_END = _sh(2025, 12, 31)


def _stock(code: str, *, daily_months: int = 40, m1_months: int = 13,
           status: str = "ok") -> dict:
    """一只股的两条 export_log 记录。

    ⚠️ 照**真实导出**的形态造：`status != "ok"` 的行 rows=0、时间戳为 None
    （真实导出里 301583.SZ 正是这样，qmt_ingest 刻意保留条目）。
    照想象造样本让两个生产缺陷藏了三年且测试全绿。
    """
    if status != "ok":
        return {(code, "1m"): _entry(code, "1m", status=status, rows=0),
                (code, "daily"): _entry(code, "daily", status=status, rows=0)}
    return {
        (code, "1m"): _entry(code, "1m", first=_start_of_span(m1_months), last=_END),
        (code, "daily"): _entry(code, "daily", first=_start_of_span(daily_months), last=_END),
    }


def test_a_stock_passing_all_three_predicates_is_kept():
    codes, stats = prefilter(_stock("600000.SH"))
    assert codes == ["600000.SH"]
    assert stats.total == 1 and stats.passed == 1


def test_daily_span_boundary_39_passes_and_38_is_rejected():
    """spec §9 验收 3：`k_daily == 39` 放行 / `== 38` 剔。"""
    kept, _ = prefilter(_stock("600000.SH", daily_months=39))
    assert kept == ["600000.SH"]
    dropped, stats = prefilter(_stock("600000.SH", daily_months=38))
    assert dropped == [] and stats.rejected_daily_span == 1


def test_1m_span_boundary_8_passes_and_7_is_rejected():
    """spec §9 验收 3：`k_1m == 8` 放行 / `== 7` 剔。"""
    kept, _ = prefilter(_stock("600000.SH", m1_months=8))
    assert kept == ["600000.SH"]
    dropped, stats = prefilter(_stock("600000.SH", m1_months=7))
    assert dropped == [] and stats.rejected_1m_span == 1


def test_a_non_ok_status_is_rejected_without_touching_its_empty_timestamps():
    """真实导出里 301583.SZ 两个周期都是 `empty`、时间戳为空。

    先算月跨度、再看 status 的实现会在这里炸 —— 那正是 `qmt_ingest`
    在真实数据上栽过的顺序错误（整份 log 崩掉，5607 只好股一只都导不进来）。
    """
    entries = _stock("301583.SZ", status="empty")
    assert entries[("301583.SZ", "1m")].first_time is None      # 前提成立才算数
    codes, stats = prefilter(entries)
    assert codes == [] and stats.rejected_status == 1


def test_a_stock_missing_one_period_is_rejected():
    """两个周期缺一不可 —— 缺 daily 就没有 k_daily 可算。"""
    entries = {("430047.BJ", "1m"): _entry("430047.BJ", "1m",
                                           first=_start_of_span(13), last=_END)}
    codes, stats = prefilter(entries)
    assert codes == [] and stats.rejected_incomplete == 1


def test_the_four_rejection_counters_partition_the_universe():
    """`total == passed + 四条之和` —— 每只被剔的股**恰好**归因一次。

    这条挡的是「同一只股被记进两个计数器」与「某条剔除路径忘了计数」：
    两者都会让写进 manifest 的那份「关于数据源的第一份真实观测」自相矛盾。
    """
    entries: dict = {}
    entries.update(_stock("600000.SH"))                     # 通过
    entries.update(_stock("600004.SH", daily_months=10))    # 判据 (b)
    entries.update(_stock("000001.SZ", m1_months=3))        # 判据 (c)
    entries.update(_stock("301583.SZ", status="empty"))     # 判据 (a)
    entries[("430047.BJ", "1m")] = _entry("430047.BJ", "1m",
                                          first=_start_of_span(13), last=_END)  # 缺 daily
    codes, s = prefilter(entries)
    assert s.total == 5
    assert codes == ["600000.SH"]
    assert (s.passed + s.rejected_incomplete + s.rejected_status
            + s.rejected_daily_span + s.rejected_1m_span) == s.total


def test_output_order_does_not_depend_on_export_log_row_order():
    """dict 的迭代顺序 = `export_log.csv` 的**行序**。

    下游 `freeze_universe` 要对这个列表做 seeded 打乱，而打乱结果依赖入参顺序。
    若此处不排序，「同 seed 同一批股 → 同一个宇宙」这条本 spec 全部可复现性设计
    赖以成立的性质，就会**悄悄地**取决于 QMT 导出时的行序。
    """
    a: dict = {}
    for c in ("600000.SH", "000001.SZ", "430047.BJ"):
        a.update(_stock(c))
    b: dict = {}
    for c in ("430047.BJ", "600000.SH", "000001.SZ"):
        b.update(_stock(c))
    assert list(a) != list(b)                    # 两份输入的行序确实不同
    assert prefilter(a)[0] == prefilter(b)[0]
    assert prefilter(a)[0] == sorted(prefilter(a)[0])


def test_stats_travel_as_a_non_required_manifest_key():
    """预筛统计走「非必需顶层键」通道 —— **不 bump `manifest_version`**。

    靠的是 `validate_manifest` 的「原样返回、绝不做白名单投影」（O4-F10）
    与两个提交入口的 `_carry_forward_persisted_keys`（S2-F40）。
    """
    import qmt_manifest

    _, stats = prefilter(_stock("600000.SH"))
    field = stats.as_manifest_field()
    assert field == {"total": 1, "rejected_incomplete": 0, "rejected_status": 0,
                     "rejected_daily_span": 0, "rejected_1m_span": 0, "passed": 1}
    assert "prefilter_stats" not in qmt_manifest.REQUIRED_KEYS
    assert qmt_manifest.MANIFEST_VERSION == 1
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q
```

Expected: FAIL —— `ImportError: cannot import name 'prefilter' from 'qmt_pool'`。

- [ ] **Step 3: 写最小实现**

追加到 `backend/qmt_pool.py`（并在文件顶部补 `from dataclasses import dataclass`
与 `from qmt_ingest import ExportLogEntry`）：

```python
@dataclass(frozen=True)
class PrefilterStats:
    """§4.3 的预筛统计 ——「关于数据源的第一份真实观测」，随 manifest 落盘。

    四个 `rejected_*` 之间**互斥且穷尽**：每只被剔的股按 `prefilter` 的判定次序
    恰好归因到一条上，`total == passed + 四条之和` 是不变量。
    """
    total: int
    rejected_incomplete: int
    rejected_status: int
    rejected_daily_span: int
    rejected_1m_span: int
    passed: int

    def as_manifest_field(self) -> dict:
        """写进 manifest 的 `prefilter_stats`（**非必需**顶层键，不 bump 版本）。"""
        return {"total": self.total,
                "rejected_incomplete": self.rejected_incomplete,
                "rejected_status": self.rejected_status,
                "rejected_daily_span": self.rejected_daily_span,
                "rejected_1m_span": self.rejected_1m_span,
                "passed": self.passed}


def prefilter(entries: dict[tuple[str, str], ExportLogEntry]
              ) -> tuple[list[str], PrefilterStats]:
    """§4.3 的三条判据（外加一条「两个周期缺一不可」）。

    返回 `(codes, stats)`，`codes` **按字典序升序**。

    ⚠️ **判定次序是判据的一部分**：`status != "ok"` 的行没有有效时间戳
    （真实导出里 `301583.SZ` 的 `first_time`/`last_time` 都是空的，`qmt_ingest`
    保留条目并置 `None`），先算月跨度会当场炸。**先看 status，再碰时间戳** ——
    这正是 `qmt_ingest` 自己在真实数据上栽过的那一跤。

    ⚠️ **升序返回不是可有可无的整洁**：下游 `freeze_universe` 对它做 seeded 打乱，
    而打乱结果依赖入参**顺序**；dict 的迭代顺序就是 `export_log.csv` 的行序。
    不排序的话，「同 seed → 同一个宇宙」会悄悄依赖 QMT 导出时的行序。

    ⚠️ **三条判据都是保守下界，不替代真门**：真门照样在 pilot 里跑。实测在当前
    这份导出上只筛掉 8.4%（S3-F2），实现者不得据此假定宇宙会被大幅缩小。
    """
    by_code: dict[str, dict[str, ExportLogEntry]] = {}
    for (code, period), ent in entries.items():
        by_code.setdefault(code, {})[period] = ent

    passed: list[str] = []
    n_incomplete = n_status = n_daily = n_1m = 0

    for code in sorted(by_code):
        per = by_code[code]
        e_1m, e_daily = per.get("1m"), per.get("daily")
        if e_1m is None or e_daily is None:
            n_incomplete += 1
        elif e_1m.status != "ok" or e_daily.status != "ok":
            n_status += 1
        elif month_span(e_daily.first_time, e_daily.last_time) < MIN_DAILY_MONTHS:
            n_daily += 1
        elif month_span(e_1m.first_time, e_1m.last_time) < MIN_1M_MONTHS:
            n_1m += 1
        else:
            passed.append(code)

    return passed, PrefilterStats(
        total=len(by_code), rejected_incomplete=n_incomplete,
        rejected_status=n_status, rejected_daily_span=n_daily,
        rejected_1m_span=n_1m, passed=len(passed))
```

- [ ] **Step 4: 跑测试确认它绿**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q
```

Expected: 15 passed。

- [ ] **Step 5: 变异验证 —— 逐条证明四个判据各有专属档**

```bash
cd backend
cp qmt_pool.py /tmp/qmt_pool.pristine.py
run_mut () {   # $1 = 描述, $2 = 原文, $3 = 替换
  ../.venv/bin/python - "$2" "$3" <<'PY'
import pathlib, sys
p = pathlib.Path("qmt_pool.py"); s = p.read_text(encoding="utf-8")
assert sys.argv[1] in s, f"锚点失效：{sys.argv[1]!r}"      # 锚点找不到必须报错
p.write_text(s.replace(sys.argv[1], sys.argv[2], 1), encoding="utf-8")
PY
  find . -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null
  echo "--- 变异：$1"
  PYTHONDONTWRITEBYTECODE=1 ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q -p no:cacheprovider 2>&1 | tail -3
  cp /tmp/qmt_pool.pristine.py qmt_pool.py
  find . -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null
}
run_mut "日线阈值 39 → 38" "< MIN_DAILY_MONTHS" "< MIN_DAILY_MONTHS - 1"
run_mut "1m 阈值 8 → 7"    "< MIN_1M_MONTHS"    "< MIN_1M_MONTHS - 1"
run_mut "status 门取反"     'e_1m.status != "ok" or e_daily.status != "ok"' "False"
run_mut "缺周期不再剔"      "e_1m is None or e_daily is None" "False"
run_mut "取消升序"          "for code in sorted(by_code):" "for code in by_code:"
diff -q qmt_pool.py /tmp/qmt_pool.pristine.py && echo "已复原到 pristine"
```

Expected: **五组全部见红**，且红的分别是对应的边界档 / status 档 / 缺周期档 / 行序档。
「status 门取反」那组必须红在 `test_a_non_ok_status_is_rejected_...`（而不是红在
一个 TypeError 上——若红成 TypeError，说明**次序**没被真正钉住，回去看实现）。

> ⚠️ 变异验证自己也会假阴性：收集器只认 `FAILED` 不认 `ERROR` 时，「整套没跑起来」
> 会被报成「零红」。故上面用 `tail -3` 看 pytest 自己的汇总行，不自造收集器。

- [ ] **Step 6: 跑全量 + 提交**

```bash
cd backend && ../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/s3-t3.xml && \
../.venv/bin/python -c "import xml.etree.ElementTree as ET;r=ET.parse('/tmp/s3-t3.xml').getroot();print({k:sum(int(s.get(k,0)) for s in r.iter('testsuite')) for k in ('tests','skipped','failures','errors')})"
cd .. && git add backend/qmt_pool.py backend/tests/test_qmt_pool.py
git commit -m "feat(4b-S3): export_log 预筛三条判据 + 互斥穷尽的统计（§4.3）"
```

Expected: `skipped=0 failures=0 errors=0`。

**→ PR S3-1 到此可开。**

---

### Task 4: `freeze_universe` —— 分层 + 各层独立 seeded 打乱

**Files:**
- Modify: `backend/qmt_manifest.py`（把 `_STOCK_CODE_RE` 提为公开 `STOCK_CODE_RE`）
- Modify: `backend/qmt_pool.py`
- Test: `backend/tests/test_qmt_pool.py`

**Interfaces:**
- Consumes: `qmt_manifest.MARKETS`、`qmt_manifest.STOCK_CODE_RE`（本 task 提为公开）
- Produces: `freeze_universe(codes: Iterable[str], *, seed: str) -> dict[str, list[str]]`

> **为什么提公开而不在 `qmt_pool` 里复制一条正则**：`qmt_normalize._STOCK_CODE_RE`
> 用的是 `$`，`qmt_manifest._STOCK_CODE_RE` 用的是 `\Z` —— 前者**容忍一个尾随换行**
> （`"600000.SH\n"` 会被判合法）。本仓已经有一条登记在案的残留是「同一判据两份近似
> 实现」。写侧与读侧对「什么是合法股票代码」必须用**同一个对象**，否则下一次谁改了
> 一边，另一边悄悄漂走。
>
> 📌 **顺带发现、本片不修（如实登记）**：`qmt_normalize.is_valid_stock_code`
> 是**信任边界**入口（docstring 自称「把 DB 派生的 stock_code 用于路径拼接前先过
> 这一关」），而它用 `$` ⇒ `"600000.SH\n"` 通过。目前不构成穿越（尾随换行在
> POSIX 下是合法文件名字符），但属同族松口。**归 S5 或独立 PR**，本片不越界改它。

- [ ] **Step 1: 写失败测试**

追加到 `backend/tests/test_qmt_pool.py`：

```python
from qmt_manifest import MARKETS, validate_manifest
from qmt_pool import freeze_universe


def _bootstrap_manifest(universe: dict) -> dict:
    """「刚冻结完宇宙、一只股都还没拷」—— S4 会写出的第一份 manifest。

    形状逐字对着 `qmt_manifest` 的读侧枚举造：`partial` 级的存根必须是
    `passes: []`（O4-F2：首次 fetch 崩在半路时磁盘上就是这个样子）。
    """
    elog = "e" * 64
    return {
        "manifest_version": 1,
        "seed": "s-2026-09-05",
        "source_snapshot": {"export_log_sha256": elog, "universe": universe},
        "source_mount": {"fstype": "smbfs",
                         "device": "//agate@192.168.5.151/QMT_Export",
                         "source_root_relative": "front_ratio_cn_stocks_ab_bj"},
        "pool_order": {mk: [] for mk in MARKETS},
        "cursor": {mk: 0 for mk in MARKETS},
        "files": [],
        "staged_export_log": {"relative_path": "export_log.csv",
                              "bytes": 2399554, "sha256": elog},
        "source_verification": "partial",
        "source_verification_evidence": {"level": "partial", "passes": []},
    }


_SAMPLE = ["600000.SH", "600004.SH", "600006.SH", "600008.SH",
           "000001.SZ", "000002.SZ", "000004.SZ",
           "430047.BJ", "430090.BJ"]


def test_codes_are_split_into_three_layers_by_suffix():
    uni = freeze_universe(_SAMPLE, seed="s1")
    assert set(uni) == set(MARKETS)
    assert sorted(uni["SH"]) == ["600000.SH", "600004.SH", "600006.SH", "600008.SH"]
    assert sorted(uni["SZ"]) == ["000001.SZ", "000002.SZ", "000004.SZ"]
    assert sorted(uni["BJ"]) == ["430047.BJ", "430090.BJ"]


def test_the_same_seed_and_the_same_stocks_give_a_byte_identical_universe():
    assert freeze_universe(_SAMPLE, seed="s1") == freeze_universe(_SAMPLE, seed="s1")


def test_a_different_seed_gives_a_different_order():
    a, b = freeze_universe(_SAMPLE, seed="s1"), freeze_universe(_SAMPLE, seed="s2")
    assert a != b
    assert {mk: sorted(a[mk]) for mk in MARKETS} == {mk: sorted(b[mk]) for mk in MARKETS}


def test_the_universe_does_not_depend_on_the_input_order():
    """`random.shuffle` 的输出取决于入参顺序，而入参来自 dict 的迭代顺序。"""
    assert (freeze_universe(_SAMPLE, seed="s1")
            == freeze_universe(list(reversed(_SAMPLE)), seed="s1")
            == freeze_universe(set(_SAMPLE), seed="s1"))


def test_changing_one_layer_does_not_disturb_the_others():
    """spec §4.4：各层用各自的种子，「改动某层配额不会扰动其他层的顺序」。

    逐层增量补拉的正确性完全建立在「其他层顺序不变」上 —— 共用一个 rng 的话，
    SH 层多一只股就会把 SZ/BJ 的整个顺序推移，而 `cursor` 还指着老位置。
    """
    base = freeze_universe(_SAMPLE, seed="s1")
    more = freeze_universe(_SAMPLE + ["000005.SZ"], seed="s1")
    assert more["SH"] == base["SH"]
    assert more["BJ"] == base["BJ"]
    assert more["SZ"] != base["SZ"]          # 这一层本来就该变


def test_an_empty_layer_is_legal():
    uni = freeze_universe(["600000.SH"], seed="s1")
    assert uni["SZ"] == [] and uni["BJ"] == []


def test_a_malformed_code_is_refused():
    """冻结名单是补拉游标的**唯一锚点**，坏码必须当场拒。

    `qmt_ingest._norm_code` 对认不出的标识值是原样 `strip()` 返回的，
    一份被污染的 export_log 能把 `"foo"` 送到这里。放过去的话，
    整棵 staging 要到读侧校验才被判死 —— 离病因十万八千里。
    """
    for bad in ("foo", "600000.XX", "600000.SH\n", "", "../etc/passwd"):
        with pytest.raises(QmtSchemaError, match="不合法的股票代码"):
            freeze_universe(["600000.SH", bad], seed="s1")


def test_a_duplicated_code_is_refused():
    """名单里重复 ⇒ 两个 `universe_idx` 指向同一只股，锚点语义当场崩。

    ⚠️ 读侧 `_validate_source_snapshot` **不查层内唯一性**（只查每个元素
    是合法代码且后缀相符）。故这道门只能立在写侧。
    """
    with pytest.raises(QmtSchemaError, match="重复"):
        freeze_universe(["600000.SH", "600000.SH"], seed="s1")


def test_the_frozen_universe_passes_the_read_side_validator():
    """跨模块契约钉：S3 产出的宇宙必须被 S2a 的读侧校验接受。

    「写侧形状与读侧要求不配对」在本仓已经栽过三次（S2-F3 / R94-F2 / O4-F13），
    每次都是「本工具诚实产出的 manifest 被本工具自己判非法」。
    """
    m = _bootstrap_manifest(freeze_universe(_SAMPLE, seed="s1"))
    assert validate_manifest(m) is m          # 原样返回即通过
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q
```

Expected: FAIL —— `ImportError: cannot import name 'STOCK_CODE_RE' from 'qmt_manifest'`。

- [ ] **Step 3: 把 `_STOCK_CODE_RE` 提为公开**

```bash
cd backend
grep -c "_STOCK_CODE_RE" qmt_manifest.py                 # 先记下引用数
../.venv/bin/python - <<'PY'
import pathlib
p = pathlib.Path("qmt_manifest.py"); s = p.read_text(encoding="utf-8")
n = s.count("_STOCK_CODE_RE")
assert n > 0, "锚点失效：qmt_manifest.py 里找不到 _STOCK_CODE_RE"
p.write_text(s.replace("_STOCK_CODE_RE", "STOCK_CODE_RE"), encoding="utf-8")
print("已改", n, "处")
PY
grep -n "_STOCK_CODE_RE" qmt_manifest.py tests/test_qmt_manifest.py   # 期望：零命中
```

并把定义处的注释补一句（说明它为什么是公开的）：

```python
# `\Z` 不是 `$`：…（原注释保留）…
# **公开**（S3 Task 4）：写侧（qmt_pool 冻结宇宙）与读侧必须用**同一个对象**判定
# 「什么是合法股票代码」。qmt_normalize 里那份用的是 `$`、容忍一个尾随换行，
# 两份近似实现迟早漂开。
STOCK_CODE_RE = re.compile(r"^\d+\.(SH|SZ|BJ)\Z")
```

- [ ] **Step 4: 写 `freeze_universe`**

追加到 `backend/qmt_pool.py`（顶部补 `import random`、
`from typing import Iterable`、`from qmt_manifest import MARKETS, STOCK_CODE_RE`）：

```python
def freeze_universe(codes: Iterable[str], *, seed: str) -> dict[str, list[str]]:
    """§4.4：按 code 后缀分三层，各层内**独立** seeded 打乱，产出冻结名单。

    ⚠️ **各层用各自的种子** `f"{seed}:{market}"`：spec 明写「改动某层配额不会扰动
    其他层的顺序」。共用一个 rng 的话，SH 层多一只股就会把 SZ/BJ 的整个顺序推移，
    而逐层增量补拉的正确性完全建立在「其他层顺序不变」上 —— `cursor[SZ]` 还指着
    老位置，指向的却已是另一只股。

    ⚠️ **自己再排一次序**：`random.shuffle` 的输出取决于入参顺序，而入参往往来自
    dict 的迭代顺序（= `export_log.csv` 的行序）。不排序的话，「同 seed → 同一个
    宇宙」会悄悄依赖 QMT 导出时的行序。

    ⚠️ **坏码与重复当场拒**：这份名单是补拉游标的唯一锚点。放过去的话，整棵
    staging 要到读侧校验才被判死，而那时已经离病因十万八千里；重复更狠 ——
    读侧 `_validate_source_snapshot` **不查层内唯一性**，两个下标指向同一只股
    会一路活到 `pool_order` 的锚点校验处才爆。
    """
    layers: dict[str, list[str]] = {mk: [] for mk in MARKETS}
    seen: set[str] = set()
    for code in sorted(codes):
        if not isinstance(code, str) or STOCK_CODE_RE.match(code) is None:
            raise QmtSchemaError(
                f"freeze_universe 收到不合法的股票代码 {code!r}——"
                "`export_log` 的标识列可能被污染（`qmt_ingest._norm_code` 对认不出的"
                "值是原样返回的）。冻结名单是补拉游标的唯一锚点，坏码不许进。")
        if code in seen:
            raise QmtSchemaError(
                f"freeze_universe 收到重复的股票代码 {code!r}——"
                "同一只股占两个 universe_idx 会让锚点语义当场失效。")
        seen.add(code)
        layers[code.rsplit(".", 1)[1]].append(code)

    for mk in MARKETS:
        random.Random(f"{seed}:{mk}").shuffle(layers[mk])
    return layers
```

- [ ] **Step 5: 跑测试确认它绿**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py tests/test_qmt_manifest.py -q
```

Expected: 全绿（`test_qmt_manifest.py` 那一支证明改名没碰坏 S2a/S2b）。

- [ ] **Step 6: 变异验证**

```bash
cd backend
cp qmt_pool.py /tmp/qmt_pool.pristine.py
run_mut "各层共用一个 rng" \
  'random.Random(f"{seed}:{mk}").shuffle(layers[mk])' \
  'random.Random(seed).shuffle(layers[mk])'
run_mut "取消入参排序" "for code in sorted(codes):" "for code in codes:"
run_mut "不查重复"     "if code in seen:" "if False:"
run_mut "不查坏码"     "STOCK_CODE_RE.match(code) is None" "False"
diff -q qmt_pool.py /tmp/qmt_pool.pristine.py && echo "已复原到 pristine"
```

Expected: 四组各自见红，且「共用一个 rng」那组红的是
`test_changing_one_layer_does_not_disturb_the_others`。

> ⚠️ 「取消入参排序」那组要特别看：`test_the_universe_does_not_depend_on_the_input_order`
> 里传 `set(_SAMPLE)` 的那一支**必须**红。若只有 `reversed` 那支红，说明判别力不足
> —— set 的迭代顺序在同一次进程内是稳定的，光靠它证不了什么。

- [ ] **Step 7: 跑全量 + 提交**

```bash
cd backend && ../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/s3-t4.xml && \
../.venv/bin/python -c "import xml.etree.ElementTree as ET;r=ET.parse('/tmp/s3-t4.xml').getroot();print({k:sum(int(s.get(k,0)) for s in r.iter('testsuite')) for k in ('tests','skipped','failures','errors')})"
cd .. && git add backend/qmt_pool.py backend/qmt_manifest.py backend/tests/test_qmt_pool.py
git commit -m "feat(4b-S3): 分层 seeded 冻结宇宙（§4.4）+ 股票代码判据提为公开单一来源"
```

---

### Task 5: `resolve_quota` —— 默认配额与覆盖

**Files:**
- Modify: `backend/qmt_pool.py`
- Test: `backend/tests/test_qmt_pool.py`

**Interfaces:**
- Produces: `DEFAULT_QUOTA: dict[str, int]`、`resolve_quota(overrides: Mapping[str, int] | None = None) -> dict[str, int]`

- [ ] **Step 1: 写失败测试**

```python
from qmt_pool import DEFAULT_QUOTA, resolve_quota


def test_the_default_quota_matches_the_spec():
    assert resolve_quota() == {"SH": 120, "SZ": 160, "BJ": 120}


def test_overriding_one_layer_leaves_the_others_at_the_default():
    assert resolve_quota({"BJ": 40}) == {"SH": 120, "SZ": 160, "BJ": 40}


def test_zero_is_a_legal_quota():
    """把某层配额设为 0 = 「这一层一只都不拉」，是合法意图，不是错误。"""
    assert resolve_quota({"BJ": 0})["BJ"] == 0


def test_an_unknown_market_is_refused():
    with pytest.raises(ValueError, match="未知市场"):
        resolve_quota({"HK": 10})


def test_a_non_integer_or_negative_quota_is_refused():
    for bad in (-1, 1.5, "120", True, None):
        with pytest.raises(ValueError, match="非负整数"):
            resolve_quota({"SH": bad})


def test_the_returned_mapping_is_a_copy_of_the_default():
    """调用方改返回值不许污染下一次调用的默认值。"""
    q = resolve_quota()
    q["SH"] = 1
    assert resolve_quota()["SH"] == 120
    assert DEFAULT_QUOTA["SH"] == 120
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q -k quota
```

Expected: FAIL —— `ImportError: cannot import name 'resolve_quota'`。

- [ ] **Step 3: 写实现**

```python
# §4.4 的默认配额。约 400 只 ≈ 1.7 GiB（2026-08-23 实测单股两文件均值约 4.6 MB）。
# BJ 给 120 而不按地板（≥8）等比缩到约 32：BJ 的**成功率**才是稀缺资源，
# 详见 spec §4.4 与 S3-F1。
DEFAULT_QUOTA = {"SH": 120, "SZ": 160, "BJ": 120}


def resolve_quota(overrides: "Mapping[str, int] | None" = None) -> dict[str, int]:
    """§4.4 的默认配额 + `--quota SH=..,SZ=..,BJ=..` 覆盖（命令行接线在 S5）。

    ⚠️ **配额是「累计目标」，不是「每批数量」**（S3-F3）：`quota[mk]` 表示这个
    staging 在该层**总共**要尝试到 `universe[mk]` 的第几个。合成方式见 `fresh_slots`。

    ⚠️ 返回**副本**：调用方改它不许污染下一次调用。
    """
    out = dict(DEFAULT_QUOTA)
    for mk, value in dict(overrides or {}).items():
        if mk not in MARKETS:
            raise ValueError(f"--quota 出现未知市场 {mk!r}，只认 {list(MARKETS)}")
        if isinstance(value, bool) or not isinstance(value, int) or value < 0:
            raise ValueError(f"--quota 的 {mk} 必须是非负整数，读到 {value!r}")
        out[mk] = value
    return out
```

（把 Task 4 加的那行 import 改成 `from typing import Iterable, Mapping` —— **一行**，
不是两行；`typing` 而非 `collections.abc`，跟随 `qmt_manifest.py:28` 的既有写法。）

- [ ] **Step 4: 跑测试确认它绿 + 全量 + 提交**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q && \
../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/s3-t5.xml && \
../.venv/bin/python -c "import xml.etree.ElementTree as ET;r=ET.parse('/tmp/s3-t5.xml').getroot();print({k:sum(int(s.get(k,0)) for s in r.iter('testsuite')) for k in ('tests','skipped','failures','errors')})"
cd .. && git add backend/qmt_pool.py backend/tests/test_qmt_pool.py
git commit -m "feat(4b-S3): 默认配额与 --quota 覆盖的解析（§4.4）"
```

**→ PR S3-2 到此可开。**

---

### Task 6: `fresh_slots` —— 从游标续到累计目标

**Files:**
- Modify: `backend/qmt_pool.py`
- Test: `backend/tests/test_qmt_pool.py`

**Interfaces:**
- Consumes: `freeze_universe` 的产物、manifest 的 `cursor`、`resolve_quota` 的产物
- Produces:
  - `Slot`（frozen dataclass，字段 `code: str` / `market: str` / `universe_idx: int`）
  - `fresh_slots(universe: dict, cursor: dict, quota: dict) -> list[Slot]`

- [ ] **Step 1: 写失败测试**

```python
from qmt_pool import Slot, fresh_slots


def _uni(n_sh: int = 5, n_sz: int = 4, n_bj: int = 3) -> dict:
    """直接造宇宙，绕开 shuffle —— 本 task 测的是区间算术，不是打乱。"""
    return {"SH": [f"60{i:04d}.SH" for i in range(n_sh)],
            "SZ": [f"00{i:04d}.SZ" for i in range(n_sz)],
            "BJ": [f"43{i:04d}.BJ" for i in range(n_bj)]}


def test_a_first_run_takes_the_quota_from_the_head_of_each_layer():
    uni = _uni()
    slots = fresh_slots(uni, {"SH": 0, "SZ": 0, "BJ": 0}, {"SH": 2, "SZ": 1, "BJ": 0})
    assert [(s.market, s.universe_idx) for s in slots] == [
        ("SH", 0), ("SH", 1), ("SZ", 0)]
    assert slots[0] == Slot(code=uni["SH"][0], market="SH", universe_idx=0)


def test_every_slot_anchors_to_the_frozen_universe():
    uni = _uni()
    for s in fresh_slots(uni, {"SH": 0, "SZ": 0, "BJ": 0}, {"SH": 5, "SZ": 4, "BJ": 3}):
        assert uni[s.market][s.universe_idx] == s.code


def test_rerunning_with_the_same_quota_takes_nothing_new():
    """**S3-F3 的正向档**：配额是累计目标，重跑不会再拉一批。

    按「每批数量」实现的话，这里会再拿到 2 个 SH 槽位 ——
    而操作者以为自己只是重跑了一次。
    """
    assert fresh_slots(_uni(), {"SH": 2, "SZ": 1, "BJ": 0},
                       {"SH": 2, "SZ": 1, "BJ": 0}) == []


def test_raising_the_quota_continues_from_the_cursor():
    """**S3-F3 的反向档**：提高配额才拉下一批，且从游标处续、零重复零遗漏。"""
    uni = _uni()
    slots = fresh_slots(uni, {"SH": 2, "SZ": 0, "BJ": 0}, {"SH": 4, "SZ": 0, "BJ": 0})
    assert [s.universe_idx for s in slots] == [2, 3]
    assert all(s.market == "SH" for s in slots)


def test_a_quota_larger_than_the_layer_takes_the_whole_layer():
    """spec §4.4：「配额大于可用数不是错误，取全部即可」。"""
    slots = fresh_slots(_uni(n_sh=3), {"SH": 0, "SZ": 0, "BJ": 0},
                        {"SH": 999, "SZ": 0, "BJ": 0})
    assert [s.universe_idx for s in slots] == [0, 1, 2]


def test_a_quota_lowered_below_the_cursor_takes_nothing_rather_than_going_backwards():
    assert fresh_slots(_uni(), {"SH": 4, "SZ": 0, "BJ": 0},
                       {"SH": 1, "SZ": 0, "BJ": 0}) == []


def test_an_exhausted_layer_is_a_legal_terminal_state():
    """`cursor == len(universe[mk])` 是「池穷尽」这个合法终态，不是错误。"""
    assert fresh_slots(_uni(n_sh=3), {"SH": 3, "SZ": 0, "BJ": 0},
                       {"SH": 120, "SZ": 0, "BJ": 0}) == []


def test_a_negative_cursor_is_refused_rather_than_silently_wrapping():
    """⚠️ Python 的负下标会**静默**取到另一只股。

    `range(-2, 120)` 会产出 `universe[-2]`、`universe[-1]` —— 两个来自层**尾部**
    的股，而 `Slot.universe_idx` 记的是 -2/-1。这不是崩溃，是一份看起来完全
    正常、却指错了股的工作单。
    """
    with pytest.raises(ValueError, match="越界"):
        fresh_slots(_uni(), {"SH": -2, "SZ": 0, "BJ": 0}, {"SH": 3, "SZ": 0, "BJ": 0})


def test_a_cursor_past_the_end_of_the_layer_is_refused():
    with pytest.raises(ValueError, match="越界"):
        fresh_slots(_uni(n_sh=3), {"SH": 4, "SZ": 0, "BJ": 0},
                    {"SH": 3, "SZ": 0, "BJ": 0})


def test_a_negative_quota_is_refused_rather_than_silently_emptying_the_batch():
    """⚠️ 负配额不会崩，只会让 `range(start, 负数)` 变成空批。

    而**空批与「这一层池穷尽了」在外部完全不可区分** —— 操作者读到的是
    「没候选了」，真相是账本被改过。`quota` 是**非必需持久化键**，
    读侧 `validate_manifest` 一个字都不校验（同 `failures`，见 S2-F64）。
    """
    with pytest.raises(ValueError, match="必须是非负整数"):
        fresh_slots(_uni(), {"SH": 0, "SZ": 0, "BJ": 0},
                    {"SH": -1, "SZ": 0, "BJ": 0})


def test_a_map_missing_a_market_is_refused_rather_than_raising_a_bare_keyerror():
    """缺一层要给出说得清的错，而不是一个来路不明的 KeyError。"""
    with pytest.raises(ValueError, match="缺市场"):
        fresh_slots(_uni(), {"SH": 0, "SZ": 0}, {"SH": 1, "SZ": 0, "BJ": 0})
    with pytest.raises(ValueError, match="缺市场"):
        fresh_slots(_uni(), {"SH": 0, "SZ": 0, "BJ": 0}, {"SH": 1, "BJ": 0})
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q -k "slot or cursor or quota_lar or exhaust"
```

Expected: FAIL —— `ImportError: cannot import name 'Slot'`。

- [ ] **Step 3: 写实现**

```python
@dataclass(frozen=True)
class Slot:
    """一个「要尝试的槽位」= 冻结名单里的一个位置。

    `universe_idx` 随槽位走到底（进 `pool_order` / `failures`），
    因为它才是 pilot 的消费顺序锚点（R12-F1）。
    """
    code: str
    market: str
    universe_idx: int


def fresh_slots(universe: dict, cursor: dict, quota: dict) -> list[Slot]:
    """从各层游标续到该层的**累计目标**（S3-F3）。

    区间 `[cursor[mk], min(quota[mk], len(universe[mk])))`。
    `cursor >= quota` 时该层返回空 —— **用同样的配额重跑不会再拉一批**。

    ⚠️ **必须自己再查一次游标边界**：读侧 `_validate_cursor` 已经查过，但那是
    「从磁盘读回来」这条路；本函数是**纯内存**接口，S4 完全可能在崩溃恢复里
    改完 `cursor` 直接调它。而游标为负时 `range(-2, n)` 会产出 `universe[-2]`
    这样的**尾部**元素 —— 不崩溃、不报错，只是给出一份指错股的工作单。

    ⚠️ **`quota` 同样要守**（开工前扫描 · 裁定 2）：给 `cursor` 立守卫的那条理由
    对 `quota` 一字不差地成立，而我当初只看见了手上那一个字段 —— 这是本仓
    「**按字段穷尽、而不是按判据句穷尽**」那条教训的又一次复现。更要紧的是
    `quota` 与 `failures` 同属**非必需持久化键**，读侧 `validate_manifest`
    **一个字都不校验**（S2-F64 已实证这类字段的危险）：一个被改成负数的 `quota`
    会让本层静默空跑，而外部把它读成「池穷尽」。
    """
    for name, m in (("cursor", cursor), ("quota", quota)):
        missing = [mk for mk in MARKETS if mk not in m]
        if missing:
            raise ValueError(f"{name} 缺市场 {missing} —— 三层必须齐全，不许缺")

    out: list[Slot] = []
    for mk in MARKETS:
        layer = universe[mk]
        start = cursor[mk]
        if not isinstance(start, int) or isinstance(start, bool) or not 0 <= start <= len(layer):
            raise ValueError(
                f"cursor[{mk}] = {start!r} 越界（本层冻结名单长度 {len(layer)}，"
                f"合法范围 0..{len(layer)}，取到 {len(layer)} 表示该层已取遍）")
        cap = quota[mk]
        if not isinstance(cap, int) or isinstance(cap, bool) or cap < 0:
            raise ValueError(
                f"quota[{mk}] = {cap!r} 必须是非负整数 —— 负值会让本层的区间退化成"
                "空批，而**空批与「池穷尽」在外部完全不可区分**：操作者会读到一份"
                "「这一层没候选了」的结论，真相却是账本被改过。")
        for idx in range(start, min(cap, len(layer))):
            out.append(Slot(code=layer[idx], market=mk, universe_idx=idx))
    return out
```

- [ ] **Step 4: 跑测试 + 变异验证**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q
cp qmt_pool.py /tmp/qmt_pool.pristine.py
# ⚠️ run_mut 定义在 Task 3 Step 5；新开的 shell 要先把它重新粘一遍。
run_mut "配额当成每批数量" \
  "for idx in range(start, min(quota[mk], len(layer))):" \
  "for idx in range(start, min(start + quota[mk], len(layer))):"
run_mut "不查游标边界" "not 0 <= start <= len(layer)" "False"
run_mut "不查配额"     "or cap < 0" "or False"
run_mut "不查三层齐全" "if missing:" "if False:"
diff -q qmt_pool.py /tmp/qmt_pool.pristine.py && echo "已复原到 pristine"
```

Expected：第一组必须红在 `test_rerunning_with_the_same_quota_takes_nothing_new`
（**这就是 S3-F3 的判别力证明**）；第二组红在两个越界档。

- [ ] **Step 5: 全量 + 提交**

```bash
cd backend && ../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/s3-t6.xml && \
../.venv/bin/python -c "import xml.etree.ElementTree as ET;r=ET.parse('/tmp/s3-t6.xml').getroot();print({k:sum(int(s.get(k,0)) for s in r.iter('testsuite')) for k in ('tests','skipped','failures','errors')})"
cd .. && git add backend/qmt_pool.py backend/tests/test_qmt_pool.py
git commit -m "feat(4b-S3): 按层从游标续到累计配额的新槽位派生（S3-F3）"
```

---

### Task 7: `retry_slots` + `plan_batch` —— 本批完整工作单

**Files:**
- Modify: `backend/qmt_pool.py`
- Test: `backend/tests/test_qmt_pool.py`

**Interfaces:**
- Consumes: `Slot` / `fresh_slots`（Task 6）、`qmt_manifest.ManifestInvalidError`
- Produces:
  - `RETRY_LIMIT: int`（= 2）
  - `retry_slots(universe: dict, failures) -> list[Slot]`
  - `plan_batch(universe, cursor, quota, *, failures=()) -> list[Slot]`

- [ ] **Step 1: 写失败测试**

```python
from qmt_manifest import ManifestInvalidError
from qmt_pool import RETRY_LIMIT, plan_batch, retry_slots


def _fail(uni: dict, mk: str, idx: int, attempts: int) -> dict:
    return {"stock_code": uni[mk][idx], "market": mk,
            "universe_idx": idx, "attempts": attempts,
            "reason": "fetch_missing_file"}


def test_entries_below_the_retry_limit_are_retried():
    uni = _uni()
    got = retry_slots(uni, [_fail(uni, "SH", 1, 0), _fail(uni, "SZ", 0, 1)])
    assert [(s.market, s.universe_idx) for s in got] == [("SH", 1), ("SZ", 0)]


def test_an_entry_at_the_retry_limit_is_not_retried():
    """spec §4.4：`attempts < 2` 才自动重试；仍失败则加一后不再自动重试。"""
    uni = _uni()
    assert RETRY_LIMIT == 2
    assert retry_slots(uni, [_fail(uni, "SH", 1, RETRY_LIMIT)]) == []


def test_a_corrupt_entry_is_refused_even_when_it_would_not_be_retried():
    """⚠️ 校验必须跑遍**每一条**，不能因为「反正不重试」就跳过。

    「断定不是目标」与「判据够不着」混成同一个 `continue`，两者会**互相掩盖**：
    漏站看起来像正常跳过。这条台账读侧从不校验（S2-F64 实测坐实
    `validate_manifest` 的判据里没有 `failures`），本函数是它唯一的门。
    """
    uni = _uni()
    bad = _fail(uni, "SH", 1, RETRY_LIMIT)
    bad["stock_code"] = "not-a-code"
    with pytest.raises(ManifestInvalidError, match="不是合法股票代码"):
        retry_slots(uni, [bad])


def test_a_negative_universe_idx_is_refused_rather_than_wrapping():
    uni = _uni()
    bad = _fail(uni, "SH", 1, 0)
    bad["universe_idx"] = -1
    with pytest.raises(ManifestInvalidError, match="非负整数"):
        retry_slots(uni, [bad])


def test_an_anchor_that_does_not_match_the_frozen_universe_is_refused():
    """锚点对不上 = 这份 manifest 被编辑过，或来自另一次 fetch。"""
    uni = _uni()
    bad = _fail(uni, "SH", 1, 0)
    bad["universe_idx"] = 2               # 指向另一只股
    with pytest.raises(ManifestInvalidError, match="锚点对不上"):
        retry_slots(uni, [bad])


def test_a_market_that_disagrees_with_the_code_suffix_is_refused():
    uni = _uni()
    bad = _fail(uni, "SH", 1, 0)
    bad["market"] = "SZ"
    with pytest.raises(ManifestInvalidError, match="后缀与"):
        retry_slots(uni, [bad])


def test_a_missing_key_is_refused():
    uni = _uni()
    for key in ("stock_code", "market", "universe_idx", "attempts"):
        bad = _fail(uni, "SH", 1, 0)
        del bad[key]
        with pytest.raises(ManifestInvalidError, match=f"缺 {key}"):
            retry_slots(uni, [bad])


def test_plan_batch_puts_retries_before_fresh_slots():
    """spec §4.4：「**先重试** `attempts < 2` 的条目，**再从 `cursor` 继续**」。"""
    uni = _uni()
    got = plan_batch(uni, {"SH": 2, "SZ": 0, "BJ": 0}, {"SH": 4, "SZ": 0, "BJ": 0},
                     failures=[_fail(uni, "SH", 0, 1)])
    assert [s.universe_idx for s in got] == [0, 2, 3]


def test_a_retry_does_not_consume_quota():
    """重试的槽位早已被 `cursor` 走过；配额记的是「累计尝试到第几个」。

    若重试占配额，一层里失败得越多、能新拉的股越少 —— 而 `cursor` 照样在推进，
    池子会**安静地**缩水，最终报出假的 `pool_exhausted`。
    """
    uni = _uni()
    got = plan_batch(uni, {"SH": 2, "SZ": 0, "BJ": 0}, {"SH": 2, "SZ": 0, "BJ": 0},
                     failures=[_fail(uni, "SH", 0, 0)])
    assert [(s.market, s.universe_idx) for s in got] == [("SH", 0)]


def test_plan_batch_without_failures_equals_fresh_slots():
    uni = _uni()
    cur, quo = {"SH": 0, "SZ": 0, "BJ": 0}, {"SH": 2, "SZ": 1, "BJ": 0}
    assert plan_batch(uni, cur, quo) == fresh_slots(uni, cur, quo)
```

- [ ] **Step 2: 跑测试确认它红**

```bash
cd backend && ../.venv/bin/python -m pytest tests/test_qmt_pool.py -q -k "retry or plan_batch"
```

Expected: FAIL —— `ImportError: cannot import name 'RETRY_LIMIT'`。

- [ ] **Step 3: 写实现**

（顶部把 import 补成 `from qmt_manifest import MARKETS, STOCK_CODE_RE, ManifestInvalidError`。）

```python
# §4.4「有界重试」：补拉时只重试 attempts < 2 的条目（一次重试，覆盖瞬时网络故障）。
RETRY_LIMIT = 2


def retry_slots(universe: dict, failures) -> list[Slot]:
    """补拉时要先重试的槽位（§4.4 有界重试）。

    ⚠️ **读侧校验完全不碰 `failures`**（S2-F64 实测坐实：`validate_manifest` 的
    判据里没有它）。于是这份台账**从未被任何人校验过**就交到这里，而它能授权
    「再去拷一次」——一个能授权做事的结构，自己必须先被校验（R21-F3 同族）。

    ⚠️ **每一条都要过校验，包括不会被重试的那些**：把「attempts 到顶所以跳过」
    与「这条记录坏掉所以够不着」混成同一个 `continue`，两者会互相掩盖 ——
    漏站看起来就像一次正常跳过。
    """
    out: list[Slot] = []
    for i, rec in enumerate(failures):
        where = f"failures[{i}]"
        if not isinstance(rec, dict):
            raise ManifestInvalidError(f"{where} 必须是对象，读到 {type(rec).__name__}")
        for key in ("stock_code", "market", "universe_idx", "attempts"):
            if key not in rec:
                raise ManifestInvalidError(f"{where} 缺 {key}")

        code, market = rec["stock_code"], rec["market"]
        if not isinstance(code, str) or STOCK_CODE_RE.match(code) is None:
            raise ManifestInvalidError(f"{where}.stock_code 不是合法股票代码：{code!r}")
        if market not in MARKETS:
            raise ManifestInvalidError(
                f"{where}.market = {market!r} 不在 {list(MARKETS)} 内")
        if not code.endswith("." + market):
            raise ManifestInvalidError(
                f"{where}.stock_code = {code!r} 的后缀与 market {market!r} 不符")

        idx, attempts = rec["universe_idx"], rec["attempts"]
        for key, value in (("universe_idx", idx), ("attempts", attempts)):
            if isinstance(value, bool) or not isinstance(value, int) or value < 0:
                raise ManifestInvalidError(
                    f"{where}.{key} 必须是非负整数，读到 {value!r}")

        layer = universe[market]
        if idx >= len(layer):
            raise ManifestInvalidError(
                f"{where}.universe_idx = {idx} 越界（本层冻结名单长度 {len(layer)}）")
        if layer[idx] != code:
            raise ManifestInvalidError(
                f"{where} 的锚点对不上：universe[{market}][{idx}] 是 {layer[idx]!r}，"
                f"而这条记录自称是 {code!r}。这份 manifest 被编辑过或来自另一次 fetch。")

        if attempts < RETRY_LIMIT:
            out.append(Slot(code=code, market=market, universe_idx=idx))
    return out


def plan_batch(universe: dict, cursor: dict, quota: dict, *, failures=()) -> list[Slot]:
    """本批的完整工作单：**先重试、再续新**（§4.4）。

    ⚠️ **重试项不占配额**：它们的槽位早已被 `cursor` 走过，而配额记的是
    「累计尝试到第几个」。若让重试占配额，一层里失败得越多能新拉的股越少，
    池子会**安静地**缩水，最终报出假的 `pool_exhausted`。
    """
    return retry_slots(universe, failures) + fresh_slots(universe, cursor, quota)
```

- [ ] **Step 4: 变异验证 —— 重点钉「校验跑遍每一条」**

```bash
cd backend
cp qmt_pool.py /tmp/qmt_pool.pristine.py
# ⚠️ run_mut 定义在 Task 3 Step 5；新开的 shell 要先把它重新粘一遍。
run_mut "先判 attempts 再校验（漏站）" \
  '        if attempts < RETRY_LIMIT:\n            out.append(' \
  '        if attempts < RETRY_LIMIT:\n            out.append('
run_mut "重试占配额" "return retry_slots(universe, failures) + fresh_slots(universe, cursor, quota)" \
  "r = retry_slots(universe, failures)\n    q = {mk: max(0, quota[mk] - sum(1 for s in r if s.market == mk)) for mk in MARKETS}\n    return r + fresh_slots(universe, cursor, q)"
run_mut "锚点不查"   "if layer[idx] != code:" "if False:"
run_mut "重试上限 2 → 3" "attempts < RETRY_LIMIT" "attempts < RETRY_LIMIT + 1"
diff -q qmt_pool.py /tmp/qmt_pool.pristine.py && echo "已复原到 pristine"
```

> ⚠️ 第一组（「漏站」）**没法用字符串替换表达**——它要求把整段校验挪进
> `if attempts < RETRY_LIMIT:` 之内。**手工做这一组**：把校验体缩进到该分支里，
> 跑 `test_a_corrupt_entry_is_refused_even_when_it_would_not_be_retried`，
> 确认它红；然后 `cp /tmp/qmt_pool.pristine.py qmt_pool.py` 复原。
> 上面那行 `run_mut` 写成了恒等替换，**是占位提醒，不是有效变异** —— 别把它的
> 「零红」当成结论。

Expected：后三组各自见红；第一组手工做，必须红在那条专属档上。

- [ ] **Step 5: 全量 + 提交**

```bash
cd backend && ../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/s3-t7.xml && \
../.venv/bin/python -c "import xml.etree.ElementTree as ET;r=ET.parse('/tmp/s3-t7.xml').getroot();print({k:sum(int(s.get(k,0)) for s in r.iter('testsuite')) for k in ('tests','skipped','failures','errors')})"
cd .. && git add backend/qmt_pool.py backend/tests/test_qmt_pool.py
git commit -m "feat(4b-S3): 失败台账的有界重试 + 本批工作单（先重试再续新，重试不占配额）"
```

**→ PR S3-3 到此可开，S3 交付完毕。**

---

## 偏离登记（本计划相对 spec 字面的决定，逐条写明理由）

实施中新增的偏离续编 D8、D9……，每条都要写「怎么发现的 / 改了什么 / 为什么不改另一种」。

| # | 偏离 | spec 怎么写的 | 为什么这样定 |
|---|---|---|---|
| D1 | 预筛加第四条剔除判据「两个周期缺一不可」 | §4.3 只列三条 | 缺 daily 就没有 `k_daily` 可算。不单列的话，这只股要么在算月数时炸、要么被静默算进别的计数器 —— 两种都让那份「关于数据源的第一份真实观测」失真。真实导出里这一档命中 0 只（11216 = 5608×2 恰好配平），但它是**判据**该有的完整性，不是为真实数据服务的 |
| D2 | `prefilter` 输出按 code 升序 | 未提 | `random.shuffle` 的输出取决于入参顺序，而入参来自 dict 迭代序 = `export_log.csv` 行序。不排序则「同 seed → 同一个宇宙」悄悄依赖 QMT 导出时的行序 —— 而**整套 seeded 设计存在的唯一理由**就是可复现 |
| D3 | `freeze_universe` 拒绝重复代码 | 未提；读侧 `_validate_source_snapshot` **也不查**层内唯一性 | 重复 ⇒ 两个 `universe_idx` 指向同一只股，锚点语义失效。读侧不查 ⇒ 这道门只能立在写侧。**不顺手去加读侧判据**：S2 的教训是「中途给读侧加判据」正是 75 条 finding 的主要来源，且要先证明 spec 的状态机产不出这种合法形态（S2-F8 的原型）。**登记给 S5** |
| D4 | `_STOCK_CODE_RE` 提为公开 `STOCK_CODE_RE`（改 `qmt_manifest.py`） | 未提 | 写侧与读侧对「什么是合法股票代码」必须用**同一个对象**。本仓已有一条登记在案的残留是「同一判据两份近似实现」；且 `qmt_normalize` 那份用 `$`（容忍尾随换行）、`qmt_manifest` 那份用 `\Z`，两者**本来就已经不等价** |
| D5 | `fresh_slots` 自查游标边界（读侧 `_validate_cursor` 已查过一遍） | 未提 | 两条路：读侧管「从磁盘读回来」，本函数是**纯内存**接口，S4 的崩溃恢复会改完 `cursor` 直接调它。而负游标下 `range(-2, n)` 取到的是层**尾部**元素 —— **不崩溃、不报错**，只给出一份指错股的工作单 |
| D6 | `month_span` 拒绝 `None` 时间戳 | 未提 | 让「status 门必须排在月数计算之前」这条**次序纪律**在够不着时响亮地失败，而不是抛一个来路不明的 `TypeError`。`qmt_ingest` 正是在这条次序上栽过（整份 log 崩掉） |
| D7 | 预筛统计的字段名与形状定为 `{total, rejected_incomplete, rejected_status, rejected_daily_span, rejected_1m_span, passed}` | §4.3 只说「各条命中数」 | 需要一个可机械检查的不变量（`total == passed + 四条之和`）来挡住「同一只股被记两次」与「某条剔除路径忘了计数」。**走非必需顶层键通道，不 bump `manifest_version`** |
| D9 | `month_span` 的两种失败分属**两个**异常族（区间反向 → `QmtSchemaError`；空时间戳 → `ValueError`）| 未提 | 见 Global Constraints 的异常族表。**判据是「坏数据从哪来」**：区间反向是 export_log 的内容坏了，空时间戳是调用方没先跑 status 门。混同的话，S5 的 CLI 会把一个内部次序 bug 报成「你的 export_log 有问题」，把操作者指向一个不存在的问题。⚠️ 因 `QmtSchemaError` 是 `ValueError` 子类，**必须有一档专门断言「这个不是 QmtSchemaError」**，否则两种实现都绿 |
| D10 | `fresh_slots` 同时守 `cursor` 与 `quota`，并要求三层齐全 | 未提 | 开工前扫描 · 裁定 2。给 `cursor` 立守卫的理由对 `quota` 一字不差地成立，而我当初只守了一个 —— 「按字段穷尽而非按判据句穷尽」的又一次复现。`quota` 与 `failures` 同属**非必需持久化键**，读侧 `validate_manifest` 一个字都不校验（S2-F64）；负配额 ⇒ 静默空批 ⇒ 外部读成「池穷尽」|
| D8 | 母 spec `2026-07-26-qmt-plan4-pilot-shipment-design.md:331` 里**同一句被推翻的措辞照旧留着**，本片刻意不改 | —— | A8 的第一次真跑（**改 spec 之前**）就把它揪了出来。但那份文件头一行写着「**本文件已切分，不再作为实施依据**」「**保留为 R1–R97 的完整历史账本，一个字节未删**」——改它等于毁掉它自己声明的不变量。故 A8 的判据**只扫现行 4b spec**，并放行 S3-F1 那条更正记录自身的引用。⚠️ 这也是本片第一条被机械检查抓到的东西：写 A8 时我原本打算「全 docs 扫，命中即失败」，那样会**永远失败**（更正记录必然引用旧措辞），是一条自己把自己钉死的判据 |

---

## 非程序员验收清单

> **怎么用**：把下面每一行**单独**复制到终端里回车，看它自己打出的「通过 / 失败」。
> 每行都是完整的一条命令，不要合并、不要换行。
> **第 0 行先跑**（它切到项目目录并准备好环境）：

```
cd "/Users/maziming/Coding/Prj_Kline trainer/backend"
```

> ⚠️ 上面这一行指向的是**主仓**——合并之后才是验收现场。**合并之前**要在开发分支上试跑的话，
> 第 0 行改成
> `cd "/Users/maziming/Coding/Prj_Kline trainer/.dev/worktree/qmt-4b-s3/backend"`，
> 其余 A1–A9 一个字都不用改（worktree 里的 `.venv` 是指向主仓的软链，
> 故 `../.venv/bin/python` 两边写法相同）。

| # | 动作（复制这一行去跑） | 预期 | 通过与否 |
|---|---|---|---|
| **A1** | `../.venv/bin/python -m pytest tests/ -q -rs --junitxml=/tmp/s3-a1.xml > /tmp/s3-a1.log 2>&1; ../.venv/bin/python -c "import xml.etree.ElementTree as ET;r=ET.parse('/tmp/s3-a1.xml').getroot();d={k:sum(int(s.get(k,0)) for s in r.iter('testsuite')) for k in ('tests','skipped','failures','errors')};print(d);print('A1 通过' if d['failures']==0 and d['errors']==0 and d['skipped']==0 else 'A1 失败')"` | 打印一行字典后打印 **`A1 通过`**；字典里 `failures`、`errors`、`skipped` 三项**都是 0** | ⬜ |
| **A2** | `../.venv/bin/python -c "from qmt_pool import prefilter; import tests.test_qmt_pool as t; print('A2 通过' if (prefilter(t._stock('600000.SH',daily_months=39))[0]==['600000.SH'] and prefilter(t._stock('600000.SH',daily_months=38))[0]==[] and prefilter(t._stock('600000.SH',m1_months=8))[0]==['600000.SH'] and prefilter(t._stock('600000.SH',m1_months=7))[0]==[]) else 'A2 失败')"` | 打印 **`A2 通过`**（日线满 39 个月放行、38 个月剔；1 分钟线满 8 个月放行、7 个月剔） | ⬜ |
| **A3** | `../.venv/bin/python -c "from qmt_pool import freeze_universe as f; c=['600000.SH','600004.SH','000001.SZ','430047.BJ']; print('A3 通过' if f(c,seed='s')==f(list(reversed(c)),seed='s')==f(set(c),seed='s') else 'A3 失败')"` | 打印 **`A3 通过`**（同一批股、同一个种子，无论输入顺序怎么变，冻结出的名单逐字相同） | ⬜ |
| **A4** | `../.venv/bin/python -c "from qmt_pool import freeze_universe as f; c=['600000.SH','600004.SH','600006.SH','000001.SZ','000002.SZ']; a=f(c,seed='s'); b=f(c+['000003.SZ'],seed='s'); print('A4 通过' if a['SH']==b['SH'] and a['SZ']!=b['SZ'] else 'A4 失败')"` | 打印 **`A4 通过`**（深圳层多一只股，上海层的顺序**一个字都不变**） | ⬜ |
| **A5** | `../.venv/bin/python -c "from qmt_pool import fresh_slots; u={'SH':['60%04d.SH'%i for i in range(5)],'SZ':[],'BJ':[]}; z={'SH':0,'SZ':0,'BJ':0}; first=fresh_slots(u,z,{'SH':2,'SZ':0,'BJ':0}); again=fresh_slots(u,{'SH':2,'SZ':0,'BJ':0},{'SH':2,'SZ':0,'BJ':0}); print('A5 通过' if len(first)==2 and again==[] else 'A5 失败')"` | 打印 **`A5 通过`**（第一次拉 2 只；**用同样的配额再跑一次，一只都不再拉**） | ⬜ |
| **A6** | `../.venv/bin/python -c "from qmt_pool import freeze_universe; import tests.test_qmt_pool as t; from qmt_manifest import validate_manifest; m=t._bootstrap_manifest(freeze_universe(t._SAMPLE,seed='s')); print('A6 通过' if validate_manifest(m) is m else 'A6 失败')"` | 打印 **`A6 通过`**（本片产出的名单，能被上一片写好的账本校验原样接受） | ⬜ |
| **A7** | `../.venv/bin/python -c "import re;s=open('qmt_pool.py',encoding='utf-8').read();bad=[w for w in ('open(','os.remove','os.rename','asyncpg','requests','socket') if w in s];print('A7 通过' if not bad else 'A7 失败：'+str(bad))"` | 打印 **`A7 通过`**（本片是纯计算，不碰文件、不碰数据库、不联网） | ⬜ |
| **A8** | `cd "/Users/maziming/Coding/Prj_Kline trainer" && ./.venv/bin/python -c "import pathlib;f='docs/superpowers/specs/2026-07-27-qmt-plan4b-fetch-design.md';L=[(i,l) for i,l in enumerate(pathlib.Path(f).read_text(encoding='utf-8').splitlines(),1) if '预计仅 100~150 只' in l and 'S3-F1' not in l];print('A8 通过' if not L else 'A8 失败：'+str(L))"` | 打印 **`A8 通过`**（被实测推翻的那句旧说法，在**现行** 4b spec 里只剩 S3-F1 更正记录那一处引用，正文里一处不剩） | ⬜ |

| **A9** | `cd "/Users/maziming/Coding/Prj_Kline trainer" && ./.venv/bin/python -c "import pathlib;t=pathlib.Path('docs/superpowers/specs/2026-07-27-qmt-plan4b-fetch-design.md').read_text(encoding='utf-8');need=['197/324','5139 / 5608','累计目标','S3-F1','S3-F2','S3-F3'];miss=[w for w in need if w not in t];print('A9 通过' if not miss else 'A9 失败，缺：'+str(miss))"` | 打印 **`A9 通过`**（三处实测更正与三条 S3-F 记录都真的写进 spec 了） | ⬜ |

> ⚠️ **「通过与否」这一列在真跑过之后才填**。本仓的纪律是：没在这棵树上新鲜跑过，
> 就不能写它通过。禁用措辞：「验证通过即可」「看起来正常」「应该没问题」。

---

## 交付口径（写 PR 正文与汇报时逐字照用）

**这一片交付的是**：从 `export_log.csv` 的解析结果，到「冻结的分层候选名单 + 本批要尝试的槽位清单」这条**纯计算**管线。预筛三条判据的边界是精确的；同一个种子加同一批股，永远冻出同一份名单；用同样的配额重跑不会重复拉取。

**⛔ 这一片没有交付的**：
- `backend/qmt_fetch.py` **仍然不存在**（零实现）。
- **一个字节都没拷过**，也**没有连过 SMB**。本片全部验证跑在内存里。
- 之后还剩 S4（拷贝引擎）/ S5（源边界闸 + 命令行），然后才是 4c。

**⛔ 禁述**（沿用 4b/4c 的既有口径）：「4b 完成」「SMB 拉取做好了」「真实数据接入完成」「pilot 已完成」「100 股已出货」。

---

## 给 S4 / S5 的硬性交接（不写下来就会丢）

1. **启动序列第一步必须调 `begin_run`**，且在任何拷贝 / 建目录 / 提交之前 —— `qmt_manifest` 结构上强制不了这个顺序（S2-F20 已登记的残留）。
2. **续跑循环必须以 `pool_order` 为准跳过已成功槽位，不得只看 `cursor`**（S2-F8 移交）。崩溃恢复第③档会把 `cursor` 退到已有池条目之前，只看 `cursor` 会重拷并撞上「层内 `universe_idx` 唯一」。
3. **`committed_bytes` 的读侧必需性**要 bump `manifest_version`（S2-F42 移交）。
4. **`universe` 的层内唯一性读侧判据**（本片 D3）：写侧已挡住，读侧仍不查。加之前先证明 spec 的状态机产不出这种合法形态。
5. **`qmt_normalize.is_valid_stock_code` 用 `$` 容忍尾随换行**（本片 Task 4 顺带发现，未修）。它自称是路径拼接前的信任边界入口。
6. **重试成功后要把条目移出 `failures`**（O2-F14 + S2-F28）：`retry_slots` 只负责挑出该重试的槽位，**移出**是提交侧的事，且移出必须自证成功（池里有锚点条目 + `files` 恰好两条）。

---

## 评审轮次记录

| 轮 | 通道 | 判决 | finding 数 | 处置 |
|---|---|---|---|---|
| （待填） | | | | |

**收口规则**（沿用本仓约定）：连续三轮出现「归因率高（本轮缺陷由上轮修复引入）/ 判据总量不降 / 集中度不变」三项全中，就必须换方案而不是继续修。

---

## Self-Review（写完计划后自查，已执行）

**1. spec 覆盖**：§4.2（储备池只依赖 `export_log.csv`）→ 本计划全程只吃 `parse_export_log` 的产物，不碰 `stock_universe_with_name.csv`，Task 3 的入参类型即为证。§4.3 三条判据 → Task 3，边界档见 Task 3 Step 1。§4.4 分层 / seeded / 配额 / 冻结宇宙 / 按层续 / 有界重试 → Task 4~7。
**未覆盖且已明确移交**：§4.4 的 `cursor` 推进与 `pool_order` 追加（**写盘时**发生，S2b 已实现提交入口，推进动作属 S4）、崩溃恢复三档（S4）、`--max-bytes`（S4）、`source_snapshot.export_log_sha256` 的计算（要读文件，S5）。

**2. 占位符扫描**：无 TBD / TODO / 「类似 Task N」/ 「加上适当的错误处理」。每个写代码的 step 都带完整代码块。**唯一一处刻意的例外**已明写：Task 7 Step 4 的第一组变异无法用字符串替换表达，就地写明「手工做，且那行 `run_mut` 是占位提醒、零红不算结论」。

**3. 类型一致性**：`Slot(code, market, universe_idx)` 在 Task 6 定义，Task 7 沿用同名同序。`freeze_universe` 返回 `dict[str, list[str]]`，被 Task 6/7 当 `universe` 消费，键均取自 `MARKETS`。`prefilter` 返回 `(list[str], PrefilterStats)`，Task 4 消费其第一项。`resolve_quota` 返回 `dict[str, int]`，Task 6 当 `quota` 消费。`ManifestInvalidError` 仅在 Task 7 使用，导入路径 `qmt_manifest` 与实际定义一致（`qmt_manifest.py:112`）。

**4. 已知的计划自身风险**：`run_mut` 这个 shell 函数定义在 Task 3 Step 5，后续 task 在**新开的 shell** 里用它会「command not found」——两处用到的地方都已就地加了提醒。
