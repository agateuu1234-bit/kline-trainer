# Plan 1b — M0.2 REST API 契约冻结 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 冻结 M0.2 REST API 契约——`backend/openapi.yaml` 完整 OpenAPI 3.0 文档（3 个 endpoint + LeaseResponse/TrainingSetMetaItem/ErrorResponse schema），配套 pytest 断言 + GitHub Actions CI validation。

**Architecture:** 两层验证延续 Plan 1 pattern：
- **本地快速迭代**：pytest 调用 `openapi-spec-validator` 做 OpenAPI 3.0 语法校验 + 手写 contract invariants 断言（必带 lease_id、409/404、content_hash `^[0-9a-f]{8}$` pattern 等）
- **CI 运行时验证**：新建 `.github/workflows/openapi-smoke.yml`，跑同一套 pytest（runtime 一致性证明）。单 job，ubuntu-latest + python3 + read-only permissions + pinned checkout SHA（匹配 Plan 1 R10 trust-boundary hardening）

**Tech Stack:** OpenAPI 3.0.3 / Python 3 + pytest + openapi-spec-validator + PyYAML / GitHub Actions。

**依赖**：Plan 1（M0.1 DDL）已 merged（PR #23）。Plan 1b 不依赖 Plan 1 文件，但在 `content_hash CHAR(8) 小写 hex` 格式上与 M0.1 对齐（schema.sql `ck_content_hash_crc32_lowercase` CHECK 约束）。

**Scope 说明**：
- 本 plan 只冻结 **REST 协议契约**（OpenAPI YAML），**不实现**后端逻辑（FastAPI routes、lease 状态机、schedulers 等——那些是 Plan 2 B3/B4 scope）
- 本 plan 不影响 Plan 1 merged artifacts
- Task 数比 Plan 1 少（无 §15.3 template 补齐 Task，那已在 Plan 1 完成）

**产出位置汇总**：

| 文件 | 类型 | 对应 spec |
|---|---|---|
| `backend/openapi.yaml` | OpenAPI 3.0 契约 | M0.2 |
| `backend/tests/test_openapi.py` | pytest + openapi-spec-validator | M0.2 验收 |
| `backend/requirements-dev.txt` | 追加 `openapi-spec-validator>=0.7` 和 `pyyaml>=6` | M0.2 验收 |
| `.github/workflows/openapi-smoke.yml` | CI 语法+不变量验证 | M0.2 CI 验收 |
| `scripts/acceptance/plan_1b_m0_2_rest_api.sh` | 聚合验收脚本 | 本 plan 验收 |

**Plan 1 R11 residual 复用说明**：Plan 1 round 11 的 HIGH finding（journal state CHECK）已登记延至 Plan 3 (P2/P4 模块) 落地，不影响本 plan scope。

---

## Task 1: OpenAPI 3.0 文档 + requirements-dev.txt 追加 + pytest 断言

**Files:**
- Create: `backend/openapi.yaml`
- Modify: `backend/requirements-dev.txt`（追加 `openapi-spec-validator>=0.7` 和 `pyyaml>=6`）
- Create: `backend/tests/test_openapi.py`

**背景**：对应 spec M0.2 + `kline_trainer_plan_v1.5.md` §8.1。3 个 endpoint + 3 条关键契约约束（confirm 必带 lease_id / confirm 幂等 / Lease TTL=10 分钟 / Unix 秒 UTC）。`content_hash` 字段格式 `^[0-9a-f]{8}$`（8 字符小写 hex CRC32），与 Plan 1 PostgreSQL `ck_content_hash_crc32_lowercase` CHECK 对齐。

- [ ] **Step 1: 写入 `backend/openapi.yaml`**

```yaml
openapi: 3.0.3
info:
  title: Kline Trainer API
  version: "1.4"
  description: >
    Kline Trainer 后端 REST API。Lease-based 训练组预占-下载-确认流程。
    详见 kline_trainer_modules_v1.4.md §M0.2 及 kline_trainer_plan_v1.5.md §8.1。
servers:
  - url: http://{host}:{port}
    variables:
      host:
        default: localhost
      port:
        default: "8000"

paths:
  /training-sets/meta:
    get:
      summary: 批量预占 N 个 training set
      operationId: reserveTrainingSets
      parameters:
        - in: query
          name: count
          required: true
          schema:
            type: integer
            minimum: 1
            maximum: 100
          description: 预占数量
      responses:
        '200':
          description: 预占成功；标记这 N 个 training set 为 reserved 状态（lease TTL=10 分钟）
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaseResponse'

  /training-set/{id}/download:
    get:
      summary: 下载已预占的 training set zip
      operationId: downloadTrainingSet
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: zip 文件流
          headers:
            Content-MD5:
              description: zip 文件 MD5 hash（客户端可二次校验）
              schema:
                type: string
          content:
            application/zip:
              schema:
                type: string
                format: binary

  /training-set/{id}/confirm:
    post:
      summary: 确认已下载 training set，标记为 sent；幂等
      operationId: confirmTrainingSet
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
        - in: query
          name: lease_id
          required: true
          schema:
            type: string
            format: uuid
          description: 必须匹配 /training-sets/meta 返回的 lease_id
      responses:
        '200':
          description: 确认成功（幂等：同一 (id, lease_id) 重复调用仍返回 200）
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required: [ok]
        '409':
          description: lease 过期或 lease_id 不匹配
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: training set 不存在
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

components:
  schemas:
    LeaseResponse:
      type: object
      required: [lease_id, expires_at, sets]
      properties:
        lease_id:
          type: string
          format: uuid
          description: UUID v4
        expires_at:
          type: string
          format: date-time
          description: ISO8601 UTC；Lease TTL = 10 分钟
        sets:
          type: array
          items:
            $ref: '#/components/schemas/TrainingSetMetaItem'

    TrainingSetMetaItem:
      type: object
      required: [id, stock_code, stock_name, filename, schema_version, content_hash]
      properties:
        id:
          type: integer
        stock_code:
          type: string
        stock_name:
          type: string
        filename:
          type: string
        schema_version:
          type: integer
        content_hash:
          type: string
          minLength: 8
          maxLength: 8
          pattern: '^[0-9a-f]{8}$'
          description: zip 文件 CRC32 十六进制（8 字符小写；与 PostgreSQL ck_content_hash_crc32_lowercase CHECK 对齐）

    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
          enum: [lease_expired, not_found]
```

- [ ] **Step 2: 追加依赖到 `backend/requirements-dev.txt`**

当前文件内容：

```
pytest>=8.0
httpx>=0.27
pglast>=6,<8
```

改为：

```
pytest>=8.0
httpx>=0.27
pglast>=6,<8
openapi-spec-validator>=0.7
pyyaml>=6
```

然后安装：

```bash
cd backend && python3 -m pip install -r requirements-dev.txt && cd ..
```

Expected: `openapi-spec-validator` 和 `pyyaml` 成功安装；`python3 -c "import openapi_spec_validator, yaml; print('ok')"` 输出 `ok`。

- [ ] **Step 3: 写 pytest 断言**

写入 `backend/tests/test_openapi.py`：

```python
"""Validate backend/openapi.yaml against OpenAPI 3.0 spec + M0.2 contract invariants.

Local layer of the two-layer validation (see Plan 1b Architecture).
CI layer in .github/workflows/openapi-smoke.yml runs the same tests on Ubuntu.
"""
from __future__ import annotations

from pathlib import Path

import yaml
from openapi_spec_validator import validate

SPEC_PATH = Path(__file__).parent.parent / "openapi.yaml"


def _load_spec() -> dict:
    with SPEC_PATH.open("r", encoding="utf-8") as f:
        return yaml.safe_load(f)


def test_openapi_yaml_is_valid_openapi3():
    """Spec must pass official OpenAPI 3.0 schema validation."""
    validate(_load_spec())


def test_three_endpoints_present():
    """M0.2 requires exactly these 3 paths (meta / download / confirm)."""
    spec = _load_spec()
    paths = set(spec["paths"].keys())
    assert paths == {
        "/training-sets/meta",
        "/training-set/{id}/download",
        "/training-set/{id}/confirm",
    }, f"unexpected paths: {paths}"


def test_meta_count_query_has_bounds():
    """count parameter must be 1..100 (spec says N is arbitrary but sane bounds required)."""
    spec = _load_spec()
    params = spec["paths"]["/training-sets/meta"]["get"]["parameters"]
    count_param = next(p for p in params if p["name"] == "count")
    assert count_param["required"] is True
    assert count_param["schema"]["minimum"] == 1
    assert count_param["schema"]["maximum"] == 100


def test_confirm_requires_lease_id_as_uuid():
    """M0.2 关键契约约束 1: confirm 必带 lease_id；spec 要求 UUID v4 format."""
    spec = _load_spec()
    params = spec["paths"]["/training-set/{id}/confirm"]["post"]["parameters"]
    lease_param = next((p for p in params if p["name"] == "lease_id"), None)
    assert lease_param is not None, "lease_id parameter missing"
    assert lease_param["required"] is True
    assert lease_param["schema"]["format"] == "uuid"


def test_confirm_returns_200_409_404():
    """M0.2 confirm 三种响应：200 成功 / 409 lease 过期 / 404 不存在。"""
    spec = _load_spec()
    responses = spec["paths"]["/training-set/{id}/confirm"]["post"]["responses"]
    assert set(responses.keys()) == {"200", "409", "404"}


def test_confirm_200_has_ok_boolean():
    """200 response body = { ok: true }."""
    spec = _load_spec()
    ok_prop = (
        spec["paths"]["/training-set/{id}/confirm"]["post"]["responses"]["200"]
        ["content"]["application/json"]["schema"]["properties"]["ok"]
    )
    assert ok_prop["type"] == "boolean"


def test_content_hash_pattern_matches_sql_check():
    """content_hash 必须 `^[0-9a-f]{8}$`（与 Plan 1 PostgreSQL ck_content_hash_crc32_lowercase 对齐）."""
    spec = _load_spec()
    ch = spec["components"]["schemas"]["TrainingSetMetaItem"]["properties"]["content_hash"]
    assert ch["minLength"] == 8
    assert ch["maxLength"] == 8
    assert ch["pattern"] == r"^[0-9a-f]{8}$"


def test_error_response_enum_is_exact():
    """ErrorResponse.error enum 只含 lease_expired / not_found。"""
    spec = _load_spec()
    err_enum = spec["components"]["schemas"]["ErrorResponse"]["properties"]["error"]["enum"]
    assert set(err_enum) == {"lease_expired", "not_found"}


def test_lease_response_required_fields():
    """LeaseResponse 必含 lease_id / expires_at / sets 三字段。"""
    spec = _load_spec()
    lr = spec["components"]["schemas"]["LeaseResponse"]
    assert set(lr["required"]) == {"lease_id", "expires_at", "sets"}
    assert lr["properties"]["lease_id"]["format"] == "uuid"
    assert lr["properties"]["expires_at"]["format"] == "date-time"


def test_training_set_meta_item_required_fields():
    """TrainingSetMetaItem 必含 id/stock_code/stock_name/filename/schema_version/content_hash。"""
    spec = _load_spec()
    item = spec["components"]["schemas"]["TrainingSetMetaItem"]
    assert set(item["required"]) == {
        "id", "stock_code", "stock_name",
        "filename", "schema_version", "content_hash",
    }


def test_download_has_content_md5_header():
    """M0.2 download 响应带 Content-MD5 头（客户端二次校验）."""
    spec = _load_spec()
    headers = (
        spec["paths"]["/training-set/{id}/download"]["get"]["responses"]["200"]["headers"]
    )
    assert "Content-MD5" in headers
```

- [ ] **Step 4: 运行失败路径（sanity check）**

```bash
cp backend/openapi.yaml /tmp/openapi.yaml.bak
python3 -c "
import yaml
with open('backend/openapi.yaml') as f: spec = yaml.safe_load(f)
# 故意删除 confirm 的 lease_id 参数
params = spec['paths']['/training-set/{id}/confirm']['post']['parameters']
spec['paths']['/training-set/{id}/confirm']['post']['parameters'] = [p for p in params if p['name'] != 'lease_id']
with open('backend/openapi.yaml', 'w') as f: yaml.dump(spec, f)
"
cd backend && python3 -m pytest tests/test_openapi.py::test_confirm_requires_lease_id_as_uuid -v; RC=$?; cd ..
mv /tmp/openapi.yaml.bak backend/openapi.yaml
[[ $RC -ne 0 ]] && echo "FAIL expected"
```

Expected: `test_confirm_requires_lease_id_as_uuid` FAILED；shell 打印 `FAIL expected`。

- [ ] **Step 5: 运行通过路径**

```bash
cd backend && python3 -m pytest tests/test_openapi.py -v && cd ..
```

Expected: 11 passed；末尾 `11 passed in X.XXs`。

- [ ] **Step 6: 提交**

```bash
git add backend/openapi.yaml backend/requirements-dev.txt backend/tests/test_openapi.py
git commit -m "feat(m0.2): OpenAPI 3.0 REST contract v1.4 + pytest invariants

11 contract invariants covered:
- 3 endpoints exact set (meta/download/confirm)
- count 1..100 bounds
- confirm lease_id UUID required
- 200/409/404 response set
- 200 ok:boolean body shape
- content_hash pattern aligned with PG ck_content_hash_crc32_lowercase
- ErrorResponse enum = {lease_expired, not_found}
- LeaseResponse/TrainingSetMetaItem required fields
- Content-MD5 header on download"
```

---

## Task 2: GitHub Actions CI workflow（OpenAPI 验证）

**Files:**
- Create: `.github/workflows/openapi-smoke.yml`

**背景（trust-boundary 对齐 Plan 1 R10）**：单 job workflow，runs-on ubuntu-latest + `permissions: contents: read`（least privilege）+ `actions/checkout` pinned to SHA（与 codeowners-config-check.yml / schema-smoke.yml 同 SHA）。

**为何独立 workflow（不合并进 schema-smoke.yml）**：
- Path 触发可精细化：openapi 改动只跑 openapi-smoke，不拖累 schema-smoke 起 postgres container
- Workflow 职责清晰（OpenAPI vs DDL）

- [ ] **Step 1: 写入 `.github/workflows/openapi-smoke.yml`**

> **注意**：`.github/workflows/` 对 Claude Edit 工具 deny，需**在你自己终端**手动创建文件。内容如下：

```yaml
name: OpenAPI Smoke Test

on:
  pull_request:
    paths:
      - 'backend/openapi.yaml'
      - 'backend/tests/test_openapi.py'
      - 'backend/requirements-dev.txt'
      - '.github/workflows/openapi-smoke.yml'
  push:
    branches: [main]

# Trust-boundary hardening（对齐 Plan 1 R10 + project convention）
# - Least-privilege token: read-only repo contents
# - actions/checkout pinned to full SHA
permissions:
  contents: read

jobs:
  openapi-validate:
    name: OpenAPI 3.0 spec + contract invariants
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Setup Python 3.11
        uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b  # v5.3.0
        with:
          python-version: '3.11'
      - name: Install dev deps
        working-directory: backend
        run: python -m pip install -r requirements-dev.txt
      - name: Run OpenAPI pytest
        working-directory: backend
        run: python -m pytest tests/test_openapi.py -v
```

- [ ] **Step 2: 本地 YAML validity check**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/openapi-smoke.yml'))" && echo OK
```

Expected: 输出 `OK`，退出码 0。

- [ ] **Step 3: 失败路径 sanity check**

```bash
cp .github/workflows/openapi-smoke.yml /tmp/oss.yml.bak
echo "broken: yaml: here:" > .github/workflows/openapi-smoke.yml
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/openapi-smoke.yml'))" 2>&1 | tail -3 || echo "YAML FAIL expected"
mv /tmp/oss.yml.bak .github/workflows/openapi-smoke.yml
```

Expected: PyYAML 抛 parser error；shell 打印 `YAML FAIL expected`。

- [ ] **Step 4: 真 CI 验证留给 PR 阶段**

本地 PASS ≡ "文件存在 + YAML 合法 + permissions 设对 + SHA pinned"。真正的 GH Actions 执行结果只有在 push + PR 后能看到（验收项 3 涉及）。

- [ ] **Step 5: 提交**

```bash
git add .github/workflows/openapi-smoke.yml
git commit -m "feat(plan-1b): CI openapi-smoke workflow (OpenAPI 3.0 + pytest invariants)

Separate workflow from schema-smoke.yml for path-trigger precision + cleaner
concerns. Follows project convention: permissions=contents:read + actions
pinned to full SHA (alignment with Plan 1 R10 trust-boundary hardening)."
```

---

## Task 3: 聚合 acceptance 脚本

**Files:**
- Create: `scripts/acceptance/plan_1b_m0_2_rest_api.sh`

**背景**：对齐 Plan 1 的 `plan_1_m0_1_db_schema.sh` 模式，单脚本一键验收 M0.2 全部交付物。

- [ ] **Step 1: 写聚合脚本**

写入 `scripts/acceptance/plan_1b_m0_2_rest_api.sh`：

```bash
#!/usr/bin/env bash
# Plan 1b 聚合验收：M0.2 REST API 契约
# 涵盖：openapi.yaml 语法 + 11 条契约不变量 pytest + CI workflow 存在
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO_ROOT"

PASS=0
FAIL=0
declare -a FAILED

run() {
  local label="$1"; shift
  echo ""
  echo "========== $label =========="
  if "$@"; then
    echo "OK: $label"
    PASS=$((PASS + 1))
  else
    echo "NG: $label"
    FAIL=$((FAIL + 1))
    FAILED+=("$label")
  fi
}

# ---- 文件存在性 ----
run "file: openapi.yaml exists" test -s backend/openapi.yaml
run "file: test_openapi.py exists" test -s backend/tests/test_openapi.py
run "file: openapi-smoke.yml exists" test -s .github/workflows/openapi-smoke.yml

# ---- 契约测试 ----
run "pytest: 11 OpenAPI invariants" \
    bash -c "cd backend && python3 -m pytest tests/test_openapi.py -q"

# ---- CI workflow YAML 合法 ----
run "yaml: openapi-smoke.yml parse" \
    python3 -c "import yaml; yaml.safe_load(open('.github/workflows/openapi-smoke.yml'))"

# ---- 汇总 ----
echo ""
echo "============================================"
echo "Plan 1b (M0.2 REST API) acceptance: $PASS passed, $FAIL failed"
if [[ $FAIL -gt 0 ]]; then
  echo "Failed items:"
  for f in "${FAILED[@]}"; do echo "  - $f"; done
  echo "PLAN 1b FAIL"
  exit 1
fi
echo "PLAN 1b PASS"
```

然后 `chmod +x scripts/acceptance/plan_1b_m0_2_rest_api.sh`。

- [ ] **Step 2: 运行通过路径**

```bash
./scripts/acceptance/plan_1b_m0_2_rest_api.sh
```

Expected: 末行 `Plan 1b (M0.2 REST API) acceptance: 5 passed, 0 failed` + `PLAN 1b PASS`；退出码 0。

- [ ] **Step 3: 运行失败路径（sanity check）**

```bash
cp backend/openapi.yaml /tmp/openapi.yaml.bak
: > backend/openapi.yaml
./scripts/acceptance/plan_1b_m0_2_rest_api.sh || echo "PLAN 1b FAIL expected"
mv /tmp/openapi.yaml.bak backend/openapi.yaml
```

Expected: 多条 NG（file 空 / pytest 失败）；shell 打印 `PLAN 1b FAIL expected`。

- [ ] **Step 4: 最终通过确认**

```bash
./scripts/acceptance/plan_1b_m0_2_rest_api.sh
```

Expected: `PLAN 1b PASS` 再次出现。

- [ ] **Step 5: 提交**

```bash
git add scripts/acceptance/plan_1b_m0_2_rest_api.sh
git commit -m "feat(plan-1b): aggregate M0.2 REST API acceptance script"
```

---

## 非 coder 验收清单（CLAUDE.md 治理 §2 / workflow-rules.json verification_template）

按 `.claude/workflow-rules.json` 的 `verification_template` 要求：中文 / action-expected-pass_fail；禁用"验证通过即可 / 看起来正常 / 应该没问题 / should work / looks fine"。

### 验收项 1：聚合脚本整体通过

| | 内容 |
|---|---|
| Action | 在仓库根目录终端运行 `./scripts/acceptance/plan_1b_m0_2_rest_api.sh` |
| Expected | 末尾依次出现 `Plan 1b (M0.2 REST API) acceptance: 5 passed, 0 failed` 和 `PLAN 1b PASS` |
| Pass / Fail | 两行同时满足 = PASS；任一不满足 = FAIL |

### 验收项 2：OpenAPI 11 条契约不变量 pytest 全绿

| | 内容 |
|---|---|
| Action | 运行 `cd backend && python3 -m pytest tests/test_openapi.py -v && cd ..` |
| Expected | 末尾 `11 passed`，无 FAILED 或 error |
| Pass / Fail | `11 passed` 且无 FAILED/error = PASS；其它 = FAIL |

### 验收项 3：CI openapi-smoke 在 PR 上真实 PASS

| | 内容 |
|---|---|
| Action | PR 开出后在 PR 页面 Checks tab 查看；或 `gh pr checks <PR_NUM>` |
| Expected | `openapi-validate` job 状态为 `✓ Success` |
| Pass / Fail | Success = PASS；failure / skipped / pending 超 5 分钟 = FAIL |

### 验收项 4：content_hash 格式与 Plan 1 DB 契约对齐

| | 内容 |
|---|---|
| Action | 运行 `grep -E "'\^\[0-9a-f\]\{8\}\$'" backend/openapi.yaml backend/sql/schema.sql` |
| Expected | 两个文件都匹配到这个 pattern（openapi.yaml 的 content_hash pattern + schema.sql 的 ck_content_hash_crc32_lowercase CHECK 表达式） |
| Pass / Fail | 两文件都有 = PASS；任一缺 = FAIL（表明 M0.1 / M0.2 契约漂移） |

### 验收项 5：提交记录完整

| | 内容 |
|---|---|
| Action | 运行 `git log --oneline plan-1b/m0.2-openapi ^origin/main \| wc -l` |
| Expected | 输出数字 ≥ 3（Task 1 / Task 2 / Task 3 各一条业务 commit；plan draft/fix commits 已先行入 branch） |
| Pass / Fail | ≥ 3 = PASS；< 3 = FAIL |

---

## Self-Review（起草后自查）

1. **Spec 覆盖（仅 M0.2）**：
   - 3 个 endpoint（meta / download / confirm）→ Task 1 Step 1 openapi.yaml ✅
   - LeaseResponse + TrainingSetMetaItem + ErrorResponse schemas → Task 1 Step 1 ✅
   - Content-MD5 header（v1.5 §8.1 细节）→ Task 1 Step 1 + pytest assertion ✅
   - 4 条关键契约约束（confirm 必带 lease_id / confirm 幂等 / Lease TTL=10 分钟 / Unix 秒 UTC）→ pytest 覆盖前 2 条 + OpenAPI description 覆盖后 2 条（幂等性 + TTL 为运行期行为，不能纯靠 OpenAPI schema 表达；留给 Plan 2 B3 FastAPI 实现时验证）
   - CI runtime 验证 → Task 2 openapi-smoke.yml ✅
   - 聚合验收 → Task 3 ✅

2. **范围外确认**（不在 Plan 1b scope）：
   - B3 FastAPI routes 实现 → Plan 2
   - lease 状态机业务逻辑 → Plan 2
   - APScheduler（定时回滚 reserved） → Plan 2
   - Swift APIClient（P1 模块消费 OpenAPI）→ Plan 3（P 层）

3. **Placeholder 扫描**：
   - 无 TBD / TODO
   - 前向引用 "Plan 2 B3" / "Plan 3 P1" 是真实依赖，非占位符

4. **类型 / 字段一致性**：
   - `content_hash ^[0-9a-f]{8}$`：openapi.yaml `pattern` + Plan 1 schema.sql `ck_content_hash_crc32_lowercase` CHECK 表达式一致（验收项 4 明确 grep 检查）
   - `lease_id UUID v4`：openapi.yaml `format: uuid` + pytest 断言 + PostgreSQL `lease_id UUID NULL` 字段类型一致
   - `expires_at ISO8601 UTC`：openapi.yaml `format: date-time`；spec M0.2 明文要求
   - `ErrorResponse.error enum [lease_expired, not_found]`：openapi.yaml + pytest 一致
   - 聚合脚本引用的测试路径 `backend/tests/test_openapi.py` 与 Task 1 创建的一致

5. **forbidden_phrases 扫描**：验收清单 / 步骤 expected 全部 "输出 XXX" / "退出码 0" 等具体判据，无禁用措辞

6. **CI trust-boundary**（对齐 Plan 1 R10）：
   - `permissions: contents: read` ✅
   - `actions/checkout@11bd71...683` 全 SHA 匹配 codeowners-config-check.yml ✅
   - `actions/setup-python@0b93645...b` 全 SHA（v5.3.0）——新增的 action，选最近稳定 tag 的 SHA
   - 路径触发：仅 4 个 openapi-相关 path，不跨 scope

7. **大小对照**：plan ~480 行 / ~22 KB，在 codex branch-diff 256 KB 预算内；远小于 Plan 1 的 48 KB

8. **依赖链**：
   - Plan 1b → 无前置 plan 依赖（但与 Plan 1 的 content_hash 格式对齐）
   - 本 plan 被：Plan 2 (B3 FastAPI 实现)、Plan 3 (P1 APIClient 实现) 引用

9. **Codex review 期望**：本 plan scope 紧、文件少（3 产出 + 2 测试），预计 1-2 轮可收敛。主要风险点：
   - 可能有 "缺 CI runtime 验证 confirm 幂等性" 类 finding（真运行时行为，不是 schema 可表达；应答：留给 Plan 2 B3 实现+集成测试）
   - 可能有 "openapi 缺 400/500 error 响应" 类 finding（spec 明确只定义 409/404）

---

## 执行切换提示

Plan 1b 推荐执行路径（scope 小，直接 subagent-driven）：

1. 分支 `plan-1b/m0.2-openapi` 已存在（本 plan 起草过程中创建）。
2. `superpowers:subagent-driven-development` 派 subagent 串行（workflow-rules 明禁并行 implementation subagent）：
   - Task 1（openapi.yaml + requirements-dev.txt + test_openapi.py）
   - Task 2（openapi-smoke.yml —— 用户终端手写，Claude 不能 Edit `.github/workflows/`）
   - Task 3（聚合 acceptance 脚本）
3. 每 subagent 内部走 TDD（先 fail-path 证测试有效，再实现通过）
4. 每 Task 后双层 review（spec + code-quality）
5. 全部通过 → `verification-before-completion` → `requesting-code-review` → push + PR
