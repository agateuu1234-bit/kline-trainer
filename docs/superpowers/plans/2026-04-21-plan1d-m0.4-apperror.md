# Plan 1d — M0.4 AppError 契约冻结 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 冻结 M0.4 `AppError` 顶层错误枚举 + 4 个 `Reason` 子枚举（Network/Persistence/Trade/TrainingSet）+ `userMessage` / `isRecoverable` / `shouldShowToast` 扩展。全部 `Error, Equatable, Sendable`。落在已有 `ios/Contracts/` SwiftPM 包（Plan 1c 交付）内，新增 1 个源文件 + 1 个测试文件。

**Architecture:**
- 值类型契约（枚举 + associated value），纯值语义，无状态、无副作用
- 扩展方法 `userMessage` 返回中文用户可读文案；`isRecoverable` 返回重试是否可能恢复；`shouldShowToast` 控制 UI 层是否弹 Toast
- 与 Plan 1c 同一 SwiftPM 包，`swift test` CLI 跑；不需要新 CI workflow（复用已有的 `.github/workflows/swift-contracts-smoke.yml`）
- **两层验证延续 Plan 1/1b/1c pattern**：本地 `swift test` + CI `swift-contracts-smoke.yml` runtime

**Tech Stack:** Swift 6.0+ / Swift Testing / SwiftPM（已有）/ 复用 `.github/workflows/swift-contracts-smoke.yml`（已 pin SHA + read-only permissions）。

**依赖（hard prereq）**：
- Plan 1（M0.1 DDL）已 merged（PR #23）
- Plan 1b（M0.2 OpenAPI）已 merged（PR #24）
- Plan 1c（M0.3 Swift 数据模型）已 merged（PR #25）—— 本 plan 在 Plan 1c 包内扩展，**必须基于含 `ios/Contracts/` 的 main**

## Scope 说明

- **scope**：AppError 类型体 + userMessage/isRecoverable/shouldShowToast 3 个 public extension method
- **不包含**：
  - M0.5 并发约定文档 → Plan 1e
  - M0.4 §"翻译规则表"（P1/P3/P4/E3/P2/UI 各模块如何转 AppError）属模块实现约束，非 AppError 类型层 → 归属 Plan 2/3 各模块实施，**但有强制 gate**（见下）
  - M0.4 禁止清单的 CI lint 规则（禁止随意 `.internalError` 等）→ 未来 governance 层或模块测试时实现

**M0.4 trust-boundary enforcement gate（codex round 1 MEDIUM finding 回应）**：

codex 指出"单拆 AppError 类型层不含翻译规则 = 后续模块可以悄悄绕过"。为防止此事，**任何 Plan 2 / Plan 3 消费 AppError 的模块 PR**必须满足 2 条强制 gate（本 plan 在此登记，Plan 2/3 plan 时作为前置 prereq）：

1. **每个模块必须有一个 test 断言 "公开 API 只抛 AppError"**：例如 P1 APIClient 的 test 文件需含 `func testApiMethodsThrowAppErrorOnly()` 枚举所有 public async throws 方法 + 断言 catch 类型是 AppError。grep 级 lint 不足以；必须是 public API surface 的编译/运行期检查。
2. **每个模块 acceptance 脚本必须 grep 排查内部错误类型泄露**：例如 B3/P1 模块：`grep -rE "(DatabaseError|URLError|APIError)" Sources/<Module>/ --include='*.swift'` 返回空（或仅在 `internal` / `fileprivate` 作用域——通过 `-B2` / `-A2` 上下文验证）。

**登记 memory**：`project_m04_translation_gate.md`（本 plan 新写）明文把上述 2 条绑到 Plan 2 B3 / Plan 3 P1/P2/P3/P4 的 PR hard prereq。Plan 1d 不亲自实施（AppError 类型层不需要），但也不 silently 跳 —— 明确把 baton 交下去。
- **不 CI 新增 workflow**：`swift-contracts-smoke.yml` path trigger 已含 `ios/Contracts/**`，本 plan 新增文件自动触发 CI 跑 `swift test`
- **不包**：Xcode 工程集成，等 Plan 2/3 阶段再做

## Brainstorming 显式 skip（per memory `feedback_skill_pipeline_3_gaps.md`）

本 plan skip `superpowers:brainstorming` stage，理由：
- spec M0.4 冻结于 `kline_trainer_modules_v1.4.md` 第 588-654 行，类型体 + Reason 枚举 + extension 方法全部逐字给出
- spec 已过 4 轮 codex + Cloud 反评审 + 56 项修订（memory `project_modules_v1.4_frozen.md`）
- 再跑 brainstorming 无额外价值，会触发 spec 冻结红线
- 按 `feedback_skill_pipeline_3_gaps.md` 规则 #3："spec-frozen 例外合理，但必须在 plan doc 里显式注记"

## 产出位置汇总

| 文件 | 类型 | 对应 spec |
|---|---|---|
| `ios/Contracts/Sources/KlineTrainerContracts/AppError.swift` | AppError + 4 Reason + 3 extension methods | M0.4 逐字 |
| `ios/Contracts/Tests/KlineTrainerContractsTests/AppErrorTests.swift` | Equatable / Sendable / userMessage / isRecoverable / shouldShowToast 测试 | M0.4 验收 |
| `scripts/acceptance/plan_1d_m0_4_apperror.sh` | 聚合验收 | 本 plan 验收 |

## 后续 plan 占位（backlog）

- Plan 1e — M0.5 并发约定文档
- Plan 1f — M0.1 CONTRACT_VERSION 矩阵 + migration rollback + R11 journal CHECK
- Plan 2 — B1/B2/B3/B4 后端（含 B3 meta 库存不足响应 / B2 训练组 UNIQUE）
- Plan 3 — P1 APIClient / P2 DownloadAcceptance / P3a/b / P4 AppDB / P5/P6

---

## Task 1: AppError 类型体 + 4 Reason 枚举（基础 + 失败测试）

**Files:**
- Create: `ios/Contracts/Sources/KlineTrainerContracts/AppError.swift`（骨架；extension 在 Task 2 落）
- Create: `ios/Contracts/Tests/KlineTrainerContractsTests/AppErrorTests.swift`（骨架；仅 Equatable 和 Sendable 测试）

**背景**：spec M0.4 核心类型层。AppError 5 cases（network / persistence / trade / trainingSet / internalError）+ 4 Reason 枚举（NetworkReason/PersistenceReason/TradeReason/TrainingSetReason）+ 所有值 `Error, Equatable, Sendable`。先建类型 + 基础 Equatable 测试，让 Task 2 的 extension 方法测试有依赖。

- [ ] **Step 1: 写 failing test（Equatable + 5 AppError cases）**

写入 `ios/Contracts/Tests/KlineTrainerContractsTests/AppErrorTests.swift`：

```swift
import Testing
import Foundation
@testable import KlineTrainerContracts

@Suite("AppError Equatable")
struct AppErrorEquatableTests {
    @Test func network_leaseExpired_vs_leaseNotFound_areDistinct() {
        let a: AppError = .network(.leaseExpired)
        let b: AppError = .network(.leaseNotFound)
        #expect(a != b)
    }

    @Test func trainingSet_versionMismatch_comparesAssociatedValues() {
        let a: AppError = .trainingSet(.versionMismatch(expected: 1, got: 2))
        let b: AppError = .trainingSet(.versionMismatch(expected: 1, got: 2))
        let c: AppError = .trainingSet(.versionMismatch(expected: 1, got: 3))
        #expect(a == b)
        #expect(a != c)
    }

    @Test func persistence_schemaMismatch_associatedValues() {
        let a: AppError = .persistence(.schemaMismatch(expected: 1, got: 2))
        let b: AppError = .persistence(.schemaMismatch(expected: 1, got: 3))
        #expect(a != b)
    }

    @Test func internalError_comparesModuleAndDetail() {
        let a = AppError.internalError(module: "P2", detail: "x")
        let b = AppError.internalError(module: "P2", detail: "x")
        let c = AppError.internalError(module: "P3", detail: "x")
        let d = AppError.internalError(module: "P2", detail: "y")
        #expect(a == b)
        #expect(a != c)
        #expect(a != d)
    }

    @Test func persistence_ioError_associatedString() {
        let a: AppError = .persistence(.ioError("disk"))
        let b: AppError = .persistence(.ioError("disk"))
        let c: AppError = .persistence(.ioError("net"))
        #expect(a == b)
        #expect(a != c)
    }
}

@Suite("AppError Sendable conformance")
struct AppErrorSendableTests {
    /// 编译时检查：AppError 及所有 Reason 必须 Sendable（跨 actor 传递）。
    /// 若任一类型缺 Sendable，下面的函数签名编译不过（@Sendable 闭包捕获必须 Sendable）。
    @Test func allTypes_sendableCompilesInActorClosure() async {
        let err: AppError = .network(.timeout)
        let nr: NetworkReason = .offline
        let pr: PersistenceReason = .diskFull
        let tr: TradeReason = .disabled
        let tsr: TrainingSetReason = .crcFailed

        // Capture each type in a @Sendable closure; compile = pass
        let _: @Sendable () -> Void = { _ = err }
        let _: @Sendable () -> Void = { _ = nr }
        let _: @Sendable () -> Void = { _ = pr }
        let _: @Sendable () -> Void = { _ = tr }
        let _: @Sendable () -> Void = { _ = tsr }
        #expect(true)
    }
}
```

- [ ] **Step 2: 跑测试（应失败：类型未定义）**

```bash
cd ios/Contracts && swift test 2>&1 | tail -15; cd ../..
```

Expected: 编译错误（`AppError` / `NetworkReason` / `PersistenceReason` / `TradeReason` / `TrainingSetReason` 未定义），退出码非 0。

- [ ] **Step 3: 写 AppError.swift 最小实现（只类型体，无 extension）**

写入 `ios/Contracts/Sources/KlineTrainerContracts/AppError.swift`：

```swift
// Kline Trainer Swift Contracts — M0.4 Error Handling
// Spec: kline_trainer_modules_v1.4.md §M0.4
//
// 顶层错误类型 AppError + 4 个 Reason 子枚举。全部值类型，Error + Equatable + Sendable。
// 设计原则（M0.4 spec 重写）：**私有错误在本模块边界内转 AppError，调用方只消费 AppError**。
// 各模块（P1/P3/P4/E3/P2/UI）如何翻译自己的内部错误到 AppError 属模块实现约束，归各自
// module plan（Plan 2/3）落地；本 plan 只冻结 AppError 类型体 + 3 个 UI 扩展方法。

import Foundation

public enum AppError: Error, Equatable, Sendable {
    case network(NetworkReason)
    case persistence(PersistenceReason)
    case trade(TradeReason)
    case trainingSet(TrainingSetReason)
    /// internalError 强制标识来源模块 + detail 消息。
    /// CI lint 规则（未来 governance 层实现）：`.internalError` 仅当错误无法归入前 4 类时允许。
    case internalError(module: String, detail: String)
}

public enum NetworkReason: Error, Equatable, Sendable {
    case timeout
    case offline
    case serverError(code: Int)
    case leaseExpired
    case leaseNotFound
}

public enum PersistenceReason: Error, Equatable, Sendable {
    case diskFull
    case dbCorrupted
    case schemaMismatch(expected: Int, got: Int)
    case ioError(String)
}

public enum TradeReason: Error, Equatable, Sendable {
    case insufficientCash
    case insufficientHolding
    case disabled
    case invalidShareCount
}

public enum TrainingSetReason: Error, Equatable, Sendable {
    case crcFailed
    case unzipFailed
    case emptyData
    case versionMismatch(expected: Int, got: Int)
    case fileNotFound
}
```

- [ ] **Step 4: 跑测试（应通过：Equatable + Sendable）**

```bash
cd ios/Contracts && swift test 2>&1 | tail -5; cd ../..
```

Expected: 累计 ≥ 31 tests passed（25 from Plan 1c + 6 new from this step），无 failed / error:。

- [ ] **Step 5: 提交**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/AppError.swift \
  ios/Contracts/Tests/KlineTrainerContractsTests/AppErrorTests.swift
git commit -m "feat(m0.4): AppError + 4 Reason enums (Equatable + Sendable)

Type bodies per spec M0.4 verbatim. 5 AppError cases with associated Reasons;
4 Reason enums (Network/Persistence/Trade/TrainingSet) per spec. All Error +
Equatable + Sendable. No extensions yet (userMessage/isRecoverable/shouldShowToast
in Task 2)."
```

---

## Task 2: AppError extensions（userMessage / isRecoverable / shouldShowToast）

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/AppError.swift`（append extension 块）
- Modify: `ios/Contracts/Tests/KlineTrainerContractsTests/AppErrorTests.swift`（append 3 个 Suite）

**背景**：3 个 extension method，UI 层消费。`userMessage` 中文用户文案；`isRecoverable` 重试是否可能恢复；`shouldShowToast` 是否弹 Toast（`trade(.disabled)` + `internalError` 返 false）。

- [ ] **Step 1: Append failing tests for userMessage**

在 `AppErrorTests.swift` 文件末尾追加：

```swift

@Suite("AppError userMessage")
struct AppErrorUserMessageTests {
    @Test func networkOffline_hasChineseMessage() {
        let m = AppError.network(.offline).userMessage
        #expect(m.contains("网络") || m.contains("连接"))
        #expect(!m.isEmpty)
    }

    @Test func allReasons_produceNonEmptyUserMessage() {
        let cases: [AppError] = [
            .network(.timeout),
            .network(.offline),
            .network(.serverError(code: 500)),
            .network(.leaseExpired),
            .network(.leaseNotFound),
            .persistence(.diskFull),
            .persistence(.dbCorrupted),
            .persistence(.schemaMismatch(expected: 1, got: 2)),
            .persistence(.ioError("x")),
            .trade(.insufficientCash),
            .trade(.insufficientHolding),
            .trade(.disabled),
            .trade(.invalidShareCount),
            .trainingSet(.crcFailed),
            .trainingSet(.unzipFailed),
            .trainingSet(.emptyData),
            .trainingSet(.versionMismatch(expected: 1, got: 2)),
            .trainingSet(.fileNotFound),
            .internalError(module: "M0", detail: "some"),
        ]
        for c in cases {
            let m = c.userMessage
            #expect(!m.isEmpty, "empty userMessage for \(c)")
        }
    }

    @Test func schemaMismatch_interpolatesNumbers() {
        let m = AppError.persistence(.schemaMismatch(expected: 3, got: 7)).userMessage
        #expect(m.contains("3") && m.contains("7"))
    }

    @Test func serverError_interpolatesCode() {
        let m = AppError.network(.serverError(code: 503)).userMessage
        #expect(m.contains("503"))
    }

    @Test func internalError_mentionsModule() {
        let m = AppError.internalError(module: "P4", detail: "xyz").userMessage
        #expect(m.contains("P4"))
    }

    /// codex round 1 MEDIUM finding：ioError raw detail 不应泄漏到 UI 文案。
    /// detail 可能含本地文件路径 / DB 底层错误 / 环境信息。
    @Test func ioError_rawDetailNotLeakedToUserMessage() {
        let sensitive = "/Users/maziming/Library/.../sensitive-path-SECRET-KEY"
        let m = AppError.persistence(.ioError(sensitive)).userMessage
        #expect(!m.contains(sensitive), "ioError raw detail leaked into userMessage")
        #expect(!m.contains("SECRET-KEY"))
        #expect(!m.contains("/Users/"))
        #expect(!m.isEmpty)  // 但仍要有通用提示
    }
}

@Suite("AppError isRecoverable")
struct AppErrorIsRecoverableTests {
    @Test func networkTransient_isRecoverable() {
        #expect(AppError.network(.timeout).isRecoverable == true)
        #expect(AppError.network(.offline).isRecoverable == true)
        #expect(AppError.network(.serverError(code: 500)).isRecoverable == true)
    }

    @Test func networkLeaseIssues_areNotRecoverable() {
        // lease 过期 / 不存在 = terminal（对当前 operation）。spec M0.2 confirm 409/404
        // 对应 P2 DownloadAcceptance 转 `rejected` 状态 + 清本地文件；重试同一 operation 无意义。
        // 客户端若要重来走更高层（P2 重新 /meta → 新 lease），不属于本 isRecoverable 契约。
        // codex round 1 [HIGH] finding 要求区分 transient 与 terminal。
        #expect(AppError.network(.leaseExpired).isRecoverable == false)
        #expect(AppError.network(.leaseNotFound).isRecoverable == false)
    }

    @Test func dbCorrupted_isNotRecoverable() {
        #expect(AppError.persistence(.dbCorrupted).isRecoverable == false)
    }

    @Test func persistenceIoErrors_areNotRecoverable() {
        #expect(AppError.persistence(.diskFull).isRecoverable == false)
        #expect(AppError.persistence(.ioError("x")).isRecoverable == false)
        #expect(AppError.persistence(.schemaMismatch(expected: 1, got: 2)).isRecoverable == false)
    }

    @Test func tradeErrors_areNotRecoverable() {
        #expect(AppError.trade(.insufficientCash).isRecoverable == false)
        #expect(AppError.trade(.insufficientHolding).isRecoverable == false)
        #expect(AppError.trade(.disabled).isRecoverable == false)
        #expect(AppError.trade(.invalidShareCount).isRecoverable == false)
    }

    @Test func trainingSetTransient_isRecoverable() {
        // crc 失败 / unzip 失败 / 数据为空 → 重下可恢复
        #expect(AppError.trainingSet(.crcFailed).isRecoverable == true)
        #expect(AppError.trainingSet(.unzipFailed).isRecoverable == true)
        #expect(AppError.trainingSet(.emptyData).isRecoverable == true)
    }

    @Test func trainingSetHardFailures_areNotRecoverable() {
        // version 不匹配 / 文件没找到 → 客户端版本不够 / 服务端清理了，重试无意义
        #expect(AppError.trainingSet(.versionMismatch(expected: 1, got: 2)).isRecoverable == false)
        #expect(AppError.trainingSet(.fileNotFound).isRecoverable == false)
    }

    @Test func internalError_isNotRecoverable() {
        #expect(AppError.internalError(module: "M", detail: "d").isRecoverable == false)
    }
}

@Suite("AppError shouldShowToast")
struct AppErrorShouldShowToastTests {
    @Test func tradeDisabled_doesNotShowToast() {
        // 按钮禁用态自然呈现，不打 Toast
        #expect(AppError.trade(.disabled).shouldShowToast == false)
    }

    @Test func internalError_doesNotShowToast() {
        // 走 debug log，不打扰用户
        #expect(AppError.internalError(module: "M", detail: "d").shouldShowToast == false)
    }

    @Test func networkOffline_showsToast() {
        #expect(AppError.network(.offline).shouldShowToast == true)
    }

    @Test func otherErrors_showToast() {
        // 默认除 trade.disabled + internalError 外都 Toast
        #expect(AppError.network(.timeout).shouldShowToast == true)
        #expect(AppError.persistence(.dbCorrupted).shouldShowToast == true)
        #expect(AppError.trade(.insufficientCash).shouldShowToast == true)
        #expect(AppError.trainingSet(.crcFailed).shouldShowToast == true)
    }
}
```

- [ ] **Step 2: 跑测试（应失败：extensions 未实现）**

```bash
cd ios/Contracts && swift test 2>&1 | tail -15; cd ../..
```

Expected: 编译错误（`userMessage` / `isRecoverable` / `shouldShowToast` 未实现），退出码非 0。

- [ ] **Step 3: Append extension 到 AppError.swift**

在 `ios/Contracts/Sources/KlineTrainerContracts/AppError.swift` 文件末尾追加：

```swift

// MARK: - UI / Recovery Extensions

public extension AppError {
    /// 用户可见文案（中文）。UI 层通过 Toast / Alert 展示。
    var userMessage: String {
        switch self {
        case .network(let r):
            switch r {
            case .timeout: return "网络超时，请稍后重试"
            case .offline: return "无网络连接"
            case .serverError(let code): return "服务器错误（\(code)）"
            case .leaseExpired: return "下载凭证已过期，请重新预占"
            case .leaseNotFound: return "下载凭证不存在"
            }
        case .persistence(let r):
            switch r {
            case .diskFull: return "存储空间不足"
            case .dbCorrupted: return "本地数据损坏"
            case .schemaMismatch(let exp, let got):
                return "数据版本不匹配（期望 \(exp)，实际 \(got)）"
            case .ioError:
                // 不暴露 raw detail（可能含路径 / DB 底层错误 / 环境信息，泄漏到 UI 不合适）。
                // detail 仅用于 debug 日志 / 崩溃上报——codex round 1 MEDIUM finding。
                return "读写失败，请稍后重试"
            }
        case .trade(let r):
            switch r {
            case .insufficientCash: return "可用资金不足"
            case .insufficientHolding: return "持仓不足"
            case .disabled: return "当前不可操作"
            case .invalidShareCount: return "股数非法"
            }
        case .trainingSet(let r):
            switch r {
            case .crcFailed: return "训练组文件校验失败"
            case .unzipFailed: return "训练组解压失败"
            case .emptyData: return "训练组数据为空"
            case .versionMismatch(let exp, let got):
                return "训练组版本不匹配（期望 \(exp)，实际 \(got)）"
            case .fileNotFound: return "训练组文件不存在"
            }
        case .internalError(let module, _):
            return "内部错误（\(module)）"
        }
    }

    /// 当前 operation 重试是否可能恢复。
    /// true = transient（同一操作重试大概率自愈）；false = terminal（同一操作再试无意义）。
    /// "用户在 UI 层点重试" 以上的恢复（比如 lease 过期后重新 /meta）属更高层状态机
    /// （P2 DownloadAcceptance），不在本契约 scope 内——codex round 1 HIGH finding 要求区分。
    var isRecoverable: Bool {
        switch self {
        case .network(.timeout), .network(.offline):
            return true                          // transient 网络问题
        case .network(.serverError):
            return true                          // 5xx/429 转瞬性居多；P1 APIClient 映射时决定具体码
        case .network(.leaseExpired), .network(.leaseNotFound):
            return false                         // terminal: 对当前 lease operation 重试无意义
        case .persistence: return false          // 磁盘 / 损坏 / 架构失配都是环境问题
        case .trade: return false                // 业务规则层，不是重试能解决
        case .trainingSet(.versionMismatch), .trainingSet(.fileNotFound):
            return false                         // 客户端版本旧 / 服务端清理：重试无意义
        case .trainingSet: return true           // 其它 training-set（crc / unzip / empty）可重下
        case .internalError: return false
        }
    }

    /// UI 是否通过 Toast 展示。
    /// trade.disabled 由按钮禁用态自然呈现，不打 Toast；internalError 走 debug log。
    var shouldShowToast: Bool {
        switch self {
        case .trade(.disabled): return false
        case .internalError: return false
        default: return true
        }
    }
}
```

- [ ] **Step 4: 跑测试（应通过）**

```bash
cd ios/Contracts && swift test 2>&1 | tail -5; cd ../..
```

Expected: 累计 ≥ 49 tests passed（25 from Plan 1c + 6 from Task 1 + 18 from Task 2），无 failed。

- [ ] **Step 5: 提交**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/AppError.swift \
  ios/Contracts/Tests/KlineTrainerContractsTests/AppErrorTests.swift
git commit -m "feat(m0.4): AppError extensions userMessage/isRecoverable/shouldShowToast

Chinese userMessage per spec M0.4; isRecoverable distinguishes transient
(network / training-set crc/unzip/empty) from hard failures (persistence /
trade / version-mismatch / fileNotFound / internalError); shouldShowToast
defaults true except trade.disabled (button-state native) and internalError
(debug log)."
```

---

## Task 3: 聚合 acceptance 脚本

**Files:**
- Create: `scripts/acceptance/plan_1d_m0_4_apperror.sh`

**背景**：对齐 Plan 1/1b/1c pattern，一键验收 M0.4 全部交付。无 CI workflow 新建（`swift-contracts-smoke.yml` path 触发覆盖），所以 acceptance 比 Plan 1c 简短。

- [ ] **Step 1: 写聚合脚本**

写入 `scripts/acceptance/plan_1d_m0_4_apperror.sh`：

```bash
#!/usr/bin/env bash
# Plan 1d 聚合验收：M0.4 AppError 契约
# 涵盖：AppError.swift + AppErrorTests.swift 存在 + swift test exit 0 + 5 keywords
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO_ROOT"

PASS=0
FAIL=0
declare -a FAILED

run() {
  local label="$1"; shift
  echo ""
  echo "========== $label =========="
  if "$@"; then
    echo "OK: $label"
    PASS=$((PASS + 1))
  else
    echo "NG: $label"
    FAIL=$((FAIL + 1))
    FAILED+=("$label")
  fi
}

# ---- 文件存在性 ----
run "file: AppError.swift"        test -s ios/Contracts/Sources/KlineTrainerContracts/AppError.swift
run "file: AppErrorTests.swift"   test -s ios/Contracts/Tests/KlineTrainerContractsTests/AppErrorTests.swift

# ---- AppError.swift 包含 4 个 Reason + 1 个 AppError 定义 ----
run "grep: AppError enum definition" grep -q '^public enum AppError:' ios/Contracts/Sources/KlineTrainerContracts/AppError.swift
run "grep: 4 Reason enums"           bash -c "grep -c '^public enum \(Network\|Persistence\|Trade\|TrainingSet\)Reason:' ios/Contracts/Sources/KlineTrainerContracts/AppError.swift | grep -q '^4$'"
run "grep: 3 extension methods"      bash -c "grep -cE '(userMessage|isRecoverable|shouldShowToast):' ios/Contracts/Sources/KlineTrainerContracts/AppError.swift | grep -q '^3$'"

# ---- Swift 测试（exit code only 以容忍格式漂移——Plan 1c R5 教训）----
run "swift test: exit 0" \
    bash -c 'cd ios/Contracts && swift test'

# ---- 汇总 ----
echo ""
echo "============================================"
echo "Plan 1d (M0.4 AppError) acceptance: $PASS passed, $FAIL failed"
if [[ $FAIL -gt 0 ]]; then
  echo "Failed items:"
  for f in "${FAILED[@]}"; do echo "  - $f"; done
  echo "PLAN 1d FAIL"
  exit 1
fi
echo "PLAN 1d PASS"
```

然后 `chmod +x scripts/acceptance/plan_1d_m0_4_apperror.sh`。

- [ ] **Step 2: 运行通过路径**

```bash
./scripts/acceptance/plan_1d_m0_4_apperror.sh
```

Expected: 末行 `Plan 1d (M0.4 AppError) acceptance: 6 passed, 0 failed` + `PLAN 1d PASS`。

- [ ] **Step 3: 运行失败路径（sanity check）**

```bash
cp ios/Contracts/Sources/KlineTrainerContracts/AppError.swift /tmp/apperror.bak
: > ios/Contracts/Sources/KlineTrainerContracts/AppError.swift
./scripts/acceptance/plan_1d_m0_4_apperror.sh || echo "PLAN 1d FAIL expected"
mv /tmp/apperror.bak ios/Contracts/Sources/KlineTrainerContracts/AppError.swift
```

Expected: 多条 NG + `PLAN 1d FAIL`。

- [ ] **Step 4: 最终通过确认**

```bash
./scripts/acceptance/plan_1d_m0_4_apperror.sh
```

Expected: `PLAN 1d PASS`。

- [ ] **Step 5: 提交**

```bash
git add scripts/acceptance/plan_1d_m0_4_apperror.sh
git commit -m "feat(plan-1d): aggregate M0.4 AppError acceptance script

6 items: 2 file existence + AppError enum + 4 Reason enums + 3 extensions +
swift test exit 0 (format-drift-tolerant per Plan 1c R5 pattern)."
```

---

## 非 coder 验收清单（CLAUDE.md 治理 §2 / workflow-rules.json verification_template）

按 `.claude/workflow-rules.json` 的 `verification_template` 要求：中文 / action-expected-pass_fail；禁用"验证通过即可 / 看起来正常 / 应该没问题 / should work / looks fine"。

**触发点**：push 后 PR 开出，**merge 前**你在终端一条条跑完，贴 stdout 到 PR 评论作为 evidence（`workflow-rules.json` acceptance_evidence 要求）。

### 验收项 1：聚合脚本整体通过

| | 内容 |
|---|---|
| Action | 在仓库根目录终端运行 `./scripts/acceptance/plan_1d_m0_4_apperror.sh` |
| Expected | 末尾依次出现 `Plan 1d (M0.4 AppError) acceptance: 6 passed, 0 failed` 和 `PLAN 1d PASS` |
| Pass / Fail | 两行同时满足 = PASS；任一不满足 = FAIL |

### 验收项 2：Swift 测试全绿

| | 内容 |
|---|---|
| Action | 运行 `cd ios/Contracts && swift test; echo "exit=$?" && cd ../..` |
| Expected | 输出 `exit=0`；输出中无 `error:` / `FAILED` / `failed` 字样 |
| Pass / Fail | `exit=0` 且无 failure 关键字 = PASS；其它 = FAIL |

### 验收项 3：CI swift-contracts-smoke 在 PR 上 PASS

| | 内容 |
|---|---|
| Action | PR 开出后 `gh pr checks <PR_NUM>` |
| Expected | `swift test on macos-15` 状态 `pass` |
| Pass / Fail | Success = PASS；failure / pending > 10 分钟 = FAIL |

### 验收项 4：AppError 5 cases + 4 Reason enums + 3 extension methods 齐全

| | 内容 |
|---|---|
| Action | 运行 `grep -c "^public enum\|userMessage:\|isRecoverable:\|shouldShowToast:" ios/Contracts/Sources/KlineTrainerContracts/AppError.swift` |
| Expected | 输出数字 ≥ 8（5 个 enum 定义 + 3 个 extension method）|
| Pass / Fail | ≥ 8 = PASS；< 8 = FAIL |

### 验收项 5：提交记录完整

| | 内容 |
|---|---|
| Action | 运行 `git log --oneline plan-1d/m0.4-apperror ^origin/main \| wc -l` |
| Expected | 输出数字 ≥ 3（Task 1 / Task 2 / Task 3 各一条业务 commit）|
| Pass / Fail | ≥ 3 = PASS；< 3 = FAIL |

### 验收项 6：与 Plan 1c Codable boundary 不 regression

| | 内容 |
|---|---|
| Action | 运行 `./scripts/acceptance/plan_1c_m0_3_swift_contracts.sh` 和 `./scripts/acceptance/plan_1b_m0_2_rest_api.sh` 和 `./scripts/acceptance/plan_1_m0_1_db_schema.sh` |
| Expected | 三个脚本都以各自的 `PLAN XX PASS` 结束，退出码全 0 |
| Pass / Fail | 三个 PASS 齐出 = PASS；任一 FAIL = FAIL |

---

## Self-Review（起草后自查）

1. **Spec 覆盖（仅 M0.4 类型体 + 3 extension）**：
   - AppError 5 cases（network / persistence / trade / trainingSet / internalError）→ Task 1 Step 3 ✅
   - 4 Reason enums（Network/Persistence/Trade/TrainingSet）→ Task 1 Step 3 ✅
   - `Error, Equatable, Sendable` conformance → Task 1 Step 3 ✅
   - `userMessage` 中文映射 → Task 2 Step 3 ✅
   - `isRecoverable` 分流 → Task 2 Step 3 ✅
   - `shouldShowToast` 分流（trade.disabled + internalError false）→ Task 2 Step 3 ✅
   - 聚合 acceptance → Task 3 ✅

2. **范围外**（不在 Plan 1d scope）：
   - M0.4 翻译规则表（P1/P3/P4/E3/P2/UI 各模块私有错误 → AppError 映射）→ Plan 2/3 各模块实施
   - M0.4 禁止清单 CI lint（禁止跨模块传私有错误 / 禁止随意 `.internalError`）→ 未来 governance 层
   - Xcode 工程集成 → Plan 2/3

3. **类型一致性**：
   - 测试 Suite 名与 extension method 名对齐：`AppErrorUserMessageTests` / `AppErrorIsRecoverableTests` / `AppErrorShouldShowToastTests`
   - AppError cases 与 Plan 1c 其它 Swift 类型独立（没有命名冲突）
   - `TrainingSetReason.versionMismatch(expected: Int, got: Int)` 和 `PersistenceReason.schemaMismatch(expected: Int, got: Int)` 签名一致（都用 `Int`，spec 要求）

4. **Placeholder 扫描**：无 TBD / TODO；前向引用 "Plan 2/3" 是真实依赖关系

5. **forbidden_phrases 扫描**：验收清单全部 action-expected-pass_fail 具体判据，无禁用措辞

6. **大小对照**：plan 约 620 行 / ~27 KB，在 codex branch-diff 256 KB 预算内；比 Plan 1c（1380 行）小一半

7. **Codex round 预估**：
   - 可能 finding：`NetworkReason.leaseExpired` 和 `NetworkReason.leaseNotFound` 是否该归 `AppError.persistence` 而非 `.network`（spec 原文归 network，我忠实抄）
   - 可能 finding：`isRecoverable` 对 `trainingSet(.versionMismatch)` 判 false，但 lease 相关也是 version 问题——spec 没明说，我按"客户端代码版本跟不上 = 重试无意义"判定
   - 可能 finding：`userMessage` 中文消息是否有更精准措辞（spec 给了示例用 ellipsis `{ ... }`，我按 spec 补齐）
   - 预估 2-3 轮收敛（类型体 spec 逐字给出，extension spec 给了 skeleton 但细节需补）

8. **依赖链**：Plan 1d 依赖 Plan 1c（`ios/Contracts/` SwiftPM 包骨架 + 已有 25 tests）；被 Plan 2/3 所有模块消费（AppError 是全局错误协议）

---

## 执行切换提示

Plan 1d 推荐执行路径（按新 checklist per memory `feedback_skill_pipeline_3_gaps.md`）：

1. **已开 worktree** `.worktrees/plan-1d/m0.4-apperror` 分支 `plan-1d/m0.4-apperror`（本 plan 起草环境）
2. `superpowers:subagent-driven-development` 串行派：
   - Task 1（AppError 类型体 + Equatable/Sendable 测试）
   - Task 2（3 extension methods + 对应 Suite；依赖 Task 1）
   - Task 3（聚合 acceptance 脚本）
3. 每 Task 内部走 TDD（先 fail 路径证测试有效，再实现通过）
4. 每 Task 后 spec + code-quality 双层 review
5. 全部通过后：
   - **正式 invoke** `superpowers:verification-before-completion`（不只是跑命令贴输出）
   - `superpowers:requesting-code-review`（全 branch self-review）
   - push + PR
6. **用户在自己终端跑 6 项非 coder 验收清单 + 贴 PR 评论**（merge 前必做 per `workflow-rules.json` acceptance_evidence）
7. 所有 evidence 齐全 + CI 绿 → merge
