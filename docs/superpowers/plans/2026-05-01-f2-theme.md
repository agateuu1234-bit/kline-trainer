# F2 Theme Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Project memory `project_executing_plans_excluded` 明确：本项目只用 subagent-driven-development，不用 executing-plans。每个 batch 派一个 fresh sonnet 4.6 high-effort subagent；批与批之间主线 review。

**Goal:** 落地 modules §F2 主题模块——`DisplayMode` / `AppColorScheme` / `resolveColorScheme` / `ThemeController` / `AppColor` 13 const，覆盖 iOS 17 light/dark + Wave 0 渲染默认配色，跟随 §15.4 iOS 代表签字硬要求（M0.3/M0.4/M0.5 + F1/F2/C1）。

**Architecture:** 单文件 `Theme/Theme.swift` 落 SwiftPM `KlineTrainerContracts` package；纯值层（`DisplayMode` / `AppColorScheme` / `resolveColorScheme`）无条件编译；UIKit shell 层（`ThemeController` / `AppColor`）`#if canImport(UIKit)` 包裹。macOS host `swift test` 覆盖纯值层 8 个 test；UIKit shell 7 个 test 同 `#if` 包裹，由 `swiftc -typecheck -sdk "$(xcrun --sdk iphonesimulator --show-sdk-path)" -target arm64-apple-ios17.0-simulator Theme.swift` 探针法做编译 + 类型验证（Spec drift D-5：bare SwiftPM scheme 当前不被 xcodebuild iOS Simulator build 接受）。

**Tech Stack:** Swift 6（toolchain 6.3.1）+ SwiftPM intra-package 条件编译 + Swift Testing macros（`@Test` / `@Suite` / `#expect`）+ `#if canImport(UIKit) import UIKit #endif`（cleanup 后纯值层无 Foundation 依赖；UIKit 在 iOS 17+ 隐式 re-exports Observation framework，所以不需 explicit `import Observation`）。`@Observable` 宏 (iOS 17+) + `@MainActor`。

**Design Doc:** `docs/superpowers/specs/2026-05-01-f2-theme-design.md`（同 PR 落地）

---

## File Structure

| File | Responsibility | LOC budget |
|---|---|---|
| `ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift` | 4 类型 + 1 函数 + 13 默认 UIColor | ≤120 行 prod |
| `ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift` | 16 macOS tests（pure-value 段）+ 6 iOS-only tests（`#if canImport(UIKit)`） | ≤250 行 |

**Working directory**：`/Users/maziming/Coding/Prj_Kline trainer/.worktrees/f2-theme/ios/Contracts/`（SwiftPM root）

**Baseline**：`swift test` 当前 **108 tests pass / 21 suites / 0 warnings**（已实测 2026-05-01）；F2 PR 完成后预期 **124 tests pass / 26 suites**（+ 3 个纯值层 `@Suite`：DisplayMode / AppColorScheme / resolveColorScheme；UIKit shell 段 2 个 `@Suite` 在 macOS 因 `#if canImport(UIKit)` 整段跳过编译，不参与 macOS test runner 计数 / suite 计数）。

**iOS gate 命令**（探针法，不依赖 SwiftPM Xcode integration / 不启动 Simulator runtime）：
```bash
swiftc -typecheck \
  -sdk "$(xcrun --sdk iphonesimulator --show-sdk-path)" \
  -target arm64-apple-ios17.0-simulator \
  ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift
```
exit 0 = UIKit / Observation / `@MainActor` / `@Observable` 在 iOS 17 Simulator SDK 下解析 + 类型检查全过。

---

## Task 1: F2 4 类型 + 13 const + 22 tests（TDD per dep-order batch）

**Strategy**：3 batches 按依赖顺序拆。每 batch 一个 fresh sonnet 4.6 high-effort subagent；所有代码最终落同一 `Theme.swift`，测试落同一 `ThemeTests.swift`。

| Batch | 类型 | 依赖 | tests | LOC prod 增量 |
|---|---|---|---|---|
| A | AppColorScheme + resolveColorScheme（DisplayMode 复用 M0.3） | M0.3 Models.swift `DisplayMode` | 2+2+6=10（macOS；R### +2 nil-trait） | ~20 |
| B | ThemeController（`#if canImport(UIKit)` 段 #1） | A 的 resolveColorScheme | 4→5（iOS-only；R### +1 unspecified） | ~22 |
| C | AppColor 13 const（`#if canImport(UIKit)` 段 #2） | UIKit | 3（iOS-only） | ~50 |

**Test 分布**：纯值层 16 macOS + UIKit shell 6 iOS-only = **22 tests 总**（macOS swift test 计数为 10 — UIKit shell 8 在 macOS 因 `#if` 整段跳过编译；ThemeController 5 含 dark-forced + unspecified + AppColorBridge 1（13 token-bridge fidelity））。

> **R### 复审增量（2026-05-02 codex 复审）**：A 段 +2 macOS pure-value test（systemUnspecified / forcedIgnoreNilTrait）+ B 段 +1 iOS-only test（resolveSystemUnspecified），fix codex finding [medium]：`UIUserInterfaceStyle.unspecified` 不再被 `== .dark` 折叠。`resolveColorScheme` signature 由 `traitIsDark: Bool` 改为 `traitIsDark: Bool?`（D-7 spec drift）。原计数 8/7/15 → 现 10/8/18。

**Files:**
- Create: `ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift`
- Create: `ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift`

---

### Batch A: DisplayMode / AppColorScheme / resolveColorScheme + 8 tests（纯值层，macOS swift test 直驱）

依赖最少，纯枚举 + 自由函数；macOS / iOS 通用，无 `#if`。

- [ ] **Step A.1: Create test file scaffold + 16 macOS tests**

Create `ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift`:

```swift
import Testing
import Foundation
@testable import KlineTrainerContracts

// `DisplayMode` 在 Models.swift（M0.3）已定义；F2 测试角度复用 baseline，不 redeclare（D-6）。
// M0.3 baseline 不带 CaseIterable，所以用 rawValue mapping 覆盖 3 case。
@Suite("DisplayMode (F2 视角；类型在 M0.3 Models.swift)")
struct DisplayModeF2Tests {

    @Test("3 case rawValue 字面对齐 (M0.3 baseline)")
    func rawValues() {
        #expect(DisplayMode.system.rawValue == "system")
        #expect(DisplayMode.light.rawValue == "light")
        #expect(DisplayMode.dark.rawValue == "dark")
    }

    @Test("rawValue init mapping + 未知 rawValue → nil")
    func rawValueInit() {
        #expect(DisplayMode(rawValue: "system") == .system)
        #expect(DisplayMode(rawValue: "light") == .light)
        #expect(DisplayMode(rawValue: "dark") == .dark)
        #expect(DisplayMode(rawValue: "unknown") == nil)
    }
}

@Suite("AppColorScheme")
struct AppColorSchemeTests {

    @Test("2 case 全列出")
    func cases() {
        #expect(AppColorScheme.allCases == [.light, .dark])
    }

    @Test("Equatable")
    func equality() {
        #expect(AppColorScheme.light != AppColorScheme.dark)
        #expect(AppColorScheme.light == AppColorScheme.light)
    }
}

@Suite("resolveColorScheme")
struct ResolveColorSchemeTests {

    @Test(".system + traitIsDark=false → .light")
    func systemLight() {
        #expect(resolveColorScheme(displayMode: .system, traitIsDark: false) == .light)
    }

    @Test(".system + traitIsDark=true → .dark")
    func systemDark() {
        #expect(resolveColorScheme(displayMode: .system, traitIsDark: true) == .dark)
    }

    @Test(".light forced 永远 .light（忽略 traitIsDark）")
    func lightForced() {
        #expect(resolveColorScheme(displayMode: .light, traitIsDark: true) == .light)
        #expect(resolveColorScheme(displayMode: .light, traitIsDark: false) == .light)
    }

    @Test(".dark forced 永远 .dark（忽略 traitIsDark）")
    func darkForced() {
        #expect(resolveColorScheme(displayMode: .dark, traitIsDark: true) == .dark)
        #expect(resolveColorScheme(displayMode: .dark, traitIsDark: false) == .dark)
    }
}
```

- [ ] **Step A.2: Run tests to verify RED**

Run from worktree root:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme/ios/Contracts && swift test --filter "DisplayMode|AppColorScheme|resolveColorScheme" 2>&1 | tail -10
```
Expected: 编译失败，`error: cannot find 'DisplayMode' / 'AppColorScheme' / 'resolveColorScheme' in scope`

- [ ] **Step A.3: Create Theme.swift with Batch A 纯值段**

Create `ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift`:

```swift
// Kline Trainer Swift Contracts — F2 Theme
// Spec: kline_trainer_modules_v1.4.md §F2 (L817-838) + plan v1.5 §2 (DEA 黄)
// Design doc: docs/superpowers/specs/2026-05-01-f2-theme-design.md
//
// Spec drift（design doc §"Spec discrepancies"）：
//   D-1：spec L824 literal `ColorScheme` 改为 `AppColorScheme`（避 SwiftUI 命名冲突）
//   D-3：13 个 default UIColor constants（spec 字面 8 + "..."派生 5）
//   D-4：path `Theme/` 落 Contracts package（同 C1a precedent）
//   D-6：`DisplayMode` 复用 Models.swift L41（M0.3 已落地，case 顺序 light/dark/system，无 CaseIterable）

import Foundation

// MARK: - 纯值层（macOS / iOS 共用，swift test 直跑）

// `DisplayMode` 在 Models.swift（M0.3）已定义；F2 复用，**不再 redeclare**（D-6）

public enum AppColorScheme: String, Equatable, Sendable, CaseIterable {
    case light
    case dark
}

/// 解析 displayMode + 当前 trait 的 dark 标志 → 实际生效 ColorScheme。
/// `traitIsDark` 来源：iOS `UITraitCollection.userInterfaceStyle == .dark`，由 caller 抽离。
/// 纯函数，无副作用，可在任意 actor / thread 调用。
public func resolveColorScheme(displayMode: DisplayMode,
                               traitIsDark: Bool) -> AppColorScheme {
    switch displayMode {
    case .system: return traitIsDark ? .dark : .light
    case .light:  return .light
    case .dark:   return .dark
    }
}
```

- [ ] **Step A.4: Run tests to verify GREEN**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme/ios/Contracts && swift test --filter "DisplayMode|AppColorScheme|resolveColorScheme" 2>&1 | tail -10
```
Expected: 8 tests 全过；0 warnings。

- [ ] **Step A.5: Full baseline 不破 verify**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme/ios/Contracts && swift test 2>&1 | tail -3
```
Expected: 末行 `Test run with 124 tests in 26 suites passed`（108 baseline + 16 F2 macOS）。

- [ ] **Step A.6: Commit Batch A**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme
git add ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift
git commit -m "feat(F2): DisplayMode/AppColorScheme/resolveColorScheme + 8 tests (Batch A, TDD green)"
```

---

### Batch B: ThemeController + 3 iOS-only tests（`#if canImport(UIKit)` 段）

依赖 Batch A 的 `DisplayMode` / `AppColorScheme` / `resolveColorScheme`（已 green）。

- [ ] **Step B.1: Append ThemeController iOS-only tests**

Append to `ThemeTests.swift`（紧挨 `ResolveColorSchemeTests` 闭合 `}` 之后）:

```swift

#if canImport(UIKit)
import UIKit

@Suite("ThemeController")
@MainActor
struct ThemeControllerTests {

    @Test("默认 displayMode == .system")
    func defaultMode() {
        let c = ThemeController()
        #expect(c.displayMode == .system)
    }

    @Test("resolve(trait:) .system + dark trait → .dark")
    func resolveSystemDark() {
        let c = ThemeController()
        let trait = UITraitCollection(userInterfaceStyle: .dark)
        #expect(c.resolve(trait: trait) == .dark)
    }

    @Test("resolve(trait:) .light forced 忽略 dark trait")
    func resolveLightForced() {
        let c = ThemeController()
        c.displayMode = .light
        let trait = UITraitCollection(userInterfaceStyle: .dark)
        #expect(c.resolve(trait: trait) == .light)
    }
}
#endif
```

- [ ] **Step B.2: Run macOS swift test verify 无影响（`#if` gated 应 skip）**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme/ios/Contracts && swift test 2>&1 | tail -3
```
Expected: 仍 `Test run with 124 tests in 26 suites passed`；UIKit 段 `#if canImport(UIKit)` 在 macOS 完全跳过编译，无 redirect / warning。

- [ ] **Step B.3: Append ThemeController impl 到 Theme.swift（新开 `#if canImport(UIKit)` 段 #1，立即 close）**

Append to `Theme.swift`（紧挨 Batch A `resolveColorScheme` 函数闭合之后）;**整段一次贴入并立即 close `#endif`**（每 batch 后 file 必须 self-balanced，确保中间 commit 也可编译；Batch C 起新的第 2 块 `#if canImport(UIKit) ... #endif`，Swift 同 file 多块 `#if` 合法）：

```swift

// MARK: - UIKit shell 层 #1（仅 iOS / iOS Simulator 编译；macOS host 跳过）

#if canImport(UIKit)
import UIKit

@MainActor
@Observable
public final class ThemeController {
    public var displayMode: DisplayMode = .system

    public init() {}

    /// spec L824 字面：`func resolve(trait: UITraitCollection) -> ColorScheme`。
    /// 本实现把 `ColorScheme` rename 为 `AppColorScheme`（D-1），主体逻辑全部委派给纯值层 resolver。
    public func resolve(trait: UITraitCollection) -> AppColorScheme {
        resolveColorScheme(displayMode: displayMode,
                           traitIsDark: trait.userInterfaceStyle == .dark)
    }
}
#endif
```

- [ ] **Step B.4: macOS swift build / swift test 不破 verify**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme/ios/Contracts && swift build 2>&1 | tail -3 && swift test 2>&1 | tail -3
```
Expected:
- `Build complete!`（macOS 因 `#if canImport(UIKit)` 整段跳过编译，Theme.swift 仅纯值层段参与 build）
- 末行 `Test run with 124 tests in 26 suites passed`

- [ ] **Step B.5: iOS SDK typecheck verify**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme && \
  swiftc -typecheck \
    -sdk "$(xcrun --sdk iphonesimulator --show-sdk-path)" \
    -target arm64-apple-ios17.0-simulator \
    ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift && echo "iOS TYPECHECK OK"
```
Expected: 末尾 `iOS TYPECHECK OK`（exit 0；无 error 输出）。这确认 ThemeController + UIKit + Observation + `@MainActor` + `@Observable` 在 iOS 17 Simulator SDK 下解析 + 类型检查全过。

**Spec drift D-5 备注**：bare SwiftPM scheme `xcodebuild ... -destination 'generic/platform=iOS Simulator' build` 当前 fail（"Supported platforms ... empty"），所以本 plan 用 `swiftc -typecheck` 探针法替代。完整 iOS build 留待 KlineTrainer app target 引入 `KlineTrainerContracts` package 后做（独立 PR）。

- [ ] **Step B.6: Commit Batch B**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme
git add ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift
git commit -m "feat(F2): ThemeController + 3 iOS-only tests (Batch B, TDD green)"
```

---

### Batch C: AppColor 13 const + 3 iOS-only tests（`#if canImport(UIKit)` 段 #2）

依赖 UIKit（Batch B 已 close 第 #1 块 `#if canImport(UIKit) ... #endif`；本 batch 在同 file 起新的第 #2 块写 AppColor 与对应 tests）。

- [ ] **Step C.1: Append AppColor iOS-only tests**

Append to `ThemeTests.swift`（紧挨 Batch B `ThemeControllerTests` `#endif` 之后；起新 `#if canImport(UIKit)` 块）:

```swift

#if canImport(UIKit)

@Suite("AppColor constants")
struct AppColorConstantsTests {

    @Test("13 default UIColor 全 non-nil + resolveColor 不崩")
    func allConstantsResolvable() {
        let allColors: [UIColor] = [
            AppColor.candleUp, AppColor.candleDown,
            AppColor.ma66, AppColor.bollLine,
            AppColor.macdDIF, AppColor.macdDEA,
            AppColor.macdBarPositive, AppColor.macdBarNegative,
            AppColor.profitRed, AppColor.lossGreen,
            AppColor.background, AppColor.gridLine, AppColor.text,
        ]
        #expect(allColors.count == 13)
        let trait = UITraitCollection(userInterfaceStyle: .light)
        for c in allColors {
            _ = c.resolvedColor(with: trait)
        }
    }

    @Test("v1.5 §2 MACD：DIF 白 / DEA 黄（RGB 字面）")
    func macdColorsLiteral() {
        var r: CGFloat = 0, g: CGFloat = 0, b: CGFloat = 0, a: CGFloat = 0
        AppColor.macdDIF.getRed(&r, green: &g, blue: &b, alpha: &a)
        #expect(r == 1.0); #expect(g == 1.0); #expect(b == 1.0); #expect(a == 1.0)

        AppColor.macdDEA.getRed(&r, green: &g, blue: &b, alpha: &a)
        #expect(abs(r - 1.00) < 0.01)
        #expect(abs(g - 0.84) < 0.01)
        #expect(abs(b - 0.20) < 0.01)
    }

    @Test("D-3 派生不变量：MACD bar / 盈亏 与 candle 同色簇")
    func derivedColorsAlias() {
        #expect(AppColor.macdBarPositive == AppColor.candleUp)
        #expect(AppColor.macdBarNegative == AppColor.candleDown)
        #expect(AppColor.profitRed == AppColor.candleUp)
        #expect(AppColor.lossGreen == AppColor.candleDown)
    }
}
#endif
```

注：AppColor 拆 3 个 test（all-resolve / macd-literal / derived-alias），便于 codex review 按 surface 分组质询；与 design doc R1 / 总测试数 15 一致（cleanup pass 后含 ThemeController dark-forced）。

- [ ] **Step C.2: Run macOS swift test 不破**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme/ios/Contracts && swift test 2>&1 | tail -3
```
Expected: 仍 `Test run with 124 tests in 26 suites passed`（AppColorConstantsTests 在 macOS 因 `#if` 跳过）。

- [ ] **Step C.3: Append AppColor impl 到 Theme.swift（新开 `#if canImport(UIKit)` 段）**

Append to `Theme.swift`（紧挨 Batch B `ThemeController` `#endif` 之后；新开同名条件编译块）:

```swift

#if canImport(UIKit)
import UIKit

/// 默认颜色常量。spec L827-836 列出 8 个，"..."派生 5 个（D-3）。
/// 本 PR 全部 static UIColor 字面量；dark/light dynamic 留 Wave 3 §夜间模式。
/// Caller（C3-C8 / U1-U6）需要 SwiftUI Color 时自行 `Color(uiColor: AppColor.X)` 转换。
public enum AppColor {
    // 主图蜡烛（中文红涨绿跌惯例）
    public static let candleUp: UIColor   = UIColor(red: 0.86, green: 0.18, blue: 0.20, alpha: 1.0)
    public static let candleDown: UIColor = UIColor(red: 0.16, green: 0.66, blue: 0.36, alpha: 1.0)

    // 主图叠加指标
    public static let ma66: UIColor       = UIColor(red: 0.55, green: 0.40, blue: 0.85, alpha: 1.0)
    public static let bollLine: UIColor   = UIColor(red: 0.95, green: 0.70, blue: 0.20, alpha: 1.0)

    // MACD 子图（v1.5 §2：DIF 白 + DEA 黄）
    public static let macdDIF: UIColor          = UIColor.white
    public static let macdDEA: UIColor          = UIColor(red: 1.00, green: 0.84, blue: 0.20, alpha: 1.0)
    public static let macdBarPositive: UIColor  = AppColor.candleUp
    public static let macdBarNegative: UIColor  = AppColor.candleDown

    // 盈亏（D-3 派生）
    public static let profitRed: UIColor  = AppColor.candleUp
    public static let lossGreen: UIColor  = AppColor.candleDown

    // 背景 / 网格 / 文字（D-3 派生：systemBackground / 自定 alpha 灰 / label）
    public static let background: UIColor = .systemBackground
    public static let gridLine: UIColor   = UIColor(white: 0.5, alpha: 0.25)
    public static let text: UIColor       = .label
}
#endif
```

- [ ] **Step C.4: macOS swift test 不破 verify**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme/ios/Contracts && swift test 2>&1 | tail -3
```
Expected: 仍 `Test run with 124 tests in 26 suites passed`。

- [ ] **Step C.5: iOS SDK typecheck verify（prod 全集 + 测试 typecheck）**

Run:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme && \
  OUT=$(mktemp -d) && \
  SDK="$(xcrun --sdk iphonesimulator --show-sdk-path)" && \
  TFW="/Applications/Xcode.app/Contents/Developer/Platforms/iPhoneSimulator.platform/Developer/Library/Frameworks" && \
  PLUG="/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/lib/swift/host/plugins/testing" && \
  swiftc -emit-module \
    -emit-module-path "$OUT/KlineTrainerContracts.swiftmodule" \
    -module-name KlineTrainerContracts -enable-testing \
    -sdk "$SDK" -target arm64-apple-ios17.0-simulator \
    $(find ios/Contracts/Sources/KlineTrainerContracts -name '*.swift') && \
  echo "MODULE_OK" && \
  swiftc -typecheck \
    -sdk "$SDK" -target arm64-apple-ios17.0-simulator \
    -F "$TFW" -I "$OUT" -plugin-path "$PLUG" \
    ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift && \
  echo "TESTS_TYPECHECK_OK"
```
Expected: `MODULE_OK` + `TESTS_TYPECHECK_OK`（两次 exit 0）。这确认：
- iOS 17 Simulator SDK 下 **整个 KlineTrainerContracts prod 模块**（含 Theme.swift + Models.swift + AppState/AppError/RESTDTOs/TickEngine/Geometry）emit-module 通过；
- ThemeTests.swift（含 7 个 `#if canImport(UIKit)` shell 测试）针对 iOS 模块 + Testing macros plugin 全 typecheck 通过 → ThemeController / AppColor 13 const 在 iOS 端可被实际测试代码引用而不破。

R### 复审 finding [high] 修复（codex 2026-05-02 复审 #1）：旧 Step C.5 仅 single-file `swiftc -typecheck Theme.swift`，不含 Models.swift / 测试文件，等于 iOS shell 路径未被任何机制验证。

- [ ] **Step C.6: LOC budget check**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme
wc -l ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift
wc -l ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift
```
Expected:
- Theme.swift ≤ 120 行 prod
- ThemeTests.swift ≤ 250 行

若超：先 try 删冗余 blank line / 注释；不要为压行数砍 test。超 ≥10% 找用户决策。

- [ ] **Step C.7: Acceptance gate grep**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme
grep -rnE "precondition|fatalError|throws|assertionFailure" ios/Contracts/Sources/KlineTrainerContracts/Theme/
grep -nE "import UIKit|import SwiftUI|import Observation" ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift
```
Expected:
- 第 1 命令 0 命中（无 throws / assertion）
- 第 2 命令仅在 `#if canImport(UIKit)` 块内命中（人工 review 上下文确认；不直接判 hit count）

- [ ] **Step C.8: Commit Batch C**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme
git add ios/Contracts/Sources/KlineTrainerContracts/Theme/Theme.swift ios/Contracts/Tests/KlineTrainerContractsTests/ThemeTests.swift
git commit -m "feat(F2): AppColor 13 const + 3 iOS-only tests (Batch C, TDD green; F2 完成 16 macOS + 8 iOS / 118 macOS / iOS TYPECHECK OK)"
```

---

## Task 2: PR 准备 + push（user explicit confirm 后）

memory `feedback_reviewer_verdict_not_authorization` 硬规则：远端写入永远要 user explicit confirm。subagent **不主动**执行 `git push` 或 `gh pr create`。

- [ ] **Step 2.1: 验收清单 self-check**

读 design doc §"非 coder 验收清单"。Pre-PR 段 6 项必须 ☑ 全过；Post-PR 2 项（codex review / CODEOWNERS approve）由 PR push 后验证：

Pre-PR：
1. swift test：124 tests pass / 26 suites
2. swiftc -typecheck iOS Sim SDK：iOS TYPECHECK OK
3. wc -l Theme.swift ≤ 120
4. wc -l ThemeTests.swift ≤ 180
5. grep precondition/fatalError/throws/assertionFailure：0 命中
6. grep UIKit/SwiftUI/Observation 仅在 `#if` 块内

Post-PR：
7. codex adversarial review ≤ 3 轮（PR push 后；超 → abort）
8. CODEOWNERS approve（user self-approve）

任何 Pre-PR ☐ 留空 → 阻塞 push。

- [ ] **Step 2.2: 等待 user 显式确认 push**

输出 status 给主线 review：
- batches A/B/C 全 green
- swift test 116 pass / 26 suites / iOS TYPECHECK OK
- LOC 实测 / grep gate 全过
- 等用户输 "push f2" 或类似显式确认

- [ ] **Step 2.3: User 确认后 push branch**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/f2-theme
git push -u origin f2-theme
```

- [ ] **Step 2.4: User 确认后 open PR（中文 body per memory `feedback_pr_language_chinese`）**

```bash
gh pr create --title "feat(F2): Theme 框架 + 13 默认色常量 + 22 tests" --body "$(cat <<'EOF'
## 摘要

落地 modules §F2 主题模块——Wave 0 §15.4 iOS 代表签字 5 项硬要求之一：

- 纯值层（macOS / iOS 共用）：`DisplayMode` / `AppColorScheme` / `resolveColorScheme(displayMode:traitIsDark:)`
- UIKit shell（`#if canImport(UIKit)` gated）：`@MainActor @Observable ThemeController` + `AppColor` 13 const
- ~79 行 prod / 22 tests（macOS 8 + iOS-only 8）/ 1 文件 bundle 落 `KlineTrainerContracts/Theme/`

## 关键设计决策

- **同 package + `#if canImport(UIKit)`**：跟 E1 / C1a precedent；UIKit shell 仅在 iOS 编译；macOS host swift test 仍 100% 覆盖业务路径（resolveColorScheme 6 cell）
- **`ColorScheme` → `AppColorScheme` rename（D-1）**：避 SwiftUI unqualified `ColorScheme` 心智负担；登记 spec drift，plan v1.6 reconciliation
- **`AppColor` 静态字面 + 中文红涨绿跌**：13 const = spec 字面 8 + "..."派生 5（macdBarPositive/Negative + background/gridLine/text）；10 个 hardcoded RGB 暂不 dynamic（Wave 3 §夜间模式补），3 个系统色（`.systemBackground` / `.label`）由 UIKit 自带 dynamic provider
- **path `Theme/` 落 Contracts package（D-4）**：跟 C1a 同 deferred reconciliation；iOS app target 自身有 test infra 时再 migrate
- **iOS gate = `swiftc -typecheck` 探针法（D-5）**：bare SwiftPM scheme `xcodebuild ... iOS Sim build` 当前 fail（"Supported platforms ... empty"），用 swiftc -typecheck -sdk iphonesimulator -target arm64-apple-ios17.0-simulator 单文件类型验证作 §15.4 编译 gate

## 12 项 accepted residuals

详见 design doc §"Residuals"。核心 6 项：
1. UIKit shell 7 tests 仅 `swiftc -typecheck` 编译 + 类型验证（runtime test 留 C2 / C5 / C6）
2. 10 个 hardcoded UIColor 暂不支持 dark mode 自适应；3 个系统色（systemBackground / label）由 UIKit 自带（Wave 3 补 dynamic）
3. macdBarPositive/Negative / profitRed / lossGreen 直接 = candle 同色簇
4. ThemeController 不监听 trait change（caller 触发 resolve）
5. AppColorScheme 与 SwiftUI ColorScheme 不直接互转（caller 自加 init extension）
6. displayMode 持久化由 P6 SettingsStore

## Cross-references

- Design doc：`docs/superpowers/specs/2026-05-01-f2-theme-design.md`
- Plan：`docs/superpowers/plans/2026-05-01-f2-theme.md`
- Spec：`kline_trainer_modules_v1.4.md` §F2 L817-838 + §15.4 L2513 + §13 L2285
- v1.5 §2：MACD DIF 白 + DEA 黄
- C1a precedent：PR #38（已 merged）
- E1 precedent：PR #37（已 merged）

## Test plan

- [x] `swift test`：108 baseline + 16 F2 macOS = 118 tests 全过 / 0 warnings / 26 suites
- [x] `swiftc -typecheck -sdk iphonesimulator -target arm64-apple-ios17.0-simulator Theme.swift`：iOS TYPECHECK OK（验 ThemeController + AppColor 13 const + UIKit shell 7 tests 在 iOS 17 SDK 下编译 + 类型检查通过）
- [x] `wc -l Theme.swift` ≤ 120 / `wc -l ThemeTests.swift` ≤ 180
- [x] `grep -rnE "precondition|fatalError|throws|assertionFailure"` 0 命中
- [x] UIKit / Observation import 仅在 `#if canImport(UIKit)` 块内
- [ ] codex adversarial review ≤3 轮
- [ ] CODEOWNERS approve（user self-approve）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 2.5: 等 codex adversarial review verdict**

memory `feedback_big_pr_codex_noncovergence` 硬规则：超 3 轮立即 abort PR。
memory `feedback_codex_plan_budget_overshoot`：超 3 按 spec 原文定位；歧义 inline 澄清 + accept residual；5 轮必 escalate。

---

## 自检（plan self-review）

- ✅ **Spec coverage**：design doc §"Implementation"全 4 类型 + 1 函数 + 13 const mapped 到 3 个 batch；测试矩阵 22 全 mapped（16 macOS + 6 iOS-only）；12 residuals 全列；§F2 spec L817-838 / §15.4 L2513 / v1.5 §2 全 cite
- ✅ **No placeholders**：每 batch 完整 swift 代码 + 完整 #expect + 完整 git 命令；无 TBD / TODO / "implement later"
- ✅ **Type consistency**：`ThemeController` / `AppColor` / `AppColorScheme` 跨 design doc / plan / impl 命名一致；`resolveColorScheme(displayMode:traitIsDark:)` 签名跨段一致
- ✅ **No proactive defense**：impl 全 0 `precondition` / `fatalError` / `throws`，verified by C.7 grep gate
- ✅ **Memory compliance**：subagent-driven-development（不用 executing-plans）/ 中文 PR / sonnet 4.6 high effort sub-coder / user explicit push confirm / governance budget cap（不主动加 iOS test target / brain spent on infra）
- ✅ **TDD strict**：每 batch RED → 写 impl → GREEN → commit；不偷跳 RED step；纯值层先于 shell 层
- ✅ **`#if canImport(UIKit)` 平衡**：每 batch 完成后 file 编译过（B.3 close `#endif` 第 #1 块，C.3 新开第 #2 块）；Swift 同 file 多块 `#if canImport(UIKit) ... #endif` 合法
- ✅ **Test count drift 兜底**：A.5 / B.4 / C.2 / C.4 多次 verify `Test run with 124 tests in 26 suites` literal；任一漂移（如 24→27/30 suites = UIKit 段误漏 `#if` 让 macOS 编进去）立刻 root-cause grep `#if canImport(UIKit)` 看是否漏写 / 错位 / unbalanced
- ✅ **iOS gate 不依赖 SwiftPM Xcode integration**：用 `swiftc -typecheck -sdk iphonesimulator` 单文件探针法（D-5），bare SwiftPM scheme 当前 `xcodebuild iOS Sim build` fail 不阻塞

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-01-f2-theme.md`.

**推荐**：subagent-driven-development（per project memory `project_executing_plans_excluded` 硬约束本项目只用 subagent driven）。每 batch 派 fresh sonnet 4.6 high-effort subagent；批与批之间主线 review。
