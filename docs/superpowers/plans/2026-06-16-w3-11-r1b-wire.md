# W3-11-R1b-wire 边缘 bounce 实时接线 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 把已就绪 bounce 物理 + R1a 几何 helper 接进 app 手势/渲染管线，使**甩动到最老边 → 弹簧 overscroll+回弹可见**（关闭 `feature-completeness: PENDING-W3-11-R1`）；reveal 单边（最新边硬钳无弹）。

**Architecture:** 机制 A（`endPan` 按速度方向分派：`v>0 ∧ .max∈bounceEdges`→对称 bounce 最老边弹 / 否则 plain decel）+ 三 clamp 层（drag full / onUpdate floor-or-full by `allowOverscroll` / reducer 无界 D2）+ `interruptDeceleration`（`isDecelerating`-guard 归一 overscroll）+ Coordinator 算 numeric bounds 喂 engine。**零改冻结 `EdgeBounceModel`**。

**Spec:** `docs/superpowers/specs/2026-06-16-w3-11-r1b-wire-design.md`（v2.1，opus xhigh R1→R2 APPROVE）+ 父 `2026-06-15-w3-11-r1-bounce-wiring-design.md`。

**Tech Stack:** Swift 6 / Swift Testing（`@Test`/`#expect`）/ host `swift test` + Mac Catalyst `xcodebuild build-for-testing`。package `ios/Contracts`（KlineTrainerContracts）。

**评审：** 改 `ios/**/*.swift`（trust-boundary）→ `codex:adversarial-review`（fallback opus 4.8 xhigh）+ Catalyst + app-build。

---

## File Structure

| 文件 | 责任 | 改动 |
|---|---|---|
| `ios/Contracts/Sources/KlineTrainerContracts/Render/RenderStateBuilder.swift` | 视口几何 + bounds helper | B4 makeViewport 单边 overscroll + `offsetBounds(engine:panel:bounds:)` 重载 |
| `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift` | 手势/减速编排 | `ActiveDecel`+`activeBounds`+onUpdate floor-or-full+新 endPan/applyPanOffset 重载+旧 endPan 清 activeBounds+`interruptDeceleration`+3 处 stop() swap |
| `ios/Contracts/Sources/KlineTrainerContracts/Render/ChartContainerView.swift` | UIKit Coordinator | onPan `.changed`/`.ended` 改新签名 + 算 bounds |
| `ios/Contracts/Tests/KlineTrainerContractsTests/Render/RenderStateBuilderTests.swift` | render 测 | B4 单边 + offsetBounds 重载 测 |
| `ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift`（新） | engine bounce 接线测 | 机制 A 分派 / clamp / 中断归一 |
| `docs/superpowers/acceptance/2026-06-16-w3-11-r1b-wire-acceptance.md`（新） | 非-coder 验收 | host 测核 + device runbook |

**任务序（串行依赖）**：T1 B4 渲染 → T2 offsetBounds 重载 → T3 engine clamp+dispatch → T4 interruptDeceleration → T5 Coordinator → T6 P7 回归+验收。T1/T2 纯 render（不依赖 engine 改动），T3/T4 依赖 T2 的重载，T5 依赖 T2+T3+T4。

---

## Task 1: B4 makeViewport 单边 overscroll（纯 render，host 全测）

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/Render/RenderStateBuilder.swift`（makeViewport pixelShift 分支 :179-180）
- Test: `ios/Contracts/Tests/KlineTrainerContractsTests/Render/RenderStateBuilderTests.swift`

- [ ] **Step 1: 写失败测试（§六.1/§六.2）**

加进 `RenderStateBuilderTests` struct（复用既有 `candles`/`bounds` fixture；mainChart 宽经 `ChartPanelFrames.split` 取真值；offset 用 autoTracking `panel(offset:)` helper——makeViewport mode-agnostic 只读 offset/visibleCount）：

```swift
// B4 单边 overscroll：offset>maxOffset（最老边）→ startIndex==0 + pixelShift==overshoot
@Test("B4 overscroll：offset>maxOffset → startIndex==0 + pixelShift==offset−maxOffset")
func b4OverscrollOldestEdge() {
    let mainW = ChartPanelFrames.split(in: Self.bounds).mainChart.width
    let ob = RenderStateBuilder.offsetBounds(mainFrameWidth: mainW, rawVisible: 80,
                                             candleCount: 200, currentIdx: 150)
    let overshoot: CGFloat = 23
    let ps = PanelViewState(period: .m3, interactionMode: .freeScrolling,
                            visibleCount: 80, offset: ob.maxOffset + overshoot, revision: 0)
    let vp = RenderStateBuilder.makeViewport(panelState: ps, candles: Self.candles(period: .m3, count: 200),
                                             tick: 150, bounds: Self.bounds)
    #expect(vp.startIndex == 0)
    #expect(abs(vp.pixelShift - overshoot) < 1e-6)   // 完整 overscroll（可 > candleStep）
    #expect(vp.visibleCount > 0)                      // slice 非空、无 OOB
}

@Test("B4 边缘钉：offset==maxOffset（最老边）→ startIndex==0 + pixelShift==0")
func b4OldestEdgePinned() {
    let mainW = ChartPanelFrames.split(in: Self.bounds).mainChart.width
    let ob = RenderStateBuilder.offsetBounds(mainFrameWidth: mainW, rawVisible: 80,
                                             candleCount: 200, currentIdx: 150)
    let ps = PanelViewState(period: .m3, interactionMode: .freeScrolling,
                            visibleCount: 80, offset: ob.maxOffset, revision: 0)
    let vp = RenderStateBuilder.makeViewport(panelState: ps, candles: Self.candles(period: .m3, count: 200),
                                             tick: 150, bounds: Self.bounds)
    #expect(vp.startIndex == 0)
    #expect(vp.pixelShift == 0)
}

@Test("B4 内段回归：offset∈(0,maxOffset) → 与现状逐字一致（wholeShift+余量）")
func b4InteriorUnchanged() {
    // 复用既有 offsetMidScroll 的几何意图：中段正 offset，startIndex∈(0,upperBound)，pixelShift==余量
    let ps = PanelViewState(period: .m3, interactionMode: .freeScrolling,
                            visibleCount: 80, offset: 35, revision: 0)   // 35 = 3.5 candle（step 依 mainW）→ 内段
    let vp = RenderStateBuilder.makeViewport(panelState: ps, candles: Self.candles(period: .m3, count: 200),
                                             tick: 150, bounds: Self.bounds)
    let step = vp.geometry.candleStep
    let wholeShift = Int((35 / step).rounded(.down))
    #expect(vp.startIndex == min(max(71 - wholeShift, 0), 71))     // base=71,upper=71
    #expect(abs(vp.pixelShift - (35 - CGFloat(wholeShift) * step)) < 1e-6)
}

@Test("B4 早 tick upperBound==0：任意 offset → pixelShift==0（硬钉，不入 overscroll 分支）L2-new(a)")
func b4EarlyTickPinned() {
    // currentIdx 小 → baseStartIndex<0 → upperBound==0。tick=10, count=200 → currentIdx=10, base=10−79<0
    let ps = PanelViewState(period: .m3, interactionMode: .freeScrolling,
                            visibleCount: 80, offset: 500, revision: 0)
    let vp = RenderStateBuilder.makeViewport(panelState: ps, candles: Self.candles(period: .m3, count: 200),
                                             tick: 10, bounds: Self.bounds)
    #expect(vp.startIndex == 0)            // upperBound==0
    #expect(vp.pixelShift == 0)            // 硬钉，未误入 overscroll
}

@Test("B4 最新边（upperBound>0）：offset<0 → startIndex==upperBound + pixelShift==0（reveal 硬钉）L1-new/L2-new(b)")
func b4NewestEdgePinnedWithScrollSpace() {
    let ps = PanelViewState(period: .m3, interactionMode: .freeScrolling,
                            visibleCount: 80, offset: -50, revision: 0)
    let vp = RenderStateBuilder.makeViewport(panelState: ps, candles: Self.candles(period: .m3, count: 200),
                                             tick: 150, bounds: Self.bounds)
    #expect(vp.startIndex == 71)           // upperBound（base=71）
    #expect(vp.pixelShift == 0)            // 最新边硬钉，无前向间隙（回归断言，同 R1a offsetBounds_minOffsetIsHardClampNotSpring）
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ios/Contracts && swift test --filter b4OverscrollOldestEdge`
Expected: FAIL（`b4OverscrollOldestEdge` 现 makeViewport 在 `startIndex==0` 钉 pixelShift=0 → pixelShift==0≠23）。

- [ ] **Step 3: 实现 B4 单边分支**

`RenderStateBuilder.swift` makeViewport，把（:179-180）：
```swift
var pixelShift = panelState.offset - CGFloat(wholeShift) * candleStep
if startIndex == 0 || startIndex == upperBound { pixelShift = 0 }
```
改为：
```swift
var pixelShift = panelState.offset - CGFloat(wholeShift) * candleStep
if startIndex == upperBound {
    pixelShift = 0                                   // 最新边硬钉（含早 tick upperBound==0；reveal 前向不可达）
} else if startIndex == 0 {
    // 最老边 overscroll：offset>maxOffset → 左露间隙（pixelShift>0=candles 右移，Geometry.swift L136）；否则钉边
    // M2：复用本函数已从 core 派生的 baseStartIndex/candleStep（勿重算 geometryCore、勿用 upperBound）
    let maxOffset = roundTripEdge(integer: baseStartIndex, step: candleStep)   // 与 offsetBounds.maxOffset 同源（D4）
    pixelShift = panelState.offset > maxOffset ? panelState.offset - maxOffset : 0
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ios/Contracts && swift test --filter "RenderStateBuilder 视口几何"`
Expected: 全 PASS（5 新测试 + 既有 makeViewport 测全绿 = 等价证明）。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/Render/RenderStateBuilder.swift \
        ios/Contracts/Tests/KlineTrainerContractsTests/Render/RenderStateBuilderTests.swift
git commit -m "feat(r1b-wire): B4 makeViewport 单边 overscroll（仅最老边，最新边硬钉）"
```

---

## Task 2: `offsetBounds(engine:panel:bounds:)` 便捷重载（Coordinator 喂 bounds 用）

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/Render/RenderStateBuilder.swift`（加 `@MainActor static func offsetBounds(engine:panel:bounds:)`）
- Test: `ios/Contracts/Tests/KlineTrainerContractsTests/Render/RenderStateBuilderTests.swift`

- [ ] **Step 1: 写失败测试**

复用**真实** helper `TrainingEnginePinchTests.engine(closes:)`（static，返回 `(TrainingEngine, () -> [FakeFrameDriver])`；已 `recordRenderBounds(Self.bounds)`）。断言重载 == 直接 extraction（M4 raw visibleCount；tick=0 也成立——got/want 用同 engine 状态，与具体 bound 值无关）：

```swift
@Test("offsetBounds(engine:panel:bounds:) 重载 == 直接 extraction（M4 raw visibleCount）")
@MainActor func offsetBoundsEngineOverload() {
    let (engine, _) = TrainingEnginePinchTests.engine()          // 真实 helper（count 200, .m3 双面板, bounds 已记录）
    let bounds = TrainingEnginePinchTests.bounds                  // = 800×600
    let got = RenderStateBuilder.offsetBounds(engine: engine, panel: .upper, bounds: bounds)
    let cs = engine.allCandles[.m3]!
    let mainW = ChartPanelFrames.split(in: bounds).mainChart.width
    let want = RenderStateBuilder.offsetBounds(
        mainFrameWidth: mainW, rawVisible: engine.upperPanel.visibleCount,
        candleCount: cs.count,
        currentIdx: RenderStateBuilder.currentCandleIndex(candles: cs, tick: engine.tick.globalTickIndex))
    #expect(got == want)
}
```
（C1/C2 修正：用真实 `TrainingEnginePinchTests.engine()`，**非**幻影 `TrainingEngine(allCandles:tradeOperations:settings:...)`——真实 init 是 `init(flow:allCandles:maxTick:initialTick:initialCapital:initialCashBalance:...)`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ios/Contracts && swift test --filter offsetBoundsEngineOverload`
Expected: **build error**（`offsetBounds(engine:panel:bounds:)` 未定义 → 整 target 编译失败；M3：API 未实现的合法"红"）。

- [ ] **Step 3: 实现重载**

`RenderStateBuilder.swift` 加（紧邻既有 `offsetBounds(mainFrameWidth:...)`）：
```swift
/// Coordinator 便捷重载（D1）：从 engine 抽取 candles/tick/visibleCount + split 出 mainChart 宽，
/// 算 numeric offset 边界喂 engine。复用 `make` 的 extraction 路径（M4：传 raw `visibleCount`，fallback 在 geometryCore 内）。
@MainActor
static func offsetBounds(engine: TrainingEngine, panel: PanelId, bounds: CGRect) -> OffsetBounds {
    let ps = (panel == .upper) ? engine.upperPanel : engine.lowerPanel
    let candles = engine.allCandles[ps.period] ?? []
    let mainW = ChartPanelFrames.split(in: bounds).mainChart.width
    let currentIdx = candles.isEmpty ? 0 : currentCandleIndex(candles: candles, tick: engine.tick.globalTickIndex)
    return offsetBounds(mainFrameWidth: mainW, rawVisible: ps.visibleCount,
                        candleCount: candles.count, currentIdx: currentIdx)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ios/Contracts && swift test --filter offsetBoundsEngineOverload`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/Render/RenderStateBuilder.swift \
        ios/Contracts/Tests/KlineTrainerContractsTests/Render/RenderStateBuilderTests.swift
git commit -m "feat(r1b-wire): offsetBounds(engine:panel:bounds:) 便捷重载（D1 Coordinator 喂 bounds）"
```

---

## Task 3: engine 三 clamp + 机制 A 分派（activeBounds + onUpdate floor-or-full + 新 endPan/applyPanOffset 重载）

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift`
- Test: `ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift`（新）

- [ ] **Step 1: 写失败测试（§六.3/4/5/6/7）**

新文件 `TrainingEngineBounceWiringTests.swift`，`@Suite("TrainingEngine bounce 接线", .serialized) @MainActor struct`。**先定本地 fixture**（照 `TrainingEnginePinchTests.engine(closes:)` 真实模式，**关键：传 `initialTick: tick` 制造有滚动空间几何**，C3）：

```swift
import Testing
import Foundation
import CoreGraphics
@testable import KlineTrainerContracts

@MainActor
@Suite("TrainingEngine bounce 接线", .serialized)
struct TrainingEngineBounceWiringTests {
    static let bounds = CGRect(x: 0, y: 0, width: 800, height: 600)   // H2：本 suite 自有 bounds

    /// 真实 init（C1）+ initialTick 线程（C3）：count=200/tick=150 → mainW=800/step=10/base=71/maxOffset=710/bounceEdges=[.max]。
    static func makeEngine(count: Int, tick: Int) -> (TrainingEngine, () -> [FakeFrameDriver]) {
        final class Box { var fakes: [FakeFrameDriver] = [] }
        let box = Box()
        let maxTick = count - 1
        let e = TrainingEngine(
            flow: NormalFlow(fees: FeeSnapshot(commissionRate: 0.0001, minCommissionEnabled: true), maxTick: maxTick),
            allCandles: TrainingEngineActionsTests.m3Candles(Array(repeating: 10, count: count)),
            maxTick: maxTick,
            initialTick: tick,                                       // C3：覆盖 flow.initialTick(0)
            initialCapital: 100_000, initialCashBalance: 100_000,
            initialUpperPeriod: .m3, initialLowerPeriod: .m3,
            decelerationDriverFactory: { onTick in
                let f = FakeFrameDriver(onTick: onTick); box.fakes.append(f); return f })
        e.recordRenderBounds(Self.bounds, panel: .upper)
        e.recordRenderBounds(Self.bounds, panel: .lower)
        return (e, { box.fakes })
    }
    // ... 测试 ...
}
```

核心测试（每个有空间测试**先断言前提 `ob.bounceEdges.contains(.max)`**，C3）：

```swift
// §六.3 drag full-clamp（新签名）
@Test("drag full-clamp：推过 maxOffset → 钳 maxOffset；推过 0 → 钳 0")
func dragFullClamp() {
    let (e, _) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))                       // 前提：有滚动空间（maxOffset≈710）
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset + 999, offsetBounds: ob, panel: .upper)
    #expect(abs(e.upperPanel.offset - ob.maxOffset) < 1e-6)     // 钳上界
    e.applyPanOffset(deltaPixels: -99999, offsetBounds: ob, panel: .upper)
    #expect(e.upperPanel.offset == 0)                            // 钳下界 0
}

// §六.4 机制 A 分派 killer：v>0 → bounce（offset 朝 maxOffset 越界过冲，M5）
@Test("dispatch v>0 有空间 → bounce overscroll（offset 朝 maxOffset 移动并越界）M5")
func dispatchPositiveBounces() {
    let (e, fakes) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset, offsetBounds: ob, panel: .upper)   // 起于最老边
    e.endPan(velocity: 4000, offsetBounds: ob, panel: .upper)                       // 强甩朝最老边
    var maxSeen = e.upperPanel.offset
    for _ in 0..<240 { _ = fakes().last?.fire(1.0/60.0); maxSeen = max(maxSeen, e.upperPanel.offset) }
    #expect(maxSeen > ob.maxOffset)                              // 越界过冲（证 bounce 非 plain，方向朝 max；量级 ~98pt opus 已估）
    #expect(abs(e.upperPanel.offset - ob.maxOffset) < 1.0)       // settle 落 maxOffset
}

// §六.4 v<0 → plain decel（offset 朝 0、不越 max）
@Test("dispatch v<0 → plain decel（offset 朝 0 单调、不越 maxOffset）")
func dispatchNegativeDecel() {
    let (e, fakes) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset * 0.5, offsetBounds: ob, panel: .upper)
    e.endPan(velocity: -4000, offsetBounds: ob, panel: .upper)
    var maxSeen = e.upperPanel.offset
    for _ in 0..<240 { _ = fakes().last?.fire(1.0/60.0); maxSeen = max(maxSeen, e.upperPanel.offset) }
    #expect(maxSeen <= ob.maxOffset + 1e-6)                      // 从不越界（plain，非 bounce）
    #expect(e.upperPanel.offset == 0)                            // full-clamp 硬停 0
}

// §六.4 C1 killer：v>0 但无滚动空间 → plain decel + full-clamp 钳 0（不 strand 正 offset）
@Test("C1：v>0 无滚动空间（bounceEdges==[]）→ plain decel full-clamp 钳 0，不 strand")
func c1NoScrollSpacePositiveNoStrand() {
    let (e, fakes) = Self.makeEngine(count: 40, tick: 39)   // count<=visibleCount(80) → base=0 → upperBound=0 → [0,0]
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.isEmpty)                              // 前提：无滚动空间
    e.beginPan(panel: .upper)
    e.endPan(velocity: 4000, offsetBounds: ob, panel: .upper)   // 正速
    for _ in 0..<240 { _ = fakes().last?.fire(1.0/60.0) }
    #expect(e.upperPanel.offset == 0)                            // 不 strand 成正值
}

// §六.5 onUpdate clamp 类型对比（H3）：bounce floor 放 overscroll vs decel full 钳两边
@Test("§六.5 onUpdate clamp 类型：bounce floor 越界 vs decel full 两边夹")
func onUpdateClampTypeContrast() {
    let (e1, f1) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e1, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))
    e1.beginPan(panel: .upper)
    e1.applyPanOffset(deltaPixels: ob.maxOffset, offsetBounds: ob, panel: .upper)
    e1.endPan(velocity: 6000, offsetBounds: ob, panel: .upper)
    var bounceMax = e1.upperPanel.offset
    for _ in 0..<60 { _ = f1().last?.fire(1.0/60.0); bounceMax = max(bounceMax, e1.upperPanel.offset) }
    #expect(bounceMax > ob.maxOffset + 10)                       // floor：明确越界（不钳上界）

    let (e2, f2) = Self.makeEngine(count: 200, tick: 150)
    e2.beginPan(panel: .upper)
    e2.applyPanOffset(deltaPixels: ob.maxOffset * 0.3, offsetBounds: ob, panel: .upper)
    e2.endPan(velocity: -6000, offsetBounds: ob, panel: .upper)
    var decelMin = e2.upperPanel.offset, decelMax = e2.upperPanel.offset
    for _ in 0..<240 { _ = f2().last?.fire(1.0/60.0); decelMin = min(decelMin, e2.upperPanel.offset); decelMax = max(decelMax, e2.upperPanel.offset) }
    #expect(decelMin >= -1e-6)                                   // full 下界：从不 <0
    #expect(decelMax <= ob.maxOffset + 1e-6)                     // full 上界：从不 >max
}
```
（C1/C2/C3 修正：真实 init + `initialTick: tick` + 前提断言；`fakes().last` 取最近建 driver——`stop()` invalidate 旧、`start` append 新，`.last` 即当前 run；fire 在 invalidate 后返 false 自失活无害，opus 已核实。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ios/Contracts && swift test --filter "TrainingEngine bounce 接线"`
Expected: **build error**（新签名 `applyPanOffset(deltaPixels:offsetBounds:panel:)`/`endPan(velocity:offsetBounds:panel:)` 未定义 → 整 target 编译失败，非 filtered red；M3：这是"红"的合法形态——API 未实现）。

- [ ] **Step 3: 实现 engine 改动**

3a. 加嵌套类型 + 存储（TrainingEngine 类体，紧邻 `lastRenderedBounds`）：
```swift
struct ActiveDecel: Equatable, Sendable {
    let bounds: RenderStateBuilder.OffsetBounds
    let allowOverscroll: Bool
}
@ObservationIgnored private var activeBounds: (upper: ActiveDecel?, lower: ActiveDecel?) = (nil, nil)
```

3b. onUpdate 改经 clamp helper（init :141-142）：
```swift
self.animators.upper.onUpdate = { [weak self] d in self?.floorOrFullClampedOffsetDelta(d, panel: .upper) }
self.animators.lower.onUpdate = { [weak self] d in self?.floorOrFullClampedOffsetDelta(d, panel: .lower) }
```
extension 加 helper：
```swift
private func activeBoundsFor(_ panel: PanelId) -> ActiveDecel? { panel == .upper ? activeBounds.upper : activeBounds.lower }
private func setActiveBounds(_ v: ActiveDecel?, panel: PanelId) {
    if panel == .upper { activeBounds.upper = v } else { activeBounds.lower = v }
}
/// 减速/bounce 每帧 delta：bounce → floor [min,+∞)（放最老边 overscroll）；decel → full [min,max]（硬停）。
private func floorOrFullClampedOffsetDelta(_ delta: CGFloat, panel: PanelId) {
    guard let a = activeBoundsFor(panel) else { applyOffsetDelta(delta, panel: panel); return }   // 无界（旧路径兼容）
    let cur = panelState(panel).offset
    let target = a.allowOverscroll
        ? max(a.bounds.minOffset, cur + delta)                                   // bounce：仅 floor
        : min(max(cur + delta, a.bounds.minOffset), a.bounds.maxOffset)          // decel：full
    if target != cur { applyOffsetDelta(target - cur, panel: panel) }            // L2-new：省 0-delta 空 bump
}
```

3c. 新 `applyPanOffset` 重载（drag full-clamp）：
```swift
/// 新签名（Coordinator 用）：drag full-clamp 到 [min,max]（不跟手过边）。
public func applyPanOffset(deltaPixels: CGFloat, offsetBounds: RenderStateBuilder.OffsetBounds, panel: PanelId) {
    let cur = panelState(panel).offset
    let target = min(max(cur + deltaPixels, offsetBounds.minOffset), offsetBounds.maxOffset)
    if target != cur { applyOffsetDelta(target - cur, panel: panel) }
}
```

3d. 新 `endPan` 重载（存 activeBounds + 机制 A 分派）：
```swift
/// 新签名（Coordinator 用）：存 numeric bounds + 按速度方向分派（机制 A，D8）。
public func endPan(velocity: CGFloat, offsetBounds: RenderStateBuilder.OffsetBounds, panel: PanelId) {
    guard case .startDeceleration(let v) = reduce(.panEnded(velocity: velocity), on: panel) else { return }
    if v > 0 && offsetBounds.bounceEdges.contains(.max) {
        setActiveBounds(ActiveDecel(bounds: offsetBounds, allowOverscroll: true), panel: panel)
        animator(for: panel).start(initialVelocity: v, fromOffset: panelState(panel).offset,
                                   minOffset: offsetBounds.minOffset, maxOffset: offsetBounds.maxOffset)
    } else {
        setActiveBounds(ActiveDecel(bounds: offsetBounds, allowOverscroll: false), panel: panel)
        animator(for: panel).start(initialVelocity: v)
    }
}
```

3e. 旧 `endPan(velocity:panel:)` 体首清 activeBounds（H1）：
```swift
public func endPan(velocity: CGFloat, panel: PanelId) {
    setActiveBounds(nil, panel: panel)   // H1：清 stale，旧路径退化无界
    if case .startDeceleration(let v) = reduce(.panEnded(velocity: velocity), on: panel) {
        animator(for: panel).start(initialVelocity: v)
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ios/Contracts && swift test --filter "TrainingEngine bounce 接线"`
Expected: 全 PASS（dragFullClamp / dispatchPositiveBounces / dispatchNegativeDecel / c1NoScrollSpacePositiveNoStrand）。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift \
        ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift
git commit -m "feat(r1b-wire): engine 三 clamp + 机制 A 速度方向分派（activeBounds + onUpdate floor-or-full + 新 endPan/applyPanOffset 重载）"
```

---

## Task 4: `interruptDeceleration`（isDecelerating-guard 归一）+ 3 处 stop() swap

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift`
- Test: `ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift`

- [ ] **Step 1: 写失败测试（§六.8）**

```swift
// H3：re-grab 中途 overscroll → beginPan 归 maxOffset
@Test("H3：bounce overscroll 中途 beginPan → offset 归 maxOffset（不 strand）")
func interruptBeginPanNormalizes() {
    let (e, fakes) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset, offsetBounds: ob, panel: .upper)
    e.endPan(velocity: 4000, offsetBounds: ob, panel: .upper)
    var sawOverscroll = false
    for _ in 0..<30 { _ = fakes().last?.fire(1.0/60.0); if e.upperPanel.offset > ob.maxOffset { sawOverscroll = true; break } }
    #expect(sawOverscroll)                              // 确处于 overscroll 中
    e.beginPan(panel: .upper)                           // re-grab 打断
    #expect(abs(e.upperPanel.offset - ob.maxOffset) < 1e-6)   // 归 maxOffset，不 strand
}

// M2-new：bounce overscroll 中途 activateDrawingTool → snapshot offset 归 maxOffset（顺序不变量 killer）
@Test("M2-new：overscroll 中途 activateDrawingTool → snap.frozen.offset==maxOffset（归一在算 range 前）")
func activateDrawingDuringOverscrollNormalizes() {
    let (e, fakes) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    #expect(ob.bounceEdges.contains(.max))
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset, offsetBounds: ob, panel: .upper)
    e.endPan(velocity: 6000, offsetBounds: ob, panel: .upper)
    var overscrolled = false
    for _ in 0..<30 { _ = fakes().last?.fire(1.0/60.0); if e.upperPanel.offset > ob.maxOffset + 5 { overscrolled = true; break } }
    #expect(overscrolled)                               // 确处于 overscroll（offset>maxOffset）
    e.activateDrawingTool(.trend, panel: .upper)        // 中断 + 归一须在算 range 前
    guard case .drawing(let snap) = e.upperPanel.interactionMode else { Issue.record("应进入 drawing"); return }
    // 区分性断言：无 interruptDeceleration 归一 → frozen.offset 会是 overscrolled(>maxOffset)；有 → ==maxOffset
    #expect(abs(snap.frozen.offset - ob.maxOffset) < 1e-6)
    #expect(snap.frozen.offset <= ob.maxOffset + 1e-6)  // 绝非 overscrolled
}

// stale 自纠正：动画 settle 后再 beginPan（isDecelerating==false）→ 不误钳
@Test("stale 自纠正：settle 后 beginPan 不误钳一个当前合法 offset")
func interruptStaleSelfCorrect() {
    let (e, fakes) = Self.makeEngine(count: 200, tick: 150)
    let ob = RenderStateBuilder.offsetBounds(engine: e, panel: .upper, bounds: Self.bounds)
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset * 0.5, offsetBounds: ob, panel: .upper)
    e.endPan(velocity: -4000, offsetBounds: ob, panel: .upper)   // decel 到 0、settle
    for _ in 0..<600 { _ = fakes().last?.fire(1.0/60.0) }
    #expect(e.upperPanel.offset == 0)
    // 再手动 drag 到合法中段，然后 beginPan（此刻 isDecelerating==false，activeBounds stale）
    e.beginPan(panel: .upper)
    e.applyPanOffset(deltaPixels: ob.maxOffset * 0.5, offsetBounds: ob, panel: .upper)
    let mid = e.upperPanel.offset
    e.beginPan(panel: .upper)                            // 无活跃动画 → 不应 clamp 改 mid
    #expect(abs(e.upperPanel.offset - mid) < 1e-6)
}
```
（`.trend` 与 `TrainingEngineDrawingHandlerH1Tests` 一致；区分性断言 = `snap.frozen.offset==maxOffset`——candleRange 两态都 startIndex=0 不区分，offset 字段才区分归一 vs overscrolled。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd ios/Contracts && swift test --filter interruptBeginPanNormalizes`
Expected: FAIL（现 beginPan 用裸 `animator.stop()`，不归一 → offset 仍 >maxOffset）。

- [ ] **Step 3: 实现 interruptDeceleration + 3 swap**

extension 加 helper：
```swift
/// 中断进行中减速/bounce（新交互起手）：停 + 仅当**活跃** run 时把 overscroll 归界内（D10）。
/// isDecelerating-guard：动画已 settle/cancel（activeBounds 残留旧几何）时不 clamp，防 stale 误钳。
private func interruptDeceleration(panel: PanelId) {
    let a = animator(for: panel)
    let wasRunning = a.isDecelerating
    a.stop()
    if wasRunning, let act = activeBoundsFor(panel) {
        let cur = panelState(panel).offset
        let clamped = min(max(cur, act.bounds.minOffset), act.bounds.maxOffset)
        if clamped != cur { _ = reduce(.offsetApplied(deltaPixels: clamped - cur), on: panel) }
    }
}
```
3 处接入（注意 `activateDrawingTool` 是**提到最顶**，非原地替换——plan R2-C1 修）：
- `beginPan`（:597）：裸 `animator(for: panel).stop()` → `interruptDeceleration(panel: panel)`。
- `applyPinch` `.began`（:629）：裸 `animator(for: panel).stop()` → `interruptDeceleration(panel: panel)`。
- `activateDrawingTool`（**R2-C1 关键**）：把 `interruptDeceleration(panel: panel)` 提到方法**第一行**（在 `guard case … = reduce(.activateDrawing(tool))` **之前**），并**删除**原 :711 的裸 `animator(for: panel).stop()`（interrupt 已含 stop）。改后体：
```swift
public func activateDrawingTool(_ tool: DrawingToolType, panel: PanelId) {
    interruptDeceleration(panel: panel)   // R2-C1：先停+归一 overscroll（可能 bump revision），再捕获 baseRev
    guard case .requestDrawingSnapshotAfterStoppingAnimator(let t, let baseRev) =
            reduce(.activateDrawing(tool), on: panel) else { return }   // baseRev = 归一后 revision
    let ps = panelState(panel)                                          // offset 已归界内
    let range = RenderStateBuilder.visibleCandleRange(
        panelState: ps, candles: allCandles[ps.period] ?? [],
        tick: tick.globalTickIndex, bounds: renderBounds(panel))
    _ = reduce(.setDrawingSnapshot(tool: t, baseRevision: baseRev, candleRange: range), on: panel)  // baseRev==revision ✓
}
```
**为何提顶**：interrupt 的 `reduce(.offsetApplied)` 归一会 bump revision；若放在 `reduce(.activateDrawing)`（捕获 baseRev）**之后**，baseRev 失配 `setDrawingSnapshot` 的 `baseRev==revision` 闸门 → 永不进 drawing（R2-C1 实证）。提顶使 baseRev 捕获归一后的 revision。对既有 H1 测试无害（旧 endPan `activeBounds==nil` → interrupt 只 stop 不 bump）。

（`stopAllDeceleration`/`switchPeriodCombo`/`advanceAndAccount` **不改**——其后 `resetOffsetAfterAutoTracking` 已把 offset 归 0。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cd ios/Contracts && swift test --filter "TrainingEngine bounce 接线"`
Expected: 全 PASS。

- [ ] **Step 5: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/TrainingEngine/TrainingEngine.swift \
        ios/Contracts/Tests/KlineTrainerContractsTests/TrainingEngineBounceWiringTests.swift
git commit -m "feat(r1b-wire): interruptDeceleration（isDecelerating-guard 归一 overscroll）+ beginPan/activateDrawingTool/pinch 接入"
```

---

## Task 5: Coordinator 接线（ChartContainerView onPan 改新签名）

**Files:**
- Modify: `ios/Contracts/Sources/KlineTrainerContracts/Render/ChartContainerView.swift`（Coordinator.attach onPan，:80-88）

- [ ] **Step 1: 改 onPan 路由（UIKit-only，host 不编译，Catalyst 闸门）**

`arbiter.onPan` 的 `.changed`/`.ended` 改：
```swift
arbiter.onPan = { [weak self] deltaX, velocityX, phase in
    guard let self, let engine = self.engine, let view = self.view else { return }
    switch phase {
    case .began:   engine.beginPan(panel: self.panel)
    case .changed:
        let b = RenderStateBuilder.offsetBounds(engine: engine, panel: self.panel, bounds: view.bounds)
        engine.applyPanOffset(deltaPixels: deltaX, offsetBounds: b, panel: self.panel)
    case .ended:
        let b = RenderStateBuilder.offsetBounds(engine: engine, panel: self.panel, bounds: view.bounds)
        engine.endPan(velocity: velocityX, offsetBounds: b, panel: self.panel)
    case .cancelled: engine.cancelPan(panel: self.panel)
    }
}
```
（`self.view` 是 `weak var view: KLineView?`，已存在；`view.bounds` 即渲染 bounds。）

- [ ] **Step 2: Catalyst build 确认编译**

Run: `cd ios/Contracts && xcodebuild build-for-testing -scheme KlineTrainerContracts -destination 'platform=macOS,variant=Mac Catalyst' 2>&1 | tail -3`
Expected: `** TEST BUILD SUCCEEDED **`。

- [ ] **Step 3: Commit**

```bash
git add ios/Contracts/Sources/KlineTrainerContracts/Render/ChartContainerView.swift
git commit -m "feat(r1b-wire): Coordinator onPan 接新 bounds 签名（喂 numeric offsetBounds 进 engine，D1）"
```

---

## Task 6: P7 回归 + 全量验证 + 验收 doc

**Files:**
- Create: `docs/superpowers/acceptance/2026-06-16-w3-11-r1b-wire-acceptance.md`

- [ ] **Step 1: 全量 host 测试（P7 §六.9）**

Run: `cd ios/Contracts && swift test 2>&1 | grep -E "Test run with"`
Expected: `Test run with N tests ... passed`（0 failures）。**逐条确认既有调旧签名的测试（freeScrolling 累加/drawing/interaction）保持绿不改**（H2：旧签名 byte-preserved）。若任一旧测红 → 排查是否误改旧签名行为（应 0 变）。

- [ ] **Step 2: Mac Catalyst build-for-testing**

Run: `cd ios/Contracts && xcodebuild build-for-testing -scheme KlineTrainerContracts -destination 'platform=macOS,variant=Mac Catalyst' 2>&1 | tail -1`
Expected: `** TEST BUILD SUCCEEDED **`。

- [ ] **Step 3: 写非-coder 验收 checklist**

`docs/superpowers/acceptance/2026-06-16-w3-11-r1b-wire-acceptance.md`：范围 gate（白名单 3 prod + 2 test + 1 验收 doc）+ host 测核（§六.1-9 各一条二元判据 + `--filter` 用函数名）+ §六.10 device runbook（甩最老边回弹 / 轻拖不弹 / 朝最新边无前向 / 满屏不动 / 旋转不 strand）+ codex APPROVE 落账。forbidden phrases 见 `.claude/workflow-rules.json`。

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/acceptance/2026-06-16-w3-11-r1b-wire-acceptance.md
git commit -m "docs(r1b-wire): 非-coder 验收 checklist（host 测核 + device runbook）"
```

---

## Self-Review（writing-plans）

1. **Spec 覆盖**：机制 A 分派(T3) / 三 clamp(T3) / B4 单边(T1) / offsetBounds 重载(T2) / interruptDeceleration(T4) / Coordinator(T5) / P7(T6) / 验收(T6) —— spec §二-§九 全有任务。
2. **Placeholder 扫描**：无 TBD；engine 测试构造引「以 `TrainingEnginePinchTests.makeEngine` 实际签名为准」（非占位，是防臆造 init 参数）。
3. **类型一致**：`OffsetBounds`/`ActiveDecel`/`bounceEdges`/`offsetBounds(engine:panel:bounds:)`/`floorOrFullClampedOffsetDelta`/`interruptDeceleration` 命名跨任务一致；新旧 `endPan`/`applyPanOffset` overload 签名一致。
4. **顺序不变量（M2-new）**：T4 显式要求 `interruptDeceleration` 在 `activateDrawingTool` 取 range 前（原地替换 :711）。
