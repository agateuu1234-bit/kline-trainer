# Plan 1f — M0.1 Schema Versioning Contract 冻结 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `kline_trainer_modules_v1.4.md` M0.1 的三类 schema 版本管理规则（CONTRACT_VERSION 矩阵 L133-148 + bump 策略二分 L137-138/L287-293 + Migration Rollback 规则 L151-157）冻结为 repo 内权威治理 doc `docs/governance/m01-schema-versioning-contract.md`，与 `docs/governance/m04-apperror-translation-gate.md` 同目录同结构。**Doc-only**，零代码，零 CI workflow 新建。

**Architecture:**
- 单一 markdown 文件 + 单一聚合 acceptance 脚本
- 文档 8 H2 章节：Status+权威来源 / 用途 / CONTRACT_VERSION 矩阵 / Bump 策略 / Migration Rollback 规则 / 应用范围 / 未来强制点 / 交叉引用
- acceptance 脚本 17 项**独立**断言（吸收 Plan 1e R7 MEDIUM 教训——不用聚合阈值，每 anchor 单独可 FAIL）
- **不动**：`kline_trainer_modules_v1.4.md`（spec 源）/ 任何 SQL 文件 / 任何 Swift / Python / `docs/governance/m04-*`（Plan 1d 落地；本 plan 引用但不改）

**Tech Stack:** Markdown / bash 4 / `grep`（POSIX）/ 复用 Plan 0a acceptance runner 模式（PASS/FAIL 计数 + 失败项汇总）。

**依赖（hard prereq）:**
- Plan 1（PR #23）merged — schema.sql / training_set_schema_v1.sql / ios/sql/app_schema_v1.sql 已存在
- Plan 1d（PR #26 + hotfix #27）merged — `docs/governance/m04-apperror-translation-gate.md` 存在（本 plan §交叉引用）

## Scope

- **In scope**：CONTRACT_VERSION 矩阵（含顶层 `"1.4"` + 5 子维度）/ bump 策略二分（破坏性 vs 本地兼容，含 DAO reader 未知 state 处理 L287-293）/ Migration Rollback 三规则（B3 / P4 / P3a owners）
- **Out of scope（已在 spec §2 登记）**：
  - 具体 migration SQL 文件（spec L161-291 的 `content_hash` 两阶段 / `0003_v1.4_purge_leased` 由 Plan 2 B3 实施）
  - `CONTRACT_VERSION` 常量代码（Python by Plan 2, Swift by Plan 3）
  - L2232 CI assert（需先有常量；登记为未来强制点）
  - M0.5 consolidation（独立 Plan 1g）

## Brainstorming 已完成

Spec 在 `docs/superpowers/specs/2026-04-22-plan1f-m0.1-schema-versioning-design.md`（本 branch commit `e1cbb65`）。用户 2026-04-22 授权 autonomous execution（memory `feedback_autonomous_execution_mandate.md`），不再逐节确认 design，信任 codex:adversarial-review 作收敛 gate。

---

## Task 1: 起草 `docs/governance/m01-schema-versioning-contract.md`

**Files:**
- Create: `docs/governance/m01-schema-versioning-contract.md`

**背景**：本 task 以 doc-only 形态把 M0.1 schema 版本规则落到 `docs/governance/`，与 Plan 1d hotfix 的 m04 stub doc 同目录，作为 Plan 2 B3 / Plan 3 P4/P3a 模块 PR 的引用锚。doc "测试" = grep 断言（每条规则关键标识符存在）。

- [ ] **Step 1: 写 fail-path baseline 断言**

```bash
test ! -e docs/governance/m01-schema-versioning-contract.md && echo "OK: file absent (fail-path baseline)"
```

Expected: `OK: file absent (fail-path baseline)`。

- [ ] **Step 2: 写文档（8 H2 章节 verbatim）**

写入 `docs/governance/m01-schema-versioning-contract.md`：

````markdown
# M0.1 Schema Versioning Contract（contract，frozen by Plan 1f）

> **Status**：frozen 2026-04-22（Plan 1f PR）。
>
> **权威来源**（spec `kline_trainer_modules_v1.4.md`）：
> - 主体条款：L133-157（v1.3 新增 / v1.4 拆分 bump 策略）
> - Bump 策略二分：L137-138 + L287-293（DAO reader 未知 state 处理）
> - 历史决议：L2407（R4-codex modify，缩小"journal state 增删均 bump 顶层"的适用范围）
> - Wave 0 交付验收项：L2097
> - CI assert 要求（未实施 / 登记未来强制点）：L2232
>
> 任何修改本文档需走 spec 更新 → 本文档同步 → codex:adversarial-review → CODEOWNERS approve（`.claude/workflow-rules.json` `trust_boundary_globs` + `codeowners_required_globs`）。

## 用途

本文件是 M0.1 schema 版本管理规则在 repo 内的**权威落地锚点**。

- spec `kline_trainer_modules_v1.4.md` §M0.1 是规范文本来源
- 本文件是 **Plan 2 B3**（PostgreSQL migration 实施者）/ **Plan 3 P4**（app.sqlite GRDB migration 实施者）/ **Plan 3 P3a**（训练组 SQLite reader 工厂）的强制引用对象
- 任何涉及 DDL 变更 / migration 文件新增 / CONTRACT_VERSION bump 的 PR 必须在描述里点名引用本文件相应章节
- 与 `docs/governance/m04-apperror-translation-gate.md`（M0.4 错误翻译 stub）形成 governance doc 体系：m04 管错误传播边界，m01 管 schema 版本边界

## CONTRACT_VERSION 矩阵

三套存储 + Swift 模型 + P2 journal states 共 5 个子版本维度，顶层 `CONTRACT_VERSION` 作总标识：

| 维度 | 当前版本 | 变更触发 bump 的条件 |
|---|---|---|
| `CONTRACT_VERSION`（顶层标识） | `"1.4"` | 跨系统或破坏性持久化变更 bump 联动；P2 本地 journal state 的**兼容新增**不联动 |
| PostgreSQL schema（`schema.sql` migration id） | `0003_v1.3` | 任何 PostgreSQL DDL 变更（含加列）；联动顶层 |
| 训练组 SQLite `PRAGMA user_version` | `1` | 训练组 schema 结构变更；联动顶层 |
| app.sqlite GRDB migration | `0003_v1.4_purge_leased` | app.sqlite DDL / 新表 / **DML 数据清理 migration**（v1.4 新增：删除 v1.3 残留 `state='leased'` journal 行）；联动顶层 |
| Swift 模型版本（`M0.3`） | `1.3` | Codable 字段 / 枚举 case 变更；联动顶层 |
| `P2 journal states` enum | `v2` | 删除 / 改 raw value / 改既有语义 / 改恢复扫描集 → bump 顶层；仅追加本地中间态 → 只 bump 本子版本，reader 须显式处理未知 state |

**存储表位 速查**（spec L129-131）：

| 存储 | 位置 | Schema 文件 |
|---|---|---|
| PostgreSQL | NAS（`backend/sql/schema.sql`） | `backend/sql/schema.sql` |
| 训练组 SQLite | 各 `.zip` 内部 | `backend/sql/training_set_schema_v1.sql` |
| app.sqlite | iOS 沙盒（GRDB 管理） | `ios/sql/app_schema_v1.sql` |

## Bump 策略（破坏性 vs 本地兼容）

Bump 策略**分两类**（spec v1.4 修订，L137-138）：

### A. 必须 bump 顶层 `CONTRACT_VERSION`（破坏性 / 跨系统变更）

触发条件（满足任一即必须联动 bump）：
- 删 state / 改 raw value / 改既有语义
- 改恢复扫描集（`retryPendingConfirmations` 扫的 state 集合变更）
- 影响 DDL（新增 / 删除列、改类型、改约束等任何 PostgreSQL / app.sqlite / 训练组 SQLite schema 变更）
- 改 OpenAPI（REST API 契约变更）
- 任何跨系统契约字段调整

### B. 只 bump 子版本，不联动顶层（本地兼容新增）

触发条件（**全部**满足才适用）：
- 仅追加本地中间态（P2 journal 等）
- raw value 追加
- 既有 state 语义不变
- DDL 不变
- 不改恢复扫描集
- 不跨 REST 传输

**前提条件**（否则不适用本条例外）：
- DAO / reader 对未知 state 显式处理（fail-closed 或忽略策略文档化）

### DAO reader 对未知 state 的处理（与 B 类 bump 策略配套，spec L287-293）

- `AcceptanceJournalDAO.listByState(_:)` / `listScanTargets()` 读取时若遇到**当前 enum 未定义**的 raw value（跨版本向前读 / 回滚后读新版本写入的行等），采用"**fail-safe 忽略**"策略：
  - 记 warning 日志（状态名、training_set_id、lease_id）
  - 该行**不进入任何恢复扫描集**（不重试 confirm、不清理本地文件）
  - 留待用户手动运维或下次版本 migration 处理
- Swift 实现提示：`P2JournalState.init(rawValue:)` 返回 `nil` 时 DAO 将该行映射为内部 `UnknownJournalRow` 类型（不对外暴露到 `AcceptanceJournalRow.state`），`listBy*` API 过滤掉该类行

## Migration Rollback 规则

三套存储的 rollback owner + 规则（spec L151-157）：

| 范围 | 规则 | Owner |
|---|---|---|
| PostgreSQL `schema.sql` | 每个 migration 提供 `forward.sql` + `rollback.sql` 或显式标注"不可逆（列/表删除）"——不可逆项不允许放进 Wave 1+ 的 migration | **B3** |
| app.sqlite GRDB migration | 每个 migration 写在 `DatabaseMigrator` 中；rollback 通过新增反向 migration 实现（GRDB 不原生支持 down）；删列 / 删表需新 migration id | **P4** |
| 训练组 SQLite | **不 rollback**；增字段就 bump `PRAGMA user_version`，旧 version reader 直接拒收（抛 `AppError.trainingSet(.versionMismatch)`） | **M0.1 策略 / P3a Factory 执行**（runtime 拒收） |

## 应用范围（哪个模块 PR 必须引用本文件）

| Plan | 模块 | 引用本文件 | 备注 |
|---|---|---|---|
| Plan 2 | B1 import_csv | 否 | 不新增 DDL / 不改 schema |
| Plan 2 | B2 generate_training_sets | 否 | 只读 schema；但若需改 `training_sets` 写入字段语义，触发 A 类 bump 必须引用 |
| Plan 2 | **B3 FastAPI 服务 + migration owner** | ✅ **强制** | 所有 `backend/sql/migrations/` 下 migration 文件对（forward/rollback）必须符合本契约 A/B 类 bump 规则 |
| Plan 2 | B4 调度器 | 否 | 不改 schema |
| Plan 3 | F1 Models | 否 | Swift 模型 Codable 字段由 M0.3 契约管（见 `ios/Contracts/`），但若改模型字段触发 A 类 bump 必须引用本文件 |
| Plan 3 | P2 DownloadAcceptanceRunner | ✅（间接） | P2 journal states enum 的 bump 策略由本文件规定；journal DAO 未知 state 处理规则由本文件规定 |
| Plan 3 | **P3a TrainingSetDBFactory** | ✅ **强制** | 训练组 SQLite reader 工厂：打开 `.zip` 后 `PRAGMA user_version` 校验 + 版本不匹配抛 `AppError.trainingSet(.versionMismatch)` |
| Plan 3 | P3b TrainingSetReader | 否 | 只读，版本检查由 P3a Factory 在打开时完成 |
| Plan 3 | **P4 AppDB** | ✅ **强制** | GRDB `DatabaseMigrator` 注册所有 migration；`0003_v1.4_purge_leased` 按本文件要求在启动时强制执行；含 `AcceptanceJournalDAO`（见 m04 gate stub + 未来 Plan 1g M0.5 Sendable 要求） |
| Plan 3 | P5 CacheManager | 否 | 无 schema |
| Plan 3 | P6 SettingsStore | 否 | 无 schema |

## 未来强制点（Plan 1f 不实施，登记 backlog）

以下增量在 Plan 1f scope **之外**，但作为 spec 长期 governance 目标登记：

- [ ] **CI assert：`CONTRACT_VERSION` 常量与本文件矩阵同步**（spec L2232，v1.3 要求）：需先等 Plan 2 B3 在 Python 侧定义 `CONTRACT_VERSION = "1.4"` 常量（或 `kline_trainer/version.py`）且 Plan 3 F1 / P4 在 Swift 侧定义对应常量，随后开一个轻量 CI workflow grep 本文件当前 `"1.4"` 字串 vs 两侧常量。**落地形态待 Plan 2 B3 + Plan 3 F1 完成后另议**。
- [ ] **具体 migration SQL 文件落地**：spec L161-231 描述的 `content_hash` 两阶段 migration（`0003_v1.3_part1/forward.sql` + `.../rollback.sql` + `part2/forward.sql` + `.../rollback.sql`）由 Plan 2 B3 首次 migration PR 落地；本文件作为 migration 文件命名规范 / rollback 格式的**引用对象**。
- [ ] **`download_acceptance_journal` 表 migration**：spec L233-263 描述的 `0002_v1.3_journal/forward.sql` + `.../rollback.sql` 同上，由 Plan 2 B3 落地。
- [ ] **`0003_v1.4_purge_leased` app.sqlite GRDB migration 注册**（spec L268-281）由 Plan 3 P4 落地。
- [ ] **跨语言 `CONTRACT_VERSION` 常量同步 lint**：当 Plan 2 Python 常量 + Plan 3 Swift 常量都存在时，可在 PR CI 中加 string-equality lint；本 backlog 项延后到两侧都落地。

## 交叉引用

- **spec 源**：`kline_trainer_modules_v1.4.md` §M0.1 数据库 Schema 契约（L125-293，其中 L133-157 / L287-293 / L2097 / L2232 为本文件冻结主体）
- **姊妹 governance doc**：`docs/governance/m04-apperror-translation-gate.md`（M0.4 错误翻译 stub，由 Plan 3 P1 闭合）
- **Plan 1e aborted 教训**：`kline_trainer_modules_v1.4.md` §M0.5 相关 concurrency 合并冻结归独立 Plan 1g
- **治理 PR 工作流**：`.claude/workflow-rules.json`（`trust_boundary_globs` + `codeowners_required_globs`）
````

- [ ] **Step 3: 验证关键字存在（≥ 8 个 anchor 独立断言；预演 Task 2 脚本会跑的核心检查）**

```bash
grep -q '"1.4"' docs/governance/m01-schema-versioning-contract.md && echo "OK: top version"
grep -q 'PostgreSQL' docs/governance/m01-schema-versioning-contract.md && echo "OK: PostgreSQL"
grep -q '训练组 SQLite' docs/governance/m01-schema-versioning-contract.md && echo "OK: training_set SQLite"
grep -q 'app.sqlite' docs/governance/m01-schema-versioning-contract.md && echo "OK: app.sqlite"
grep -q 'Swift 模型' docs/governance/m01-schema-versioning-contract.md && echo "OK: Swift model"
grep -q 'P2 journal states' docs/governance/m01-schema-versioning-contract.md && echo "OK: P2 journal"
grep -qE '破坏性' docs/governance/m01-schema-versioning-contract.md && echo "OK: 破坏性"
grep -qE '本地兼容' docs/governance/m01-schema-versioning-contract.md && echo "OK: 本地兼容"
grep -q '未知 state' docs/governance/m01-schema-versioning-contract.md && echo "OK: 未知 state"
grep -q 'forward.sql' docs/governance/m01-schema-versioning-contract.md && echo "OK: forward.sql"
grep -q 'rollback.sql' docs/governance/m01-schema-versioning-contract.md && echo "OK: rollback.sql"
grep -q 'AppError.trainingSet(.versionMismatch)' docs/governance/m01-schema-versioning-contract.md && echo "OK: versionMismatch error"
grep -qE 'B3|P4|P3a' docs/governance/m01-schema-versioning-contract.md && echo "OK: 3 owners"
grep -q 'm04-apperror-translation-gate' docs/governance/m01-schema-versioning-contract.md && echo "OK: m04 cross-ref"
```

Expected: 14 行 `OK: ...`。任一缺失 → 回 Step 2 补齐文档。

- [ ] **Step 4: 验证 H2 章节数（8 个 `^## ` 标题）**

```bash
grep -c '^## ' docs/governance/m01-schema-versioning-contract.md
```

Expected: 输出 `8`（章节：用途 / CONTRACT_VERSION 矩阵 / Bump 策略 / Migration Rollback 规则 / 应用范围 / 未来强制点 / 交叉引用，加 Status 块本身不是 H2 ——所以**只有 7 个 H2**，需重新数）。

> **章节重数**：doc 顶部 `# M0.1 ...` 是 H1；`> **Status**：...` 是 blockquote 不是 header。以下是 `^## `（H2）章节：
>
> 1. `## 用途`
> 2. `## CONTRACT_VERSION 矩阵`
> 3. `## Bump 策略（破坏性 vs 本地兼容）`
> 4. `## Migration Rollback 规则`
> 5. `## 应用范围（哪个模块 PR 必须引用本文件）`
> 6. `## 未来强制点（Plan 1f 不实施，登记 backlog）`
> 7. `## 交叉引用`
>
> **共 7 个 H2。Task 2 acceptance 脚本硬编码 `^7$`。**

修正 Step 4：

```bash
grep -c '^## ' docs/governance/m01-schema-versioning-contract.md
```

Expected: 输出 `7`。

- [ ] **Step 5: 提交**

```bash
git add docs/governance/m01-schema-versioning-contract.md
git commit -m "feat(m0.1): freeze schema versioning contract doc per spec §M0.1

7 H2 sections covering CONTRACT_VERSION matrix (top '1.4' + 5 sub
dimensions: PostgreSQL/training_set/app.sqlite/Swift/P2 journal),
bump strategy (A. breaking bump top + B. local-compat bump sub with
DAO unknown-state fail-safe-ignore rule), migration rollback rules
(B3 PostgreSQL forward/rollback pairs, P4 GRDB DatabaseMigrator +
reverse migration for down, P3a runtime versionMismatch reject for
training_set), application scope per module, future enforcement
backlog (L2232 CI assert + SQL migration files + Swift/Python
constant sync lint), and cross-references. Doc-only; no SQL, no
Swift, no Python, no CI changes. Spec source: kline_trainer_modules_v1.4.md
L125-293 (main L133-157 + L287-293)."
```

---

## Task 2: 聚合 acceptance 脚本

**Files:**
- Create: `scripts/acceptance/plan_1f_m0_1_schema_versioning.sh`

**背景**：对齐 Plan 1/1b/1c/1d pattern，一键验收 M0.1 schema 版本契约交付。**关键设计**（Plan 1e R7 MEDIUM 教训）：每个必需 anchor 做**独立**断言，**不用**聚合关键词阈值；删任何单一 anchor 必须能独立 FAIL。

- [ ] **Step 1: 写聚合脚本**

写入 `scripts/acceptance/plan_1f_m0_1_schema_versioning.sh`：

```bash
#!/usr/bin/env bash
# Plan 1f 聚合验收：M0.1 Schema Versioning Contract
# 涵盖：m01 doc 存在 + 7 H2 + 各独立 anchor（不聚合）+ 3 owner + 交叉引用 + 不 regression Plan 1/1b/1c/1d
# 关键设计：每 anchor 独立断言（per Plan 1e R7 MEDIUM fix）
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$REPO_ROOT"

PASS=0
FAIL=0
declare -a FAILED

run() {
  local label="$1"; shift
  echo ""
  echo "========== $label =========="
  if "$@"; then
    echo "OK: $label"
    PASS=$((PASS + 1))
  else
    echo "NG: $label"
    FAIL=$((FAIL + 1))
    FAILED+=("$label")
  fi
}

DOC=docs/governance/m01-schema-versioning-contract.md

# ---- 文件存在性 ----
run "file: m01 contract doc" test -s "$DOC"

# ---- H2 章节完整性（7 个 ## 顶级标题）----
run "structure: 7 H2 sections" \
    bash -c "grep -c '^## ' $DOC | grep -q '^7$'"

# ---- 5 个子版本维度独立断言 ----
run "anchor: top version literal 1.4" grep -q '"1.4"' "$DOC"
run "anchor: dim PostgreSQL"          grep -q 'PostgreSQL' "$DOC"
run "anchor: dim 训练组 SQLite"        grep -q '训练组 SQLite' "$DOC"
run "anchor: dim app.sqlite"           grep -q 'app.sqlite' "$DOC"
run "anchor: dim Swift 模型"            grep -q 'Swift 模型' "$DOC"
run "anchor: dim P2 journal states"    grep -q 'P2 journal states' "$DOC"

# ---- Bump 策略二分独立断言 ----
run "anchor: bump 破坏性"     grep -q '破坏性' "$DOC"
run "anchor: bump 本地兼容"    grep -q '本地兼容' "$DOC"
run "anchor: 未知 state 处理"  grep -q '未知 state' "$DOC"
run "anchor: fail-safe 忽略策略" grep -q 'fail-safe' "$DOC"

# ---- Migration Rollback 独立断言 ----
run "anchor: forward.sql"  grep -q 'forward.sql'  "$DOC"
run "anchor: rollback.sql" grep -q 'rollback.sql' "$DOC"
run "anchor: versionMismatch error" grep -q 'AppError.trainingSet(.versionMismatch)' "$DOC"

# ---- 3 个 owner 独立点名 ----
run "owner: B3 PostgreSQL"     grep -q 'B3' "$DOC"
run "owner: P4 app.sqlite"     grep -q 'P4' "$DOC"
run "owner: P3a 训练组 Factory" grep -q 'P3a' "$DOC"

# ---- 交叉引用 ----
run "cross-ref: spec L133-157" \
    bash -c "grep -q 'kline_trainer_modules_v1.4.md' $DOC && grep -q 'L133' $DOC"
run "cross-ref: m04 gate doc" grep -q 'm04-apperror-translation-gate' "$DOC"

# ---- m04 gate stub 仍存在（不 regression Plan 1d hotfix）----
# 注意：只断言 m04 doc 存在 + 仍 cross-ref Plan 3 P1；不断言 TODO 仍开
# （Plan 3 P1 闭合时会删除 TODO 标记，本 plan 不该阻断 Plan 3 P1）
run "regression: m04 gate stub still present" \
    test -s docs/governance/m04-apperror-translation-gate.md
run "regression: m04 still cross-refs Plan 3 P1 (闭合前 TODO 或闭合后 completed 都匹配)" \
    grep -q 'Plan 3 P1' docs/governance/m04-apperror-translation-gate.md

# ---- 不 regression Plan 1/1b/1c acceptance ----
# 注意：故意不直接 regression plan_1d_m0_4_apperror.sh——其内部 'TODO Plan 3 P1' 断言
# 是 Plan 3 P1 闭合的 transient state；本 plan 通过 Plan 1c 的 swift test 间接覆盖
# Plan 1d AppError 测试（SwiftPM 扫描 Tests/ 目录）。Plan 1d 脚本本身的 TODO 依赖
# 属 Plan 3 P1 scope 处理。
run "regression: Plan 1 (M0.1 DDL) acceptance" \
    bash -c "test -x scripts/acceptance/plan_1_m0_1_db_schema.sh && ./scripts/acceptance/plan_1_m0_1_db_schema.sh > /tmp/p1.log 2>&1"
run "regression: Plan 1b (M0.2 OpenAPI) acceptance" \
    bash -c "test -x scripts/acceptance/plan_1b_m0_2_rest_api.sh && ./scripts/acceptance/plan_1b_m0_2_rest_api.sh > /tmp/p1b.log 2>&1"
run "regression: Plan 1c (M0.3 Swift Models) acceptance (间接覆盖 Plan 1d AppError tests)" \
    bash -c "test -x scripts/acceptance/plan_1c_m0_3_swift_contracts.sh && ./scripts/acceptance/plan_1c_m0_3_swift_contracts.sh > /tmp/p1c.log 2>&1"
run "regression: Plan 1d artifact present (AppError.swift)" \
    test -s ios/Contracts/Sources/KlineTrainerContracts/AppError.swift

# ---- 汇总 ----
echo ""
echo "============================================"
echo "Plan 1f (M0.1 schema versioning) acceptance: $PASS passed, $FAIL failed"
if [[ $FAIL -gt 0 ]]; then
  echo "Failed items:"
  for f in "${FAILED[@]}"; do echo "  - $f"; done
  echo "失败日志位置（regression 段）：/tmp/p1.log, /tmp/p1b.log, /tmp/p1c.log, /tmp/p1d.log"
  echo "PLAN 1f FAIL"
  exit 1
fi
echo "PLAN 1f PASS"
```

然后赋予执行权限：

```bash
chmod +x scripts/acceptance/plan_1f_m0_1_schema_versioning.sh
```

- [ ] **Step 2: 跑通过路径**

```bash
./scripts/acceptance/plan_1f_m0_1_schema_versioning.sh
```

Expected: 末两行依次出现 `Plan 1f (M0.1 schema versioning) acceptance: 26 passed, 0 failed` + `PLAN 1f PASS`。

**26 项分解**：
- 1 doc 存在
- 1 H2 计数
- 6 子维度 anchor（top 1.4 + 5 dims: PostgreSQL / 训练组 SQLite / app.sqlite / Swift 模型 / P2 journal states）
- 4 bump 策略 anchor（破坏性 / 本地兼容 / 未知 state / fail-safe）
- 3 Migration Rollback anchor（forward.sql / rollback.sql / versionMismatch）
- 3 owner（B3 / P4 / P3a）
- 2 交叉引用（spec L133 / m04）
- 2 m04 stub regression（file 存在 / 仍 cross-ref Plan 3 P1，兼容 TODO 闭合前后）
- 3 Plan 1/1b/1c acceptance regression + 1 Plan 1d artifact-existence check（Plan 1d 脚本依赖 transient TODO state，不直接 nested call；per codex R4）

= **26 项**（与 Plan 1e 13 项相比翻倍，因 anchor 独立断言不聚合）

- [ ] **Step 3: 跑失败路径 sanity（删 doc，验证脚本能 detect 缺失）**

```bash
cp docs/governance/m01-schema-versioning-contract.md /tmp/m01.bak
: > docs/governance/m01-schema-versioning-contract.md
./scripts/acceptance/plan_1f_m0_1_schema_versioning.sh && echo "ERROR: should have failed" || echo "PLAN 1f FAIL expected"
mv /tmp/m01.bak docs/governance/m01-schema-versioning-contract.md
```

Expected: 多条 NG（文件 + H2 + 所有读 m01 doc 的 anchor 都失败）+ `PLAN 1f FAIL expected`；备份恢复后下一步重新 PASS。

- [ ] **Step 4: 跑单 anchor 删除 sanity（验证聚合不掩盖）**

```bash
# 临时删除所有包含 "未知 state" 的行
cp docs/governance/m01-schema-versioning-contract.md /tmp/m01.bak
sed -i.tmp '/未知 state/d' docs/governance/m01-schema-versioning-contract.md
./scripts/acceptance/plan_1f_m0_1_schema_versioning.sh && echo "ERROR: 单 anchor 缺失未被 detect" || echo "SINGLE-ANCHOR-DELETION FAIL expected"
mv /tmp/m01.bak docs/governance/m01-schema-versioning-contract.md
rm -f docs/governance/m01-schema-versioning-contract.md.tmp
```

Expected: `NG: anchor: 未知 state 处理` + `SINGLE-ANCHOR-DELETION FAIL expected`（此即 Plan 1e R7 MEDIUM 修复验证——单 anchor 删除**必然** trigger FAIL）。

- [ ] **Step 5: 最终通过确认 + 提交**

```bash
./scripts/acceptance/plan_1f_m0_1_schema_versioning.sh
git add scripts/acceptance/plan_1f_m0_1_schema_versioning.sh
git commit -m "feat(plan-1f): aggregate M0.1 schema versioning acceptance script

26 items with INDEPENDENT anchor assertions (Plan 1e R7 MEDIUM fix):
file existence + 7 H2 sections + 6 version dimensions (top '1.4' + 5
subs) + 4 bump strategy anchors (breaking/local-compat/unknown-state/
fail-safe) + 3 migration rollback anchors (forward.sql/rollback.sql/
versionMismatch) + 3 owners (B3/P4/P3a) + 2 cross-refs (spec L133/m04) +
2 m04 regression + 4 Plan 1/1b/1c/1d regression. Fail-path sanity +
single-anchor-deletion sanity verified; aggregate threshold NOT used,
each anchor independently triggers FAIL on removal."
```

Expected: `PLAN 1f PASS`。

---

## 非 coder 验收清单（CLAUDE.md 治理 §2 / `workflow-rules.json` `verification_template`）

**触发点**：push 后 PR 开出，**merge 前**你在自己终端一条条跑完，stdout 贴到 PR 评论作为 evidence。

### 验收项 1：聚合脚本整体通过

| | 内容 |
|---|---|
| Action | 运行 `./scripts/acceptance/plan_1f_m0_1_schema_versioning.sh` |
| Expected | 末两行：`Plan 1f (M0.1 schema versioning) acceptance: 26 passed, 0 failed` + `PLAN 1f PASS` |
| Pass / Fail | 两行同时满足 = PASS；任一不满足 = FAIL |

### 验收项 2：单 anchor 删除 sanity 确认 FAIL 行为

| | 内容 |
|---|---|
| Action | 运行 `sed '/未知 state/d' docs/governance/m01-schema-versioning-contract.md > /tmp/m01-deleted.md && grep -c '未知 state' /tmp/m01-deleted.md`（预览）；或按 Task 2 Step 4 脚本跑 |
| Expected | 预览输出 `0`（确认 sed 删行生效）；Task 2 Step 4 脚本出 `NG: anchor: 未知 state 处理` + 非零退出码 |
| Pass / Fail | 删行预览 0 + 脚本返回非零 = PASS；脚本返回 0（说明聚合阈值未 detect）= FAIL（违规）|

### 验收项 3：m01 doc 7 H2 章节完整

| | 内容 |
|---|---|
| Action | 运行 `grep '^## ' docs/governance/m01-schema-versioning-contract.md` |
| Expected | 输出 7 行：`## 用途` / `## CONTRACT_VERSION 矩阵` / `## Bump 策略（破坏性 vs 本地兼容）` / `## Migration Rollback 规则` / `## 应用范围（哪个模块 PR 必须引用本文件）` / `## 未来强制点（Plan 1f 不实施，登记 backlog）` / `## 交叉引用` |
| Pass / Fail | 7 行全齐 = PASS；少 / 多 = FAIL |

### 验收项 4：3 个 owner 全部点名

| | 内容 |
|---|---|
| Action | 运行 `for o in B3 P4 P3a; do grep -c "$o" docs/governance/m01-schema-versioning-contract.md; done` |
| Expected | 输出 3 行数字，每行都 ≥ 1 |
| Pass / Fail | 3 行均 ≥ 1 = PASS；任一 = 0 = FAIL |

### 验收项 5：m04 gate stub 没被误删

| | 内容 |
|---|---|
| Action | 运行 `test -s docs/governance/m04-apperror-translation-gate.md && grep -c 'Plan 3 P1' docs/governance/m04-apperror-translation-gate.md` |
| Expected | 文件存在 + 数字 ≥ 1（至少 cross-ref 一次 Plan 3 P1；兼容闭合前 TODO 状态 + 闭合后 "completed" 状态）|
| Pass / Fail | 文件存在 + ≥ 1 = PASS；文件被删 / 不再提 Plan 3 P1 = FAIL（违规：本 plan 不该碰 m04）|

### 验收项 6：Plan 1/1b/1c acceptance regression + Plan 1d artifact 存在

| | 内容 |
|---|---|
| Action | 依序运行 `./scripts/acceptance/plan_1_m0_1_db_schema.sh`、`./scripts/acceptance/plan_1b_m0_2_rest_api.sh`、`./scripts/acceptance/plan_1c_m0_3_swift_contracts.sh`；再运行 `test -s ios/Contracts/Sources/KlineTrainerContracts/AppError.swift; echo "exit=$?"` |
| Expected | 前 3 个脚本各自的 `PLAN XX PASS` 收尾（退出码 0）；AppError.swift test 输出 `exit=0` |
| Pass / Fail | 3 PASS + AppError.swift exist = PASS；任一 FAIL = FAIL。**注**：Plan 1d acceptance 脚本不直接调用（其 TODO 断言是 transient state，per codex R4 finding，Plan 3 P1 闭合时会自然变化；Plan 1c 的 `swift test` 间接覆盖 Plan 1d AppError 测试） |

### 验收项 7：PR files 数组仅限本 plan 4 个新文件

| | 内容 |
|---|---|
| Action | PR 开出后运行 `gh pr view <PR_NUM> --json files --jq '.files[].path'` |
| Expected | 只输出 4 个路径：`docs/superpowers/specs/2026-04-22-plan1f-m0.1-schema-versioning-design.md`、`docs/superpowers/plans/2026-04-22-plan1f-m0.1-schema-versioning.md`、`docs/governance/m01-schema-versioning-contract.md`、`scripts/acceptance/plan_1f_m0_1_schema_versioning.sh` |
| Pass / Fail | 输出列表仅含上列 4 个 = PASS；含任何 `.swift` / `.py` / `.sql` / `ios/Contracts/**` / `backend/**` = FAIL（违规：doc-only plan 不该动代码）|

### 验收项 8：codex 评审 verdict 已贴 PR

| | 内容 |
|---|---|
| Action | 运行 `gh pr view <PR_NUM> --comments` |
| Expected | 评论列表包含一条 codex verdict 评论，verdict 为 `approve` 且 round ≤ 3 |
| Pass / Fail | approve 出现 = PASS；reject 仍未收敛 / 没贴 = FAIL |

---

## Self-Review（起草后自查）

1. **Spec coverage（逐条对照 Plan 1f 设计 spec §4）**：
   - §0 Status + 权威来源 callout → Task 1 Step 2 顶部 blockquote ✅
   - §1 用途 → Task 1 Step 2 `## 用途` ✅
   - §2 CONTRACT_VERSION 矩阵 → Task 1 Step 2 `## CONTRACT_VERSION 矩阵` ✅
   - §3 Bump 策略（破坏性 vs 本地兼容，含 DAO reader 未知 state）→ Task 1 Step 2 `## Bump 策略（破坏性 vs 本地兼容）` ✅
   - §4 Migration Rollback 规则 → Task 1 Step 2 `## Migration Rollback 规则` ✅
   - §5 应用范围 → Task 1 Step 2 `## 应用范围` ✅
   - §6 未来强制点 → Task 1 Step 2 `## 未来强制点` ✅
   - §7 交叉引用 → Task 1 Step 2 `## 交叉引用` ✅

2. **章节计数一致性**：
   - spec §4 写 "8 节"（含 §0）；实际 markdown `## ` H2 只有 7 个（Status 是 blockquote 非 H2）
   - Task 1 Step 4 修正 expected = 7；Task 2 Step 1 acceptance 脚本硬编码 `^7$`；Task 2 Step 1 description 写 "7 H2"；非 coder 验收项 3 列出 7 行
   - **全一致 at 7** ✅

3. **Acceptance 项数一致性**：
   - Task 2 Step 2 description 写 24 项；Task 2 Step 5 commit message 写 24 项；非 coder 验收项 1 写 24 项；脚本实际 `run "..."` 调用次数：数一下 — 1(file) + 1(H2) + 6(dims) + 4(bump) + 3(rollback) + 3(owner) + 2(cross-ref) + 2(m04 regression) + 4(plan regression) = **25 项**
   - **不一致！** 我上面数 24，实际是 25。修复：把 "4 bump 策略 anchor" 数成 4（破坏性/本地兼容/未知 state/fail-safe）✅；3 migration rollback 数成 3 ✅；但实际 1+1+6+4+3+3+2+2+4 = 26？让我重数：
     - file: 1
     - H2: 1
     - 6 dims anchor: 6
     - bump: 4
     - rollback: 3
     - owner: 3
     - cross-ref: 2
     - m04 regression: 2
     - Plan regression: 4
   - 1+1+6+4+3+3+2+2+4 = **26**
   - 全面修：把 24 改为 26，所有三处（Task 2 Step 2 expected / Task 2 Step 5 commit msg / 非 coder 验收项 1）
   - **行动**：本 self-review 后 inline 修

4. **Placeholder scan**：
   - 无 TBD / TODO 在 Task body（§未来强制点 的 `[ ]` 是登记的 backlog 项，含明确 owner + 触发条件）✅
   - 前向引用 "Plan 2 B3 / Plan 3 P4 / Plan 3 P3a" 是真实依赖关系 ✅

5. **Type consistency**：
   - `CONTRACT_VERSION`（大写下划线常量名）在全文一致 ✅
   - `forward.sql` / `rollback.sql` 小写一致 ✅
   - `AppError.trainingSet(.versionMismatch)` 与 Plan 1d M0.4 AppError 契约一致 ✅
   - 模块 ID B3 / P4 / P3a 与 modules v1.4 表一致 ✅

6. **forbidden_phrases 扫描**：8 项验收清单全部 action-expected-pass_fail，无 "验证通过即可 / looks fine" 等 ✅

7. **大小**：plan ~680 行 / ~28 KB；doc artifact ~170 行 / ~9 KB；acceptance ~85 行 / ~4 KB。codex branch-diff 256 KB 预算内。

8. **Codex round 预估**（post-Plan 1e lesson）：
   - 主要 cross-ref（spec L2407 R4 / L2097 / L287-293 / L2232）都已在权威来源 callout 显式标出，减少"codex 引另一处 spec 原文"的 drift 命中
   - 可能 finding：codex 可能要求加 "`0003_v1.3` PostgreSQL migration id 具体构成"细节——但 spec §M0.1 只写 migration id 不写完整 migration 文件结构，本 plan out-of-scope 正确
   - 预估 1-3 轮收敛（比 Plan 1e 好，因为 spec 源定位更清）；超 5 轮 escalate per memory

---

## 执行切换提示

按用户 2026-04-22 autonomous execution 授权（memory `feedback_autonomous_execution_mandate.md`），默认 **Subagent-Driven**：

1. worktree `.worktrees/plan-1f/m0.1-schema-versioning`，分支 `plan-1f/m0.1-schema-versioning`（已开）
2. `superpowers:subagent-driven-development` 串行派：
   - Task 1（m01 doc 起草）
   - Task 2（聚合 acceptance；依赖 Task 1）
3. 每 Task 内部按 step 走（doc-only，"TDD 退化" = Task 2 Step 4 含 single-anchor-deletion sanity 等价红→绿验证）
4. 每 Task 后 spec + code-quality 双层 review
5. 全部 task 完成后：
   - `superpowers:verification-before-completion`
   - `superpowers:requesting-code-review`
   - 本地 codex:adversarial-review（branch-diff）
6. codex approve 后 → **pause，向用户 escalate** 准备 push + PR + 非 coder 验收清单（用户终端执行）
7. merge 前所有 evidence 齐全 + codex approve + CODEOWNERS approve + CI 不 regression
