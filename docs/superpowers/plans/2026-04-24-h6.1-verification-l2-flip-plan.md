# H6.1: verification-before-completion L2 Flip Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Flip `.claude/config/skill-invoke-enforced.json` 里 `superpowers:verification-before-completion.mode` 为 `"block"`，同时扩展 `_initial` 的 `legal_next_set` 防 session resume 误挡；加 8 个单元测试锁定 L2/L4 行为；用户 override codex verdict 走 merge。

**Architecture:** Config-only 改动，**不动 hook 本体**。2 行 production config + 1 个新测试类（8 个 test + 4 个 helper）加到既有 `tests/hooks/test_skill_invoke_check.py`。TDD 流：先写所有测试→看 2 个失败→依次加 config 行使其通过。

**Tech Stack:** JSON (config), Python 3 / pytest (tests), bash (hook not modified)。

**Pre-conditions:**
- 当前 branch: `hardening-6.1/verification-l2-flip`（在 `.worktrees/h6.1/verification-l2-flip/`）
- spec 已 commit：`docs/superpowers/specs/2026-04-24-h6.1-verification-l2-flip-design.md`（13 rounds codex reviewed, final HEAD `3e940dd`）
- 用户决定走 **P_user_override**：接受 codex R13 的 "_initial 豁免 = bypass" 为 user-ratified residual，走 `attest-override.sh` 绕过 codex required check
- 用户 memory 已更新 pattern: `project_h6_initial_legal_next_set_exemption_pattern.md`（H6.2-H6.10 复用）

**Budget** (per `feedback_big_pr_codex_noncovergence.md`):
- Production code: 2 行（≤50 ✓）
- Tests: 8 个（≤10 ✓）+ ≤240 行 LOC
- Codex rounds on implementation: ≤3（spec 阶段已用光 13 轮，实现阶段重启预算）

---

## File Structure

- **Modify**: `.claude/config/skill-invoke-enforced.json` (2 lines total)
  - Line 23 区域：`superpowers:verification-before-completion.mode` observe→block (1a)
  - Line 42-48 区域：`_initial` 数组加一行 `"superpowers:verification-before-completion"` (1c)
- **Modify**: `tests/hooks/test_skill_invoke_check.py` (append at end of file)
  - Add one new class `TestVerificationBeforeCompletionBlockFlip` (~240 lines)

## Task 1: Write all 8 failing tests

**Files:**
- Modify: `tests/hooks/test_skill_invoke_check.py` (append new class at end)

### Step 1.1: Append the test class

- [ ] **Step 1.1: Add `TestVerificationBeforeCompletionBlockFlip` to `tests/hooks/test_skill_invoke_check.py`**

在文件末尾添加（保留前面所有既有类，不动）：

```python


class TestVerificationBeforeCompletionBlockFlip:
    """H6.1: verification-before-completion mode flipped to block.
    复用 _set_mode 显式锁定 "block"（防回滚静默）。
    复用 TestL5CodexGate 同款 state seeding + session id 约定。"""

    DETERMINISTIC_SESSION_ID = "01HQTESTAAAAAAAAAAAAAAAAAA"

    def _sid_hash8(self, sid=None):
        import hashlib
        sid = sid or self.DETERMINISTIC_SESSION_ID
        return hashlib.sha256(sid.encode()).hexdigest()[:8]

    def _seed_tdd_stage(self, session_id=None):
        """Pre-seed state file with last_stage=test-driven-development
        so transition → verification-before-completion is legal at L4."""
        import hashlib
        state_dir = Path(".claude/state/skill-stage")
        state_dir.mkdir(parents=True, exist_ok=True)
        wt_h = hashlib.sha256(os.getcwd().encode()).hexdigest()[:8]
        sid_h = self._sid_hash8(session_id)
        sf = state_dir / f"{wt_h}-{sid_h}.json"
        sf.write_text(json.dumps({
            "version": "1",
            "last_stage": "superpowers:test-driven-development",
            "last_stage_time_utc": "2026-04-22T00:00:00Z",
            "worktree_path": os.getcwd(),
            "session_id": session_id or self.DETERMINISTIC_SESSION_ID,
            "drift_count": 0,
            "transition_history": [],
        }))
        return sf

    def _seed_stage(self, stage, session_id=None):
        """Generalized version of _seed_tdd_stage for any predecessor."""
        import hashlib
        state_dir = Path(".claude/state/skill-stage")
        state_dir.mkdir(parents=True, exist_ok=True)
        wt_h = hashlib.sha256(os.getcwd().encode()).hexdigest()[:8]
        sid_h = self._sid_hash8(session_id)
        sf = state_dir / f"{wt_h}-{sid_h}.json"
        sf.write_text(json.dumps({
            "version": "1",
            "last_stage": stage,
            "last_stage_time_utc": "2026-04-22T00:00:00Z",
            "worktree_path": os.getcwd(),
            "session_id": session_id or self.DETERMINISTIC_SESSION_ID,
            "drift_count": 0,
            "transition_history": [],
        }))
        return sf

    def _block_env(self):
        return {
            "CLAUDE_SESSION_ID": self.DETERMINISTIC_SESSION_ID,
            "CLAUDE_SESSION_START_UTC": "2026-04-22T00:00:00Z",
        }

    def test_l2_missing_invoke_blocks(self, tmp_path):
        sf = self._seed_tdd_stage()
        bak = _set_mode("superpowers:verification-before-completion", "block")
        try:
            tp = _write_transcript(
                tmp_path,
                "Skill gate: superpowers:verification-before-completion\n\nx",
                tool_uses=[{"name": "Read", "input": {"file_path": "/a"}}],
            )
            rc, stdout, _ = _run_hook(tp, env_extra=self._block_env())
            assert '"decision":"block"' in stdout.replace(" ", "")
            assert "声明但响应未 Skill tool invoke" in stdout
        finally:
            _restore(bak)
            sf.unlink(missing_ok=True)

    def test_l2_valid_invoke_passes(self, tmp_path):
        sf = self._seed_tdd_stage()
        bak = _set_mode("superpowers:verification-before-completion", "block")
        try:
            tp = _write_transcript(
                tmp_path,
                "Skill gate: superpowers:verification-before-completion\n\nx",
                tool_uses=[{"name": "Skill",
                            "input": {"skill": "superpowers:verification-before-completion"}}],
            )
            rc, stdout, _ = _run_hook(tp, env_extra=self._block_env())
            assert '"decision":"block"' not in stdout.replace(" ", "")
        finally:
            _restore(bak)
            sf.unlink(missing_ok=True)

    def test_l2_wrong_skill_invoke_blocks(self, tmp_path):
        sf = self._seed_tdd_stage()
        bak = _set_mode("superpowers:verification-before-completion", "block")
        try:
            tp = _write_transcript(
                tmp_path,
                "Skill gate: superpowers:verification-before-completion\n\nx",
                tool_uses=[{"name": "Skill",
                            "input": {"skill": "superpowers:brainstorming"}}],
            )
            rc, stdout, _ = _run_hook(tp, env_extra=self._block_env())
            assert '"decision":"block"' in stdout.replace(" ", "")
            assert "声明但响应未 Skill tool invoke" in stdout
        finally:
            _restore(bak)
            sf.unlink(missing_ok=True)

    def test_committed_config_is_block(self):
        """Codex R2 F2 fix: guard against _set_mode masking a regressed commit."""
        cfg = json.loads(Path(".claude/config/skill-invoke-enforced.json").read_text())
        assert cfg["enforce"]["superpowers:verification-before-completion"]["mode"] == "block", \
            "H6.1 flip regressed: committed config must be 'block', not 'observe'"

    def test_l4_codex_to_verification_still_blocks(self, tmp_path):
        """Codex R12 F1 fix: 1b was reverted — codex → verification 直通
        构成 bypass（跳过 writing-plans + TDD）。本测试断言该 transit 仍挡。"""
        sf = self._seed_stage("codex:adversarial-review")
        bak = _set_mode("superpowers:verification-before-completion", "block")
        try:
            tp = _write_transcript(
                tmp_path,
                "Skill gate: superpowers:verification-before-completion\n\nx",
                tool_uses=[{"name": "Skill",
                            "input": {"skill": "superpowers:verification-before-completion"}}],
            )
            rc, stdout, _ = _run_hook(tp, env_extra=self._block_env())
            assert '"decision":"block"' in stdout.replace(" ", ""), \
                "codex → verification bypass must stay blocked (R12 F1)"
            assert "非法 transition" in stdout
        finally:
            _restore(bak)
            sf.unlink(missing_ok=True)

    def test_l2_block_drift_log_matches_monitoring_predicate(self, tmp_path):
        """Codex R9 F2 fix: verify emitted drift-log entry has the exact
        fields (gate_skill / config_mode / drift_kind) the §6 revert
        predicate reads."""
        sf = self._seed_tdd_stage()
        bak = _set_mode("superpowers:verification-before-completion", "block")
        drift_path = Path(".claude/state/skill-invoke-drift.jsonl")
        drift_path.parent.mkdir(parents=True, exist_ok=True)
        lines_before = drift_path.read_text().count('\n') if drift_path.exists() else 0
        try:
            tp = _write_transcript(
                tmp_path,
                "Skill gate: superpowers:verification-before-completion\n\nx",
                tool_uses=[{"name": "Read", "input": {"file_path": "/a"}}],
            )
            _run_hook(tp, env_extra=self._block_env())
            new_lines = drift_path.read_text().splitlines()[lines_before:]
            relevant = []
            for line in new_lines:
                try:
                    e = json.loads(line)
                except json.JSONDecodeError:
                    continue
                if e.get("gate_skill") == "superpowers:verification-before-completion":
                    relevant.append(e)
            assert relevant, \
                "drift log must include an entry for the blocked gate"
            e = relevant[-1]
            assert e["config_mode"] == "block"
            assert e["drift_kind"] in ("gate_declared_no_invoke", "illegal_transition")
        finally:
            _restore(bak)
            sf.unlink(missing_ok=True)

    def test_l4_initial_to_verification_allowed(self, tmp_path):
        """D_narrow 方案核心断言（覆盖 codex R2/R5/R6/R9/R11 F1）：
        加了 1c legal_next_set 后，`_initial` → verification-before-completion
        transition 合法，session resume / state reset 场景不被 L4 误挡。"""
        import hashlib
        state_dir = Path(".claude/state/skill-stage")
        state_dir.mkdir(parents=True, exist_ok=True)
        wt_h = hashlib.sha256(os.getcwd().encode()).hexdigest()[:8]
        sid_h = self._sid_hash8()
        sf = state_dir / f"{wt_h}-{sid_h}.json"
        sf.unlink(missing_ok=True)  # ensure _initial

        bak = _set_mode("superpowers:verification-before-completion", "block")
        try:
            tp = _write_transcript(
                tmp_path,
                "Skill gate: superpowers:verification-before-completion\n\nx",
                tool_uses=[{"name": "Skill",
                            "input": {"skill": "superpowers:verification-before-completion"}}],
            )
            rc, stdout, _ = _run_hook(tp, env_extra=self._block_env())
            assert '"decision":"block"' not in stdout.replace(" ", ""), \
                "`_initial` → verification must pass under D_narrow (1c legal_next_set 豁免)"
        finally:
            _restore(bak)
            sf.unlink(missing_ok=True)

    def test_l4_brainstorming_to_verification_still_blocks(self, tmp_path):
        """D_narrow 保留场景 #5 的 L4 保护：brainstorming 不是 verification 的
        合法前序，从 brainstorming 直接跳 verification 仍 block — 防"脑暴后假装完成"。"""
        sf = self._seed_stage("superpowers:brainstorming")
        bak = _set_mode("superpowers:verification-before-completion", "block")
        try:
            tp = _write_transcript(
                tmp_path,
                "Skill gate: superpowers:verification-before-completion\n\nx",
                tool_uses=[{"name": "Skill",
                            "input": {"skill": "superpowers:verification-before-completion"}}],
            )
            rc, stdout, _ = _run_hook(tp, env_extra=self._block_env())
            assert '"decision":"block"' in stdout.replace(" ", "")
            assert "非法 transition" in stdout
            assert "last_stage=superpowers:brainstorming" in stdout
        finally:
            _restore(bak)
            sf.unlink(missing_ok=True)
```

### Step 1.2: Run test file to see `test_committed_config_is_block` and `test_l4_initial_to_verification_allowed` fail

- [ ] **Step 1.2: Run tests — 2 个应失败，其他 6 个应通过**

Run from worktree root:
```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
pytest tests/hooks/test_skill_invoke_check.py::TestVerificationBeforeCompletionBlockFlip -v
```

**Expected**:
- `test_l2_missing_invoke_blocks` → **PASSED**
- `test_l2_valid_invoke_passes` → **PASSED**
- `test_l2_wrong_skill_invoke_blocks` → **PASSED**
- `test_committed_config_is_block` → **FAILED**（config 还是 `"observe"`；失败断言 `H6.1 flip regressed: committed config must be 'block', not 'observe'`）
- `test_l4_codex_to_verification_still_blocks` → **PASSED**
- `test_l2_block_drift_log_matches_monitoring_predicate` → **PASSED**
- `test_l4_initial_to_verification_allowed` → **FAILED**（1c legal_next_set 未加，`_initial → verification` 目前是非法 transition → block → 断言 `not in` 失败）
- `test_l4_brainstorming_to_verification_still_blocks` → **PASSED**

命令行输出应类似：`6 passed, 2 failed`。**如果不是 6 passed / 2 failed**，在继续前停下调查（可能是 test fixture 命中了意外路径或 helper 有 bug）。

### Step 1.3: Commit tests

- [ ] **Step 1.3: Commit tests (red phase)**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
git add tests/hooks/test_skill_invoke_check.py
git commit -m "test: H6.1 add TestVerificationBeforeCompletionBlockFlip (red phase)

All 8 tests for the H6.1 flip; 2 fail (expected):
- test_committed_config_is_block: config not yet flipped to block
- test_l4_initial_to_verification_allowed: 1c legal_next_set not yet added

Red → green next in Task 2 (1a mode flip) + Task 3 (1c _initial exemption).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
"
```

**Expected**: commit succeeds with 1 file changed, ~240 insertions.

---

## Task 2: 1a — Flip mode observe → block

**Files:**
- Modify: `.claude/config/skill-invoke-enforced.json`（找到 `superpowers:verification-before-completion.mode`）

### Step 2.1: Flip mode field

- [ ] **Step 2.1: 编辑 `.claude/config/skill-invoke-enforced.json`**

找到这一行（当前在 line 23 附近）：
```json
"superpowers:verification-before-completion": { "mode": "observe", "flip_phase": "H6.1" },
```

改成：
```json
"superpowers:verification-before-completion": { "mode": "block", "flip_phase": "H6.1" },
```

**仅改 `"observe"` → `"block"`**，其他字段不动。

### Step 2.2: 运行 test_committed_config_is_block

- [ ] **Step 2.2: 验证 #4 测试 green**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
pytest tests/hooks/test_skill_invoke_check.py::TestVerificationBeforeCompletionBlockFlip::test_committed_config_is_block -v
```

**Expected**: `1 passed`。

### Step 2.3: 运行 #7 测试（应仍失败）

- [ ] **Step 2.3: 确认 test_l4_initial_to_verification_allowed 仍失败**

```bash
pytest tests/hooks/test_skill_invoke_check.py::TestVerificationBeforeCompletionBlockFlip::test_l4_initial_to_verification_allowed -v
```

**Expected**: `1 failed` — 因为 1c 还没做。失败原因：`_initial` → verification 被 L4 挡。

### Step 2.4: Commit 1a

- [ ] **Step 2.4: Commit**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
git add .claude/config/skill-invoke-enforced.json
git commit -m "config: H6.1 1a flip verification-before-completion mode to block

Per spec §3.1 row 1a. Enables L2 gate_declared_no_invoke enforcement
for superpowers:verification-before-completion.

Turns test_committed_config_is_block green. test_l4_initial_to_
verification_allowed still fails until Task 3 (1c) lands.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
"
```

---

## Task 3: 1c — Add verification to `_initial` legal_next_set

**Files:**
- Modify: `.claude/config/skill-invoke-enforced.json`（找到 `mini_state.legal_next_set._initial` 数组）

### Step 3.1: 扩展 `_initial` 数组

- [ ] **Step 3.1: 编辑 `.claude/config/skill-invoke-enforced.json` 的 `mini_state.legal_next_set._initial`**

当前 `_initial` 数组（line 42-48 附近）：
```json
"_initial": [
    "superpowers:using-git-worktrees",
    "superpowers:brainstorming",
    "superpowers:writing-plans",
    "superpowers:systematic-debugging",
    "superpowers:finishing-a-development-branch"
],
```

改为（加 1 行）：
```json
"_initial": [
    "superpowers:using-git-worktrees",
    "superpowers:brainstorming",
    "superpowers:writing-plans",
    "superpowers:systematic-debugging",
    "superpowers:finishing-a-development-branch",
    "superpowers:verification-before-completion"
],
```

**仅加一条 array 元素**，其他不动。注意上一行末尾的逗号需要补上。

### Step 3.2: 运行 test_l4_initial_to_verification_allowed

- [ ] **Step 3.2: 验证 #7 测试 green**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
pytest tests/hooks/test_skill_invoke_check.py::TestVerificationBeforeCompletionBlockFlip::test_l4_initial_to_verification_allowed -v
```

**Expected**: `1 passed`。

### Step 3.3: 运行整个新测试类

- [ ] **Step 3.3: 8 个全 green 回归**

```bash
pytest tests/hooks/test_skill_invoke_check.py::TestVerificationBeforeCompletionBlockFlip -v
```

**Expected**: `8 passed`。如果任一 fail，停下调查。

### Step 3.4: 运行整个 test_skill_invoke_check.py 回归

- [ ] **Step 3.4: 回归既有测试**

```bash
pytest tests/hooks/test_skill_invoke_check.py -v
```

**Expected**: 全部 test（既有 + 新增 8 个）都 passed，无 fail/error。若既有测试有 fail，停下。

### Step 3.5: Commit 1c

- [ ] **Step 3.5: Commit**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
git add .claude/config/skill-invoke-enforced.json
git commit -m "config: H6.1 1c add verification to _initial legal_next_set

Per spec §3.1 row 1c (D_narrow pattern). _initial → verification-
before-completion now legal, so session resume / state reset does
NOT trigger L4 illegal_transition on a correct verification invoke.

Turns test_l4_initial_to_verification_allowed green. All 8 tests
in TestVerificationBeforeCompletionBlockFlip now pass.

Reuse pattern documented in memory:
project_h6_initial_legal_next_set_exemption_pattern.md for H6.2-H6.10.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
"
```

---

## Task 4: 跑 codex:adversarial-review on branch diff（plan-phase review）

### Step 4.1: 跑 codex-attest.sh

- [ ] **Step 4.1: 从 main 仓库根跑 codex review（plan-phase）**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer
.claude/scripts/codex-attest.sh --scope branch-diff \
  --head hardening-6.1/verification-l2-flip \
  --base main
```

**Expected (pessimistic)**: verdict=`needs-attention`，finding 类似 R13 F1（_initial 豁免 = bypass）。
**Expected (optimistic)**: verdict=`approve`（因 plan 已 codify 了 spec 的所有 decisions，codex 可能接受）。

**Budget**: 如 approve，进 Task 5 attest；如 needs-attention 且 finding 是 **R13 F1 reraise**（_initial bypass），**不修**，直接用 attest-override 机制走 user override 路径。如 needs-attention 但是 **新 concrete finding**（非 R13 重复），评估是否修；超 3 轮 abort。

### Step 4.2: 记录 verdict

- [ ] **Step 4.2: 将 codex verdict 记录到 plan 文档**

根据 codex-attest.sh 输出：
- 如 approve：继续 Task 5（ledger 已自动写入）
- 如 needs-attention：继续 Task 6（user override）

---

## Task 5 (路径 A — codex approve): Push + PR

### Step 5.1: Push branch

- [ ] **Step 5.1: 从 worktree push**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
git push -u origin hardening-6.1/verification-l2-flip
```

### Step 5.2: 创建 PR（中文 body，per memory feedback_pr_language_chinese.md）

- [ ] **Step 5.2: gh pr create**

```bash
cd /Users/maziming/Coding/Prj_Kline\ trainer/.worktrees/h6.1/verification-l2-flip
gh pr create --title "H6.1: verification-before-completion L2 block flip (+ _initial 豁免)" --body "$(cat <<'EOF'
## 概述

ζ 渐进路径第 1 步（H6.1）：把 `superpowers:verification-before-completion` 的 `mode` 从 `observe` 翻为 `block`，同时加 `_initial` 的 legal_next_set 豁免以消除 session resume 的 L4 误挡。

## 改动范围

- **生产代码**：`.claude/config/skill-invoke-enforced.json` 改动 2 行（mode 翻转 + `_initial` 数组加 verification-before-completion）
- **测试**：`tests/hooks/test_skill_invoke_check.py` 新增 `TestVerificationBeforeCompletionBlockFlip` 类，8 个 test + 4 个 helper（~240 行）
- **文档**：
  - `docs/superpowers/specs/2026-04-24-h6.1-verification-l2-flip-design.md`（spec，13 轮 codex adversarial review 迭代产出）
  - `docs/superpowers/plans/2026-04-24-h6.1-verification-l2-flip-plan.md`（本 plan）

## 为什么

- L2 `gate_declared_no_invoke` 硬强制：agent 不能仅贴 `Skill gate: verification-before-completion` label 而不真 invoke skill
- L4 `_initial → verification` 豁免：解决 session resume / state reset 的合法工作误挡担忧

## Residuals（用户显式 accept）

codex R13 的 _initial 豁免 "bypass" concern 是 user-ratified residual — 用户设计意图是"session 开头的第一个 skill 不做强制要求"。L2 enforcement 已达成，skill 内部 semantic checks 兜底。

## Test plan

- [ ] 合并前：`pytest tests/hooks/test_skill_invoke_check.py -v` 全绿
- [ ] 合并后首个 session：任何误声明 `Skill gate: superpowers:verification-before-completion` 但没真 invoke 的响应，Stop hook 应返回 `{"decision":"block"}` 且 reason 含 "声明但响应未 Skill tool invoke"
- [ ] 合并后首周：查 `.claude/state/skill-invoke-drift.jsonl` 是否有 `gate_skill: superpowers:verification-before-completion` + `config_mode: block` + `drift_kind: illegal_transition` 且属合法工作流，出现即触发 revert（spec §6 风险 2'）

## Co-Authored 与 Codex 信息

13 轮 codex adversarial review 历史见 branch commit log。spec 最终版 commit: `3e940dd`。
EOF
)"
```

### Step 5.3: Review + merge

- [ ] **Step 5.3: 等 CI + user review + merge**

等 GitHub CI 跑完；用户 review PR；push "squash merge" 按钮。

**Merge 后**：跑 §8 acceptance checks（spec 里的 5 项），并继续 Task 7。

---

## Task 6 (路径 B — codex needs-attention, user override per P_user_override)

### Step 6.1: 确认是否为 R13 F1 重复（_initial 豁免 = bypass）

- [ ] **Step 6.1: 人工判断 codex finding 是否是 R13 F1 的 reraise**

如果 finding 本质在说"\_initial 豁免构成 bypass" → **用户已接受为 residual，不修**，继续 Task 6.2。

如果 finding 是**新的具体缺陷**（不是 R13 F1 重复）：评估是否修。若是可在 scope 内修（如 typo / 文字一致性），修，回到 Task 4 重跑；若是架构层（如要改 hook） abort，升级给用户决定。

### Step 6.2: User attest-override（必须用户亲自在终端跑）

- [ ] **Step 6.2: 让用户在终端跑 attest-override**

告诉用户：
> codex R13 只 needs-attention 因为 _initial 豁免的 bypass concern（已在 spec 里 accept 为 residual）。请你在终端跑以下命令 override codex verdict，写 override ledger entry：
>
> ```bash
> cd /Users/maziming/Coding/Prj_Kline\ trainer
> .claude/scripts/attest-override.sh hardening-6.1/verification-l2-flip "H6.1 _initial exemption accepted as user-ratified residual per spec §6 after 13 rounds codex adversarial review; L2 enforcement achieved, L4 exemption is deliberate design per memory project_h6_initial_legal_next_set_exemption_pattern.md"
> ```
>
> （用户 MUST 在普通终端运行，不能是 Claude 代跑 — attest-override.sh 会拒绝 PPID 是 claude/node 的调用）

### Step 6.3: 等用户确认 override 已写

- [ ] **Step 6.3: 用户回报 override ledger 已写**

等用户说 "done" 或类似。然后回到 Task 5 push + PR。

---

## Task 7: Post-merge 验收 + memory 更新

### Step 7.1: 跑 spec §8 的 5 项检查（非 coder 验收）

- [ ] **Step 7.1: merge 后在一个新 session 里，由 agent 代跑 spec §8 Check 1-3，报结果给用户**

- Check 1: `jq '.enforce."superpowers:verification-before-completion".mode' .claude/config/skill-invoke-enforced.json` → 期望 `"block"`
- Check 2: `pytest tests/hooks/test_skill_invoke_check.py::TestVerificationBeforeCompletionBlockFlip -v` → 期望 `8 passed`
- Check 3: `pytest tests/hooks/test_skill_invoke_check.py -v` → 期望无 fail/error

- [ ] **Step 7.2: Check 4 + 5 请用户 1 周内自己观察**

spec §8 Check 4/5 是 post-merge 长期观察条件：
- Check 4：实证 block 发生（任何错误 gate 声明场景被 hook stdout `{"decision":"block"}` 拒）
- Check 5：无误挡（legitimate workflow 不被 block）

如 Check 5 触发 false positive → revert PR（per §5.1 两行 config revert）。

### Step 7.3: 更新 memory — 本 PR 完成记录

- [ ] **Step 7.3: Agent 写新 memory 文件 `project_h6_1_merged.md`**

记录：
- PR number
- merge commit SHA
- codex final verdict（approve 或 user-override）
- 本 PR 用到的 D_narrow pattern 参考
- H6.2-H6.10 下一步期望

### Step 7.4: 在 writing-plans 里对 follow-on 提 `/schedule` 建议

- [ ] **Step 7.4: 收尾回复里建议 /schedule**

给用户建议：
> Want me to /schedule an agent in 1 week to check `.claude/state/skill-invoke-drift.jsonl` for legitimate-workflow false positives (spec §8 Check 5 monitoring)?

---

## Self-Review Summary

- **Spec coverage**: §3.1 所有行（1a + 1c + tests）都对应 Task 1-3。§3.2 scenarios 全部被 test 覆盖（§4.2 matrix 每行一个 test）。§5/6/7/8 在 Task 5-7 实施。Scope Out（§2）无 impl 要求。
- **Placeholder scan**: 全文无 TBD / TODO / FIXME。每个 Step 给出 actual 命令 / actual 代码。
- **Type consistency**: 测试类名 `TestVerificationBeforeCompletionBlockFlip` 一致（§4.1 代码和 §8 Check 2 一致）。helper 名 `_seed_tdd_stage` / `_seed_stage` / `_sid_hash8` / `_block_env` 一致。config 键名 `superpowers:verification-before-completion` 全文一致。

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-24-h6.1-verification-l2-flip-plan.md`.

两种执行选项：

1. **Subagent-Driven（推荐）** — 每个 Task 派一个 fresh subagent；中间两阶段 review；快速迭代。
2. **Inline Execution** — 本 session 继续用 `superpowers:executing-plans` batch 执行，checkpoints review。

**选哪个？**
